# 2026-09-09 実装状態レビューの対応状況

更新: 2026-09-10 22:25。対象は[実装状態レビュー](2026-09-09-実装状態レビュー.md)。2026-09-07の補修番号や合致UIのR番号と混同しない。原レビュー本文は変更しない。

利用者の再確認により、報告のない指摘も実装・検証・報告まで対応する。**未解消のままリリースしない。** 指摘番号を新たな完成タスクとして加算しない。確定完成度はmainと同一SHAのCIが成功した原計画IDだけで数える。

| 指摘 | 現在の状態 | 残る対応・完了の証拠 |
|---|---|---|
| R01 保存失敗で控えを消す | 先行修正と報告あり | `報告記録.md`の2026-09-09外部レビュー対応と実保存障害の回帰。図面の新しい復元も同じ保全条件で確認中 |
| R02 遅れた保存が別文書を変更 | 先行修正と報告あり | 文書ID/世代/保存先の照合、切替・追加編集・保存失敗の回帰。図面の保存にも継続適用 |
| R03 部品へ戻ると図面を破棄 | 先行修正と報告あり | 未保存確認、取消時の図面/Undo保持の回帰 |
| R04 正確な中間値の丸め | 先行修正と報告あり | exactな変数表を文書反映と再計算まで共有する回帰。後続板金/APIでも同じ公開口を使う |
| R05 無次元値とinch | 先行修正と報告あり | パラメータの単位コンテキストを各入力へ接続した回帰。K係数も無次元として接続する |
| R06 ZIP確保前の上限 | **実装修正・手元回帰成功、main未反映** | raw DEFLATEの実展開長を出力確保前に確認し固定outへ展開。虚偽宣言200万バイト・総量残り超過でinflate 0回を`readArchiveAllocation.test.ts`で確認。通常ZIP/ZIP64/旧ファイルのIO回帰も成功 |
| R07 Web読込前サイズ | **実装修正・手元回帰成功、main未反映** | picker/input/形式付き入口が共通`readBrowserFile`を経由。サイズ不正時arrayBuffer 0回、正常読込・取消・同期例外・失敗後の保存先保持を含むUI63件成功。通常ゲート`codex-p8-tables-p10-full6`のE2E全149件も成功 |
| R08 number公開時Infinity | 先行修正と報告あり | 通常/exact/パラメータ適用で範囲外を断る回帰。有限Decimalと有限numberを区別 |
| R09 図面JSON | **実装修正・手元回帰成功、main未反映** | 既存の非有限/縮尺拒否に、詳細図の正半径・正縮尺、方向の非ゼロ/非平行、注記高と線幅の意味検証を追加。`drawingNumbers.test.ts`を含む図面読込回帰が成功。負座標・符号付き偏差・線幅0は保持 |
| R10 ZIP CRC | **実装修正・手元回帰成功、main未反映** | 実際の出力CRC-32と中央/局所目録の一致を確認。圧縮あり/なし、ZIP64、descriptor署名あり/なし・CRCと署名が同値の互換試験が成功 |
| R11 CSP/Electron権限 | **実画面回帰成功、main未反映** | Chromium/Firefoxで画面のeval・外部接続・inline script拒否と正規Workerの形状生成を確認。実Electronでも権限拒否・新規・保存再開・復元・出力・F1が成功。OCCT用例外は正規Worker応答のみ。Web公開headersとDesktop独自スキームへ共通CSPを設定 |
| R12 Desktop/Firefox | **通常検査へ接続、Windows実行成功、main未反映** | `security-e2e1`7件、`security-crossflows1`11件成功。実Electron1操作フローとFirefoxの部品保存編集・控え復元・幾何公差・図面復元を通常Playwrightへ追加。Linuxではxvfbとブラウザー依存を接続済みだが実行は次のCI待ち。今回の通常ゲート`codex-p8-tables-p10-full6`も全149件成功（Windows）。Linuxの同一SHA CIは引き続き未確認 |
| R13 検査中の再書換 | **Windows実試験成功、main未反映** | HEAD/index/追跡済み・非無視untrackedの実内容SHA256を比較。隔離repoでdirty再書換（長さ/mtime/status同一）、追加/削除/改名、HEADだけ/indexだけの変化、読取不能を検出。`security-guard1`全成功。Linux固有の改行/大小文字名はLinux実行待ち |
| R14 責務集中 | **部品codecを分割・回帰成功、他境界は継続** | 次に板金を追加する部品JSONを責務別24codecへ分割し、板金用2codecを追加（現在26個）。入口655行、最大codec545行。257宣言と26保存returnの同一性、型/lint、既存IO回帰を確認。逆依存・再肥大化をESLintで検出。数値入力・解決・表示等は触る機能の境界で続行 |
| R15 依存監視 | **main反映・初回更新実行成功** | `479001f`で設定がmainへ反映。alerts/security updatesは20:31有効化。[npm初回](https://github.com/oltotlo79-rgb/PointerCAD/actions/runs/34479279078)・[Actions初回](https://github.com/oltotlo79-rgb/PointerCAD/actions/runs/34479278904)がsuccess。[監視手順](../security/dependency-monitoring.md)に従って運用継続 |

P8/P9の26件はmainへ送信済み、同一SHA`479001f`のCIはsuccess（run34479276101）。確定449/552件（81.3%）。共通読込防御を優先修正したうえでP10の25件を進めている。P12-26/27、P13-4/5/7にある統合受入は、そのフェーズまで修正を待つ指示ではない。P10の保存完了もR06/R07/R10を残した状態で宣言しない。

レビュー§7の速度改善は、同じ受入を保ったまま変更境界を分離し、後続準備を検査待ちに行い、各ゲートの回数と所要を記録する。通常の手動5段・pre-commit4段・pre-push5段を独断で省略しない。
