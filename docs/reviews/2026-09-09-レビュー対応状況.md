# 2026-09-09 実装状態レビューの対応状況

更新: 2026-09-10 21:07。対象は[実装状態レビュー](2026-09-09-実装状態レビュー.md)。2026-09-07の補修番号や合致UIのR番号と混同しない。原レビュー本文は変更しない。

利用者の再確認により、報告のない指摘も実装・検証・報告まで対応する。**未解消のままリリースしない。** 指摘番号を新たな完成タスクとして加算しない。確定完成度はmainと同一SHAのCIが成功した原計画IDだけで数える。

| 指摘 | 現在の状態 | 残る対応・完了の証拠 |
|---|---|---|
| R01 保存失敗で控えを消す | 先行修正と報告あり | `報告記録.md`の2026-09-09外部レビュー対応と実保存障害の回帰。図面の新しい復元も同じ保全条件で確認中 |
| R02 遅れた保存が別文書を変更 | 先行修正と報告あり | 文書ID/世代/保存先の照合、切替・追加編集・保存失敗の回帰。図面の保存にも継続適用 |
| R03 部品へ戻ると図面を破棄 | 先行修正と報告あり | 未保存確認、取消時の図面/Undo保持の回帰 |
| R04 正確な中間値の丸め | 先行修正と報告あり | exactな変数表を文書反映と再計算まで共有する回帰。後続板金/APIでも同じ公開口を使う |
| R05 無次元値とinch | 先行修正と報告あり | パラメータの単位コンテキストを各入力へ接続した回帰。K係数も無次元として接続する |
| R06 ZIP確保前の上限 | **未解消** | 展開器へ出力予算を渡し、虚偽サイズでも生成/保持量が定義予算を超えない検査。最終的な拒否だけで閉じない |
| R07 Web読込前サイズ | **未解消** | picker/input/形式付き入口を共通化。File.sizeの有限・安全整数・非負・上限を本文確保前に検査し、超過時arrayBuffer 0回 |
| R08 number公開時Infinity | 先行修正と報告あり | 通常/exact/パラメータ適用で範囲外を断る回帰。有限Decimalと有限numberを区別 |
| R09 図面JSON | **一部修正、未完** | 現コードは有限数、sheet/viewの正縮尺と未知欄内の非有限値を検査する。線幅、方向ベクトル、詳細図等の全既知欄の意味制約を監査・回帰化し、JSONからの拒否理由まで記録 |
| R10 ZIP CRC | **未解消** | 圧縮あり/なし/データディスクリプターで実データのCRC-32を照合。元ZIP成功・内容だけ変更したZIP失敗を回帰化 |
| R11 CSP/Electron権限 | **未解消** | ローカル資産/WASM/Worker/画像/印刷の必要範囲でCSPを設定。sessionのrequest/checkを明示拒否し、正規の保存・ヘルプを維持 |
| R12 Desktop/Firefox | **一部対応、未完** | Desktopの単体検査は通常ゲートに接続済み。実Electronの観察とChromiumはFirefoxの代替にしない。両実行時の主要保存/復元/形状/図面を通常CIへ接続 |
| R13 検査中の再書換 | **未解消** | Pushの状態文字列比較を内容指紋へ変更。開始時dirty A→B、削除/追加/改名も隔離一時repoで検出 |
| R14 責務集中 | **継続対応、未完** | codec/移行/共通ガード、数値入力/確定、参照解決、表示、Worker寿命の境界から分割。既存API・状態所有者・回帰を維持。単なる移動や行数減少で完了にしない |
| R15 依存監視 | **設定とGitHub有効化済み、mainと定期実行の確認待ち** | 2026-09-10 20:31にalerts/security updatesをAPIで有効化し再照会。npm/Actions設定と[監視手順](../security/dependency-monitoring.md)を追加。未解決一覧は照会時0件。main反映と最初の定期実行を確認して記録する |

進行中のP8/P9の26件の全体検査・実Desktop受入・main着地を完了し、次の機能を接続する前に共通の読込防御を優先して直す。P12-26/27、P13-4/5/7にある統合受入は、そのフェーズまで修正を待つ指示ではない。P10の保存完了もR06/R07/R10を残した状態で宣言しない。

レビュー§7の速度改善は、同じ受入を保ったまま変更境界を分離し、後続準備を検査待ちに行い、各ゲートの回数と所要を記録する。通常の手動5段・pre-commit4段・pre-push5段を独断で省略しない。
