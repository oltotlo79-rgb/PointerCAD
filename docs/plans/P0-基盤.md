# P0「基盤」実装計画書

> **作業担当(サブエージェント)向け:** 本計画は `superpowers:subagent-driven-development`(推奨)または `superpowers:executing-plans` でタスク単位に実行する。各手順は `- [ ]` のチェックボックスで進捗を追う。
> **gitへの書き込みは統括だけが行う。** 作業担当は commit / push / checkout / merge / stash / reset を一切行わない(`rules/01-役割と委譲.md` §1)。

**目的:** 要件 `docs/requirements.md` §9 の P0 完了条件「箱形状が Web 版と Electron 版の両方で表示・視点操作できる」を満たす基盤を作る。

**構成方針:** pnpm workspaces のモノレポに要件§6.2 の7パッケージ+2アプリを作り、依存方向 `apps → ui → model → kernel / expression` を TypeScript プロジェクト参照と ESLint で機械的に固定する。幾何演算は Web Worker 内の OCCT(opencascade.js)で行い、UI は `model` 経由でメッシュを受け取る(`rules/04-設計の規律.md`)。

**技術構成:** TypeScript(strict) / Vite / React + Zustand / Three.js(WebGL2) / opencascade.js(OCCT WASM) / Comlink / Electron / Vitest / Playwright / ESLint(flat config + typescript-eslint)

---

## 0. 統括への確認事項

計画の実行前に統括の判断が要る点。各項目に作業担当の推奨案を添える。**特に §0.2 と §0.3 は依存追加・検査定義に関わるため、承認が無いと着手できない。**

| # | 論点 | 作業担当の推奨案と根拠 |
|---|---|---|
| 0.1 | **TypeScript のバージョン** | npm の `latest` は **7.0.2** だが、`typescript-eslint@8.69.0` の peer 範囲は `>=4.8.4 <6.1.0` で 7 系を受け付けない(registry 実測)。`typescript@6.0.0` は未公開(404、`beta` タグが `6.0.0-beta`)。→ **`typescript@5.9.3`(5系の最新)を採用**する。7系への移行は typescript-eslint が対応してから別タスクで行う。 |
| 0.2 | **opencascade.js の版** | `latest`=**1.1.1**、`beta`=**2.0.0-beta.b5ff984**。1.1.1 は `dist/` に `opencascade.wasm.js` + `opencascade.wasm.wasm`(**65.9MB**)のみで **TypeScript 型定義が無い**。beta は `opencascade.full.d.ts` を同梱し wasm は **50.3MB**、`node.js`(Node 用ローダ)も同梱。公式 issue #154 では「full ビルドなら `BRep_Tool.Triangulation` が動く(型が未バインドになるのはカスタムビルドのみ)」と確認済み。→ **beta `2.0.0-beta.b5ff984` を完全固定で採用**。要件§6.1「特定バージョンにピン止めして更新は検証付き」に合致する。型定義が無い 1.1.1 は strict TypeScript 方針(§6.1)と両立しない。 |
| 0.3 | **`test:e2e` を P0 で定義するか** | **定義しないことを推奨**(スクリプト名は `e2e` にする)。理由:①`scripts/check.ps1` は `test:e2e` があれば **pre-commit でも**実行する。50MB の WASM を含む Web 版のビルド+ブラウザ起動が毎コミットに入り、「タスクごとに都度コミット」(`rules/03-品質ゲート.md` §6)と両立しない。②`.github/workflows/ci.yml` に Playwright のブラウザ導入手順が無く、追加には ci.yml の変更(統括承認)が要る。③検査の単一正本(§7.2)により CI 専用の e2e ジョブを ci.yml へ直接書くことはできない。→ P0 では Playwright の設定とスモークテスト本体は作り、`e2e` スクリプトとして手動実行できる状態にする。P1 で「pre-commit は typecheck/lint/test/build、pre-push で e2e」に `check.ps1` を分けるときに `test:e2e` へ改名する(`check.ps1` と `rules/03-品質ゲート.md` §7.1 を同じコミットで更新)。 |
| 0.4 | **Playwright の `webServer` 起動の扱い** | Playwright の設定が `vite preview` を起動して終了時に落とす動作が、`rules/02-禁止事項.md`「開発サーバーを起動しない」に触れるかの解釈。→ **「ヘッドレスで完結するテスト実行」に含まれるものとして許可**を推奨。利用者の画面に窓は出ず、Playwright が確実に終了させるため。 |
| 0.5 | **CI の環境変数** | Node 22 + `corepack enable` で `packageManager: pnpm@11.25.0` を使うとき、corepack がダウンロード確認プロンプトを出して非対話環境で失敗する可能性がある(未確認)。→ P0 では ci.yml を変更しない前提で進め、**初回の CI 実行で失敗したら** `env: COREPACK_ENABLE_DOWNLOAD_PROMPT: 0` の追加を統括へ提案する。 |
| 0.6 | **electron-builder を P0 に含めるか** | **含めない**ことを推奨。P0 の完了条件は「両環境で表示・視点操作できる」であり、`electron .` での起動で満たせる。NSIS/AppImage のパッケージングは `rules/05-リリース.md` の対象で P8。ルートの `build` に electron-builder が入ると CI が毎回インストーラを生成して極端に遅くなる。 |
| 0.7 | **`@types/node` と CI の Node の不一致** | `@types/node` の `latest` は **26.4.1**、CI の Node は **22**。→ **26.4.1 を採用**し、P0 では `node:path` / `node:url` / `node:fs` の長期安定 API しか使わないことで実害を避ける。Node 24 以降への CI 更新は ci.yml の変更(統括承認)なので P0 では行わない。 |
| 0.8 | **strict の追加フラグ** | `noUncheckedIndexedAccess` と `exactOptionalPropertyTypes` は幾何配列の操作で `!` の乱用を誘発しやすい。`rules/02-禁止事項.md` は**緩める**ことを禁じているので、**P0 では入れず、後から足す**のが安全。→ `strict: true` + `noUnusedLocals` / `noUnusedParameters` / `noImplicitOverride` / `noImplicitReturns` / `noFallthroughCasesInSwitch` で開始する。 |
| 0.9 | **座標系** | 要件に上下方向の指定が無い。Blender 互換(要件§3-2)と JIS 図面の慣習に合わせ **Z-up(Three.js の `camera.up` を `(0,0,1)` にする)** を推奨。要件§12「Blender 互換ショートカットの具体割当表」は P0〜P1 決定なので、ここで決めて記録する。 |
| 0.10 | **Vite 8 系での失敗時の退避** | `vite@8.2.2` は Rolldown 系で、Emscripten のグルーコードの扱いが 7 系と変わる可能性がある(未確認)。→ タスク5で不具合が出た場合、`vite@7.3.6` + `@vitejs/plugin-react@5.x` への引き下げを統括へ提案する(`@vitejs/plugin-react@6.1.1` の peer は `vite ^8.0.0` のため両方を同時に下げる必要がある)。 |
| 0.11 | **`ui` から `model` 経由でメッシュを得る制約** | `rules/04-設計の規律.md`「UIから幾何カーネルへ直接依存しない」を P0 から守るため、`packages/model` に「箱1個のフィーチャー履歴」という最小のドキュメントモデルを置き、`ui` は `model` の `PartMesh` 型だけを見る。`kernel` の型を `model` が再輸出することもしない(独自型に変換する)。→ P0 の `model` は P2 の履歴機能の土台になる。 |
| 0.12 | **`.gitattributes` のバイナリ指定** | 現状 `*.wasm` 等の `-text` 指定が無い。P0 の設計では WASM をリポジトリへ置かない(`node_modules` から読む)ため**変更不要**。要件§10 のリスク対策として自前 Emscripten ビルドへ移る場合は、そのときに `.gitattributes` の更新を統括へ提案する。 |

### 0.a 統括の決定(2026-09-02)

作業担当は本表の決定に従う。§0 の推奨案と食い違う場合は本表が優先する。

| # | 決定 |
|---|---|
| 0.1 | 承認: `typescript@5.9.3`。 |
| 0.2 | 承認: `opencascade.js@2.0.0-beta.b5ff984` を完全固定。 |
| 0.3 | 承認: P0 では `e2e` スクリプト名(`test:e2e` は定義しない)。`check.ps1` の pre-commit / pre-push 分離と `test:e2e` への改名は P1 の残件として `docs/報告記録.md` に記録する。 |
| 0.4 | 承認: Playwright の `webServer` による `vite preview` の起動・終了はヘッドレステストの一部として許可(窓を出さないこと)。 |
| 0.5 | 変更: タスク1 で `.github/workflows/ci.yml` の pnpm 導入ステップに `env: COREPACK_ENABLE_DOWNLOAD_PROMPT: "0"` を**先に**追加してよい(統括承認済み)。それ以外の ci.yml 変更は不可。 |
| 0.6 | 承認: electron-builder は P8。 |
| 0.7 | 変更: `@types/node` は CI の Node 22 に合わせ **22 系の最新**を採用する(タスク1 で registry から実在版を確認して固定)。26 系は採らない。 |
| 0.8 | 承認: `strict` + `noUnusedLocals` / `noUnusedParameters` / `noImplicitOverride` / `noImplicitReturns` / `noFallthroughCasesInSwitch`。 |
| 0.9 | 承認: Z-up。決定として `docs/報告記録.md` に記録する。 |
| 0.10 | 承認: 不具合時は引き下げ前に統括へ報告(勝手に下げない)。 |
| 0.11 | 承認。 |
| 0.12 | 承認: `.gitattributes` 変更なし。 |

---

## 1. 依存パッケージ一覧

- バージョンはすべて **npm registry の `https://registry.npmjs.org/<名前>/latest` を実際に取得して確認**した実在の最新安定版(2026-09-02 時点)。取得できなかったものは「未確認」と明記した。
- **範囲指定(`^` / `~`)を使わず完全固定**する(要件§6.1「依存はすべて lockfile で固定」)。
- バージョンの正本は `pnpm-workspace.yaml` の `catalog:` に**1箇所だけ**書き、各 `package.json` は `"catalog:"` と書いて参照する(pnpm の catalog 機能)。表の「記載先」は `"catalog:"` を書く package.json を指す。

| パッケージ | バージョン | 用途 | 記載先(catalog 参照を書く package.json) |
|---|---|---|---|
| `typescript` | 5.9.3 | 型検査(strict)、プロジェクト参照 | ルート `package.json` |
| `eslint` | 10.9.1 | 書き方の点検(flat config) | ルート |
| `@eslint/js` | 10.0.1 | ESLint 推奨ルール集 | ルート |
| `typescript-eslint` | 8.69.0 | TypeScript 用パーサ+型情報つきルール | ルート |
| `globals` | 17.12.0 | flat config のグローバル定義(browser/node) | ルート |
| `eslint-plugin-react-hooks` | 7.1.1 | React フックのルール(peer は eslint ^10 対応済み) | ルート |
| `@types/node` | 26.4.1 | Node API の型(§0.7 参照) | ルート |
| `vite` | 8.2.2 | ビルドと開発サーバー | ルート / `apps/web` / `apps/desktop` |
| `vitest` | 4.1.11 | ユニットテスト(peer は `vite ^6 \|\| ^7 \|\| ^8`。8.2.2 と整合) | ルート / 各 `packages/*` |
| `@vitejs/plugin-react` | 6.1.1 | React の JSX 変換と Fast Refresh(peer `vite ^8.0.0`) | `apps/web` / `apps/desktop` |
| `@playwright/test` | 1.62.1 | E2E スモーク(§0.3 の通り P0 では `e2e` スクリプト) | ルート |
| `opencascade.js` | 2.0.0-beta.b5ff984 | 幾何カーネル(OCCT WASM)。§0.2 の根拠で beta を採用 | `packages/kernel` |
| `comlink` | 4.4.2 | Web Worker との型付き RPC(要件§6.1) | `packages/kernel` / `packages/model` |
| `three` | 0.185.1 | 3D 描画(WebGL2)。自前の型定義は持たない(`types` フィールド無しを実測) | `packages/ui` |
| `@types/three` | 0.185.4 | three の型定義(three 0.185 と minor 一致) | `packages/ui` |
| `react` | 19.2.8 | UI フレームワーク | `packages/ui` / `apps/web` / `apps/desktop` |
| `react-dom` | 19.2.8 | React の DOM レンダラ | `packages/ui` / `apps/web` / `apps/desktop` |
| `@types/react` | 19.2.18 | React の型定義 | `packages/ui` |
| `@types/react-dom` | 19.2.5 | react-dom の型定義 | `packages/ui` |
| `zustand` | 5.0.15 | 状態管理(ストア1本、`rules/04`) | `packages/ui` |
| `electron` | 44.1.1 | デスクトップシェル | `apps/desktop` |
| `pnpm` | 11.25.0 | パッケージマネージャ(`packageManager` フィールドと corepack 用) | ルート `package.json` の `packageManager` |

### 1.1 P0 では入れない依存(理由つき)

| パッケージ | 入れない理由 | 入れる時期 |
|---|---|---|
| `electron-builder`(26.15.3 を確認済み) | §0.6。ルートの `build` に入ると CI が毎回インストーラを生成する | P8(`rules/05-リリース.md`) |
| `decimal.js` | 式エンジンは P1 の範囲(要件§9) | P1 |
| `jsdom` / `@testing-library/react`(16.3.3) / `@testing-library/dom` | `@testing-library/react@16.3.3` は `@testing-library/dom ^10.0.0` を**必須** peer に持ち、3パッケージ増える。P0 の `ui` のテストは純関数(カメラ演算・グリッド間隔・ビューキューブ領域判定・文字列リソース)に絞り、実際の描画は Playwright スモークと統括の手動確認で担保する | P1 以降で React コンポーネントの振る舞いを検証する必要が出たとき |
| `vite-plugin-wasm` / `vite-plugin-top-level-await` | §2 の採用方式ではバンドラに WASM を解析させないため不要。代替案(§2.2 の案3)を採るときのみ必要 | 必要になったとき(統括承認) |
| `ws` | `opencascade.js` の peer(`^8.5.0`、最新 8.21.3 を確認済み)。pnpm の `auto-install-peers` が自動導入するため `package.json` へは書かない | — |

### 1.2 未確認のまま残した点

1. `opencascade.js` の `dist/node.d.ts` と `dist/opencascade.full.d.ts` の**既定エクスポートの型**(クラスか関数か)。`dist/index.js` の実装が `new mainJS({...})` を使っていることは実測済みなので `new` 形が正しい可能性が高いが、`.d.ts` の実物は 5MB 級で未読。→ タスク3の手順1で `node_modules` 内を Grep して確認する。
2. `GCPnts_TangentialDeflection_2` と `BRepAdaptor_Curve_2` の**オーバーロード番号**。OCCT の C++ 宣言順から推定した番号であり、生成された `.d.ts` は未読。→ タスク4の手順1で確認する。
3. pnpm 11 における `onlyBuiltDependencies` の**記述場所**(`pnpm-workspace.yaml` か `package.json` の `pnpm` フィールドか)。pnpm 10 で `pnpm-workspace.yaml` へ移った経緯があるが 11 の実挙動は未確認。→ タスク1の手順で `pnpm install` の出力を見て確認する。
4. `vite@8.2.2` が `node_modules` 内の **50.3MB の `.wasm` を `?url` で** dev / build 両方で正しく扱えるか。→ タスク5と タスク15で実測する。
5. CI の corepack がダウンロード確認プロンプトを出すか(§0.5)。
6. `opencascade.js` の各版の**公開日**。registry の `time` フィールドは未取得。

---

## 2. OCCT WASM の読み込み方式(Vite / Electron / Node の3環境)

### 2.1 採用方式

`opencascade.js@2.0.0-beta.b5ff984` の `dist/index.js` は先頭で `import ocFullWasm from "./opencascade.full.wasm";` を実行する(実測)。**この形の `.wasm` 直接 import は Vite が特別扱いするため、パッケージのルート(`opencascade.js`)を import すると Vite のビルドが不安定になる。** そこで `dist/index.js` は使わず、同梱の Emscripten ファクトリを自分で呼ぶ。`dist/index.js` の中身は次のとおりで(実測)、我々が再現すべき処理は `locateFile` の3行だけである。

```js
// node_modules/opencascade.js/dist/index.js(実測。参考のため引用)
const initOpenCascade = ({ mainJS = ocFullJS, mainWasm = ocFullWasm, worker, libs = [], module = {} } = {}) =>
  new Promise((resolve) => {
    new mainJS({
      locateFile(path) {
        if (path.endsWith('.wasm')) return mainWasm;
        if (path.endsWith('.worker.js') && !!worker) return worker;
        return path;
      },
      ...module,
    }).then(async (oc) => { /* libs の読み込み */ resolve(oc); });
  });
```

| 環境 | 採用する読み込み方 |
|---|---|
| **Web(Vite dev / build)** | `packages/kernel/src/occt/loadOcct.browser.ts` で、グルーコードと WASM の**両方を `?url` で URL としてだけ取得**し、グルーコードは実行時に `import(/* @vite-ignore */ url)` で読む。バンドラは 2 つのファイルを「資産」として出力するだけで中身を解析しないため、Emscripten のグルーに含まれる Node 専用分岐(`fs` / `path` / `module` の import)がブラウザ向けビルドで解決不能になる問題が起きない。さらに `optimizeDeps.exclude: ['opencascade.js']` と `build.target: 'esnext'`、`worker.format: 'es'` を Vite 設定に入れる。 |
| **Electron** | レンダラの中身は Web と同一の `packages/ui` を Vite でビルドしたもの。**`file://` ではなく独自スキーム `app://` で配信する**(`protocol.registerSchemesAsPrivileged` + `protocol.handle`)。理由:①`file://` では Emscripten の `fetch()` による WASM 取得が Chromium にブロックされる、②`?url` が返す絶対パスが `file://` ではファイルシステム根に解決されて壊れる、③`app://` なら COOP/COEP を応答ヘッダで付けられ Web 版と同じ隔離状態を再現できる(FR-1003)。開発時は `PCAD_DEV_SERVER_URL` 環境変数があればその URL を読み込み、Web 版と完全に同じ経路になる。 |
| **Node(Vitest)** | `packages/kernel/src/occt/loadOcct.node.ts` で `opencascade.js/dist/node.js` を import する。このファイルは `globalThis.__dirname` と `globalThis.require` を補ってから WASM の絶対パスを組み立てるため(実測)、バンドラなしで動く。Vitest は `node_modules` の依存を既定で外部化するので Node の ESM がそのまま読む。**50MB の WASM のコンパイルに数秒〜十数秒かかる**(公式 issue #18「initOpenCascade very slow with node.js」)ので `testTimeout: 120000` / `hookTimeout: 180000` を設定し、`pool: 'forks'` + `singleFork: true` でプロセス内に1インスタンスだけ作って共有する。 |

`opencascade.js` の `package.json` には `exports` フィールドが無い(実測)ため、`opencascade.js/dist/opencascade.full.js` のようなサブパス import が許される。`"type": "module"`、`"types": "dist/index.d.ts"`。`dist/` に `.worker.js` が無いことから **`opencascade.full` は単一スレッドビルド**であり、SharedArrayBuffer は必須ではない。COOP/COEP は要件 FR-1003 の明示要求と将来のマルチスレッド版のために設定する。

### 2.2 うまくいかない場合の代替(上から順に試す)

1. **静的 import へ戻す:** `import ocFactory from 'opencascade.js/dist/opencascade.full.js'` を通常の import にし、`optimizeDeps.exclude` と `build.rollupOptions.external` で Node 組み込みモジュール(`fs` / `path` / `module` / `url` / `crypto` / `worker_threads`)を外部化する。ブラウザでは当該分岐が実行されないため実害は出ない。
2. **資産として自前で複製する:** `apps/web` と `apps/desktop` のビルド時に、20行程度のローカル Vite プラグイン(新規依存なし)で `node_modules/opencascade.js/dist/opencascade.full.{js,wasm}` を出力先の `occt/` へコピーし、`/occt/opencascade.full.js` を実行時 import する。バンドラの関与を完全に断てる。
3. **プラグインを追加する:** `vite-plugin-wasm` + `vite-plugin-top-level-await` を入れる。**新規依存なので統括承認が必要**(`rules/02-禁止事項.md`)。
4. **Vite を 7 系へ下げる:** §0.10 のとおり `vite@7.3.6` + `@vitejs/plugin-react@5.x`。**統括承認が必要。**
5. **自前 Emscripten ビルド:** 要件§10 が P0 での逃げ道確保を求めている手段。OCCT のうち P0〜P2 で使うクラス(`BRepPrimAPI_MakeBox`、`BRepMesh_IncrementalMesh`、`BRep_Tool`、`TopExp_Explorer`、`TopoDS`、`Poly_Triangulation`、`Handle_Poly_Triangulation`、`Poly_Connect`、`StdPrs_ToolTriangulatedShape`、`TColgp_Array1OfDir`、`BRepAdaptor_Curve`、`GCPnts_TangentialDeflection`)だけを列挙したカスタムビルド定義を作り、`opencascade.js` の Docker ビルダで生成する。**issue #154 の通りカスタムビルドでは `Handle_Poly_Triangulation` を明示的にバインド一覧へ入れないと `BRep_Tool.Triangulation` が「unbound type」で失敗する。** 生成物はリポジトリへ vendor することになるため `.gitattributes` の更新と併せて**統括承認が必要**。P0 ではここまで進めず、手順だけを本節に記録して逃げ道とする。
6. **別パッケージへの乗り換え:** `occt-wasm`(OCCT を WASM 化し TypeScript API と Worker 対応を持つ、約4MB brotli)という選択肢が存在する。ただし要件§6.1 は `opencascade.js` を名指ししているため、採用するなら**要件定義書の改訂が必要**。P0 では採らない。

---

## 3. ファイル構成

すべて**新規作成**(既存ファイルは `docs/requirements.md`、`scripts/check.ps1`、`.github/workflows/ci.yml`、`.gitignore`、`.gitattributes` のみで、本計画ではこれらを一切変更しない)。

```
package.json                                  ルート。typecheck/lint/test/build を必ず定義
pnpm-workspace.yaml                           workspace 定義 + catalog(バージョンの正本)
tsconfig.base.json                            strict 設定の共通部分
tsconfig.json                                 解決用(files: [] + references)
tsconfig.tools.json                           設定ファイル群(*.config.ts, e2e/**)の型検査
eslint.config.js                              flat config。依存方向の機械施行を含む

packages/expression/{package.json,tsconfig.json,vitest.config.ts}
packages/expression/src/index.ts
packages/expression/src/version.test.ts

packages/help-content/{package.json,tsconfig.json,vitest.config.ts}
packages/help-content/src/index.ts
packages/help-content/src/topics.test.ts
packages/help-content/docs/ja/viewport.md

packages/kernel/{package.json,tsconfig.json,vitest.config.ts}
packages/kernel/src/index.ts
packages/kernel/src/types.ts                  MeshData / BoxParameters / TessellationOptions
packages/kernel/src/vite-env.d.ts             ?url の型(vite/client)
packages/kernel/src/occt/loadOcct.node.ts
packages/kernel/src/occt/loadOcct.browser.ts
packages/kernel/src/occt/makeBox.ts
packages/kernel/src/occt/tessellate.ts
packages/kernel/src/occt/extractEdges.ts
packages/kernel/src/occt/loadOcct.node.test.ts
packages/kernel/src/occt/boxTessellation.test.ts
packages/kernel/src/worker/kernelApi.ts       Worker の中でも外でも使える API 実装
packages/kernel/src/worker/kernelApi.test.ts
packages/kernel/src/worker/kernel.worker.ts   Comlink.expose
packages/kernel/src/client/createKernelWorker.ts

packages/model/{package.json,tsconfig.json,vitest.config.ts}
packages/model/src/index.ts
packages/model/src/types.ts                   PartMesh / Feature / PartDocument
packages/model/src/kernelBridge.ts            model→kernel の唯一の接点
packages/model/src/createBoxPartDocument.ts
packages/model/src/createBoxPartDocument.test.ts
packages/model/src/recompute.ts
packages/model/src/recompute.test.ts

packages/drawing/{package.json,tsconfig.json,vitest.config.ts}
packages/drawing/src/index.ts
packages/drawing/src/paperSize.test.ts

packages/io/{package.json,tsconfig.json,vitest.config.ts}
packages/io/src/index.ts
packages/io/src/schemaVersion.test.ts

packages/ui/{package.json,tsconfig.json,vitest.config.ts}
packages/ui/src/index.ts
packages/ui/src/i18n/ja.json                  UI 文字列の正本(NFR-MA-5)
packages/ui/src/i18n/t.ts
packages/ui/src/i18n/i18n.test.ts             キー網羅 + .tsx への日本語直書き検出
packages/ui/src/store/useAppStore.ts          Zustand ストア(1本)
packages/ui/src/viewport/cameraMath.ts        FR-101/102/108 の純関数
packages/ui/src/viewport/cameraMath.test.ts
packages/ui/src/viewport/gridMath.ts          FR-104 の純関数
packages/ui/src/viewport/gridMath.test.ts
packages/ui/src/viewport/createViewportScene.ts
packages/ui/src/viewport/attachCameraControls.ts
packages/ui/src/viewport/ViewportCanvas.tsx
packages/ui/src/viewcube/viewCubeMath.ts      FR-103 の純関数
packages/ui/src/viewcube/viewCubeMath.test.ts
packages/ui/src/viewcube/faceTexture.ts
packages/ui/src/viewcube/createViewCubeScene.ts
packages/ui/src/viewcube/ViewCube.tsx
packages/ui/src/app/PointerCadApp.tsx         起動時に箱を計算して表示する入口
packages/ui/src/shell/AppShell.tsx            §7.1 の固定区画
packages/ui/src/shell/Toolbar.tsx
packages/ui/src/shell/FeatureTree.tsx
packages/ui/src/shell/PropertyPanel.tsx
packages/ui/src/shell/StatusBar.tsx
packages/ui/src/shell/appShell.css

apps/web/{package.json,tsconfig.json,vite.config.ts,index.html}
apps/web/src/main.tsx
apps/web/public/_headers                      Cloudflare Pages の COOP/COEP(FR-1003)

apps/desktop/{package.json,tsconfig.json,index.html}
apps/desktop/vite.main.config.ts
apps/desktop/vite.preload.config.ts
apps/desktop/vite.renderer.config.ts
apps/desktop/src/main/main.ts
apps/desktop/src/main/appProtocol.ts
apps/desktop/src/preload/preload.ts
apps/desktop/src/renderer/main.tsx

e2e/playwright.config.ts
e2e/tests/smoke.spec.ts
```

**テストはソースと同じ `src/` 配下に `*.test.ts` として置く。** 理由:プロジェクト参照(`composite: true` + `rootDir: "src"`)の対象にテストも入るので `tsc --build` で型検査され、typescript-eslint の `projectService` もテストを解決できる。`test/` を別ディレクトリにすると `rootDir` の外になり、型検査から漏れるか `rootDir` を緩める必要が出る。

---

## 4. 共通の作業規約(全タスク共通)

- **gitへ書き込まない。** 変更したファイルの一覧と検査結果を統括へ報告する。
- **依存を勝手に足さない。** 本計画書の §1 の表にあるものだけを入れる。表に無いものが必要になったら**作業を止めて統括へ提案**する(`rules/02-禁止事項.md`)。
- **`eslint-disable` / `@ts-ignore` / `@ts-expect-error` / `any` / `as unknown as` で赤を消さない。** 必要なら理由を添えて統括へ提案する。
- **テストの期待値を緩めない。** 実測が期待値と違ったら、値を書き換えるのではなく**実測値を統括へ報告**する。
- **ブラウザの窓・Electron・開発サーバーを起動しない。** ヘッドレスのテスト実行は可。
- 検査は必ず `pnpm run typecheck` / `pnpm run lint` / `pnpm run test` / `pnpm run build` の4つ、または `scripts/check.ps1` で行う。**CI 専用のコマンドを作らない**(`rules/03-品質ゲート.md` §7.2)。
- 各タスクの最後に、統括が `git diff` を読むための報告(変更ファイル、検査の実行コマンドと出力の要点、実測値)を出す。

---

## タスク一覧と依存関係

| # | タスク | 先行 | 主な要件 ID |
|---|---|---|---|
| 1 | ルートの足場(workspace / tsconfig / ESLint / スクリプト) | — | NFR-MA-1, NFR-MA-3 |
| 2 | `expression` と `help-content` の骨組み | 1 | NFR-MA-1, FR-901 |
| 3 | `kernel` の骨組みと OCCT の Node 起動確認 | 1 | NFR-MA-2, 要件§6.1 |
| 4 | 箱の生成とテッセレーション(Node ヘッドレス検証) | 3 | NFR-MA-2, NFR-RE-3 |
| 5 | `kernel` の Worker + Comlink + ブラウザ用ローダ | 4 | NFR-PF-4 |
| 6 | `model` パッケージ(ドキュメントモデルと再計算) | 5 | NFR-MA-1, FR-501 |
| 7 | `drawing` と `io` の骨組み | 1 | NFR-MA-1 |
| 8 | `ui` の骨組み(i18n / Zustand ストア) | 6 | NFR-MA-5, rules/04 |
| 9 | 画面シェル(§7.1 の固定区画) | 8 | 要件§7.1, FR-905, NFR-UX-6 |
| 10 | カメラ操作とグリッド間隔の純関数 | 8 | FR-101, FR-102, FR-104, FR-108 |
| 11 | Three.js ビューポート | 9, 10 | FR-101, FR-102, FR-104, FR-105, FR-108 |
| 12 | ビューキューブの純関数 | 10 | FR-103 |
| 13 | ビューキューブの描画とアニメーション | 11, 12 | FR-103 |
| 14 | `ui` の統合(`model` 経由で箱を表示) | 11, 13 | 要件§6.3, rules/04 |
| 15 | `apps/web`(Vite + COOP/COEP) | 14 | FR-1001, FR-1002, FR-1003 |
| 16 | `apps/desktop`(Electron) | 14 | 要件§1.5, §6.1 |
| 17 | Playwright スモークテスト | 15 | NFR-MA-2 |
| 18 | 全体検査と P0 完了確認の準備 | 16, 17 | 要件§9 |

---

### タスク1: ルートの足場

**先行タスク:** なし
**対応要件:** NFR-MA-1(モノレポでパッケージ境界を明確化)、NFR-MA-3(CI で lint・型検査・全テスト・ビルド)

**ファイル:**
- 新規: `package.json`
- 新規: `pnpm-workspace.yaml`
- 新規: `tsconfig.base.json`
- 新規: `tsconfig.json`
- 新規: `tsconfig.tools.json`
- 新規: `eslint.config.js`

- [ ] **手順1: `pnpm-workspace.yaml` を作る(バージョンの正本)**

```yaml
packages:
  - "packages/*"
  - "apps/*"

# 依存のバージョンはここに1箇所だけ書く。各 package.json は "catalog:" と書いて参照する。
# 範囲指定(^ / ~)を使わず完全固定する(docs/requirements.md §6.1)。
catalog:
  "@eslint/js": 10.0.1
  "@playwright/test": 1.62.1
  "@types/node": 26.4.1
  "@types/react": 19.2.18
  "@types/react-dom": 19.2.5
  "@types/three": 0.185.4
  "@vitejs/plugin-react": 6.1.1
  comlink: 4.4.2
  electron: 44.1.1
  eslint: 10.9.1
  eslint-plugin-react-hooks: 7.1.1
  globals: 17.12.0
  opencascade.js: 2.0.0-beta.b5ff984
  react: 19.2.8
  react-dom: 19.2.8
  three: 0.185.1
  typescript: 5.9.3
  typescript-eslint: 8.69.0
  vite: 8.2.2
  vitest: 4.1.11
  zustand: 5.0.15

# Electron はインストール後スクリプトでバイナリを取得する。pnpm 10 以降は
# 既定でインストール後スクリプトを実行しないため、明示的に許可する。
onlyBuiltDependencies:
  - electron
```

- [ ] **手順2: ルート `package.json` を作る**

`typecheck` / `lint` / `test` / `build` の4つは `scripts/check.ps1` が存在を必須とする(欠けたら fail-closed)。

```json
{
  "name": "pointercad",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.25.0",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "typecheck": "tsc --build --pretty",
    "lint": "eslint .",
    "test": "pnpm --recursive --if-present run test",
    "build": "pnpm --recursive --if-present run build",
    "e2e": "playwright test --config e2e/playwright.config.ts",
    "clean": "tsc --build --clean"
  },
  "devDependencies": {
    "@eslint/js": "catalog:",
    "@playwright/test": "catalog:",
    "@types/node": "catalog:",
    "eslint": "catalog:",
    "eslint-plugin-react-hooks": "catalog:",
    "globals": "catalog:",
    "typescript": "catalog:",
    "typescript-eslint": "catalog:",
    "vite": "catalog:",
    "vitest": "catalog:"
  }
}
```

`pnpm --recursive run` は既定でルートプロジェクトを含めない(`--include-workspace-root` を付けない限り)ため、`test` / `build` が自分自身を再帰呼び出しすることはない。`--if-present` により、まだ `test` を持たないパッケージがあっても失敗しない。

- [ ] **手順3: `tsconfig.base.json` を作る**

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022"],
    "module": "ESNext",
    "moduleResolution": "bundler",
    "moduleDetection": "force",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "verbatimModuleSyntax": true,
    "jsx": "react-jsx",

    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noImplicitOverride": true,
    "noImplicitReturns": true,
    "noFallthroughCasesInSwitch": true,

    "composite": true,
    "declaration": true,
    "declarationMap": true,
    "emitDeclarationOnly": true,
    "sourceMap": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true
  }
}
```

`skipLibCheck: true` は必須。`opencascade.js` の `opencascade.full.d.ts` が非常に大きく、これを型検査すると `tsc` が実用的な時間で終わらないため。

- [ ] **手順4: ルート `tsconfig.json`(解決用)を作る**

参照はパッケージを作るタスクごとに1行ずつ足していく。存在しないディレクトリを参照すると `tsc --build` が失敗するため、この時点では `tsconfig.tools.json` だけを参照する。

```json
{
  "files": [],
  "references": [
    { "path": "./tsconfig.tools.json" }
  ]
}
```

- [ ] **手順5: `tsconfig.tools.json` を作る**

`vite.config.ts` などの設定ファイルもプロジェクトに含めないと、`tsc --build` の型検査から漏れ、typescript-eslint の `projectService` も解決できない。出力先を `dist/tools` にするのは、`.gitignore` の `dist/` に一致させて追跡対象を汚さないため。

```json
{
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "rootDir": ".",
    "outDir": "./dist/tools",
    "types": ["node"]
  },
  "include": [
    "*.config.ts",
    "packages/*/*.config.ts",
    "apps/*/*.config.ts",
    "e2e/**/*.ts"
  ]
}
```

- [ ] **手順6: `eslint.config.js` を作る**

依存方向(`rules/04-設計の規律.md`)を `no-restricted-imports` で機械的に施行する。逆流をレビューの目視ではなく `pnpm run lint` で落とす。

```js
// @ts-check
import js from '@eslint/js';
import globals from 'globals';
import reactHooks from 'eslint-plugin-react-hooks';
import tseslint from 'typescript-eslint';

/** 依存方向 apps → ui → model → kernel / expression を機械的に守らせる(rules/04-設計の規律.md)。 */
const layerRules = [
  { files: ['packages/expression/**/*.ts'], forbidden: ['@pointercad/*'] },
  { files: ['packages/help-content/**/*.ts'], forbidden: ['@pointercad/*'] },
  { files: ['packages/kernel/**/*.ts'], forbidden: ['@pointercad/*'] },
  {
    files: ['packages/model/**/*.ts'],
    forbidden: ['@pointercad/ui', '@pointercad/drawing', '@pointercad/io', '@pointercad/help-content'],
  },
  { files: ['packages/drawing/**/*.ts'], forbidden: ['@pointercad/ui', '@pointercad/io'] },
  { files: ['packages/io/**/*.ts'], forbidden: ['@pointercad/ui', '@pointercad/drawing'] },
  // UI から幾何カーネルへ直接依存しない(ドキュメントモデル経由)
  { files: ['packages/ui/**/*.{ts,tsx}'], forbidden: ['@pointercad/kernel', 'opencascade.js', 'opencascade.js/*'] },
  { files: ['apps/**/*.{ts,tsx}'], forbidden: ['@pointercad/kernel', 'opencascade.js', 'opencascade.js/*'] },
];

export default tseslint.config(
  {
    ignores: ['**/dist/**', '**/node_modules/**', 'playwright-report/**', 'test-results/**'],
  },
  js.configs.recommended,
  ...tseslint.configs.recommended,
  {
    // 型情報を使うルールはソースとテストにだけ適用する。
    // 設定ファイル(*.config.ts)は projectService の対象外なので型情報なしのルールだけが効く。
    files: ['packages/*/src/**/*.{ts,tsx}', 'apps/*/src/**/*.{ts,tsx}'],
    extends: [...tseslint.configs.recommendedTypeChecked],
    languageOptions: {
      parserOptions: {
        projectService: true,
        tsconfigRootDir: import.meta.dirname,
      },
      globals: { ...globals.browser, ...globals.node },
    },
  },
  {
    files: ['packages/ui/src/**/*.tsx', 'apps/*/src/**/*.tsx'],
    plugins: { 'react-hooks': reactHooks },
    rules: {
      'react-hooks/rules-of-hooks': 'error',
      'react-hooks/exhaustive-deps': 'warn',
    },
  },
  ...layerRules.map(({ files, forbidden }) => ({
    files,
    rules: {
      'no-restricted-imports': [
        'error',
        {
          patterns: [
            {
              group: forbidden,
              message:
                '依存方向 apps → ui → model → kernel / expression に違反しています(rules/04-設計の規律.md)。',
            },
          ],
        },
      ],
    },
  })),
);
```

- [ ] **手順7: 依存を導入する**

```
pnpm install
```

期待結果: 終了コード 0。`pnpm-lock.yaml` が生成される。出力に `Ignored build scripts` の警告が出ていないこと(出た場合は `onlyBuiltDependencies` の記述場所が違う可能性がある。§1.2-3 の未確認事項。`pnpm-workspace.yaml` で効かなければ `package.json` の `"pnpm": { "onlyBuiltDependencies": ["electron"] }` を試し、**どちらが効いたかを統括へ報告する**)。

- [ ] **手順8: 4つの検査を通す**

```
pnpm run typecheck
pnpm run lint
pnpm run test
pnpm run build
```

期待結果: 4つとも終了コード 0。この時点ではワークスペースにパッケージが1つも無いため、`test` と `build` は「該当なし」で成功する。

- [ ] **手順9: 統括の検査入口でも通ることを確認する**

```
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/check.ps1
```

期待結果: 最後に `[OK] 全ての検査に合格しました` が出て終了コード 0。`[NG] ルート package.json に必須スクリプトがありません` が出ないこと。

- [ ] **手順10: 統括へ報告する**

報告に含める: 作成した6ファイル、`pnpm install` で導入されたパッケージ総数、`onlyBuiltDependencies` がどちらの場所で効いたか、`pnpm-lock.yaml` の `lockfileVersion`、4つの検査の終了コード。**gitへは書き込まない。**

---

### タスク2: `expression` と `help-content` の骨組み

**先行タスク:** 1
**対応要件:** NFR-MA-1、FR-901(ヘルプは Markdown ベースで管理)

**ファイル:**
- 新規: `packages/expression/package.json`
- 新規: `packages/expression/tsconfig.json`
- 新規: `packages/expression/vitest.config.ts`
- 新規: `packages/expression/src/index.ts`
- 新規: `packages/expression/src/version.test.ts`
- 新規: `packages/help-content/package.json`
- 新規: `packages/help-content/tsconfig.json`
- 新規: `packages/help-content/vitest.config.ts`
- 新規: `packages/help-content/src/index.ts`
- 新規: `packages/help-content/src/topics.test.ts`
- 新規: `packages/help-content/docs/ja/viewport.md`
- 変更: `tsconfig.json`(`references` に2行追加)

- [ ] **手順1: `packages/expression/package.json` を作る**

`main` を `./src/index.ts` にしてバンドラ(Vite / Vitest)には TypeScript のソースを直接渡し、`types` は `tsc --build` が出す `./dist/index.d.ts` を指す。これで開発時のビルド待ちが無くなり、型はプロジェクト参照で厳密に解決される。

```json
{
  "name": "@pointercad/expression",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "catalog:"
  }
}
```

- [ ] **手順2: `packages/expression/tsconfig.json` を作る**

```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": "./src",
    "outDir": "./dist"
  },
  "include": ["src"]
}
```

- [ ] **手順3: `packages/expression/vitest.config.ts` を作る**

```ts
import { defineConfig } from 'vitest/config';

export default defineConfig({
  test: {
    environment: 'node',
    include: ['src/**/*.test.ts'],
  },
});
```

- [ ] **手順4: `packages/expression/src/index.ts` を作る**

```ts
/**
 * 式パーサーと任意精度評価(要件 FR-2xx)。評価器の実装は P1。
 * このパッケージは他の @pointercad/* に依存しない純粋ロジックとする。
 */

/** このパッケージが受け付ける式の記法バージョン。保存した式を読み戻すときの互換判定に使う。 */
export const EXPRESSION_SYNTAX_VERSION = 1;

/** 角度の単位。度を既定とする(FR-205)。 */
export type AngleUnit = 'degree' | 'radian';

export const DEFAULT_ANGLE_UNIT: AngleUnit = 'degree';
```

- [ ] **手順5: `packages/expression/src/version.test.ts` を作る**

```ts
import { describe, expect, it } from 'vitest';

import { DEFAULT_ANGLE_UNIT, EXPRESSION_SYNTAX_VERSION } from './index.js';

describe('expression パッケージの骨組み', () => {
  it('式の記法バージョンは 1 である', () => {
    expect(EXPRESSION_SYNTAX_VERSION).toBe(1);
  });

  it('角度の既定単位は度である(FR-205)', () => {
    expect(DEFAULT_ANGLE_UNIT).toBe('degree');
  });
});
```

- [ ] **手順6: `packages/help-content/package.json` を作る**

```json
{
  "name": "@pointercad/help-content",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "catalog:"
  }
}
```

- [ ] **手順7: `packages/help-content/tsconfig.json` と `packages/help-content/vitest.config.ts` を作る**

`tsconfig.json` は手順2 と同じ内容、`vitest.config.ts` は手順3 と同じ内容をそのまま書く。

- [ ] **手順8: `packages/help-content/docs/ja/viewport.md` を作る**

`rules/05-リリース.md` §11.3 のとおり、内部用語(B-rep、テッセレーション、Worker 等)を使わず、利用者が画面で見て操作することだけを書く。

```markdown
# 画面を回す・動かす・拡大する

3D の表示は次の操作で動かせます。

| やりたいこと | 操作 |
|---|---|
| 見る向きを変える | マウスの中ボタンを押しながら動かす |
| 表示を平行に動かす | Shift キーを押しながら、マウスの中ボタンで動かす |
| 拡大・縮小する | マウスホイールを回す |
| 最初の向きに戻す | Home キーを押す |

中ボタンが無いマウスやノートパソコンのタッチパッドでは、次の操作でも同じことができます。

| やりたいこと | 操作 |
|---|---|
| 見る向きを変える | Alt キーを押しながら、左ボタンで動かす |
| 表示を平行に動かす | Alt キーと Shift キーを押しながら、左ボタンで動かす |
| 拡大・縮小する | タッチパッドを2本の指で上下になぞる |

画面の右上には向きを示す立方体があります。「前」「後」「右」「左」「上」「下」と書かれた面をクリックすると、その向きから見た表示に切り替わります。角や辺をクリックすると、斜めから見た表示になります。立方体をそのままドラッグしても向きを変えられます。
```

- [ ] **手順9: `packages/help-content/src/index.ts` を作る**

```ts
/**
 * ヘルプ文書(要件 FR-901)。Markdown を docs/ja 配下で管理する。
 * 機能追加のたびにヘルプの追加を必須とする(NFR-MA-4)。
 */

/** ヘルプの1項目。id はコンテキストヘルプ(FR-903)から参照する。 */
export interface HelpTopic {
  readonly id: string;
  readonly title: string;
  /** packages/help-content からの相対パス。 */
  readonly path: string;
}

export const HELP_TOPICS: readonly HelpTopic[] = [
  { id: 'viewport', title: '画面を回す・動かす・拡大する', path: 'docs/ja/viewport.md' },
];

export function findHelpTopic(id: string): HelpTopic | undefined {
  return HELP_TOPICS.find((topic) => topic.id === id);
}
```

- [ ] **手順10: `packages/help-content/src/topics.test.ts` を作る**

「ヘルプに無い機能はリリースに含めない」(`rules/04`)を守るため、目録に書いたファイルが実在することを機械的に確かめる。

```ts
import { existsSync } from 'node:fs';
import { dirname, resolve } from 'node:path';
import { fileURLToPath } from 'node:url';

import { describe, expect, it } from 'vitest';

import { findHelpTopic, HELP_TOPICS } from './index.js';

const packageRoot = resolve(dirname(fileURLToPath(import.meta.url)), '..');

describe('ヘルプの目録', () => {
  it('目録に載っている Markdown はすべて実在する', () => {
    for (const topic of HELP_TOPICS) {
      expect(existsSync(resolve(packageRoot, topic.path)), topic.path).toBe(true);
    }
  });

  it('id が重複しない', () => {
    const ids = HELP_TOPICS.map((topic) => topic.id);
    expect(new Set(ids).size).toBe(ids.length);
  });

  it('ビューポートの説明を id で引ける(FR-903 の土台)', () => {
    expect(findHelpTopic('viewport')?.path).toBe('docs/ja/viewport.md');
  });

  it('存在しない id では undefined を返す', () => {
    expect(findHelpTopic('この項目はない')).toBeUndefined();
  });
});
```

- [ ] **手順11: ルート `tsconfig.json` の `references` に2件を足す**

```json
{
  "files": [],
  "references": [
    { "path": "./tsconfig.tools.json" },
    { "path": "./packages/expression" },
    { "path": "./packages/help-content" }
  ]
}
```

- [ ] **手順12: 検査する**

```
pnpm install
pnpm run typecheck
pnpm run lint
pnpm run test
pnpm run build
```

期待結果: すべて終了コード 0。`pnpm run test` の出力に **テストファイル 2、テスト 6 件が passed**(`expression` 2件、`help-content` 4件)と表示される。

- [ ] **手順13: 統括へ報告する**

報告に含める: 作成した11ファイルと変更した1ファイル、テストの件数、4つの検査の終了コード。**gitへは書き込まない。**

---

### タスク3: `kernel` の骨組みと OCCT の Node 起動確認

**このタスクが P0 全体で最も不確実性が高い。** 失敗を早く見つけるために他の UI 作業より先に置く。

**先行タスク:** 1
**対応要件:** NFR-MA-2(カーネル演算は Node 上でヘッドレス実行して検証)、要件§6.1(opencascade.js のバージョン固定)

**ファイル:**
- 新規: `packages/kernel/package.json`
- 新規: `packages/kernel/tsconfig.json`
- 新規: `packages/kernel/vitest.config.ts`
- 新規: `packages/kernel/src/index.ts`
- 新規: `packages/kernel/src/types.ts`
- 新規: `packages/kernel/src/vite-env.d.ts`
- 新規: `packages/kernel/src/occt/loadOcct.node.ts`
- 新規: `packages/kernel/src/occt/loadOcct.node.test.ts`
- 変更: `tsconfig.json`(`references` に1行追加)

- [ ] **手順1: 先に実物の型定義を確認する(実装前に必ず行う)**

`pnpm install` 後に、次を Grep して**実際のエクスポート形と名前**を確かめる。**推測で書かない。**

```
node_modules/opencascade.js/dist/node.d.ts
node_modules/opencascade.js/dist/index.d.ts
```

確認すること:
1. `node.d.ts` の既定エクスポートの宣言(`export default function initOpenCascade(...): Promise<OpenCascadeInstance>` の形か、クラスか)。
2. `OpenCascadeInstance` 型の輸出元(`opencascade.js/dist/opencascade.full.js` から `export type` できるか)。
3. `opencascade.full.d.ts` に `BRepPrimAPI_MakeBox_2` が「3つの `Standard_Real` を取るコンストラクタ」として存在するか。

**§1.2-1 の未確認事項。実物が下記の想定と違ったら、勝手に近い名前へ置き換えず、確認した実名を統括へ報告してから実装する。**

- [ ] **手順2: `packages/kernel/package.json` を作る**

```json
{
  "name": "@pointercad/kernel",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "test": "vitest run"
  },
  "dependencies": {
    "comlink": "catalog:",
    "opencascade.js": "catalog:"
  },
  "devDependencies": {
    "vite": "catalog:",
    "vitest": "catalog:"
  }
}
```

`vite` は devDependency に入れる。`?url` の型を `vite/client` から取るため(手順6)。

- [ ] **手順3: `packages/kernel/tsconfig.json` を作る**

```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": "./src",
    "outDir": "./dist",
    "lib": ["ES2022", "DOM", "WebWorker"],
    "types": ["node"]
  },
  "include": ["src"]
}
```

`DOM` と `WebWorker` を両方入れるのは、同じパッケージに Worker 側(`kernel.worker.ts`)と呼び出し側(`createKernelWorker.ts`)が同居するため。

- [ ] **手順4: `packages/kernel/vitest.config.ts` を作る**

```ts
import { defineConfig } from 'vitest/config';

export default defineConfig({
  test: {
    environment: 'node',
    include: ['src/**/*.test.ts'],
    // 50MB 超の WASM のコンパイルに時間がかかる(opencascade.js の既知の性質)。
    // 上限を短くすると環境差で不安定になるため長めに取る。
    testTimeout: 120_000,
    hookTimeout: 180_000,
    // OCCT を1プロセスに1インスタンスだけ作って共有する。
    pool: 'forks',
    poolOptions: { forks: { singleFork: true } },
  },
});
```

- [ ] **手順5: `packages/kernel/src/types.ts` を作る**

```ts
/** 表示用の三角形メッシュ。内部単位は mm(NFR-RE-3)。 */
export interface MeshData {
  /** 頂点座標。x, y, z の順に 3 個ずつ並ぶ。 */
  readonly positions: Float32Array;
  /** 頂点法線。positions と同じ長さ。 */
  readonly normals: Float32Array;
  /** 三角形の頂点番号。3 個ずつ並ぶ。 */
  readonly indices: Uint32Array;
  /** 稜線の線分列。線分1本あたり 6 個(始点 xyz + 終点 xyz)。 */
  readonly edgePositions: Float32Array;
  readonly triangleCount: number;
  readonly faceCount: number;
  readonly edgeCount: number;
}

/** 箱の寸法(mm)。 */
export interface BoxParameters {
  readonly dx: number;
  readonly dy: number;
  readonly dz: number;
}

/** テッセレーションの粗さ。既定値は opencascade.js の実例に合わせる。 */
export interface TessellationOptions {
  /** 弦の最大ずれ(mm)。小さいほど細かい。 */
  readonly linearDeflection?: number;
  /** 角度のずれ(ラジアン)。 */
  readonly angularDeflection?: number;
}

export const DEFAULT_LINEAR_DEFLECTION = 0.1;
export const DEFAULT_ANGULAR_DEFLECTION = 0.5;
```

- [ ] **手順6: `packages/kernel/src/vite-env.d.ts` を作る**

```ts
/// <reference types="vite/client" />
```

これで `import url from '....wasm?url'` が `string` として型付けされる。

- [ ] **手順7: `packages/kernel/src/occt/loadOcct.node.ts` を作る**

```ts
import initOpenCascade from 'opencascade.js/dist/node.js';
import type { OpenCascadeInstance } from 'opencascade.js/dist/opencascade.full.js';

/**
 * Node(Vitest)で OCCT を読み込む。
 * opencascade.js/dist/node.js は __dirname と require を補ってから
 * dist/opencascade.full.wasm を絶対パスで読むため、バンドラなしで動く。
 * 初期化に数秒〜十数秒かかるので、プロセス内で1回だけ実行して使い回す。
 */
let cached: Promise<OpenCascadeInstance> | undefined;

export function loadOcctForNode(): Promise<OpenCascadeInstance> {
  cached ??= initOpenCascade();
  return cached;
}
```

`initOpenCascade()` の戻り値の型が `Promise<OpenCascadeInstance>` にならない場合(手順1 で確認)、`as` による強制変換をせず、**確認した実際の型に合わせて宣言を書き直す**。どうしても合わないときは統括へ報告する。

- [ ] **手順8: `packages/kernel/src/index.ts` を作る**

```ts
export type { BoxParameters, MeshData, TessellationOptions } from './types.js';
export { DEFAULT_ANGULAR_DEFLECTION, DEFAULT_LINEAR_DEFLECTION } from './types.js';
```

- [ ] **手順9: `packages/kernel/src/occt/loadOcct.node.test.ts` を作る**

```ts
import { beforeAll, describe, expect, it } from 'vitest';

import { loadOcctForNode } from './loadOcct.node.js';

describe('Node での OCCT の読み込み', () => {
  let oc: Awaited<ReturnType<typeof loadOcctForNode>>;
  let elapsedMs = 0;

  beforeAll(async () => {
    const startedAt = performance.now();
    oc = await loadOcctForNode();
    elapsedMs = performance.now() - startedAt;
  });

  it('OCCT のインスタンスが得られる', () => {
    expect(oc).toBeTypeOf('object');
  });

  it('箱を作るクラスが束縛されている', () => {
    expect(oc.BRepPrimAPI_MakeBox_2).toBeTypeOf('function');
  });

  it('テッセレーションに使うクラスが束縛されている', () => {
    expect(oc.BRepMesh_IncrementalMesh_2).toBeTypeOf('function');
    expect(oc.TopExp_Explorer_1).toBeTypeOf('function');
    expect(oc.TopLoc_Location_1).toBeTypeOf('function');
    expect(oc.Poly_Connect_2).toBeTypeOf('function');
    expect(oc.TColgp_Array1OfDir_2).toBeTypeOf('function');
    expect(oc.BRep_Tool.Triangulation).toBeTypeOf('function');
    expect(oc.StdPrs_ToolTriangulatedShape.Normal).toBeTypeOf('function');
    expect(oc.TopoDS.Face_1).toBeTypeOf('function');
  });

  it('2回目の読み込みは同じインスタンスを返す', async () => {
    expect(await loadOcctForNode()).toBe(oc);
  });

  it('初期化の実測時間を記録する(上限は vitest.config.ts の testTimeout)', () => {
    // 数値そのものは環境差が大きいので上限判定はしない。統括への報告用に出力する。
    console.log(`OCCT 初期化時間: ${elapsedMs.toFixed(0)} ms`);
    expect(elapsedMs).toBeGreaterThan(0);
  });
});
```

- [ ] **手順10: ルート `tsconfig.json` の `references` に `{ "path": "./packages/kernel" }` を足す**

- [ ] **手順11: 検査する**

```
pnpm install
pnpm --filter @pointercad/kernel run test
pnpm run typecheck
pnpm run lint
pnpm run test
pnpm run build
```

期待結果: `kernel` のテストが **5 件すべて passed**。`console.log` に `OCCT 初期化時間: N ms` が出る。全体検査4つが終了コード 0。

- [ ] **手順12: 統括へ報告する(重要)**

報告に含める:
- 手順1 で確認した `node.d.ts` の既定エクスポートの実際の宣言(そのまま引用する)。
- `OCCT 初期化時間` の実測値(ms)。
- `pnpm --filter @pointercad/kernel run test` の合計所要時間。
- 束縛されていなかったクラスがあればその名前(1つでもあれば §2.2 の代替案へ進む判断材料になる)。
- `node_modules/opencascade.js/dist/opencascade.full.wasm` の実測ファイルサイズ。

**gitへは書き込まない。**

---

### タスク4: 箱の生成とテッセレーション(Node ヘッドレス検証)

**先行タスク:** 3
**対応要件:** NFR-MA-2、NFR-RE-3(内部単位は mm)

**ファイル:**
- 新規: `packages/kernel/src/occt/makeBox.ts`
- 新規: `packages/kernel/src/occt/tessellate.ts`
- 新規: `packages/kernel/src/occt/extractEdges.ts`
- 新規: `packages/kernel/src/occt/boxTessellation.test.ts`
- 変更: `packages/kernel/src/index.ts`

- [ ] **手順1: 実物の型定義でクラス名とオーバーロード番号を確認する(実装前に必ず行う)**

`node_modules/opencascade.js/dist/opencascade.full.d.ts` を Grep して次を確かめる。**§1.2-2 の未確認事項。**

| 確認するもの | 想定 |
|---|---|
| `BRepPrimAPI_MakeBox_` の各オーバーロード | `_2` が `(dx: number, dy: number, dz: number)` |
| `BRepAdaptor_Curve_` の各オーバーロード | `_2` が `(E: TopoDS_Edge)` |
| `GCPnts_TangentialDeflection_` の各オーバーロード | `_2` が `(C, AngularDeflection, CurvatureDeflection, MinimumOfPoints, UTol, theMinLen)` |
| `Poly_Triangulation` のメンバ | `NbNodes()` / `Node(i)` / `NbTriangles()` / `Triangles()` |

想定と違ったら**近い名前で当てずっぽうに置き換えず、確認した実名を統括へ報告してから実装する**(`rules/06-過去の失敗と対策.md`「実在しないファイル名・関数名で無効な作業をさせる」の再発防止)。

なお、下記の実装は opencascade.js 公式サンプル `opencascade.js-examples/src/common/visualize.js` の実コードを基にしており、`TopExp_Explorer_1` + `Init()`、`TopoDS.Face_1`、`BRepMesh_IncrementalMesh_2(face, 0.1, false, 0.5, false)`、`BRep_Tool.Triangulation(face, location, 0)`、`Poly_Connect_2`、`StdPrs_ToolTriangulatedShape.Normal`、`TColgp_Array1OfDir_2`、`face.Orientation_1()`、`triangulation.Triangles()` は動作実績のある呼び方である。

- [ ] **手順2: `packages/kernel/src/occt/makeBox.ts` を作る**

embind のオブジェクトは自動で解放されないため、生成物と一緒に解放手続きを返す。

```ts
import type { OpenCascadeInstance, TopoDS_Shape } from 'opencascade.js/dist/opencascade.full.js';

import type { BoxParameters } from '../types.js';

/** OCCT の形状と、その形状を作るために確保した領域の解放手続き。 */
export interface OcctShapeHandle {
  readonly shape: TopoDS_Shape;
  delete(): void;
}

/**
 * 原点を角とする直方体を作る(FR-401 の押し出しに先立つ P0 の確認用形状)。
 * 単位は mm(NFR-RE-3)。
 */
export function makeBox(oc: OpenCascadeInstance, parameters: BoxParameters): OcctShapeHandle {
  const { dx, dy, dz } = parameters;
  if (!(dx > 0) || !(dy > 0) || !(dz > 0)) {
    throw new Error(`箱の寸法は正の数である必要があります: dx=${dx}, dy=${dy}, dz=${dz}`);
  }

  const maker = new oc.BRepPrimAPI_MakeBox_2(dx, dy, dz);
  const shape = maker.Shape();

  return {
    shape,
    delete(): void {
      shape.delete();
      maker.delete();
    },
  };
}
```

- [ ] **手順3: `packages/kernel/src/occt/tessellate.ts` を作る**

```ts
import type { OpenCascadeInstance, TopoDS_Shape } from 'opencascade.js/dist/opencascade.full.js';

import {
  DEFAULT_ANGULAR_DEFLECTION,
  DEFAULT_LINEAR_DEFLECTION,
  type TessellationOptions,
} from '../types.js';

export interface SurfaceMesh {
  readonly positions: Float32Array;
  readonly normals: Float32Array;
  readonly indices: Uint32Array;
  readonly triangleCount: number;
  readonly faceCount: number;
}

/**
 * 形状の全ての面を三角形メッシュへ変換し、1つのバッファへまとめる。
 * OCCT の配列は 1 始まり、three.js の頂点番号は 0 始まりなので変換時に 1 を引く。
 */
export function tessellate(
  oc: OpenCascadeInstance,
  shape: TopoDS_Shape,
  options: TessellationOptions = {},
): SurfaceMesh {
  const linearDeflection = options.linearDeflection ?? DEFAULT_LINEAR_DEFLECTION;
  const angularDeflection = options.angularDeflection ?? DEFAULT_ANGULAR_DEFLECTION;

  const positions: number[] = [];
  const normals: number[] = [];
  const indices: number[] = [];
  let faceCount = 0;

  const incrementalMesh = new oc.BRepMesh_IncrementalMesh_2(
    shape,
    linearDeflection,
    false,
    angularDeflection,
    false,
  );

  const explorer = new oc.TopExp_Explorer_1();
  for (
    explorer.Init(shape, oc.TopAbs_ShapeEnum.TopAbs_FACE, oc.TopAbs_ShapeEnum.TopAbs_SHAPE);
    explorer.More();
    explorer.Next()
  ) {
    const face = oc.TopoDS.Face_1(explorer.Current());
    const location = new oc.TopLoc_Location_1();
    const triangulationHandle = oc.BRep_Tool.Triangulation(face, location, 0);

    if (triangulationHandle.IsNull()) {
      triangulationHandle.delete();
      location.delete();
      face.delete();
      continue;
    }

    const triangulation = triangulationHandle.get();
    const nodeCount = triangulation.NbNodes();
    const nodeOffset = positions.length / 3;
    const transformation = location.Transformation();

    for (let i = 1; i <= nodeCount; i += 1) {
      const node = triangulation.Node(i);
      const moved = node.Transformed(transformation);
      positions.push(moved.X(), moved.Y(), moved.Z());
      node.delete();
      moved.delete();
    }

    const polyConnect = new oc.Poly_Connect_2(triangulationHandle);
    const nodeNormals = new oc.TColgp_Array1OfDir_2(1, nodeCount);
    oc.StdPrs_ToolTriangulatedShape.Normal(face, polyConnect, nodeNormals);
    for (let i = nodeNormals.Lower(); i <= nodeNormals.Upper(); i += 1) {
      const direction = nodeNormals.Value(i);
      const moved = direction.Transformed(transformation);
      normals.push(moved.X(), moved.Y(), moved.Z());
      direction.delete();
      moved.delete();
    }

    // 面の向きが反転している場合は、三角形の頂点順を入れ替えて表を外向きに揃える。
    const reversed = face.Orientation_1() !== oc.TopAbs_Orientation.TopAbs_FORWARD;
    const triangles = triangulation.Triangles();
    for (let i = 1; i <= triangulation.NbTriangles(); i += 1) {
      const triangle = triangles.Value(i);
      const a = nodeOffset + triangle.Value(1) - 1;
      const b = nodeOffset + triangle.Value(2) - 1;
      const c = nodeOffset + triangle.Value(3) - 1;
      if (reversed) {
        indices.push(b, a, c);
      } else {
        indices.push(a, b, c);
      }
      triangle.delete();
    }

    triangles.delete();
    nodeNormals.delete();
    polyConnect.delete();
    transformation.delete();
    triangulationHandle.delete();
    location.delete();
    face.delete();
    faceCount += 1;
  }

  explorer.delete();
  incrementalMesh.delete();

  return {
    positions: new Float32Array(positions),
    normals: new Float32Array(normals),
    indices: new Uint32Array(indices),
    triangleCount: indices.length / 3,
    faceCount,
  };
}
```

- [ ] **手順4: `packages/kernel/src/occt/extractEdges.ts` を作る**

```ts
import type { OpenCascadeInstance, TopoDS_Shape } from 'opencascade.js/dist/opencascade.full.js';

import {
  DEFAULT_ANGULAR_DEFLECTION,
  DEFAULT_LINEAR_DEFLECTION,
  type TessellationOptions,
} from '../types.js';

export interface EdgeLines {
  /** 線分1本あたり 6 個(始点 xyz + 終点 xyz)。 */
  readonly positions: Float32Array;
  readonly edgeCount: number;
}

/** 稜線を折れ線へ分解する(FR-105 のワイヤーフレーム/シェーディング+エッジ表示に使う)。 */
export function extractEdges(
  oc: OpenCascadeInstance,
  shape: TopoDS_Shape,
  options: TessellationOptions = {},
): EdgeLines {
  const linearDeflection = options.linearDeflection ?? DEFAULT_LINEAR_DEFLECTION;
  const angularDeflection = options.angularDeflection ?? DEFAULT_ANGULAR_DEFLECTION;

  const segments: number[] = [];
  let edgeCount = 0;

  const explorer = new oc.TopExp_Explorer_1();
  for (
    explorer.Init(shape, oc.TopAbs_ShapeEnum.TopAbs_EDGE, oc.TopAbs_ShapeEnum.TopAbs_SHAPE);
    explorer.More();
    explorer.Next()
  ) {
    const edge = oc.TopoDS.Edge_1(explorer.Current());
    const adaptor = new oc.BRepAdaptor_Curve_2(edge);
    const discretizer = new oc.GCPnts_TangentialDeflection_2(
      adaptor,
      angularDeflection,
      linearDeflection,
      2,
      1.0e-9,
      1.0e-7,
    );

    for (let i = 1; i < discretizer.NbPoints(); i += 1) {
      const from = discretizer.Value(i);
      const to = discretizer.Value(i + 1);
      segments.push(from.X(), from.Y(), from.Z(), to.X(), to.Y(), to.Z());
      from.delete();
      to.delete();
    }

    discretizer.delete();
    adaptor.delete();
    edge.delete();
    edgeCount += 1;
  }
  explorer.delete();

  return { positions: new Float32Array(segments), edgeCount };
}
```

- [ ] **手順5: `packages/kernel/src/index.ts` を更新する**

```ts
export type { BoxParameters, MeshData, TessellationOptions } from './types.js';
export { DEFAULT_ANGULAR_DEFLECTION, DEFAULT_LINEAR_DEFLECTION } from './types.js';
export { makeBox, type OcctShapeHandle } from './occt/makeBox.js';
export { tessellate, type SurfaceMesh } from './occt/tessellate.js';
export { extractEdges, type EdgeLines } from './occt/extractEdges.js';
```

- [ ] **手順6: `packages/kernel/src/occt/boxTessellation.test.ts` を作る(先に書いて失敗させる)**

10 × 20 × 30 mm の箱の期待値の根拠: 直方体の各平面は境界の 4 頂点だけで三角形分割できるので、面ごとに **節点 4 個 / 三角形 2 個**。6 面で節点 24 個、三角形 12 個。座標は 24 × 3 = 72 個、頂点番号は 12 × 3 = 36 個。稜線は 12 本で、すべて直線なので分割点は各 2 点 = 線分 1 本 = 6 個 → 72 個。

```ts
import { beforeAll, describe, expect, it } from 'vitest';

import { extractEdges } from './extractEdges.js';
import { loadOcctForNode } from './loadOcct.node.js';
import { makeBox } from './makeBox.js';
import { tessellate } from './tessellate.js';

const BOX = { dx: 10, dy: 20, dz: 30 } as const;

function boundingBox(positions: Float32Array): { min: number[]; max: number[] } {
  const min = [Number.POSITIVE_INFINITY, Number.POSITIVE_INFINITY, Number.POSITIVE_INFINITY];
  const max = [Number.NEGATIVE_INFINITY, Number.NEGATIVE_INFINITY, Number.NEGATIVE_INFINITY];
  for (let i = 0; i < positions.length; i += 3) {
    for (let axis = 0; axis < 3; axis += 1) {
      const value = positions[i + axis] as number;
      min[axis] = Math.min(min[axis] as number, value);
      max[axis] = Math.max(max[axis] as number, value);
    }
  }
  return { min, max };
}

describe('10 x 20 x 30 mm の箱のテッセレーション', () => {
  let oc: Awaited<ReturnType<typeof loadOcctForNode>>;

  beforeAll(async () => {
    oc = await loadOcctForNode();
  });

  it('寸法が 0 以下なら理由つきで失敗する(NFR-UX-5)', () => {
    expect(() => makeBox(oc, { dx: 0, dy: 1, dz: 1 })).toThrow(/箱の寸法は正の数/);
    expect(() => makeBox(oc, { dx: 1, dy: -2, dz: 1 })).toThrow(/箱の寸法は正の数/);
  });

  it('面 6 枚・三角形 12 枚・節点 24 個になる', () => {
    const handle = makeBox(oc, BOX);
    try {
      const mesh = tessellate(oc, handle.shape);
      expect(mesh.faceCount).toBe(6);
      expect(mesh.triangleCount).toBe(12);
      expect(mesh.positions.length).toBe(72);
      expect(mesh.normals.length).toBe(72);
      expect(mesh.indices.length).toBe(36);
    } finally {
      handle.delete();
    }
  });

  it('頂点番号がすべて節点数の範囲に収まる', () => {
    const handle = makeBox(oc, BOX);
    try {
      const mesh = tessellate(oc, handle.shape);
      const nodeCount = mesh.positions.length / 3;
      for (const index of mesh.indices) {
        expect(index).toBeLessThan(nodeCount);
      }
    } finally {
      handle.delete();
    }
  });

  it('外接直方体が (0,0,0)-(10,20,30) になる', () => {
    const handle = makeBox(oc, BOX);
    try {
      const { min, max } = boundingBox(tessellate(oc, handle.shape).positions);
      expect(min[0]).toBeCloseTo(0, 6);
      expect(min[1]).toBeCloseTo(0, 6);
      expect(min[2]).toBeCloseTo(0, 6);
      expect(max[0]).toBeCloseTo(BOX.dx, 6);
      expect(max[1]).toBeCloseTo(BOX.dy, 6);
      expect(max[2]).toBeCloseTo(BOX.dz, 6);
    } finally {
      handle.delete();
    }
  });

  it('法線がすべて単位ベクトルで、成分が軸方向のいずれかになる', () => {
    const handle = makeBox(oc, BOX);
    try {
      const { normals } = tessellate(oc, handle.shape);
      for (let i = 0; i < normals.length; i += 3) {
        const x = normals[i] as number;
        const y = normals[i + 1] as number;
        const z = normals[i + 2] as number;
        expect(Math.hypot(x, y, z)).toBeCloseTo(1, 6);
        // 直方体なので、どの法線も軸に平行(絶対値の和が 1)。
        expect(Math.abs(x) + Math.abs(y) + Math.abs(z)).toBeCloseTo(1, 6);
      }
    } finally {
      handle.delete();
    }
  });

  it('稜線 12 本が線分 12 本として取り出せる', () => {
    const handle = makeBox(oc, BOX);
    try {
      const edges = extractEdges(oc, handle.shape);
      expect(edges.edgeCount).toBe(12);
      expect(edges.positions.length).toBe(72);
      const { min, max } = boundingBox(edges.positions);
      expect(min[0]).toBeCloseTo(0, 6);
      expect(max[2]).toBeCloseTo(BOX.dz, 6);
    } finally {
      handle.delete();
    }
  });
});
```

- [ ] **手順7: テストが落ちることを確かめる**

```
pnpm --filter @pointercad/kernel run test
```

期待結果: `boxTessellation.test.ts` が「モジュールが見つからない」または関数未定義で失敗する(手順2〜4 をまだ書いていない場合)。手順2〜4 を先に書いた場合はこの手順を飛ばしてよい。

- [ ] **手順8: テストを通す**

```
pnpm --filter @pointercad/kernel run test
```

期待結果: `loadOcct.node.test.ts` 5 件 + `boxTessellation.test.ts` 6 件 = **11 件すべて passed**。

**実測が期待値と違ったとき**(例: 三角形が 12 でなく別の数になる)は、`toBe(12)` を実測値へ書き換えてはならない(`rules/02-禁止事項.md`「テストの期待値を安易に書き換えて赤を消さない」)。実測値と、それがどの面で起きたかを統括へ報告し、判断を仰ぐ。

- [ ] **手順9: 全体検査を通す**

```
pnpm run typecheck
pnpm run lint
pnpm run test
pnpm run build
```

期待結果: 4つとも終了コード 0。

- [ ] **手順10: 統括へ報告する**

報告に含める: 手順1 で確認したオーバーロード番号(想定と違った場合は実名)、テスト 11 件の結果、`kernel` のテスト全体の実測所要時間、期待値と実測が食い違った項目があればその数値。**gitへは書き込まない。**

---

### タスク5: `kernel` の Worker + Comlink + ブラウザ用ローダ

**先行タスク:** 4
**対応要件:** NFR-PF-4(幾何カーネルは Web Worker 上で実行し、計算中も UI がフリーズしない)、要件§6.1(カーネル実行 = Web Worker + Comlink)

**ファイル:**
- 新規: `packages/kernel/src/occt/loadOcct.browser.ts`
- 新規: `packages/kernel/src/worker/kernelApi.ts`
- 新規: `packages/kernel/src/worker/kernel.worker.ts`
- 新規: `packages/kernel/src/worker/kernelApi.test.ts`
- 新規: `packages/kernel/src/client/createKernelWorker.ts`
- 変更: `packages/kernel/src/index.ts`

- [ ] **手順1: `packages/kernel/src/occt/loadOcct.browser.ts` を作る**

`§2.1` の採用方式。**バンドラに Emscripten のグルーコードを解析させない**ことが要点。

```ts
import ocGlueUrl from 'opencascade.js/dist/opencascade.full.js?url';
import ocWasmUrl from 'opencascade.js/dist/opencascade.full.wasm?url';
import type { OpenCascadeInstance } from 'opencascade.js/dist/opencascade.full.js';

/**
 * Emscripten が出力したグルーコードのファクトリ。
 * opencascade.js の dist/index.js が `new mainJS({ locateFile })` として呼んでいる形に合わせる。
 */
type OcctModuleFactory = new (moduleOptions: {
  locateFile(path: string): string;
}) => Promise<OpenCascadeInstance>;

let cached: Promise<OpenCascadeInstance> | undefined;

/**
 * ブラウザ(および Web Worker)で OCCT を読み込む。
 *
 * グルーコードと wasm を ?url で「場所」としてだけ受け取り、グルーコードは実行時の
 * import() で読む。こうするとバンドラはこの 2 ファイルを資産として出力するだけで
 * 中身を解析しないため、グルーコードに含まれる Node 専用の分岐(fs / path / module の
 * 読み込み)がブラウザ向けビルドで解決できずに失敗する問題が起きない。
 */
export function loadOcctForBrowser(): Promise<OpenCascadeInstance> {
  cached ??= (async (): Promise<OpenCascadeInstance> => {
    const glueModule = (await import(/* @vite-ignore */ ocGlueUrl)) as {
      default: OcctModuleFactory;
    };
    return await new glueModule.default({
      locateFile: (path: string): string => (path.endsWith('.wasm') ? ocWasmUrl : path),
    });
  })();
  return cached;
}
```

- [ ] **手順2: `packages/kernel/src/worker/kernelApi.ts` を作る**

Worker の中身と、Node で直接呼べる実装を分ける。こうすると Worker を起動できない Node 環境でも API の中身をテストできる。

```ts
import type { OpenCascadeInstance } from 'opencascade.js/dist/opencascade.full.js';

import { extractEdges } from '../occt/extractEdges.js';
import { makeBox } from '../occt/makeBox.js';
import { tessellate } from '../occt/tessellate.js';
import type { BoxParameters, MeshData, TessellationOptions } from '../types.js';

/** UI 側から Comlink 越しに呼べる幾何カーネルの窓口。 */
export interface KernelApi {
  /** 直方体を作って表示用メッシュを返す。 */
  tessellateBox(parameters: BoxParameters, options?: TessellationOptions): Promise<MeshData>;
}

/**
 * OCCT の読み込み手続きを受け取って KernelApi を組み立てる。
 * ブラウザでは loadOcctForBrowser、Node のテストでは loadOcctForNode を渡す。
 */
export function createKernelApi(loadOcct: () => Promise<OpenCascadeInstance>): KernelApi {
  return {
    async tessellateBox(parameters, options = {}): Promise<MeshData> {
      const oc = await loadOcct();
      const handle = makeBox(oc, parameters);
      try {
        const surface = tessellate(oc, handle.shape, options);
        const edges = extractEdges(oc, handle.shape, options);
        return {
          positions: surface.positions,
          normals: surface.normals,
          indices: surface.indices,
          edgePositions: edges.positions,
          triangleCount: surface.triangleCount,
          faceCount: surface.faceCount,
          edgeCount: edges.edgeCount,
        };
      } finally {
        handle.delete();
      }
    },
  };
}
```

- [ ] **手順3: `packages/kernel/src/worker/kernel.worker.ts` を作る**

```ts
import * as Comlink from 'comlink';

import { loadOcctForBrowser } from '../occt/loadOcct.browser.js';
import { createKernelApi } from './kernelApi.js';

// UI スレッドでは幾何演算をしない(rules/04-設計の規律.md、NFR-PF-4)。
Comlink.expose(createKernelApi(loadOcctForBrowser));
```

- [ ] **手順4: `packages/kernel/src/client/createKernelWorker.ts` を作る**

`new Worker(new URL('...', import.meta.url), { type: 'module' })` はこの字面のままでないとバンドラが Worker として認識しない。**書き換えない。**

```ts
/**
 * 幾何カーネルの Worker を起動する。
 * new Worker(new URL(...), { type: 'module' }) はこの字面のままでバンドラが解釈するため、
 * 変数へ切り出したり文字列を組み立てたりしてはいけない。
 */
export function createKernelWorker(): Worker {
  return new Worker(new URL('../worker/kernel.worker.js', import.meta.url), {
    type: 'module',
    name: 'pointercad-kernel',
  });
}
```

`.js` 拡張子で書くのは `verbatimModuleSyntax` + `moduleResolution: bundler` の TypeScript 規約に合わせるため。Vite は `.js` を同名の `.ts` へ解決する。**もしタスク15 のビルドで Worker が解決できなかった場合は `.ts` へ変えて再確認し、どちらで通ったかを統括へ報告する。**

- [ ] **手順5: `packages/kernel/src/worker/kernelApi.test.ts` を作る**

```ts
import { describe, expect, it } from 'vitest';

import { loadOcctForNode } from '../occt/loadOcct.node.js';
import { createKernelApi } from './kernelApi.js';

describe('KernelApi', () => {
  const api = createKernelApi(loadOcctForNode);

  it('箱のメッシュを1回の呼び出しで面・稜線ともに返す', async () => {
    const mesh = await api.tessellateBox({ dx: 10, dy: 20, dz: 30 });
    expect(mesh.faceCount).toBe(6);
    expect(mesh.triangleCount).toBe(12);
    expect(mesh.edgeCount).toBe(12);
    expect(mesh.positions.length).toBe(72);
    expect(mesh.indices.length).toBe(36);
    expect(mesh.edgePositions.length).toBe(72);
  });

  it('粗さを指定しても直方体の三角形数は変わらない', async () => {
    const mesh = await api.tessellateBox(
      { dx: 10, dy: 20, dz: 30 },
      { linearDeflection: 0.01, angularDeflection: 0.1 },
    );
    expect(mesh.triangleCount).toBe(12);
  });

  it('寸法が不正なら理由つきで失敗する', async () => {
    await expect(api.tessellateBox({ dx: -1, dy: 1, dz: 1 })).rejects.toThrow(/箱の寸法は正の数/);
  });
});
```

- [ ] **手順6: `packages/kernel/src/index.ts` を更新する**

```ts
export type { BoxParameters, MeshData, TessellationOptions } from './types.js';
export { DEFAULT_ANGULAR_DEFLECTION, DEFAULT_LINEAR_DEFLECTION } from './types.js';
export { makeBox, type OcctShapeHandle } from './occt/makeBox.js';
export { tessellate, type SurfaceMesh } from './occt/tessellate.js';
export { extractEdges, type EdgeLines } from './occt/extractEdges.js';
export { createKernelApi, type KernelApi } from './worker/kernelApi.js';
export { createKernelWorker } from './client/createKernelWorker.js';
```

`loadOcct.browser.js` と `loadOcct.node.js` は輸出しない。環境に依存する読み込み手続きをパッケージの外へ出さないため。

- [ ] **手順7: 検査する**

```
pnpm --filter @pointercad/kernel run test
pnpm run typecheck
pnpm run lint
pnpm run test
pnpm run build
```

期待結果: `kernel` のテストが **14 件すべて passed**(11 + 3)。全体検査4つが終了コード 0。この時点では Worker の実動作は確認できない(タスク15 のビルドとタスク17 のスモークで確認する)。

- [ ] **手順8: 統括へ報告する**

報告に含める: 作成した5ファイルと変更した1ファイル、テスト 14 件の結果、`loadOcct.browser.ts` が型検査を通ったか(`?url` の型が `vite/client` から取れたか)。**gitへは書き込まない。**

---

### タスク6: `model` パッケージ(ドキュメントモデルと再計算)

**先行タスク:** 5
**対応要件:** NFR-MA-1(UI から幾何カーネルへ直接依存しない)、FR-501(フィーチャーツリー)、要件§6.3(データフロー)

**ファイル:**
- 新規: `packages/model/package.json`
- 新規: `packages/model/tsconfig.json`
- 新規: `packages/model/vitest.config.ts`
- 新規: `packages/model/src/index.ts`
- 新規: `packages/model/src/types.ts`
- 新規: `packages/model/src/kernelBridge.ts`
- 新規: `packages/model/src/createBoxPartDocument.ts`
- 新規: `packages/model/src/createBoxPartDocument.test.ts`
- 新規: `packages/model/src/recompute.ts`
- 新規: `packages/model/src/recompute.test.ts`
- 変更: `tsconfig.json`(`references` に1行追加)

- [ ] **手順1: `packages/model/package.json` を作る**

```json
{
  "name": "@pointercad/model",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "test": "vitest run"
  },
  "dependencies": {
    "@pointercad/expression": "workspace:*",
    "@pointercad/kernel": "workspace:*",
    "comlink": "catalog:"
  },
  "devDependencies": {
    "vitest": "catalog:"
  }
}
```

- [ ] **手順2: `packages/model/tsconfig.json` を作る**

`references` に書いたパッケージ以外は import できなくなる。これが依存方向の型レベルの施行になる。

```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": "./src",
    "outDir": "./dist",
    "lib": ["ES2022", "DOM"],
    "types": ["node"]
  },
  "include": ["src"],
  "references": [
    { "path": "../expression" },
    { "path": "../kernel" }
  ]
}
```

- [ ] **手順3: `packages/model/vitest.config.ts` を作る**

```ts
import { defineConfig } from 'vitest/config';

export default defineConfig({
  test: {
    environment: 'node',
    include: ['src/**/*.test.ts'],
  },
});
```

- [ ] **手順4: `packages/model/src/types.ts` を作る**

`kernel` の型をそのまま再輸出せず、`model` が自分の型を持つ。これで `ui` は `kernel` の存在を知らずに済む(`rules/04-設計の規律.md`)。

```ts
/** 表示用のメッシュ。model が UI へ渡す唯一の幾何データ形式。 */
export interface PartMesh {
  readonly positions: Float32Array;
  readonly normals: Float32Array;
  readonly indices: Uint32Array;
  /** 稜線の線分列。線分1本あたり 6 個。 */
  readonly edgePositions: Float32Array;
  readonly triangleCount: number;
}

/** 直方体フィーチャー。P0 で扱う唯一の種類。 */
export interface BoxFeature {
  readonly id: string;
  readonly kind: 'box';
  /** フィーチャーツリーに表示する名前(FR-501)。 */
  readonly name: string;
  readonly dx: number;
  readonly dy: number;
  readonly dz: number;
}

export type Feature = BoxFeature;

/** 部品(パート)ドキュメント。フィーチャー履歴を 1 本持つ(要件§2)。 */
export interface PartDocument {
  readonly id: string;
  readonly name: string;
  readonly features: readonly Feature[];
}

/** 再計算の結果。失敗しても例外にせず、理由を持ち回る(FR-504、NFR-RE-1)。 */
export type RecomputeResult =
  | { readonly status: 'ok'; readonly mesh: PartMesh }
  | { readonly status: 'error'; readonly featureId: string; readonly message: string };
```

- [ ] **手順5: `packages/model/src/kernelBridge.ts` を作る**

```ts
import { createKernelWorker, type KernelApi } from '@pointercad/kernel';
import * as Comlink from 'comlink';

import type { PartMesh } from './types.js';

/** model から幾何カーネルへの唯一の接点。ここ以外から kernel を呼ばない。 */
export interface KernelBridge {
  tessellateBox(dx: number, dy: number, dz: number): Promise<PartMesh>;
  dispose(): void;
}

/** Web Worker 内の幾何カーネルへつなぐ。ブラウザ・Electron のレンダラでのみ使える。 */
export function createKernelBridge(): KernelBridge {
  const worker = createKernelWorker();
  const remote = Comlink.wrap<KernelApi>(worker);

  return {
    async tessellateBox(dx, dy, dz): Promise<PartMesh> {
      const mesh = await remote.tessellateBox({ dx, dy, dz });
      return {
        positions: mesh.positions,
        normals: mesh.normals,
        indices: mesh.indices,
        edgePositions: mesh.edgePositions,
        triangleCount: mesh.triangleCount,
      };
    },
    dispose(): void {
      remote[Comlink.releaseProxy]();
      worker.terminate();
    },
  };
}
```

- [ ] **手順6: `packages/model/src/createBoxPartDocument.ts` を作る**

```ts
import type { PartDocument } from './types.js';

/** P0 の確認用の既定寸法(mm)。 */
export const DEFAULT_BOX_SIZE = { dx: 40, dy: 60, dz: 20 } as const;

/** 直方体 1 個だけの部品ドキュメントを作る。P2 で作るフィーチャー履歴の最小形。 */
export function createBoxPartDocument(
  size: { dx: number; dy: number; dz: number } = DEFAULT_BOX_SIZE,
): PartDocument {
  return {
    id: 'part-1',
    name: '部品1',
    features: [
      {
        id: 'feature-1',
        kind: 'box',
        name: '直方体1',
        dx: size.dx,
        dy: size.dy,
        dz: size.dz,
      },
    ],
  };
}
```

- [ ] **手順7: `packages/model/src/recompute.ts` を作る**

```ts
import type { KernelBridge } from './kernelBridge.js';
import type { PartDocument, RecomputeResult } from './types.js';

/**
 * フィーチャー履歴を先頭から順に評価してメッシュを得る(要件§6.3)。
 * 失敗しても例外を投げずエラー箇所を返す(FR-504、NFR-RE-1)。
 */
export async function recomputePart(
  document: PartDocument,
  bridge: KernelBridge,
): Promise<RecomputeResult> {
  const feature = document.features.at(-1);
  if (feature === undefined) {
    return { status: 'error', featureId: '', message: 'フィーチャーが1つもありません。' };
  }

  try {
    const mesh = await bridge.tessellateBox(feature.dx, feature.dy, feature.dz);
    return { status: 'ok', mesh };
  } catch (error) {
    return {
      status: 'error',
      featureId: feature.id,
      message: error instanceof Error ? error.message : String(error),
    };
  }
}
```

- [ ] **手順8: `packages/model/src/createBoxPartDocument.test.ts` を作る**

```ts
import { describe, expect, it } from 'vitest';

import { createBoxPartDocument, DEFAULT_BOX_SIZE } from './createBoxPartDocument.js';

describe('直方体1個の部品ドキュメント', () => {
  it('既定寸法は 40 x 60 x 20 mm', () => {
    expect(DEFAULT_BOX_SIZE).toEqual({ dx: 40, dy: 60, dz: 20 });
  });

  it('フィーチャーを1件だけ持つ', () => {
    const document = createBoxPartDocument();
    expect(document.features).toHaveLength(1);
    expect(document.features[0]?.kind).toBe('box');
    expect(document.features[0]?.name).toBe('直方体1');
  });

  it('寸法を指定するとフィーチャーへ反映される', () => {
    const document = createBoxPartDocument({ dx: 1, dy: 2, dz: 3 });
    expect(document.features[0]).toMatchObject({ dx: 1, dy: 2, dz: 3 });
  });
});
```

- [ ] **手順9: `packages/model/src/recompute.test.ts` を作る**

Worker は Node で起動できないので、`KernelBridge` の偽物を渡して再計算の筋だけを検証する。OCCT を読み込まないので高速に終わる。

```ts
import { describe, expect, it, vi } from 'vitest';

import { createBoxPartDocument } from './createBoxPartDocument.js';
import type { KernelBridge } from './kernelBridge.js';
import { recomputePart } from './recompute.js';
import type { PartMesh } from './types.js';

const EMPTY_MESH: PartMesh = {
  positions: new Float32Array([0, 0, 0]),
  normals: new Float32Array([0, 0, 1]),
  indices: new Uint32Array([0, 0, 0]),
  edgePositions: new Float32Array([0, 0, 0, 1, 0, 0]),
  triangleCount: 1,
};

function createFakeBridge(
  tessellateBox: KernelBridge['tessellateBox'],
): KernelBridge {
  return { tessellateBox, dispose: vi.fn() };
}

describe('部品の再計算', () => {
  it('フィーチャーの寸法をそのままカーネルへ渡す', async () => {
    const tessellateBox = vi.fn(async () => EMPTY_MESH);
    const result = await recomputePart(
      createBoxPartDocument({ dx: 11, dy: 22, dz: 33 }),
      createFakeBridge(tessellateBox),
    );

    expect(tessellateBox).toHaveBeenCalledWith(11, 22, 33);
    expect(result.status).toBe('ok');
  });

  it('カーネルが失敗しても例外を投げず、エラー箇所と理由を返す(FR-504)', async () => {
    const result = await recomputePart(
      createBoxPartDocument(),
      createFakeBridge(async () => {
        throw new Error('フィレットが成立しません');
      }),
    );

    expect(result).toEqual({
      status: 'error',
      featureId: 'feature-1',
      message: 'フィレットが成立しません',
    });
  });

  it('フィーチャーが空なら理由つきで失敗する', async () => {
    const result = await recomputePart(
      { id: 'part-1', name: '部品1', features: [] },
      createFakeBridge(async () => EMPTY_MESH),
    );

    expect(result.status).toBe('error');
  });
});
```

- [ ] **手順10: `packages/model/src/index.ts` を作り、ルート `tsconfig.json` に参照を足す**

```ts
export type {
  BoxFeature,
  Feature,
  PartDocument,
  PartMesh,
  RecomputeResult,
} from './types.js';
export { createBoxPartDocument, DEFAULT_BOX_SIZE } from './createBoxPartDocument.js';
export { createKernelBridge, type KernelBridge } from './kernelBridge.js';
export { recomputePart } from './recompute.js';
```

ルート `tsconfig.json` の `references` へ `{ "path": "./packages/model" }` を足す。

- [ ] **手順11: 検査する**

```
pnpm install
pnpm --filter @pointercad/model run test
pnpm run typecheck
pnpm run lint
pnpm run test
pnpm run build
```

期待結果: `model` のテストが **6 件すべて passed**、実測 1 秒未満(OCCT を読み込まないため)。全体検査4つが終了コード 0。

- [ ] **手順12: 依存方向の施行が効くことを確かめる**

`packages/model/src/recompute.ts` の先頭へ一時的に `import '@pointercad/ui';` を書き足して `pnpm run lint` を実行し、`依存方向 apps → ui → model → kernel / expression に違反しています` というエラーが出ることを確認する。**確認後に必ずこの行を消し、再度 `pnpm run lint` が通ることを確かめる。**

- [ ] **手順13: 統括へ報告する**

報告に含める: 作成した10ファイルと変更した1ファイル、テスト 6 件の結果、手順12 で ESLint が実際に違反を検出したかどうか。**gitへは書き込まない。**

---

### タスク7: `drawing` と `io` の骨組み

**先行タスク:** 1(タスク2〜6 とは独立。並行して進めてよい)
**対応要件:** NFR-MA-1

**ファイル:**
- 新規: `packages/drawing/package.json`
- 新規: `packages/drawing/tsconfig.json`
- 新規: `packages/drawing/vitest.config.ts`
- 新規: `packages/drawing/src/index.ts`
- 新規: `packages/drawing/src/paperSize.test.ts`
- 新規: `packages/io/package.json`
- 新規: `packages/io/tsconfig.json`
- 新規: `packages/io/vitest.config.ts`
- 新規: `packages/io/src/index.ts`
- 新規: `packages/io/src/schemaVersion.test.ts`
- 変更: `tsconfig.json`(`references` に2行追加)

- [ ] **手順1: `packages/drawing/package.json` を作る**

```json
{
  "name": "@pointercad/drawing",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "test": "vitest run"
  },
  "dependencies": {
    "@pointercad/model": "workspace:*"
  },
  "devDependencies": {
    "vitest": "catalog:"
  }
}
```

- [ ] **手順2: `packages/drawing/tsconfig.json` と `vitest.config.ts` を作る**

`tsconfig.json`(タスク6 手順2 と同じ形。参照先だけ違う):

```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": "./src",
    "outDir": "./dist"
  },
  "include": ["src"],
  "references": [{ "path": "../model" }]
}
```

`vitest.config.ts` はタスク2 手順3 と同じ内容。

- [ ] **手順3: `packages/drawing/src/index.ts` を作る**

```ts
/**
 * 図面生成(要件 FR-7xx)。投影・寸法・JIS スタイル・SVG 生成は P6 で実装する。
 * P0 では用紙の既定値だけを定義する。
 */

export interface PaperSize {
  readonly id: 'A3-landscape' | 'A4-landscape' | 'A4-portrait';
  readonly label: string;
  /** 幅(mm)。 */
  readonly width: number;
  /** 高さ(mm)。 */
  readonly height: number;
}

/** A3 横を既定とする(FR-701)。 */
export const PAPER_SIZES: readonly PaperSize[] = [
  { id: 'A3-landscape', label: 'A3 横', width: 420, height: 297 },
  { id: 'A4-landscape', label: 'A4 横', width: 297, height: 210 },
  { id: 'A4-portrait', label: 'A4 縦', width: 210, height: 297 },
];

export const DEFAULT_PAPER_SIZE_ID = 'A3-landscape';

/** JIS の標準縮尺系列(FR-703)。 */
export const STANDARD_SCALES: readonly number[] = [
  1 / 10, 1 / 5, 1 / 2, 1, 2, 5, 10,
];
```

- [ ] **手順4: `packages/drawing/src/paperSize.test.ts` を作る**

```ts
import { describe, expect, it } from 'vitest';

import { DEFAULT_PAPER_SIZE_ID, PAPER_SIZES, STANDARD_SCALES } from './index.js';

describe('図面の用紙と縮尺', () => {
  it('既定の用紙は A3 横である(FR-701)', () => {
    const paper = PAPER_SIZES.find((size) => size.id === DEFAULT_PAPER_SIZE_ID);
    expect(paper).toMatchObject({ width: 420, height: 297 });
  });

  it('A4 は縦横どちらも用意されている', () => {
    const ids = PAPER_SIZES.map((size) => size.id);
    expect(ids).toContain('A4-landscape');
    expect(ids).toContain('A4-portrait');
  });

  it('縮尺は小さい順に並び、等倍を含む(FR-703)', () => {
    expect(STANDARD_SCALES).toContain(1);
    expect([...STANDARD_SCALES].sort((a, b) => a - b)).toEqual([...STANDARD_SCALES]);
  });
});
```

- [ ] **手順5: `packages/io/package.json` を作る**

```json
{
  "name": "@pointercad/io",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "test": "vitest run"
  },
  "dependencies": {
    "@pointercad/model": "workspace:*"
  },
  "devDependencies": {
    "vitest": "catalog:"
  }
}
```

- [ ] **手順6: `packages/io/tsconfig.json` と `vitest.config.ts` を作る**

手順2 と同じ内容(参照先も `../model` で同じ)。

- [ ] **手順7: `packages/io/src/index.ts` を作る**

```ts
/**
 * ファイル入出力(要件 FR-8xx)。.pcad / STEP / STL / 3MF / OBJ / glTF は P2 以降で実装する。
 * P0 ではスキーマバージョンと対応形式の一覧だけを定義する。
 */

/** .pcad の document.json に埋め込むスキーマバージョン(要件§8)。 */
export const PCAD_SCHEMA_VERSION = 1;

export type ExportFormat = 'step' | 'stl' | '3mf' | 'obj' | 'glb';
export type ImportFormat = 'step' | 'stl' | 'obj';

export const EXPORT_FORMATS: readonly ExportFormat[] = ['step', 'stl', '3mf', 'obj', 'glb'];
export const IMPORT_FORMATS: readonly ImportFormat[] = ['step', 'stl', 'obj'];

/** 読み込めるファイルは書き出しもできること(要件 FR-802 / FR-803 の整合)。 */
export function canRoundTrip(format: ImportFormat): boolean {
  return (EXPORT_FORMATS as readonly string[]).includes(format);
}
```

- [ ] **手順8: `packages/io/src/schemaVersion.test.ts` を作る**

```ts
import { describe, expect, it } from 'vitest';

import { canRoundTrip, EXPORT_FORMATS, IMPORT_FORMATS, PCAD_SCHEMA_VERSION } from './index.js';

describe('入出力の骨組み', () => {
  it('.pcad のスキーマバージョンは 1 である(要件§8)', () => {
    expect(PCAD_SCHEMA_VERSION).toBe(1);
  });

  it('書き出しは STEP / STL / 3MF / OBJ / glTF に対応する(FR-803)', () => {
    expect([...EXPORT_FORMATS]).toEqual(['step', 'stl', '3mf', 'obj', 'glb']);
  });

  it('読み込める形式はすべて書き出しもできる', () => {
    for (const format of IMPORT_FORMATS) {
      expect(canRoundTrip(format), format).toBe(true);
    }
  });
});
```

- [ ] **手順9: ルート `tsconfig.json` の `references` に2件を足し、検査する**

```
pnpm install
pnpm run typecheck
pnpm run lint
pnpm run test
pnpm run build
```

期待結果: 全体検査4つが終了コード 0。`drawing` 3 件、`io` 3 件のテストが passed。

- [ ] **手順10: 統括へ報告する**

報告に含める: 作成した10ファイルと変更した1ファイル、テストの件数。**gitへは書き込まない。**

---

### タスク8: `ui` の骨組み(文字列リソースと Zustand ストア)

**先行タスク:** 6
**対応要件:** NFR-MA-5(UI 文字列は全てリソースファイルに分離)、`rules/04-設計の規律.md`(状態は Zustand ストア1本)

**ファイル:**
- 新規: `packages/ui/package.json`
- 新規: `packages/ui/tsconfig.json`
- 新規: `packages/ui/vitest.config.ts`
- 新規: `packages/ui/src/index.ts`
- 新規: `packages/ui/src/i18n/ja.json`
- 新規: `packages/ui/src/i18n/t.ts`
- 新規: `packages/ui/src/i18n/i18n.test.ts`
- 新規: `packages/ui/src/store/useAppStore.ts`
- 変更: `tsconfig.json`(`references` に1行追加)

- [ ] **手順1: `packages/ui/package.json` を作る**

`@pointercad/kernel` を**依存に書かない**こと。UI から幾何カーネルへ直接依存しない(`rules/04-設計の規律.md`)。

```json
{
  "name": "@pointercad/ui",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./src/index.ts"
    },
    "./style.css": "./src/shell/appShell.css"
  },
  "scripts": {
    "test": "vitest run"
  },
  "dependencies": {
    "@pointercad/drawing": "workspace:*",
    "@pointercad/expression": "workspace:*",
    "@pointercad/help-content": "workspace:*",
    "@pointercad/io": "workspace:*",
    "@pointercad/model": "workspace:*",
    "react": "catalog:",
    "react-dom": "catalog:",
    "three": "catalog:",
    "zustand": "catalog:"
  },
  "devDependencies": {
    "@types/react": "catalog:",
    "@types/react-dom": "catalog:",
    "@types/three": "catalog:",
    "vitest": "catalog:"
  }
}
```

- [ ] **手順2: `packages/ui/tsconfig.json` を作る**

```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": "./src",
    "outDir": "./dist",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "types": ["node"]
  },
  "include": ["src"],
  "references": [
    { "path": "../drawing" },
    { "path": "../expression" },
    { "path": "../help-content" },
    { "path": "../io" },
    { "path": "../model" }
  ]
}
```

`../kernel` を参照に入れないことで、`ui` から `@pointercad/kernel` を import すると型検査で落ちるようになる。

- [ ] **手順3: `packages/ui/vitest.config.ts` を作る**

React のレンダリングはテストせず、純関数と文字列リソースだけを検証する(§1.1 の判断)。よって DOM 環境は不要。

```ts
import { defineConfig } from 'vitest/config';

export default defineConfig({
  test: {
    environment: 'node',
    include: ['src/**/*.test.ts'],
  },
});
```

- [ ] **手順4: `packages/ui/src/i18n/ja.json` を作る**

UI に出る文字列はすべてここに置く。コンポーネントへ直書きしない(NFR-MA-5)。キーは平坦にして型で網羅を保証する。

```json
{
  "app.title": "PointerCAD",
  "toolbar.mode.modeling": "モデリング",
  "toolbar.mode.assembly": "アセンブリ",
  "toolbar.mode.drawing": "図面",
  "toolbar.projection.perspective": "透視投影",
  "toolbar.projection.orthographic": "平行投影",
  "toolbar.projection.tooltip": "見え方を切り替えます(遠近をつける/つけない)",
  "toolbar.displayStyle.shaded": "面のみ",
  "toolbar.displayStyle.shadedWithEdges": "面と稜線",
  "toolbar.displayStyle.wireframe": "稜線のみ",
  "toolbar.displayStyle.tooltip": "立体の見せ方を切り替えます",
  "toolbar.grid.label": "グリッド",
  "toolbar.grid.tooltip": "方眼と XYZ 軸の表示を切り替えます",
  "toolbar.home.label": "ホーム視点",
  "toolbar.home.tooltip": "斜め上から見た最初の向きに戻します(Home キー)",
  "featureTree.title": "モデルブラウザ",
  "featureTree.empty": "まだ何もありません。",
  "propertyPanel.title": "プロパティ",
  "propertyPanel.empty": "選択されているものはありません。",
  "propertyPanel.triangleCount": "三角形の数",
  "statusBar.ready": "中ボタンのドラッグで向きを変え、ホイールで拡大・縮小できます。",
  "statusBar.loading": "形を計算しています…",
  "statusBar.unit": "単位: mm",
  "statusBar.error": "計算に失敗しました:",
  "viewCube.front": "前",
  "viewCube.back": "後",
  "viewCube.right": "右",
  "viewCube.left": "左",
  "viewCube.top": "上",
  "viewCube.bottom": "下",
  "emptyState.firstStep": "点をプロットして最初の形を作りましょう。"
}
```

- [ ] **手順5: `packages/ui/src/i18n/t.ts` を作る**

```ts
import ja from './ja.json';

/** ja.json に実在するキーだけを受け付ける。打ち間違いは型検査で落ちる。 */
export type MessageKey = keyof typeof ja;

const messages: Readonly<Record<MessageKey, string>> = ja;

/** UI 文字列を引く。表示は日本語のみで開始する(NFR-MA-5)。 */
export function t(key: MessageKey): string {
  return messages[key];
}

/** テストと網羅確認のために全キーを公開する。 */
export const MESSAGE_KEYS = Object.keys(ja) as readonly MessageKey[];
```

- [ ] **手順6: `packages/ui/src/i18n/i18n.test.ts` を作る**

文字列の直書きを「気をつける」ではなく検査で止める(`rules/06-過去の失敗と対策.md`「対策は仕組みへ落とし込む」)。

```ts
import { readdirSync, readFileSync } from 'node:fs';
import { dirname, join, resolve } from 'node:path';
import { fileURLToPath } from 'node:url';

import { describe, expect, it } from 'vitest';

import { MESSAGE_KEYS, t } from './t.js';

const sourceRoot = resolve(dirname(fileURLToPath(import.meta.url)), '..');

/** 日本語(ひらがな・カタカナ・漢字・全角記号)。 */
const JAPANESE = /[　-〿぀-ゟ゠-ヿ一-鿿＀-￯]/;

function listFiles(directory: string, extension: string): string[] {
  const found: string[] = [];
  for (const entry of readdirSync(directory, { withFileTypes: true })) {
    const fullPath = join(directory, entry.name);
    if (entry.isDirectory()) {
      found.push(...listFiles(fullPath, extension));
    } else if (entry.name.endsWith(extension)) {
      found.push(fullPath);
    }
  }
  return found;
}

/** 注釈は日本語で書いてよいので、判定の前に取り除く。 */
function stripComments(source: string): string {
  return source.replace(/\/\*[\s\S]*?\*\//g, '').replace(/(^|[^:])\/\/.*$/gm, '$1');
}

describe('UI 文字列リソース(NFR-MA-5)', () => {
  it('キーが 1 件以上あり、すべて空でない文字列を返す', () => {
    expect(MESSAGE_KEYS.length).toBeGreaterThan(0);
    for (const key of MESSAGE_KEYS) {
      expect(t(key).length, key).toBeGreaterThan(0);
    }
  });

  it('区画の見出しとステータスバーの案内が定義されている(FR-905)', () => {
    expect(t('featureTree.title')).toBe('モデルブラウザ');
    expect(t('propertyPanel.title')).toBe('プロパティ');
    expect(t('statusBar.unit')).toBe('単位: mm');
  });

  it('ビューキューブの 6 面の表記が揃っている(FR-103)', () => {
    expect([
      t('viewCube.front'),
      t('viewCube.back'),
      t('viewCube.right'),
      t('viewCube.left'),
      t('viewCube.top'),
      t('viewCube.bottom'),
    ]).toEqual(['前', '後', '右', '左', '上', '下']);
  });

  it('コンポーネント(.tsx)へ日本語を直書きしていない', () => {
    const offenders: string[] = [];
    for (const file of listFiles(sourceRoot, '.tsx')) {
      if (JAPANESE.test(stripComments(readFileSync(file, 'utf8')))) {
        offenders.push(file);
      }
    }
    expect(offenders, 'ja.json へ移してください').toEqual([]);
  });
});
```

- [ ] **手順7: `packages/ui/src/store/useAppStore.ts` を作る**

コンポーネントの `useState` はホバー等の一時状態だけに使い、共有状態はこの1本に集める(`rules/04-設計の規律.md`)。

```ts
import type { PartMesh } from '@pointercad/model';
import { create } from 'zustand';

/** 透視投影 / 平行投影(FR-102)。 */
export type ProjectionMode = 'perspective' | 'orthographic';

/** 表示スタイル 3 種(FR-105)。 */
export type DisplayStyle = 'shaded' | 'shadedWithEdges' | 'wireframe';

export interface AppState {
  readonly documentName: string;
  readonly featureNames: readonly string[];
  readonly mesh: PartMesh | null;
  readonly isComputing: boolean;
  readonly errorMessage: string | null;
  readonly projection: ProjectionMode;
  readonly displayStyle: DisplayStyle;
  readonly showGrid: boolean;
  /** ホーム視点への復帰要求を数える(FR-108)。増えるたびにビューポートが反応する。 */
  readonly homeViewRequestCount: number;

  setDocument(name: string, featureNames: readonly string[]): void;
  setMesh(mesh: PartMesh): void;
  setComputing(isComputing: boolean): void;
  setError(message: string | null): void;
  setProjection(projection: ProjectionMode): void;
  setDisplayStyle(displayStyle: DisplayStyle): void;
  setShowGrid(showGrid: boolean): void;
  requestHomeView(): void;
}

export const useAppStore = create<AppState>()((set) => ({
  documentName: '',
  featureNames: [],
  mesh: null,
  isComputing: false,
  errorMessage: null,
  projection: 'perspective',
  displayStyle: 'shadedWithEdges',
  showGrid: true,
  homeViewRequestCount: 0,

  setDocument: (documentName, featureNames) => {
    set({ documentName, featureNames });
  },
  setMesh: (mesh) => {
    set({ mesh, errorMessage: null, isComputing: false });
  },
  setComputing: (isComputing) => {
    set({ isComputing });
  },
  setError: (errorMessage) => {
    set({ errorMessage, isComputing: false });
  },
  setProjection: (projection) => {
    set({ projection });
  },
  setDisplayStyle: (displayStyle) => {
    set({ displayStyle });
  },
  setShowGrid: (showGrid) => {
    set({ showGrid });
  },
  requestHomeView: () => {
    set((state) => ({ homeViewRequestCount: state.homeViewRequestCount + 1 }));
  },
}));
```

- [ ] **手順8: `packages/ui/src/index.ts` を作り、ルート `tsconfig.json` に参照を足す**

```ts
export { t, MESSAGE_KEYS, type MessageKey } from './i18n/t.js';
export { useAppStore, type AppState, type DisplayStyle, type ProjectionMode } from './store/useAppStore.js';
```

ルート `tsconfig.json` の `references` へ `{ "path": "./packages/ui" }` を足す。

- [ ] **手順9: 検査する**

```
pnpm install
pnpm --filter @pointercad/ui run test
pnpm run typecheck
pnpm run lint
pnpm run test
pnpm run build
```

期待結果: `ui` のテストが **4 件すべて passed**。全体検査4つが終了コード 0。

- [ ] **手順10: 統括へ報告する**

報告に含める: 作成した8ファイルと変更した1ファイル、`ja.json` のキー数、テストの件数。**gitへは書き込まない。**

---

### タスク9: 画面シェル(要件§7.1 の固定区画)

**先行タスク:** 8
**対応要件:** 要件§7.1、FR-905(ステータスバーガイド)、FR-904(ツールチップ)、NFR-UX-6(空状態ガイド)、FR-501(フィーチャーツリー)

**ファイル:**
- 新規: `packages/ui/src/shell/AppShell.tsx`
- 新規: `packages/ui/src/shell/Toolbar.tsx`
- 新規: `packages/ui/src/shell/FeatureTree.tsx`
- 新規: `packages/ui/src/shell/PropertyPanel.tsx`
- 新規: `packages/ui/src/shell/StatusBar.tsx`
- 新規: `packages/ui/src/shell/appShell.css`
- 変更: `packages/ui/src/index.ts`

区画は「ツールバー / フィーチャーツリー / ビューポート+ビューキューブ / プロパティ / ステータスバー」の5つに固定し、増やさない(`rules/04-設計の規律.md`)。

- [ ] **手順1: `packages/ui/src/shell/appShell.css` を作る**

要件§12 の「配色テーマは未定(ダークテーマ基調を想定)」に沿ってダーク基調にする。

```css
.pcad-shell {
  display: grid;
  grid-template-rows: auto 1fr auto;
  height: 100%;
  min-height: 0;
  font-family: "Yu Gothic UI", "Meiryo", system-ui, sans-serif;
  font-size: 13px;
  color: #e6e6e6;
  background: #1e1e1e;
}

.pcad-shell__body {
  display: grid;
  grid-template-columns: 240px 1fr 280px;
  min-height: 0;
}

.pcad-toolbar {
  display: flex;
  flex-wrap: wrap;
  gap: 12px;
  align-items: center;
  padding: 6px 10px;
  background: #252526;
  border-bottom: 1px solid #3c3c3c;
}

.pcad-toolbar__group {
  display: flex;
  gap: 4px;
  align-items: center;
}

.pcad-toolbar__button {
  padding: 4px 10px;
  color: inherit;
  cursor: pointer;
  background: #333336;
  border: 1px solid #45454a;
  border-radius: 3px;
}

.pcad-toolbar__button[aria-pressed="true"] {
  background: #0e639c;
  border-color: #1b80c4;
}

.pcad-panel {
  min-width: 0;
  min-height: 0;
  overflow: auto;
  background: #252526;
}

.pcad-panel--left {
  border-right: 1px solid #3c3c3c;
}

.pcad-panel--right {
  border-left: 1px solid #3c3c3c;
}

.pcad-panel__title {
  padding: 6px 10px;
  margin: 0;
  font-size: 12px;
  font-weight: 600;
  color: #b9b9b9;
  border-bottom: 1px solid #3c3c3c;
}

.pcad-panel__list {
  padding: 4px 10px;
  margin: 0;
  list-style: none;
}

.pcad-panel__empty {
  padding: 10px;
  color: #9a9a9a;
}

.pcad-viewport {
  position: relative;
  min-width: 0;
  min-height: 0;
  background: #2b2b2e;
}

.pcad-viewport__canvas {
  display: block;
  width: 100%;
  height: 100%;
}

.pcad-viewport__empty-state {
  position: absolute;
  bottom: 16px;
  left: 50%;
  padding: 6px 12px;
  color: #cfcfcf;
  background: rgb(0 0 0 / 45%);
  border-radius: 4px;
  transform: translateX(-50%);
  pointer-events: none;
}

.pcad-viewcube {
  position: absolute;
  top: 12px;
  right: 12px;
  width: 120px;
  height: 120px;
}

.pcad-statusbar {
  display: flex;
  gap: 16px;
  padding: 4px 10px;
  background: #007acc;
  border-top: 1px solid #3c3c3c;
}

.pcad-statusbar__spacer {
  flex: 1;
}

.pcad-statusbar--error {
  background: #a1260d;
}
```

- [ ] **手順2: `packages/ui/src/shell/Toolbar.tsx` を作る**

**日本語の文字列を直書きしない。** すべて `t()` で引く(タスク8 手順6 のテストが検出する)。

```tsx
import { t } from '../i18n/t.js';
import { useAppStore } from '../store/useAppStore.js';

export function Toolbar(): React.JSX.Element {
  const projection = useAppStore((state) => state.projection);
  const displayStyle = useAppStore((state) => state.displayStyle);
  const showGrid = useAppStore((state) => state.showGrid);
  const setProjection = useAppStore((state) => state.setProjection);
  const setDisplayStyle = useAppStore((state) => state.setDisplayStyle);
  const setShowGrid = useAppStore((state) => state.setShowGrid);
  const requestHomeView = useAppStore((state) => state.requestHomeView);

  return (
    <div className="pcad-toolbar">
      <div className="pcad-toolbar__group">
        <span>{t('toolbar.mode.modeling')}</span>
      </div>

      <div className="pcad-toolbar__group" title={t('toolbar.projection.tooltip')}>
        <button
          type="button"
          className="pcad-toolbar__button"
          aria-pressed={projection === 'perspective'}
          onClick={() => {
            setProjection('perspective');
          }}
        >
          {t('toolbar.projection.perspective')}
        </button>
        <button
          type="button"
          className="pcad-toolbar__button"
          aria-pressed={projection === 'orthographic'}
          onClick={() => {
            setProjection('orthographic');
          }}
        >
          {t('toolbar.projection.orthographic')}
        </button>
      </div>

      <div className="pcad-toolbar__group" title={t('toolbar.displayStyle.tooltip')}>
        <button
          type="button"
          className="pcad-toolbar__button"
          aria-pressed={displayStyle === 'shaded'}
          onClick={() => {
            setDisplayStyle('shaded');
          }}
        >
          {t('toolbar.displayStyle.shaded')}
        </button>
        <button
          type="button"
          className="pcad-toolbar__button"
          aria-pressed={displayStyle === 'shadedWithEdges'}
          onClick={() => {
            setDisplayStyle('shadedWithEdges');
          }}
        >
          {t('toolbar.displayStyle.shadedWithEdges')}
        </button>
        <button
          type="button"
          className="pcad-toolbar__button"
          aria-pressed={displayStyle === 'wireframe'}
          onClick={() => {
            setDisplayStyle('wireframe');
          }}
        >
          {t('toolbar.displayStyle.wireframe')}
        </button>
      </div>

      <div className="pcad-toolbar__group">
        <button
          type="button"
          className="pcad-toolbar__button"
          title={t('toolbar.grid.tooltip')}
          aria-pressed={showGrid}
          onClick={() => {
            setShowGrid(!showGrid);
          }}
        >
          {t('toolbar.grid.label')}
        </button>
        <button
          type="button"
          className="pcad-toolbar__button"
          title={t('toolbar.home.tooltip')}
          onClick={requestHomeView}
        >
          {t('toolbar.home.label')}
        </button>
      </div>
    </div>
  );
}
```

- [ ] **手順3: `packages/ui/src/shell/FeatureTree.tsx` を作る**

```tsx
import { t } from '../i18n/t.js';
import { useAppStore } from '../store/useAppStore.js';

export function FeatureTree(): React.JSX.Element {
  const documentName = useAppStore((state) => state.documentName);
  const featureNames = useAppStore((state) => state.featureNames);

  return (
    <section className="pcad-panel pcad-panel--left">
      <h2 className="pcad-panel__title">{t('featureTree.title')}</h2>
      {featureNames.length === 0 ? (
        <p className="pcad-panel__empty">{t('featureTree.empty')}</p>
      ) : (
        <ul className="pcad-panel__list">
          <li>{documentName}</li>
          {featureNames.map((name) => (
            <li key={name}>{name}</li>
          ))}
        </ul>
      )}
    </section>
  );
}
```

- [ ] **手順4: `packages/ui/src/shell/PropertyPanel.tsx` を作る**

```tsx
import { t } from '../i18n/t.js';
import { useAppStore } from '../store/useAppStore.js';

export function PropertyPanel(): React.JSX.Element {
  const mesh = useAppStore((state) => state.mesh);

  return (
    <section className="pcad-panel pcad-panel--right">
      <h2 className="pcad-panel__title">{t('propertyPanel.title')}</h2>
      {mesh === null ? (
        <p className="pcad-panel__empty">{t('propertyPanel.empty')}</p>
      ) : (
        <ul className="pcad-panel__list">
          <li>
            {t('propertyPanel.triangleCount')}: {mesh.triangleCount}
          </li>
        </ul>
      )}
    </section>
  );
}
```

- [ ] **手順5: `packages/ui/src/shell/StatusBar.tsx` を作る**

現在の状況を常時示す(FR-905)。

```tsx
import { t } from '../i18n/t.js';
import { useAppStore } from '../store/useAppStore.js';

export function StatusBar(): React.JSX.Element {
  const isComputing = useAppStore((state) => state.isComputing);
  const errorMessage = useAppStore((state) => state.errorMessage);

  const className =
    errorMessage === null ? 'pcad-statusbar' : 'pcad-statusbar pcad-statusbar--error';

  return (
    <div className={className}>
      <span>
        {errorMessage !== null
          ? `${t('statusBar.error')} ${errorMessage}`
          : isComputing
            ? t('statusBar.loading')
            : t('statusBar.ready')}
      </span>
      <span className="pcad-statusbar__spacer" />
      <span>{t('statusBar.unit')}</span>
    </div>
  );
}
```

- [ ] **手順6: `packages/ui/src/shell/AppShell.tsx` を作る**

ビューポートとビューキューブはタスク11・13 で差し込む。この時点では枠だけを置く。

```tsx
import { FeatureTree } from './FeatureTree.js';
import { PropertyPanel } from './PropertyPanel.js';
import { StatusBar } from './StatusBar.js';
import { Toolbar } from './Toolbar.js';
import { t } from '../i18n/t.js';

export function AppShell(): React.JSX.Element {
  return (
    <div className="pcad-shell">
      <Toolbar />
      <div className="pcad-shell__body">
        <FeatureTree />
        <div className="pcad-viewport">
          <p className="pcad-viewport__empty-state">{t('emptyState.firstStep')}</p>
        </div>
        <PropertyPanel />
      </div>
      <StatusBar />
    </div>
  );
}
```

- [ ] **手順7: `packages/ui/src/index.ts` に AppShell を足す**

```ts
export { t, MESSAGE_KEYS, type MessageKey } from './i18n/t.js';
export { useAppStore, type AppState, type DisplayStyle, type ProjectionMode } from './store/useAppStore.js';
export { AppShell } from './shell/AppShell.js';
```

- [ ] **手順8: 検査する**

```
pnpm run typecheck
pnpm run lint
pnpm run test
pnpm run build
```

期待結果: 4つとも終了コード 0。とくに `i18n.test.ts` の「コンポーネント(.tsx)へ日本語を直書きしていない」が **passed** であること(ここで落ちたら `ja.json` へ移す。テストを緩めない)。

- [ ] **手順9: 統括へ報告する**

報告に含める: 作成した6ファイルと変更した1ファイル、日本語直書き検査の結果。**gitへは書き込まない。**

---

### タスク10: カメラ操作とグリッド間隔の純関数

**先行タスク:** 8
**対応要件:** FR-101(Blender 互換の視点操作)、FR-102(投影切替)、FR-104(グリッド間隔の自動スケール)、FR-108(ホーム視点)

three.js に触れない純粋な計算だけを切り出し、数値で検証する。描画側(タスク11)はこの関数を呼ぶだけにする。

**ファイル:**
- 新規: `packages/ui/src/viewport/cameraMath.ts`
- 新規: `packages/ui/src/viewport/cameraMath.test.ts`
- 新規: `packages/ui/src/viewport/gridMath.ts`
- 新規: `packages/ui/src/viewport/gridMath.test.ts`

座標系は **Z 軸を上**とする(§0.9)。

- [ ] **手順1: `packages/ui/src/viewport/cameraMath.ts` を作る**

```ts
/** カメラの位置を、注視点まわりの球面座標で表す。単位は mm とラジアン。 */
export interface OrbitState {
  /** 方位角。+X から +Y へ向かう向きが正。 */
  readonly azimuth: number;
  /** 仰角。XY 平面が 0、+Z が正。 */
  readonly elevation: number;
  /** 注視点からの距離(mm)。 */
  readonly distance: number;
  /** 注視点の座標(mm)。 */
  readonly target: readonly [number, number, number];
}

/** 真上・真下でカメラの上方向が反転しないよう、仰角にわずかな余裕を残す。 */
export const MAX_ELEVATION = Math.PI / 2 - 1e-4;
export const MIN_DISTANCE = 0.1;
export const MAX_DISTANCE = 100_000;

/** 1 ピクセルのドラッグで回る角度(ラジアン)。 */
export const ORBIT_RADIANS_PER_PIXEL = 0.008;
/** ホイール 1 段(deltaY = 100)あたりの拡大率。 */
export const ZOOM_FACTOR_PER_NOTCH = 1.1;
/** 透視投影の垂直画角(ラジアン)。 */
export const VERTICAL_FIELD_OF_VIEW = (50 * Math.PI) / 180;

/** ホーム視点は等角(斜め上)から見る(FR-108)。 */
export const HOME_ORBIT: OrbitState = {
  azimuth: Math.PI / 4,
  elevation: Math.atan(Math.SQRT1_2),
  distance: 200,
  target: [0, 0, 0],
};

export function clamp(value: number, min: number, max: number): number {
  return Math.min(Math.max(value, min), max);
}

/** 中ボタンのドラッグで視点を回す(FR-101)。 */
export function orbit(state: OrbitState, deltaXPixels: number, deltaYPixels: number): OrbitState {
  return {
    ...state,
    azimuth: state.azimuth - deltaXPixels * ORBIT_RADIANS_PER_PIXEL,
    elevation: clamp(
      state.elevation + deltaYPixels * ORBIT_RADIANS_PER_PIXEL,
      -MAX_ELEVATION,
      MAX_ELEVATION,
    ),
  };
}

/** ホイールで拡大・縮小する(FR-101)。deltaY が正なら遠ざかる。 */
export function zoom(state: OrbitState, wheelDeltaY: number): OrbitState {
  const notches = wheelDeltaY / 100;
  return {
    ...state,
    distance: clamp(
      state.distance * Math.pow(ZOOM_FACTOR_PER_NOTCH, notches),
      MIN_DISTANCE,
      MAX_DISTANCE,
    ),
  };
}

/** カメラの位置(ワールド座標)。Z 軸が上。 */
export function cameraPosition(state: OrbitState): [number, number, number] {
  const horizontal = state.distance * Math.cos(state.elevation);
  return [
    state.target[0] + horizontal * Math.cos(state.azimuth),
    state.target[1] + horizontal * Math.sin(state.azimuth),
    state.target[2] + state.distance * Math.sin(state.elevation),
  ];
}

/** 画面 1 ピクセルが注視点の面上で何 mm に相当するか。 */
export function worldUnitsPerPixel(distance: number, viewportHeightPixels: number): number {
  return (2 * distance * Math.tan(VERTICAL_FIELD_OF_VIEW / 2)) / viewportHeightPixels;
}

/** Shift+中ボタンのドラッグで平行移動する(FR-101)。掴んだ点が指に追従する量だけ動かす。 */
export function pan(
  state: OrbitState,
  deltaXPixels: number,
  deltaYPixels: number,
  viewportHeightPixels: number,
): OrbitState {
  const scale = worldUnitsPerPixel(state.distance, viewportHeightPixels);

  // 画面右方向のワールドベクトル(Z 軸が上なので水平面内で方位角に直交する)。
  const right: [number, number, number] = [-Math.sin(state.azimuth), Math.cos(state.azimuth), 0];
  // 画面上方向のワールドベクトル。
  const up: [number, number, number] = [
    -Math.sin(state.elevation) * Math.cos(state.azimuth),
    -Math.sin(state.elevation) * Math.sin(state.azimuth),
    Math.cos(state.elevation),
  ];

  return {
    ...state,
    target: [
      state.target[0] - (right[0] * deltaXPixels - up[0] * deltaYPixels) * scale,
      state.target[1] - (right[1] * deltaXPixels - up[1] * deltaYPixels) * scale,
      state.target[2] - (right[2] * deltaXPixels - up[2] * deltaYPixels) * scale,
    ],
  };
}

/** 平行投影のときの表示範囲の高さ(mm)。透視投影と見た目の大きさを合わせる(FR-102)。 */
export function orthographicFrustumHeight(distance: number): number {
  return 2 * distance * Math.tan(VERTICAL_FIELD_OF_VIEW / 2);
}
```

- [ ] **手順2: `packages/ui/src/viewport/cameraMath.test.ts` を作る(先に書いて失敗させる)**

ホーム視点の期待値の根拠: 仰角 `atan(1/√2)` のとき `cos(elevation) = √(2/3)`、`sin(elevation) = 1/√3`。方位角 45° と合わせると、カメラ位置の3成分はすべて `distance/√3 = 200/1.7320508… = 115.47005383792515` になる(等角視)。

```ts
import { describe, expect, it } from 'vitest';

import {
  cameraPosition,
  clamp,
  HOME_ORBIT,
  MAX_DISTANCE,
  MAX_ELEVATION,
  MIN_DISTANCE,
  orbit,
  ORBIT_RADIANS_PER_PIXEL,
  orthographicFrustumHeight,
  pan,
  worldUnitsPerPixel,
  zoom,
} from './cameraMath.js';

describe('視点の回転(FR-101)', () => {
  it('横に 100 ピクセル動かすと方位角が 0.8 ラジアン減る', () => {
    const rotated = orbit(HOME_ORBIT, 100, 0);
    expect(rotated.azimuth).toBeCloseTo(HOME_ORBIT.azimuth - 100 * ORBIT_RADIANS_PER_PIXEL, 12);
    expect(rotated.azimuth).toBeCloseTo(Math.PI / 4 - 0.8, 12);
  });

  it('縦に動かしても仰角は真上・真下を越えない', () => {
    expect(orbit(HOME_ORBIT, 0, 100_000).elevation).toBeCloseTo(MAX_ELEVATION, 12);
    expect(orbit(HOME_ORBIT, 0, -100_000).elevation).toBeCloseTo(-MAX_ELEVATION, 12);
  });

  it('回転しても距離と注視点は変わらない', () => {
    const rotated = orbit(HOME_ORBIT, 37, -21);
    expect(rotated.distance).toBe(HOME_ORBIT.distance);
    expect(rotated.target).toEqual(HOME_ORBIT.target);
  });
});

describe('拡大・縮小(FR-101)', () => {
  it('ホイール 1 段で距離が 1.1 倍になる', () => {
    expect(zoom({ ...HOME_ORBIT, distance: 100 }, 100).distance).toBeCloseTo(110, 12);
  });

  it('逆向きのホイール 1 段で距離が 1.1 分の 1 になる', () => {
    expect(zoom({ ...HOME_ORBIT, distance: 100 }, -100).distance).toBeCloseTo(100 / 1.1, 12);
  });

  it('距離は下限と上限で止まる', () => {
    expect(zoom({ ...HOME_ORBIT, distance: MIN_DISTANCE }, -100_000).distance).toBe(MIN_DISTANCE);
    expect(zoom({ ...HOME_ORBIT, distance: MAX_DISTANCE }, 100_000).distance).toBe(MAX_DISTANCE);
  });
});

describe('ホーム視点(FR-108)', () => {
  it('等角視になり、カメラ位置の 3 成分が等しい', () => {
    const [x, y, z] = cameraPosition(HOME_ORBIT);
    const expected = 200 / Math.sqrt(3);
    expect(expected).toBeCloseTo(115.47005383792515, 12);
    expect(x).toBeCloseTo(expected, 9);
    expect(y).toBeCloseTo(expected, 9);
    expect(z).toBeCloseTo(expected, 9);
  });

  it('注視点をずらすとカメラ位置も同じだけずれる', () => {
    const [x, y, z] = cameraPosition({ ...HOME_ORBIT, target: [10, 20, 30] });
    const expected = 200 / Math.sqrt(3);
    expect(x).toBeCloseTo(expected + 10, 9);
    expect(y).toBeCloseTo(expected + 20, 9);
    expect(z).toBeCloseTo(expected + 30, 9);
  });
});

describe('平行移動(FR-101)', () => {
  it('1 ピクセルあたりの移動量が画角と距離から決まる', () => {
    // 2 * 100 * tan(25°) / 1000
    expect(worldUnitsPerPixel(100, 1000)).toBeCloseTo(0.09326021998623846, 12);
  });

  it('真上から見ているとき、右へ 10 ピクセル動かすと注視点が -Y へ動く', () => {
    const state = { azimuth: 0, elevation: MAX_ELEVATION, distance: 100, target: [0, 0, 0] } as const;
    const moved = pan(state, 10, 0, 1000);
    const scale = worldUnitsPerPixel(100, 1000);
    expect(moved.target[0]).toBeCloseTo(0, 9);
    expect(moved.target[1]).toBeCloseTo(-10 * scale, 9);
    expect(moved.target[2]).toBeCloseTo(0, 9);
  });

  it('平行移動しても向きと距離は変わらない', () => {
    const moved = pan(HOME_ORBIT, 12, 34, 800);
    expect(moved.azimuth).toBe(HOME_ORBIT.azimuth);
    expect(moved.elevation).toBe(HOME_ORBIT.elevation);
    expect(moved.distance).toBe(HOME_ORBIT.distance);
  });
});

describe('投影の切り替え(FR-102)', () => {
  it('平行投影の表示高さは距離に比例する', () => {
    expect(orthographicFrustumHeight(100)).toBeCloseTo(93.26021998623846, 9);
    expect(orthographicFrustumHeight(200)).toBeCloseTo(2 * 93.26021998623846, 9);
  });
});

describe('clamp', () => {
  it('範囲の内外を正しく丸める', () => {
    expect(clamp(5, 0, 10)).toBe(5);
    expect(clamp(-1, 0, 10)).toBe(0);
    expect(clamp(11, 0, 10)).toBe(10);
  });
});
```

- [ ] **手順3: `packages/ui/src/viewport/gridMath.ts` を作る**

```ts
import { MIN_DISTANCE } from './cameraMath.js';

/** 画面に入れたい方眼のおよその本数。 */
export const TARGET_GRID_LINE_COUNT = 10;

/** 方眼の間隔として使う数列(1, 2, 5 の並び)。 */
const SPACING_SERIES = [1, 2, 5] as const;

/**
 * カメラ距離に応じた方眼の間隔(mm)を返す(FR-104)。
 * 1 → 2 → 5 → 10 → 20 → 50 … の順に切り替わる。
 */
export function gridSpacing(cameraDistance: number): number {
  const raw = Math.max(cameraDistance, MIN_DISTANCE) / TARGET_GRID_LINE_COUNT;
  const exponent = Math.floor(Math.log10(raw));
  for (const factor of SPACING_SERIES) {
    const candidate = factor * Math.pow(10, exponent);
    if (candidate >= raw - 1e-12) {
      return candidate;
    }
  }
  return Math.pow(10, exponent + 1);
}

/** 方眼の広がり(片側の長さ、mm)。間隔の 20 倍を目安に、遠景で描きすぎないようにする。 */
export function gridExtent(spacing: number): number {
  return spacing * 20;
}

/** XYZ 軸を描く長さ(mm)。方眼と同じ広がりに揃える。 */
export function axisLength(spacing: number): number {
  return gridExtent(spacing);
}
```

- [ ] **手順4: `packages/ui/src/viewport/gridMath.test.ts` を作る**

```ts
import { describe, expect, it } from 'vitest';

import { axisLength, gridExtent, gridSpacing } from './gridMath.js';

describe('方眼の間隔の自動調整(FR-104)', () => {
  it.each([
    [100, 10],
    [200, 20],
    [250, 50],
    [500, 50],
    [1000, 100],
    [30, 5],
    [10, 1],
    [5, 0.5],
    [0.5, 0.05],
  ])('カメラ距離 %d mm では間隔 %d mm', (distance, expected) => {
    expect(gridSpacing(distance)).toBeCloseTo(expected, 9);
  });

  it('距離が大きくなると間隔は決して小さくならない', () => {
    let previous = 0;
    for (let distance = 0.1; distance < 10_000; distance *= 1.3) {
      const spacing = gridSpacing(distance);
      expect(spacing).toBeGreaterThanOrEqual(previous);
      previous = spacing;
    }
  });

  it('間隔は必ず 1, 2, 5 のいずれかを 10 のべき乗倍した値になる', () => {
    for (let distance = 0.1; distance < 10_000; distance *= 1.7) {
      const spacing = gridSpacing(distance);
      const normalized = spacing / Math.pow(10, Math.floor(Math.log10(spacing)));
      expect([1, 2, 5].some((factor) => Math.abs(normalized - factor) < 1e-9)).toBe(true);
    }
  });

  it('方眼と軸の広がりは間隔の 20 倍', () => {
    expect(gridExtent(10)).toBe(200);
    expect(axisLength(10)).toBe(200);
  });
});
```

- [ ] **手順5: 検査する**

```
pnpm --filter @pointercad/ui run test
pnpm run typecheck
pnpm run lint
pnpm run test
pnpm run build
```

期待結果: `ui` のテストファイルが 3 つ(`i18n.test.ts`、`cameraMath.test.ts`、`gridMath.test.ts`)実行され、**失敗が 0 件**。件数の内訳は `i18n` 4 件、`cameraMath` 13 件、`gridMath` 12 件(`it.each` の 9 ケースを 9 件として数える)で合計 29 件を見込むが、**合格条件は「失敗 0 件」であり、件数は実測値をそのまま報告する。**

- [ ] **手順6: 統括へ報告する**

報告に含める: 作成した4ファイル、テストの実測件数、失敗 0 件であること。**gitへは書き込まない。**

---

### タスク11: Three.js ビューポート

**先行タスク:** 9, 10
**対応要件:** FR-101、FR-102、FR-104、FR-105、FR-108、NFR-PF-1(60fps 維持)

**ファイル:**
- 新規: `packages/ui/src/viewport/createViewportScene.ts`
- 新規: `packages/ui/src/viewport/attachCameraControls.ts`
- 新規: `packages/ui/src/viewport/ViewportCanvas.tsx`
- 変更: `packages/ui/src/shell/AppShell.tsx`
- 変更: `packages/ui/src/index.ts`

- [ ] **手順1: `packages/ui/src/viewport/createViewportScene.ts` を作る**

three.js の既定は Y 軸が上なので、`up` を Z 軸へ変える(§0.9)。

```ts
import type { PartMesh } from '@pointercad/model';
import * as THREE from 'three';

import type { DisplayStyle, ProjectionMode } from '../store/useAppStore.js';
import { cameraPosition, orthographicFrustumHeight, VERTICAL_FIELD_OF_VIEW, type OrbitState } from './cameraMath.js';
import { axisLength, gridExtent, gridSpacing } from './gridMath.js';

export interface ViewportScene {
  render(orbit: OrbitState, projection: ProjectionMode, displayStyle: DisplayStyle, showGrid: boolean): void;
  setMesh(mesh: PartMesh | null): void;
  resize(widthPixels: number, heightPixels: number): void;
  dispose(): void;
}

const UP_AXIS = new THREE.Vector3(0, 0, 1);

export function createViewportScene(canvas: HTMLCanvasElement): ViewportScene {
  const renderer = new THREE.WebGLRenderer({ canvas, antialias: true });
  renderer.setPixelRatio(Math.min(globalThis.devicePixelRatio, 2));
  renderer.setClearColor(0x2b2b2e, 1);

  const scene = new THREE.Scene();
  scene.add(new THREE.AmbientLight(0xffffff, 1.4));
  const keyLight = new THREE.DirectionalLight(0xffffff, 2.2);
  keyLight.position.set(1, 1, 2);
  scene.add(keyLight);

  const perspectiveCamera = new THREE.PerspectiveCamera(
    (VERTICAL_FIELD_OF_VIEW * 180) / Math.PI,
    1,
    0.05,
    200_000,
  );
  perspectiveCamera.up.copy(UP_AXIS);

  const orthographicCamera = new THREE.OrthographicCamera(-1, 1, 1, -1, -200_000, 200_000);
  orthographicCamera.up.copy(UP_AXIS);

  const solid = new THREE.Mesh(
    new THREE.BufferGeometry(),
    new THREE.MeshStandardMaterial({ color: 0xb0b6c0, roughness: 0.65, metalness: 0.05, side: THREE.DoubleSide }),
  );
  solid.visible = false;
  scene.add(solid);

  const edges = new THREE.LineSegments(
    new THREE.BufferGeometry(),
    new THREE.LineBasicMaterial({ color: 0x101014 }),
  );
  edges.visible = false;
  scene.add(edges);

  const gridGroup = new THREE.Group();
  scene.add(gridGroup);
  let grid: THREE.GridHelper | null = null;
  let axes: THREE.AxesHelper | null = null;
  let currentSpacing = 0;

  let width = 1;
  let height = 1;

  function rebuildGrid(spacing: number): void {
    if (grid !== null) {
      gridGroup.remove(grid);
      grid.geometry.dispose();
      (grid.material as THREE.Material).dispose();
    }
    if (axes !== null) {
      gridGroup.remove(axes);
      axes.geometry.dispose();
      (axes.material as THREE.Material).dispose();
    }
    const extent = gridExtent(spacing);
    grid = new THREE.GridHelper(extent * 2, Math.round((extent * 2) / spacing), 0x5a5a60, 0x3a3a40);
    // GridHelper は XZ 平面に作られるので、Z 上の座標系へ倒す。
    grid.rotation.x = Math.PI / 2;
    gridGroup.add(grid);

    axes = new THREE.AxesHelper(axisLength(spacing));
    gridGroup.add(axes);
    currentSpacing = spacing;
  }
  rebuildGrid(gridSpacing(200));

  return {
    setMesh(mesh): void {
      if (mesh === null) {
        solid.visible = false;
        edges.visible = false;
        return;
      }
      const surfaceGeometry = new THREE.BufferGeometry();
      surfaceGeometry.setAttribute('position', new THREE.BufferAttribute(mesh.positions, 3));
      surfaceGeometry.setAttribute('normal', new THREE.BufferAttribute(mesh.normals, 3));
      surfaceGeometry.setIndex(new THREE.BufferAttribute(mesh.indices, 1));
      solid.geometry.dispose();
      solid.geometry = surfaceGeometry;

      const edgeGeometry = new THREE.BufferGeometry();
      edgeGeometry.setAttribute('position', new THREE.BufferAttribute(mesh.edgePositions, 3));
      edges.geometry.dispose();
      edges.geometry = edgeGeometry;
    },

    resize(widthPixels, heightPixels): void {
      width = Math.max(widthPixels, 1);
      height = Math.max(heightPixels, 1);
      renderer.setSize(width, height, false);
    },

    render(orbit, projection, displayStyle, showGrid): void {
      const spacing = gridSpacing(orbit.distance);
      if (spacing !== currentSpacing) {
        rebuildGrid(spacing);
      }
      gridGroup.visible = showGrid;

      solid.visible = displayStyle !== 'wireframe';
      edges.visible = displayStyle !== 'shaded';

      const [x, y, z] = cameraPosition(orbit);
      const aspect = width / height;
      const camera = projection === 'perspective' ? perspectiveCamera : orthographicCamera;

      if (projection === 'perspective') {
        perspectiveCamera.aspect = aspect;
      } else {
        const frustumHeight = orthographicFrustumHeight(orbit.distance);
        orthographicCamera.top = frustumHeight / 2;
        orthographicCamera.bottom = -frustumHeight / 2;
        orthographicCamera.left = (-frustumHeight * aspect) / 2;
        orthographicCamera.right = (frustumHeight * aspect) / 2;
      }
      camera.position.set(x, y, z);
      camera.up.copy(UP_AXIS);
      camera.lookAt(orbit.target[0], orbit.target[1], orbit.target[2]);
      camera.updateProjectionMatrix();

      renderer.render(scene, camera);
    },

    dispose(): void {
      solid.geometry.dispose();
      (solid.material as THREE.Material).dispose();
      edges.geometry.dispose();
      (edges.material as THREE.Material).dispose();
      renderer.dispose();
    },
  };
}
```

- [ ] **手順2: `packages/ui/src/viewport/attachCameraControls.ts` を作る**

Blender と同じ割当に加え、中ボタンの無いマウスとタッチパッドの代替操作を用意する(FR-101)。要件§12「Blender 互換ショートカットの具体割当表(P0〜P1 決定)」に対する P0 の決定として、この表を確定させる。

| 操作 | 主 | 代替(マウスのみ / タッチパッド) |
|---|---|---|
| 視点回転 | 中ボタンドラッグ | Alt + 左ボタンドラッグ |
| 平行移動 | Shift + 中ボタンドラッグ | Alt + Shift + 左ボタンドラッグ |
| 拡大縮小 | ホイール | 2本指の上下スクロール(`wheel` イベントとして届く)、Ctrl + ホイール |
| ホーム視点 | Home キー | ツールバーのボタン |

```ts
import { HOME_ORBIT, orbit, pan, zoom, type OrbitState } from './cameraMath.js';

export interface CameraControls {
  getOrbit(): OrbitState;
  /** ビューキューブなど、外から視点を差し替えるための入口。視点の正本はこの1箇所に置く。 */
  setOrbit(next: OrbitState): void;
  goHome(): void;
  detach(): void;
}

const MIDDLE_BUTTON = 1;
const LEFT_BUTTON = 0;

/**
 * ビューポートの視点操作を canvas へ結びつける(FR-101)。
 * 状態が変わるたびに onChange を呼び、描画は呼び出し側の毎フレーム処理に任せる。
 */
export function attachCameraControls(
  canvas: HTMLCanvasElement,
  onChange: () => void,
): CameraControls {
  let state: OrbitState = HOME_ORBIT;
  let dragMode: 'orbit' | 'pan' | null = null;
  let lastX = 0;
  let lastY = 0;

  function beginDrag(event: PointerEvent): void {
    const isOrbitButton = event.button === MIDDLE_BUTTON && !event.shiftKey;
    const isPanButton = event.button === MIDDLE_BUTTON && event.shiftKey;
    const isAltOrbit = event.button === LEFT_BUTTON && event.altKey && !event.shiftKey;
    const isAltPan = event.button === LEFT_BUTTON && event.altKey && event.shiftKey;

    if (isOrbitButton || isAltOrbit) {
      dragMode = 'orbit';
    } else if (isPanButton || isAltPan) {
      dragMode = 'pan';
    } else {
      return;
    }

    lastX = event.clientX;
    lastY = event.clientY;
    canvas.setPointerCapture(event.pointerId);
    event.preventDefault();
  }

  function moveDrag(event: PointerEvent): void {
    if (dragMode === null) {
      return;
    }
    const deltaX = event.clientX - lastX;
    const deltaY = event.clientY - lastY;
    lastX = event.clientX;
    lastY = event.clientY;

    state =
      dragMode === 'orbit'
        ? orbit(state, deltaX, deltaY)
        : pan(state, deltaX, deltaY, canvas.clientHeight);
    onChange();
  }

  function endDrag(event: PointerEvent): void {
    if (dragMode === null) {
      return;
    }
    dragMode = null;
    if (canvas.hasPointerCapture(event.pointerId)) {
      canvas.releasePointerCapture(event.pointerId);
    }
  }

  function onWheel(event: WheelEvent): void {
    event.preventDefault();
    state = zoom(state, event.deltaY);
    onChange();
  }

  function onKeyDown(event: KeyboardEvent): void {
    if (event.key === 'Home') {
      state = HOME_ORBIT;
      onChange();
    }
  }

  // 中ボタンのドラッグでブラウザの自動スクロールが始まらないようにする。
  function onAuxClick(event: MouseEvent): void {
    if (event.button === MIDDLE_BUTTON) {
      event.preventDefault();
    }
  }

  canvas.addEventListener('pointerdown', beginDrag);
  canvas.addEventListener('pointermove', moveDrag);
  canvas.addEventListener('pointerup', endDrag);
  canvas.addEventListener('pointercancel', endDrag);
  canvas.addEventListener('wheel', onWheel, { passive: false });
  canvas.addEventListener('auxclick', onAuxClick);
  globalThis.addEventListener('keydown', onKeyDown);

  return {
    getOrbit: () => state,
    setOrbit: (next) => {
      state = next;
      onChange();
    },
    goHome: () => {
      state = HOME_ORBIT;
      onChange();
    },
    detach: () => {
      canvas.removeEventListener('pointerdown', beginDrag);
      canvas.removeEventListener('pointermove', moveDrag);
      canvas.removeEventListener('pointerup', endDrag);
      canvas.removeEventListener('pointercancel', endDrag);
      canvas.removeEventListener('wheel', onWheel);
      canvas.removeEventListener('auxclick', onAuxClick);
      globalThis.removeEventListener('keydown', onKeyDown);
    },
  };
}
```

- [ ] **手順3: `packages/ui/src/viewport/ViewportCanvas.tsx` を作る**

```tsx
import { useEffect, useRef } from 'react';

import { useAppStore } from '../store/useAppStore.js';
import { attachCameraControls, type CameraControls } from './attachCameraControls.js';
import { createViewportScene, type ViewportScene } from './createViewportScene.js';

export function ViewportCanvas(): React.JSX.Element {
  const canvasRef = useRef<HTMLCanvasElement>(null);
  const sceneRef = useRef<ViewportScene | null>(null);
  const controlsRef = useRef<CameraControls | null>(null);

  const mesh = useAppStore((state) => state.mesh);
  const projection = useAppStore((state) => state.projection);
  const displayStyle = useAppStore((state) => state.displayStyle);
  const showGrid = useAppStore((state) => state.showGrid);
  const homeViewRequestCount = useAppStore((state) => state.homeViewRequestCount);

  useEffect(() => {
    const canvas = canvasRef.current;
    if (canvas === null) {
      return;
    }

    const scene = createViewportScene(canvas);
    sceneRef.current = scene;

    let needsRender = true;
    const controls = attachCameraControls(canvas, () => {
      needsRender = true;
    });
    controlsRef.current = controls;

    const observer = new ResizeObserver(() => {
      scene.resize(canvas.clientWidth, canvas.clientHeight);
      needsRender = true;
    });
    observer.observe(canvas);
    scene.resize(canvas.clientWidth, canvas.clientHeight);

    let frameId = 0;
    const tick = (): void => {
      if (needsRender) {
        const { projection: p, displayStyle: d, showGrid: g } = useAppStore.getState();
        scene.render(controls.getOrbit(), p, d, g);
        needsRender = false;
      }
      frameId = globalThis.requestAnimationFrame(tick);
    };
    frameId = globalThis.requestAnimationFrame(tick);

    // 表示の設定が変わったときにも描き直す。
    const unsubscribe = useAppStore.subscribe(() => {
      needsRender = true;
    });

    return () => {
      globalThis.cancelAnimationFrame(frameId);
      unsubscribe();
      observer.disconnect();
      controls.detach();
      scene.dispose();
      sceneRef.current = null;
      controlsRef.current = null;
    };
  }, []);

  useEffect(() => {
    sceneRef.current?.setMesh(mesh);
  }, [mesh]);

  useEffect(() => {
    if (homeViewRequestCount > 0) {
      controlsRef.current?.goHome();
    }
  }, [homeViewRequestCount]);

  // projection / displayStyle / showGrid は購読で再描画を促すために読んでいる。
  void projection;
  void displayStyle;
  void showGrid;

  return <canvas ref={canvasRef} className="pcad-viewport__canvas" />;
}
```

- [ ] **手順4: `packages/ui/src/shell/AppShell.tsx` にビューポートを差し込む**

```tsx
import { ViewportCanvas } from '../viewport/ViewportCanvas.js';
import { FeatureTree } from './FeatureTree.js';
import { PropertyPanel } from './PropertyPanel.js';
import { StatusBar } from './StatusBar.js';
import { Toolbar } from './Toolbar.js';
import { t } from '../i18n/t.js';
import { useAppStore } from '../store/useAppStore.js';

export function AppShell(): React.JSX.Element {
  const mesh = useAppStore((state) => state.mesh);

  return (
    <div className="pcad-shell">
      <Toolbar />
      <div className="pcad-shell__body">
        <FeatureTree />
        <div className="pcad-viewport">
          <ViewportCanvas />
          {mesh === null ? (
            <p className="pcad-viewport__empty-state">{t('emptyState.firstStep')}</p>
          ) : null}
        </div>
        <PropertyPanel />
      </div>
      <StatusBar />
    </div>
  );
}
```

- [ ] **手順5: `packages/ui/src/index.ts` に `HOME_ORBIT` などを足す**

```ts
export { t, MESSAGE_KEYS, type MessageKey } from './i18n/t.js';
export { useAppStore, type AppState, type DisplayStyle, type ProjectionMode } from './store/useAppStore.js';
export { AppShell } from './shell/AppShell.js';
export { HOME_ORBIT, type OrbitState } from './viewport/cameraMath.js';
```

- [ ] **手順6: 検査する**

```
pnpm run typecheck
pnpm run lint
pnpm run test
pnpm run build
```

期待結果: 4つとも終了コード 0。**ブラウザや開発サーバーは起動しない。**実際の描画確認はタスク15 のビルドとタスク18 の統括の手動確認で行う。

- [ ] **手順7: 統括へ報告する**

報告に含める: 作成した3ファイルと変更した2ファイル、`three` と `@types/three` の型が噛み合ったか(`GridHelper` の材質の型など)、要件§12 の割当表を確定させた旨。**gitへは書き込まない。**

---

### タスク12: ビューキューブの純関数

**先行タスク:** 10
**対応要件:** FR-103(面・辺・頂点のクリックでその要素を正面とする視点へ遷移)

**ファイル:**
- 新規: `packages/ui/src/viewcube/viewCubeMath.ts`
- 新規: `packages/ui/src/viewcube/viewCubeMath.test.ts`

- [ ] **手順1: `packages/ui/src/viewcube/viewCubeMath.ts` を作る**

立方体の表面上の点を「面 6 + 辺 12 + 頂点 8 = 26 個」の領域のどれかへ割り当てる。各軸の座標の絶対値がしきい値を超えたら、その軸方向も向きに加える。

```ts
import { clamp, MAX_ELEVATION, type OrbitState } from '../viewport/cameraMath.js';

/** 各軸の向き。-1 / 0 / +1。 */
export type AxisSign = -1 | 0 | 1;

/** 立方体の 26 領域(面 6 + 辺 12 + 頂点 8)のいずれか。 */
export interface ViewCubeRegion {
  readonly x: AxisSign;
  readonly y: AxisSign;
  readonly z: AxisSign;
}

/** 面の中央から見て、この割合を超えたら隣の辺・頂点の領域とみなす。 */
export const REGION_THRESHOLD = 0.5;

/** 立方体の面の並び。+X が右、-Y が前、+Z が上(Z 上の座標系)。 */
export const FACE_REGIONS = {
  right: { x: 1, y: 0, z: 0 },
  left: { x: -1, y: 0, z: 0 },
  front: { x: 0, y: -1, z: 0 },
  back: { x: 0, y: 1, z: 0 },
  top: { x: 0, y: 0, z: 1 },
  bottom: { x: 0, y: 0, z: -1 },
} as const satisfies Record<string, ViewCubeRegion>;

function signOf(value: number): AxisSign {
  if (value >= REGION_THRESHOLD) {
    return 1;
  }
  if (value <= -REGION_THRESHOLD) {
    return -1;
  }
  return 0;
}

/**
 * 一辺 2(各座標が -1 〜 +1)の立方体の表面上の点から領域を求める。
 * どの軸も中央寄りだった場合(立方体の内部を指した場合)は null。
 */
export function regionFromLocalPoint(point: readonly [number, number, number]): ViewCubeRegion | null {
  const region = { x: signOf(point[0]), y: signOf(point[1]), z: signOf(point[2]) };
  if (region.x === 0 && region.y === 0 && region.z === 0) {
    return null;
  }
  return region;
}

/** 領域が表す向き(単位ベクトル)。カメラはこの向きから原点を見る。 */
export function directionFromRegion(region: ViewCubeRegion): [number, number, number] {
  const length = Math.hypot(region.x, region.y, region.z);
  return [region.x / length, region.y / length, region.z / length];
}

/** 領域をクリックしたときに向かうべき視点(FR-103)。距離と注視点は今のまま保つ。 */
export function orbitStateForRegion(region: ViewCubeRegion, current: OrbitState): OrbitState {
  const [x, y, z] = directionFromRegion(region);
  return {
    ...current,
    azimuth: Math.atan2(y, x),
    elevation: clamp(Math.asin(z), -MAX_ELEVATION, MAX_ELEVATION),
  };
}

/** 角度を -π 〜 +π へ折り返す。回転アニメーションで遠回りしないために使う。 */
export function normalizeAngle(angle: number): number {
  const wrapped = (angle + Math.PI) % (2 * Math.PI);
  return (wrapped < 0 ? wrapped + 2 * Math.PI : wrapped) - Math.PI;
}

/** 現在の視点から目標の視点へ、0 〜 1 の進み具合で補間する。 */
export function interpolateOrbit(from: OrbitState, to: OrbitState, progress: number): OrbitState {
  const ratio = clamp(progress, 0, 1);
  // 変化がゆるやかに始まり、ゆるやかに終わるようにする。
  const eased = ratio * ratio * (3 - 2 * ratio);
  return {
    ...from,
    azimuth: from.azimuth + normalizeAngle(to.azimuth - from.azimuth) * eased,
    elevation: from.elevation + (to.elevation - from.elevation) * eased,
  };
}

/** 視点の遷移にかける時間(ミリ秒)。 */
export const VIEW_TRANSITION_DURATION_MS = 300;
```

- [ ] **手順2: `packages/ui/src/viewcube/viewCubeMath.test.ts` を作る**

```ts
import { describe, expect, it } from 'vitest';

import { HOME_ORBIT, MAX_ELEVATION, type OrbitState } from '../viewport/cameraMath.js';
import {
  directionFromRegion,
  FACE_REGIONS,
  interpolateOrbit,
  normalizeAngle,
  orbitStateForRegion,
  regionFromLocalPoint,
} from './viewCubeMath.js';

describe('ビューキューブの領域判定(FR-103)', () => {
  it('面の中央は面の領域になる', () => {
    expect(regionFromLocalPoint([1, 0, 0])).toEqual(FACE_REGIONS.right);
    expect(regionFromLocalPoint([0, -1, 0])).toEqual(FACE_REGIONS.front);
    expect(regionFromLocalPoint([0, 0, 1])).toEqual(FACE_REGIONS.top);
  });

  it('面の端寄り(しきい値未満)はまだ面の領域', () => {
    expect(regionFromLocalPoint([1, 0.2, -0.1])).toEqual(FACE_REGIONS.right);
  });

  it('辺の近くは 2 軸の領域になる', () => {
    expect(regionFromLocalPoint([1, 0.9, 0])).toEqual({ x: 1, y: 1, z: 0 });
  });

  it('頂点の近くは 3 軸の領域になる', () => {
    expect(regionFromLocalPoint([0.9, 0.8, 0.7])).toEqual({ x: 1, y: 1, z: 1 });
  });

  it('中央付近は領域なし', () => {
    expect(regionFromLocalPoint([0.1, -0.2, 0.3])).toBeNull();
  });

  it('26 種類すべてを取り出せる', () => {
    const found = new Set<string>();
    for (const x of [-1, 0, 1]) {
      for (const y of [-1, 0, 1]) {
        for (const z of [-1, 0, 1]) {
          const region = regionFromLocalPoint([x, y, z]);
          if (region !== null) {
            found.add(`${region.x},${region.y},${region.z}`);
          }
        }
      }
    }
    expect(found.size).toBe(26);
  });
});

describe('領域から視点を決める(FR-103)', () => {
  it('前面をクリックすると -Y の方向から見る', () => {
    const next = orbitStateForRegion(FACE_REGIONS.front, HOME_ORBIT);
    expect(next.azimuth).toBeCloseTo(-Math.PI / 2, 12);
    expect(next.elevation).toBeCloseTo(0, 12);
  });

  it('右面をクリックすると +X の方向から見る', () => {
    const next = orbitStateForRegion(FACE_REGIONS.right, HOME_ORBIT);
    expect(next.azimuth).toBeCloseTo(0, 12);
    expect(next.elevation).toBeCloseTo(0, 12);
  });

  it('上面をクリックしても真上を越えず、視界が反転しない', () => {
    const next = orbitStateForRegion(FACE_REGIONS.top, HOME_ORBIT);
    expect(next.elevation).toBeCloseTo(MAX_ELEVATION, 12);
  });

  it('頂点をクリックすると等角視の向きになる', () => {
    const [x, y, z] = directionFromRegion({ x: 1, y: 1, z: 1 });
    const expected = 1 / Math.sqrt(3);
    expect(x).toBeCloseTo(expected, 12);
    expect(y).toBeCloseTo(expected, 12);
    expect(z).toBeCloseTo(expected, 12);

    const next = orbitStateForRegion({ x: 1, y: 1, z: 1 }, HOME_ORBIT);
    expect(next.azimuth).toBeCloseTo(Math.PI / 4, 12);
    expect(next.elevation).toBeCloseTo(Math.asin(expected), 12);
  });

  it('距離と注視点は変えない', () => {
    const next = orbitStateForRegion(FACE_REGIONS.left, { ...HOME_ORBIT, distance: 77, target: [1, 2, 3] });
    expect(next.distance).toBe(77);
    expect(next.target).toEqual([1, 2, 3]);
  });
});

describe('視点の補間', () => {
  const from: OrbitState = { azimuth: 0, elevation: 0, distance: 100, target: [0, 0, 0] };

  it('進み具合 0 で出発点、1 で目標点になる', () => {
    const to: OrbitState = { ...from, azimuth: 1, elevation: 0.5 };
    expect(interpolateOrbit(from, to, 0).azimuth).toBeCloseTo(0, 12);
    expect(interpolateOrbit(from, to, 1).azimuth).toBeCloseTo(1, 12);
    expect(interpolateOrbit(from, to, 1).elevation).toBeCloseTo(0.5, 12);
  });

  it('範囲外の進み具合は 0 〜 1 に丸める', () => {
    const to: OrbitState = { ...from, azimuth: 1, elevation: 0 };
    expect(interpolateOrbit(from, to, -5).azimuth).toBeCloseTo(0, 12);
    expect(interpolateOrbit(from, to, 5).azimuth).toBeCloseTo(1, 12);
  });

  it('遠回りせず近い側へ回る', () => {
    // 0 度から 350 度へは +350 度ではなく -10 度で回る。
    const to: OrbitState = { ...from, azimuth: (350 * Math.PI) / 180 };
    const middle = interpolateOrbit(from, to, 0.5);
    expect(middle.azimuth).toBeCloseTo((-5 * Math.PI) / 180, 9);
  });

  it('角度の折り返しが -π 〜 +π に収まる', () => {
    expect(normalizeAngle(3 * Math.PI)).toBeCloseTo(Math.PI, 9);
    expect(normalizeAngle(-3 * Math.PI)).toBeCloseTo(Math.PI, 9);
    expect(normalizeAngle(0.5)).toBeCloseTo(0.5, 12);
  });
});
```

- [ ] **手順3: 検査する**

```
pnpm --filter @pointercad/ui run test
pnpm run typecheck
pnpm run lint
pnpm run test
pnpm run build
```

期待結果: `viewCubeMath.test.ts` の **15 件すべて passed**。全体検査4つが終了コード 0。

- [ ] **手順4: 統括へ報告する**

報告に含める: 作成した2ファイル、テスト 15 件の結果。**gitへは書き込まない。**

---

### タスク13: ビューキューブの描画とアニメーション

**先行タスク:** 11, 12
**対応要件:** FR-103(右上に常時表示、6面の表記、ドラッグ回転、クリックでアニメーション遷移)

**ファイル:**
- 新規: `packages/ui/src/viewcube/faceTexture.ts`
- 新規: `packages/ui/src/viewcube/createViewCubeScene.ts`
- 新規: `packages/ui/src/viewcube/ViewCube.tsx`
- 変更: `packages/ui/src/viewport/ViewportCanvas.tsx`

視点の正本は `attachCameraControls`(タスク11)が持つ1箇所だけにする。ビューキューブは自分で視点を持たず、`getOrbit` / `setOrbit` で借りる。状態を2箇所に持つと必ず食い違うため。

- [ ] **手順1: `packages/ui/src/viewcube/faceTexture.ts` を作る**

面の文字は `ja.json` から引く(NFR-MA-5)。

```ts
import * as THREE from 'three';

import { t, type MessageKey } from '../i18n/t.js';

const TEXTURE_SIZE = 128;

/** 立方体の 1 面ぶんの文字入り画像を作る。 */
export function createFaceTexture(labelKey: MessageKey): THREE.CanvasTexture {
  const canvas = globalThis.document.createElement('canvas');
  canvas.width = TEXTURE_SIZE;
  canvas.height = TEXTURE_SIZE;

  const context = canvas.getContext('2d');
  if (context === null) {
    throw new Error('ビューキューブの面を描く準備ができませんでした。');
  }

  context.fillStyle = '#d8dce4';
  context.fillRect(0, 0, TEXTURE_SIZE, TEXTURE_SIZE);
  context.strokeStyle = '#7a8394';
  context.lineWidth = 6;
  context.strokeRect(3, 3, TEXTURE_SIZE - 6, TEXTURE_SIZE - 6);

  context.fillStyle = '#1e1e1e';
  context.font = '600 56px "Yu Gothic UI", "Meiryo", sans-serif';
  context.textAlign = 'center';
  context.textBaseline = 'middle';
  context.fillText(t(labelKey), TEXTURE_SIZE / 2, TEXTURE_SIZE / 2);

  const texture = new THREE.CanvasTexture(canvas);
  texture.colorSpace = THREE.SRGBColorSpace;
  return texture;
}

/**
 * three.js の BoxGeometry の面の並び(+X, -X, +Y, -Y, +Z, -Z)に合わせた文字キー。
 * ここでの Y と Z は three.js の軸ではなく、ビューキューブを Z 上の座標系へ倒した後の意味に合わせる。
 * 立方体そのものは three.js の既定の軸で作り、シーン側で -90 度倒して Z 上に合わせる。
 */
export const FACE_LABEL_KEYS: readonly MessageKey[] = [
  'viewCube.right',
  'viewCube.left',
  'viewCube.top',
  'viewCube.bottom',
  'viewCube.back',
  'viewCube.front',
];
```

- [ ] **手順2: `packages/ui/src/viewcube/createViewCubeScene.ts` を作る**

本体のビューポートとは別の小さな `canvas` に、独立した描画器で立方体だけを描く。当たり判定は three.js の `Raycaster` で行い、当たった点を立方体のローカル座標へ直してからタスク12 の `regionFromLocalPoint` に渡す。

```ts
import * as THREE from 'three';

import { cameraPosition, type OrbitState } from '../viewport/cameraMath.js';
import { createFaceTexture, FACE_LABEL_KEYS } from './faceTexture.js';
import { regionFromLocalPoint, type ViewCubeRegion } from './viewCubeMath.js';

export interface ViewCubeScene {
  render(orbit: OrbitState): void;
  /** canvas 上の位置(0〜1 の正規化座標)から、指している領域を返す。 */
  pick(normalizedX: number, normalizedY: number): ViewCubeRegion | null;
  dispose(): void;
}

const CUBE_HALF_SIZE = 1;
const CAMERA_DISTANCE = 4;

export function createViewCubeScene(canvas: HTMLCanvasElement, sizePixels: number): ViewCubeScene {
  const renderer = new THREE.WebGLRenderer({ canvas, antialias: true, alpha: true });
  renderer.setPixelRatio(Math.min(globalThis.devicePixelRatio, 2));
  renderer.setSize(sizePixels, sizePixels, false);
  renderer.setClearAlpha(0);

  const scene = new THREE.Scene();
  scene.add(new THREE.AmbientLight(0xffffff, 2.2));

  const materials = FACE_LABEL_KEYS.map(
    (key) => new THREE.MeshBasicMaterial({ map: createFaceTexture(key) }),
  );
  const cube = new THREE.Mesh(
    new THREE.BoxGeometry(CUBE_HALF_SIZE * 2, CUBE_HALF_SIZE * 2, CUBE_HALF_SIZE * 2),
    materials,
  );
  // three.js の既定は Y 上、本アプリは Z 上。立方体を倒して軸の意味を合わせる。
  cube.rotation.x = -Math.PI / 2;
  scene.add(cube);

  const camera = new THREE.PerspectiveCamera(35, 1, 0.1, 100);
  camera.up.set(0, 0, 1);

  const raycaster = new THREE.Raycaster();

  return {
    render(orbit): void {
      const [x, y, z] = cameraPosition({ ...orbit, distance: CAMERA_DISTANCE, target: [0, 0, 0] });
      camera.position.set(x, y, z);
      camera.up.set(0, 0, 1);
      camera.lookAt(0, 0, 0);
      camera.updateProjectionMatrix();
      renderer.render(scene, camera);
    },

    pick(normalizedX, normalizedY): ViewCubeRegion | null {
      raycaster.setFromCamera(new THREE.Vector2(normalizedX, normalizedY), camera);
      const hits = raycaster.intersectObject(cube, false);
      const hit = hits[0];
      if (hit === undefined) {
        return null;
      }
      // 当たった点を立方体のローカル座標へ直し、-1 〜 +1 に正規化する。
      const local = cube.worldToLocal(hit.point.clone()).divideScalar(CUBE_HALF_SIZE);
      // 立方体は X 軸まわりに -90 度倒してあるので、Z 上の座標系へ戻す。
      return regionFromLocalPoint([local.x, -local.z, local.y]);
    },

    dispose(): void {
      cube.geometry.dispose();
      for (const material of materials) {
        material.map?.dispose();
        material.dispose();
      }
      renderer.dispose();
    },
  };
}
```

**注意:** 倒した立方体のローカル座標と Z 上のワールド座標の対応(`[local.x, -local.z, local.y]`)は、`cube.rotation.x = -Math.PI / 2` と `FACE_LABEL_KEYS` の並びの両方に依存する。タスク18 の手動確認で「前をクリックしたら前から見える」ことを必ず目で確かめ、ずれていたら**符号を当てずっぽうに変えず**、どの面をクリックしてどの向きになったかを統括へ報告してから直す。

- [ ] **手順3: `packages/ui/src/viewcube/ViewCube.tsx` を作る**

```tsx
import { useEffect, useRef } from 'react';

import type { OrbitState } from '../viewport/cameraMath.js';
import { createViewCubeScene } from './createViewCubeScene.js';
import { interpolateOrbit, orbitStateForRegion, VIEW_TRANSITION_DURATION_MS } from './viewCubeMath.js';

const CUBE_SIZE_PIXELS = 120;
/** これ以上動いたらクリックではなくドラッグとみなす(ピクセル)。 */
const DRAG_THRESHOLD_PIXELS = 4;

export interface ViewCubeProps {
  /** 現在の視点を返す。 */
  readonly getOrbit: () => OrbitState;
  /** 視点を変える。 */
  readonly setOrbit: (orbit: OrbitState) => void;
}

export function ViewCube({ getOrbit, setOrbit }: ViewCubeProps): React.JSX.Element {
  const canvasRef = useRef<HTMLCanvasElement>(null);

  useEffect(() => {
    const canvas = canvasRef.current;
    if (canvas === null) {
      return;
    }

    const scene = createViewCubeScene(canvas, CUBE_SIZE_PIXELS);

    let frameId = 0;
    let transition: { from: OrbitState; to: OrbitState; startedAt: number } | null = null;
    let dragging = false;
    let movedDistance = 0;
    let lastX = 0;
    let lastY = 0;

    const tick = (): void => {
      if (transition !== null) {
        const progress = (performance.now() - transition.startedAt) / VIEW_TRANSITION_DURATION_MS;
        setOrbit(interpolateOrbit(transition.from, transition.to, progress));
        if (progress >= 1) {
          transition = null;
        }
      }
      scene.render(getOrbit());
      frameId = globalThis.requestAnimationFrame(tick);
    };
    frameId = globalThis.requestAnimationFrame(tick);

    const onPointerDown = (event: PointerEvent): void => {
      dragging = true;
      movedDistance = 0;
      lastX = event.clientX;
      lastY = event.clientY;
      canvas.setPointerCapture(event.pointerId);
      event.preventDefault();
    };

    const onPointerMove = (event: PointerEvent): void => {
      if (!dragging) {
        return;
      }
      const deltaX = event.clientX - lastX;
      const deltaY = event.clientY - lastY;
      lastX = event.clientX;
      lastY = event.clientY;
      movedDistance += Math.abs(deltaX) + Math.abs(deltaY);

      if (movedDistance > DRAG_THRESHOLD_PIXELS) {
        transition = null;
        const current = getOrbit();
        setOrbit({
          ...current,
          azimuth: current.azimuth - deltaX * 0.012,
          elevation: current.elevation + deltaY * 0.012,
        });
      }
    };

    const onPointerUp = (event: PointerEvent): void => {
      if (!dragging) {
        return;
      }
      dragging = false;
      if (canvas.hasPointerCapture(event.pointerId)) {
        canvas.releasePointerCapture(event.pointerId);
      }
      if (movedDistance > DRAG_THRESHOLD_PIXELS) {
        return;
      }

      const bounds = canvas.getBoundingClientRect();
      const normalizedX = ((event.clientX - bounds.left) / bounds.width) * 2 - 1;
      const normalizedY = -(((event.clientY - bounds.top) / bounds.height) * 2 - 1);
      const region = scene.pick(normalizedX, normalizedY);
      if (region !== null) {
        const current = getOrbit();
        transition = {
          from: current,
          to: orbitStateForRegion(region, current),
          startedAt: performance.now(),
        };
      }
    };

    canvas.addEventListener('pointerdown', onPointerDown);
    canvas.addEventListener('pointermove', onPointerMove);
    canvas.addEventListener('pointerup', onPointerUp);
    canvas.addEventListener('pointercancel', onPointerUp);

    return () => {
      globalThis.cancelAnimationFrame(frameId);
      canvas.removeEventListener('pointerdown', onPointerDown);
      canvas.removeEventListener('pointermove', onPointerMove);
      canvas.removeEventListener('pointerup', onPointerUp);
      canvas.removeEventListener('pointercancel', onPointerUp);
      scene.dispose();
    };
  }, [getOrbit, setOrbit]);

  return (
    <canvas
      ref={canvasRef}
      className="pcad-viewcube"
      width={CUBE_SIZE_PIXELS}
      height={CUBE_SIZE_PIXELS}
    />
  );
}
```

- [ ] **手順4: `packages/ui/src/viewport/ViewportCanvas.tsx` にビューキューブを差し込む**

`return` を次に差し替え、`useState` で `controls` の準備完了を伝える。`controlsRef` は初回描画時にはまだ空なので、そのままでは `ViewCube` へ渡せないため。

```tsx
import { useEffect, useRef, useState } from 'react';

import { ViewCube } from '../viewcube/ViewCube.js';
import { useAppStore } from '../store/useAppStore.js';
import { attachCameraControls, type CameraControls } from './attachCameraControls.js';
import { createViewportScene, type ViewportScene } from './createViewportScene.js';
import type { OrbitState } from './cameraMath.js';
```

`useEffect` の中で `controlsRef.current = controls;` を書いた直後に `setControlsReady(true);` を、後始末で `setControlsReady(false);` を足す。フックの並びは次のとおり。

```tsx
  const [controlsReady, setControlsReady] = useState(false);

  const getOrbit = useCallback((): OrbitState => {
    return controlsRef.current?.getOrbit() ?? HOME_ORBIT;
  }, []);

  const setOrbit = useCallback((next: OrbitState): void => {
    controlsRef.current?.setOrbit(next);
  }, []);
```

`return` は次のようにして、canvas とビューキューブを重ねる。

```tsx
  return (
    <>
      <canvas ref={canvasRef} className="pcad-viewport__canvas" />
      {controlsReady ? <ViewCube getOrbit={getOrbit} setOrbit={setOrbit} /> : null}
    </>
  );
```

`useCallback` と `HOME_ORBIT` の import を忘れないこと(`import { useCallback, useEffect, useRef, useState } from 'react';` と `import { HOME_ORBIT, type OrbitState } from './cameraMath.js';`)。`ViewCube` の `useEffect` は `getOrbit` / `setOrbit` を依存に取るので、`useCallback` で包まないと毎回作り直しになる。

- [ ] **手順5: 検査する**

```
pnpm run typecheck
pnpm run lint
pnpm run test
pnpm run build
```

期待結果: 4つとも終了コード 0。`react-hooks/exhaustive-deps` の警告が出ていないこと。

- [ ] **手順6: 統括へ報告する**

報告に含める: 作成した3ファイルと変更した1ファイル、視点の正本を `attachCameraControls` の1箇所に置いた設計(状態の二重管理を避けるため)。**gitへは書き込まない。**

---

### タスク14: `ui` の統合(`model` 経由で箱を表示)

**先行タスク:** 11, 13
**対応要件:** 要件§6.3(データフロー)、`rules/04-設計の規律.md`(UI から幾何カーネルへ直接依存しない)、FR-504 / NFR-RE-1(失敗してもクラッシュしない)

**ファイル:**
- 新規: `packages/ui/src/app/PointerCadApp.tsx`
- 変更: `packages/ui/src/index.ts`

- [ ] **手順1: `packages/ui/src/app/PointerCadApp.tsx` を作る**

`@pointercad/model` だけを使う。`@pointercad/kernel` も `opencascade.js` も import しない(ESLint と tsconfig の参照が違反を検出する)。

```tsx
import { createBoxPartDocument, createKernelBridge, recomputePart } from '@pointercad/model';
import { useEffect } from 'react';

import { AppShell } from '../shell/AppShell.js';
import { useAppStore } from '../store/useAppStore.js';

/**
 * アプリの入口。Web 版とデスクトップ版で同じものを使う(要件§1.5、機能差を作らない)。
 * 起動時に「直方体1個の部品」を作り、Worker 内の幾何カーネルで計算して表示する。
 */
export function PointerCadApp(): React.JSX.Element {
  useEffect(() => {
    const store = useAppStore.getState();
    const document = createBoxPartDocument();
    store.setDocument(
      document.name,
      document.features.map((feature) => feature.name),
    );
    store.setComputing(true);

    const bridge = createKernelBridge();
    let cancelled = false;

    void (async () => {
      const result = await recomputePart(document, bridge);
      if (cancelled) {
        return;
      }
      if (result.status === 'ok') {
        useAppStore.getState().setMesh(result.mesh);
      } else {
        // 計算に失敗してもアプリは落とさず、理由を画面に出す(FR-504、NFR-RE-1)。
        useAppStore.getState().setError(result.message);
      }
    })();

    return () => {
      cancelled = true;
      bridge.dispose();
    };
  }, []);

  return <AppShell />;
}
```

- [ ] **手順2: `packages/ui/src/index.ts` を更新する**

```ts
export { t, MESSAGE_KEYS, type MessageKey } from './i18n/t.js';
export { useAppStore, type AppState, type DisplayStyle, type ProjectionMode } from './store/useAppStore.js';
export { AppShell } from './shell/AppShell.js';
export { PointerCadApp } from './app/PointerCadApp.js';
export { HOME_ORBIT, type OrbitState } from './viewport/cameraMath.js';
```

- [ ] **手順3: 依存方向の施行が効くことを確かめる**

`packages/ui/src/app/PointerCadApp.tsx` の先頭へ一時的に `import '@pointercad/kernel';` を書き足し、次の**両方**でエラーが出ることを確認する。

```
pnpm run typecheck
pnpm run lint
```

期待結果: `typecheck` は「参照に無いプロジェクト」の旨で失敗し、`lint` は `依存方向 apps → ui → model → kernel / expression に違反しています` を出す。**確認後に必ずこの行を消し、両方が通ることを再確認する。**

- [ ] **手順4: 検査する**

```
pnpm run typecheck
pnpm run lint
pnpm run test
pnpm run build
```

期待結果: 4つとも終了コード 0。

- [ ] **手順5: 統括へ報告する**

報告に含める: 作成した1ファイルと変更した1ファイル、手順3 で型検査と lint の**両方**が違反を検出したかどうか。**gitへは書き込まない。**

---

### タスク15: `apps/web`(Vite + COOP/COEP)

**先行タスク:** 14
**対応要件:** FR-1001(同一コードベースのフル機能 Web 版)、FR-1002(サーバーサイド処理を持たない)、FR-1003(COOP/COEP ヘッダー)、NFR-SE-1

**ファイル:**
- 新規: `apps/web/package.json`
- 新規: `apps/web/tsconfig.json`
- 新規: `apps/web/vite.config.ts`
- 新規: `apps/web/index.html`
- 新規: `apps/web/src/main.tsx`
- 新規: `apps/web/public/_headers`
- 変更: `tsconfig.json`(`references` に1行追加)

- [ ] **手順1: `apps/web/package.json` を作る**

```json
{
  "name": "@pointercad/web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build",
    "dev": "vite",
    "preview": "vite preview"
  },
  "dependencies": {
    "@pointercad/ui": "workspace:*",
    "react": "catalog:",
    "react-dom": "catalog:"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "catalog:",
    "vite": "catalog:"
  }
}
```

`dev` と `preview` は用意するが、**作業担当は実行しない**(`rules/02-禁止事項.md`)。起動は統括が行う。

- [ ] **手順2: `apps/web/tsconfig.json` を作る**

```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": "./src",
    "outDir": "./dist-types",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "types": ["vite/client"]
  },
  "include": ["src"],
  "references": [{ "path": "../../packages/ui" }]
}
```

`outDir` を `dist-types` にするのは、Vite のビルド成果物(`dist`)と型の出力先を分けるため。`.gitignore` の `dist/` は `dist-types/` に一致しないので、**このディレクトリは `git status` に未追跡として現れる。** `scripts/check.ps1` の追跡対象ガードは `--untracked-files=no` なので検査は落ちないが、統括が `.gitignore` へ `dist-types/` を足すか判断できるよう報告する。

- [ ] **手順3: `apps/web/vite.config.ts` を作る**

```ts
import react from '@vitejs/plugin-react';
import { defineConfig } from 'vite';

/** WASM の並列実行に必要な隔離状態を作る(FR-1003)。開発時も配信時と同じ条件にする。 */
const crossOriginIsolationHeaders = {
  'Cross-Origin-Opener-Policy': 'same-origin',
  'Cross-Origin-Embedder-Policy': 'require-corp',
};

export default defineConfig({
  plugins: [react()],
  // Emscripten のグルーコードを事前バンドルさせない。Node 専用の分岐が含まれるため。
  optimizeDeps: { exclude: ['opencascade.js'] },
  worker: { format: 'es' },
  build: {
    target: 'esnext',
    // 50MB の WASM が誤って埋め込まれないよう、資産の埋め込みを無効にする。
    assetsInlineLimit: 0,
  },
  server: { headers: crossOriginIsolationHeaders },
  preview: { headers: crossOriginIsolationHeaders },
});
```

- [ ] **手順4: `apps/web/public/_headers` を作る**

Cloudflare Pages は出力先の直下の `_headers` を読む。`public/` に置けば Vite がそのまま `dist/_headers` へ複製する。

```
/*
  Cross-Origin-Opener-Policy: same-origin
  Cross-Origin-Embedder-Policy: require-corp
  Cross-Origin-Resource-Policy: same-origin
  X-Content-Type-Options: nosniff
```

- [ ] **手順5: `apps/web/index.html` を作る**

```html
<!doctype html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>PointerCAD</title>
    <style>
      html,
      body,
      #root {
        height: 100%;
        margin: 0;
      }
    </style>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

- [ ] **手順6: `apps/web/src/main.tsx` を作る**

```tsx
import { PointerCadApp } from '@pointercad/ui';
import '@pointercad/ui/style.css';
import { StrictMode } from 'react';
import { createRoot } from 'react-dom/client';

const container = document.getElementById('root');
if (container === null) {
  throw new Error('画面の土台が見つかりません。');
}

createRoot(container).render(
  <StrictMode>
    <PointerCadApp />
  </StrictMode>,
);
```

- [ ] **手順7: ルート `tsconfig.json` の `references` に `{ "path": "./apps/web" }` を足し、ビルドする**

```
pnpm install
pnpm run typecheck
pnpm run lint
pnpm run test
pnpm run build
```

期待結果: 4つとも終了コード 0。**`pnpm run build` の出力を必ず確認する:**

1. `apps/web/dist/index.html` が生成されている。
2. `apps/web/dist/_headers` が生成されている。
3. `apps/web/dist/assets/` に **拡張子 `.wasm` のファイル(約 50MB)** が1つある。
4. `apps/web/dist/assets/` に **Worker のファイル**(`kernel.worker-*.js` のような名前)がある。
5. ビルド中に `Could not resolve "fs"` / `"path"` / `"module"` のような未解決の警告が出ていない。

- [ ] **手順8: ビルド成果物の中身を確かめる(ブラウザは起動しない)**

生成物の一覧とサイズを次で確認する。

```
Get-ChildItem -Recurse apps/web/dist | Select-Object FullName, Length
```

期待結果: 上記1〜4 のファイルが存在し、`.wasm` のサイズが `node_modules/opencascade.js/dist/opencascade.full.wasm` と一致する。

- [ ] **手順9: うまくいかなかった場合**

手順7 の 3〜5 のいずれかが満たせなかったら、**自己判断で依存を足さず**、§2.2 の代替案を上から順に試す。案1(静的 import へ戻す)と案2(資産として自前で複製する)は新規依存が不要なのでその場で試してよい。案3 以降は**統括の承認が必要**。試した内容と結果を全て統括へ報告する。

- [ ] **手順10: 統括へ報告する**

報告に含める: 作成した6ファイルと変更した1ファイル、`apps/web/dist` の中身の一覧(ファイル名とサイズ)、`.wasm` と Worker が出力されたか、§2.2 の代替案を使ったならどれをなぜ使ったか、`dist-types/` を `.gitignore` へ足すべきかの意見。**gitへは書き込まない。**

---

### タスク16: `apps/desktop`(Electron)

**先行タスク:** 14
**対応要件:** 要件§1.5(Windows / Linux のデスクトップ版)、§6.1(Electron)、FR-1003 との整合

**ファイル:**
- 新規: `apps/desktop/package.json`
- 新規: `apps/desktop/tsconfig.json`
- 新規: `apps/desktop/vite.main.config.ts`
- 新規: `apps/desktop/vite.preload.config.ts`
- 新規: `apps/desktop/vite.renderer.config.ts`
- 新規: `apps/desktop/index.html`
- 新規: `apps/desktop/src/main/main.ts`
- 新規: `apps/desktop/src/main/appProtocol.ts`
- 新規: `apps/desktop/src/preload/preload.ts`
- 新規: `apps/desktop/src/renderer/main.tsx`
- 変更: `tsconfig.json`(`references` に1行追加)

- [ ] **手順1: `apps/desktop/package.json` を作る**

`main` は本体プロセスのビルド成果物を指す。electron-builder は P0 に含めない(§0.6)。

```json
{
  "name": "@pointercad/desktop",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/main/main.cjs",
  "scripts": {
    "build": "vite build --config vite.main.config.ts && vite build --config vite.preload.config.ts && vite build --config vite.renderer.config.ts",
    "start": "electron ."
  },
  "dependencies": {
    "@pointercad/ui": "workspace:*",
    "react": "catalog:",
    "react-dom": "catalog:"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "catalog:",
    "electron": "catalog:",
    "vite": "catalog:"
  }
}
```

`start` は**統括だけが実行する**(`rules/01-役割と委譲.md` §1)。作業担当は実行しない。

- [ ] **手順2: `apps/desktop/tsconfig.json` を作る**

```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": "./src",
    "outDir": "./dist-types",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "types": ["node", "vite/client"]
  },
  "include": ["src"],
  "references": [{ "path": "../../packages/ui" }]
}
```

- [ ] **手順3: `apps/desktop/src/main/appProtocol.ts` を作る**

`file://` ではなく独自スキームで配信する理由は §2.1 の表のとおり。

```ts
import { net, protocol } from 'electron';
import { join, normalize, sep } from 'node:path';
import { pathToFileURL } from 'node:url';

/** 画面の配信に使う独自スキーム。Web 版と同じ「http のような」条件を再現する。 */
export const APP_SCHEME = 'app';
export const APP_ORIGIN = `${APP_SCHEME}://pointercad`;
export const APP_ENTRY_URL = `${APP_ORIGIN}/index.html`;

/**
 * app.whenReady() より前に呼ぶ必要がある。
 * fetch と CORS を有効にしないと、WASM の読み込みが Chromium にブロックされる。
 */
export function registerAppScheme(): void {
  protocol.registerSchemesAsPrivileged([
    {
      scheme: APP_SCHEME,
      privileges: {
        standard: true,
        secure: true,
        supportFetchAPI: true,
        corsEnabled: true,
        stream: true,
      },
    },
  ]);
}

/** app.whenReady() の後に呼ぶ。rendererRoot の外へは出さない。 */
export function handleAppScheme(rendererRoot: string): void {
  const root = normalize(rendererRoot);

  protocol.handle(APP_SCHEME, async (request) => {
    const url = new URL(request.url);
    const relativePath = url.pathname === '/' ? '/index.html' : url.pathname;
    const filePath = normalize(join(root, decodeURIComponent(relativePath)));

    if (filePath !== root && !filePath.startsWith(root + sep)) {
      return new Response('Forbidden', { status: 403 });
    }

    const response = await net.fetch(pathToFileURL(filePath).toString());
    const headers = new Headers(response.headers);
    // Web 版(apps/web/public/_headers)と同じ隔離状態を作る(FR-1003)。
    headers.set('Cross-Origin-Opener-Policy', 'same-origin');
    headers.set('Cross-Origin-Embedder-Policy', 'require-corp');
    headers.set('Cross-Origin-Resource-Policy', 'same-origin');

    return new Response(response.body, { status: response.status, headers });
  });
}
```

- [ ] **手順4: `apps/desktop/src/main/main.ts` を作る**

```ts
import { app, BrowserWindow } from 'electron';
import { dirname, join } from 'node:path';
import { fileURLToPath } from 'node:url';

import { APP_ENTRY_URL, handleAppScheme, registerAppScheme } from './appProtocol.js';

const currentDirectory = dirname(fileURLToPath(import.meta.url));
/** dist/main から見た画面の出力先。 */
const rendererRoot = join(currentDirectory, '..', 'renderer');
const preloadPath = join(currentDirectory, '..', 'preload', 'preload.cjs');

/** 開発時は Vite の開発サーバーを読む。統括が起動して環境変数で渡す。 */
const devServerUrl = process.env['PCAD_DEV_SERVER_URL'];

registerAppScheme();

function createMainWindow(): void {
  const window = new BrowserWindow({
    width: 1440,
    height: 900,
    backgroundColor: '#1e1e1e',
    title: 'PointerCAD',
    webPreferences: {
      preload: preloadPath,
      contextIsolation: true,
      nodeIntegration: false,
      sandbox: true,
    },
  });

  void window.loadURL(devServerUrl ?? APP_ENTRY_URL);
}

void app.whenReady().then(() => {
  handleAppScheme(rendererRoot);
  createMainWindow();

  app.on('activate', () => {
    if (BrowserWindow.getAllWindows().length === 0) {
      createMainWindow();
    }
  });
});

app.on('window-all-closed', () => {
  if (process.platform !== 'darwin') {
    app.quit();
  }
});
```

- [ ] **手順5: `apps/desktop/src/preload/preload.ts` を作る**

P0 では画面へ渡すものはプラットフォーム名だけ。要件 FR-806(デスクトップ版のファイルダイアログ)は P2 以降でここに足す。

```ts
import { contextBridge } from 'electron';

/** 画面側へ渡す最小の情報。プラットフォーム固有コードは apps/ 配下だけに置く(rules/04)。 */
contextBridge.exposeInMainWorld('pointercadDesktop', {
  platform: process.platform,
});
```

- [ ] **手順6: `apps/desktop/index.html` と `apps/desktop/src/renderer/main.tsx` を作る**

`index.html` は `apps/web/index.html`(タスク15 手順5)と同じ内容で、`src` だけ `/src/renderer/main.tsx` に変える。
`main.tsx` は `apps/web/src/main.tsx`(タスク15 手順6)と同じ内容にする。

```tsx
import { PointerCadApp } from '@pointercad/ui';
import '@pointercad/ui/style.css';
import { StrictMode } from 'react';
import { createRoot } from 'react-dom/client';

const container = document.getElementById('root');
if (container === null) {
  throw new Error('画面の土台が見つかりません。');
}

createRoot(container).render(
  <StrictMode>
    <PointerCadApp />
  </StrictMode>,
);
```

- [ ] **手順7: `apps/desktop/vite.main.config.ts` を作る**

本体プロセスは CommonJS で出す。Electron の本体プロセスで ESM を使うと `sandbox: true` の preload と噛み合わせが難しくなるため。

```ts
import { defineConfig } from 'vite';

export default defineConfig({
  build: {
    outDir: 'dist/main',
    emptyOutDir: true,
    target: 'node22',
    lib: {
      entry: 'src/main/main.ts',
      formats: ['cjs'],
      fileName: () => 'main.cjs',
    },
    rollupOptions: {
      external: ['electron', 'node:path', 'node:url', 'node:fs'],
    },
  },
});
```

- [ ] **手順8: `apps/desktop/vite.preload.config.ts` を作る**

```ts
import { defineConfig } from 'vite';

export default defineConfig({
  build: {
    outDir: 'dist/preload',
    emptyOutDir: true,
    target: 'node22',
    lib: {
      entry: 'src/preload/preload.ts',
      formats: ['cjs'],
      fileName: () => 'preload.cjs',
    },
    rollupOptions: {
      external: ['electron'],
    },
  },
});
```

- [ ] **手順9: `apps/desktop/vite.renderer.config.ts` を作る**

`base: './'` にして、資産の場所を相対で書き出す。独自スキームの下でも正しく解決される。

```ts
import react from '@vitejs/plugin-react';
import { defineConfig } from 'vite';

const crossOriginIsolationHeaders = {
  'Cross-Origin-Opener-Policy': 'same-origin',
  'Cross-Origin-Embedder-Policy': 'require-corp',
};

export default defineConfig({
  base: './',
  plugins: [react()],
  optimizeDeps: { exclude: ['opencascade.js'] },
  worker: { format: 'es' },
  build: {
    outDir: 'dist/renderer',
    emptyOutDir: true,
    target: 'esnext',
    assetsInlineLimit: 0,
  },
  server: { headers: crossOriginIsolationHeaders },
});
```

- [ ] **手順10: ルート `tsconfig.json` の `references` に `{ "path": "./apps/desktop" }` を足し、検査する**

```
pnpm install
pnpm run typecheck
pnpm run lint
pnpm run test
pnpm run build
```

期待結果: 4つとも終了コード 0。ビルド後に次が揃っている。

1. `apps/desktop/dist/main/main.cjs`
2. `apps/desktop/dist/preload/preload.cjs`
3. `apps/desktop/dist/renderer/index.html`
4. `apps/desktop/dist/renderer/assets/` の中に `.wasm`(約 50MB)と Worker のファイル

`emptyOutDir: true` を3つの設定でそれぞれ別のディレクトリに向けているので、順に実行しても互いを消さない。**もし `dist/main` のビルドが `dist/renderer` を消してしまう場合は、`emptyOutDir` を外して報告する。**

- [ ] **手順11: Electron を起動しないことを確認する**

作業担当は `pnpm --filter @pointercad/desktop run start` を**実行しない**(`rules/02-禁止事項.md`)。動作確認はタスク18 で統括が行う。

- [ ] **手順12: 統括へ報告する**

報告に含める: 作成した10ファイルと変更した1ファイル、`apps/desktop/dist` の中身の一覧(ファイル名とサイズ)、ビルド全体の実測所要時間、`pnpm install` で Electron のバイナリが取得されたか(`node_modules/electron/dist` の有無)。**gitへは書き込まない。Electron を起動しない。**

---

### タスク17: Playwright スモークテスト

**先行タスク:** 15
**対応要件:** NFR-MA-2(主要ユーザーフローは E2E テスト)

§0.3 の判断により、**ルート `package.json` には `test:e2e` ではなく `e2e` として定義する**(タスク1 で定義済み)。`scripts/check.ps1` は `test:e2e` にしか反応しないので、毎コミットの検査には入らない。

**ファイル:**
- 新規: `e2e/playwright.config.ts`
- 新規: `e2e/tests/smoke.spec.ts`

- [ ] **手順1: `e2e/playwright.config.ts` を作る**

```ts
import { defineConfig, devices } from '@playwright/test';

const PREVIEW_PORT = 4173;
const BASE_URL = `http://127.0.0.1:${PREVIEW_PORT}`;

export default defineConfig({
  testDir: './tests',
  // 50MB の WASM を読み込むため、通常の Web アプリより長く待つ。
  timeout: 180_000,
  expect: { timeout: 120_000 },
  reporter: [['list']],
  use: {
    ...devices['Desktop Chrome'],
    baseURL: BASE_URL,
  },
  webServer: {
    // apps/web の preview は COOP/COEP ヘッダーを付ける(vite.config.ts)。
    command: `pnpm --filter @pointercad/web run preview --port ${PREVIEW_PORT} --strictPort`,
    url: BASE_URL,
    reuseExistingServer: false,
    timeout: 180_000,
  },
});
```

- [ ] **手順2: `e2e/tests/smoke.spec.ts` を作る**

「Web 版が起動して canvas が描画される」ことだけを見る1件に絞る。

```ts
import { expect, test } from '@playwright/test';

test('Web 版が起動し、ビューポートに絵が描かれる', async ({ page }) => {
  const consoleErrors: string[] = [];
  page.on('console', (message) => {
    if (message.type() === 'error') {
      consoleErrors.push(message.text());
    }
  });
  page.on('pageerror', (error) => {
    consoleErrors.push(error.message);
  });

  await page.goto('/');

  // 画面が隔離状態で動いている(FR-1003)。
  expect(await page.evaluate(() => globalThis.crossOriginIsolated)).toBe(true);

  const viewport = page.locator('canvas.pcad-viewport__canvas');
  await expect(viewport).toBeVisible();

  const size = await viewport.boundingBox();
  expect(size?.width ?? 0).toBeGreaterThan(100);
  expect(size?.height ?? 0).toBeGreaterThan(100);

  // 幾何カーネルの計算が終わると、三角形の数がプロパティ欄に出る。
  await expect(page.locator('.pcad-panel--right')).toContainText('12', { timeout: 120_000 });

  // ビューキューブも常時表示されている(FR-103)。
  await expect(page.locator('canvas.pcad-viewcube')).toBeVisible();

  // 画面が真っ黒(何も描かれていない)でないことを、canvas の中身から確かめる。
  const isBlank = await viewport.evaluate((element) => {
    const canvas = element as HTMLCanvasElement;
    const context = canvas.getContext('webgl2', { preserveDrawingBuffer: true });
    return context === null;
  });
  expect(isBlank).toBe(false);

  expect(consoleErrors).toEqual([]);
});
```

**注意:** `preserveDrawingBuffer` を後から指定しても効かないため、画素そのものの検証はしない。「WebGL2 の文脈が取れること」「三角形の数が出ること」「コンソールにエラーが無いこと」の3つで代用する。より厳密な画素の確認は P7 の課題とする。

- [ ] **手順3: ブラウザを用意する(統括の承認が要る)**

Playwright は初回にブラウザのバイナリを取得する。これは依存の追加に相当するため、**実行前に統括へ承認を求める**。

```
pnpm exec playwright install chromium
```

承認が得られない場合は手順4 を飛ばし、`e2e/` のファイルだけを作って統括へ報告する。

- [ ] **手順4: ヘッドレスで実行する**

```
pnpm run e2e
```

期待結果: **1 件 passed**。Playwright が `vite preview` を起動し、テスト終了時に自動で落とす(ブラウザの窓は開かない = ヘッドレス)。

- [ ] **手順5: 全体検査に影響が無いことを確かめる**

```
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/check.ps1
```

期待結果: 出力に `(5/5) pnpm run test:e2e` が**現れない**(検査は 4 件のまま)。現れた場合は `test:e2e` という名前でスクリプトを定義してしまっているので、`e2e` へ直す。

- [ ] **手順6: 統括へ報告する**

報告に含める: 作成した2ファイル、`pnpm run e2e` の結果と実測所要時間(実行した場合)、`scripts/check.ps1` の検査件数が 4 のままであること、`playwright-report/` と `test-results/` が `.gitignore` 済みであること。**gitへは書き込まない。**

---

### タスク18: 全体検査と P0 完了確認の準備

**先行タスク:** 16, 17
**対応要件:** 要件§9 P0 の完了条件

**ファイル:** 新規・変更なし(確認と報告のみ)

- [ ] **手順1: 作り直しても通ることを確かめる**

```
pnpm run clean
pnpm install --frozen-lockfile
```

期待結果: どちらも終了コード 0。`--frozen-lockfile` が通ることは CI(`scripts/check.ps1 -Install`)が通る前提条件になる。

- [ ] **手順2: 統括と同じ入口で全検査を通す**

```
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/check.ps1
```

期待結果: `(1/4)` 〜 `(4/4)` がすべて通り、最後に `[OK] 全ての検査に合格しました`。**`[NG] 検査が追跡対象のファイルを書き換えました` が出ないこと。**

- [ ] **手順3: 作業ツリーが汚れていないことを確かめる**

```
git status --porcelain --untracked-files=no
```

期待結果: **出力が空**(検査の実行で追跡対象のファイルが1つも変わっていない)。

- [ ] **手順4: 未追跡ファイルの一覧を作る**

```
git status --porcelain
```

出力のうち `??` で始まる行を全て統括へ報告する。`.gitignore` に足すべきもの(`dist-types/` など)があれば併せて提案する。**`.gitignore` は作業担当が変更しない。**

- [ ] **手順5: 統括へ最終報告する**

報告に含める:
- `scripts/check.ps1` の全出力の要点と、4つの検査それぞれの実測所要時間。
- テストの総件数(パッケージごと)。
- `apps/web/dist` と `apps/desktop/dist` の中身の一覧とサイズ。
- 未追跡ファイルの一覧。
- §0 の確認事項のうち、実装中に判明して判断が変わったもの。
- §1.2 の未確認事項のうち、解消したものと残ったもの。

**gitへは書き込まない。Electron も開発サーバーもブラウザも起動しない。**

---

## 5. P0 完了の確認手順(統括が手動で行う)

作業担当が全タスクを終えた後、**統括だけ**が次を実行して要件§9 の P0 完了条件「箱形状が両環境で表示・視点操作できる」を確かめる。`rules/01-役割と委譲.md` §1 のとおり、アプリの起動と終了は統括の担当。二重起動を避けるため、起動前に該当プロセスと待受ポートを確認する(`rules/02-禁止事項.md`)。

### 5.1 事前確認

- [ ] `git status --porcelain --untracked-files=no` が空である。
- [ ] `scripts/check.ps1` が `[OK] 全ての検査に合格しました` で終わる。
- [ ] `git diff --cached` の全文を読み、`package.json` の依存が本計画書 §1 の表と一致し、`eslint-disable` / `@ts-ignore` / `@ts-expect-error` / `any` / `as unknown as` が混入していないことを確かめた。
- [ ] `pnpm-lock.yaml` の差分に、§1 の表に無いパッケージが直接依存として現れていない。

### 5.2 Web 版の確認

起動(統括のみ):

```
Get-NetTCPConnection -LocalPort 4173 -ErrorAction SilentlyContinue
pnpm --filter @pointercad/web run preview --port 4173 --strictPort
```

ブラウザで `http://127.0.0.1:4173` を開き、次を確認する。

- [ ] 画面が5区画(上のツールバー / 左のモデルブラウザ / 中央のビューポート / 右のプロパティ / 下のステータスバー)に分かれている(要件§7.1)。
- [ ] 中央に灰色の直方体が表示されている(**P0 完了条件の本体**)。
- [ ] 右のプロパティ欄に「三角形の数: 12」と出ている。
- [ ] 左のモデルブラウザに「部品1」「直方体1」が出ている。
- [ ] **FR-101**: マウスの中ボタンドラッグで視点が回る。
- [ ] **FR-101**: Shift + 中ボタンドラッグで表示が平行に動く。
- [ ] **FR-101**: ホイールで拡大・縮小できる。
- [ ] **FR-101(代替操作)**: Alt + 左ボタンドラッグで視点が回る。Alt + Shift + 左ボタンドラッグで平行に動く。
- [ ] **FR-102**: ツールバーの「透視投影 / 平行投影」で見え方が切り替わる。
- [ ] **FR-104**: 方眼と XYZ 軸が表示され、拡大・縮小に応じて方眼の間隔が段階的に切り替わる。ツールバーの「グリッド」で表示・非表示が切り替わる。
- [ ] **FR-105**: 「面のみ / 面と稜線 / 稜線のみ」の3つが切り替わり、見た目が変わる。
- [ ] **FR-108**: Home キー、またはツールバーの「ホーム視点」で斜め上からの向きに戻る。
- [ ] **FR-103**: 右上に立方体が常時表示され、「前」「後」「右」「左」「上」「下」の文字が読める。
- [ ] **FR-103**: 立方体をドラッグすると視点が回る。
- [ ] **FR-103**: 立方体の「前」の面をクリックすると、正面から見た向きへなめらかに(0.3 秒ほどで)切り替わる。他の5面も同様。
- [ ] **FR-103**: 立方体の辺と頂点をクリックすると、斜めから見た向きへ切り替わる。
- [ ] **FR-1003**: ブラウザの開発者ツールのコンソールで `crossOriginIsolated` が `true`。
- [ ] コンソールにエラーが出ていない。

確認後、`preview` を終了する(Ctrl+C)。

### 5.3 デスクトップ版の確認

起動(統括のみ):

```
Get-Process electron -ErrorAction SilentlyContinue
pnpm --filter @pointercad/desktop run start
```

- [ ] 窓が開き、タイトルが「PointerCAD」である。
- [ ] §5.2 と**同じ内容が同じように**表示・操作できる(要件§1.5「Web 版とデスクトップ版に機能差を作らない」)。とくに直方体の表示、視点操作、ビューキューブのクリック遷移を再確認する。
- [ ] 開発者ツール(Ctrl+Shift+I)のコンソールにエラーが出ていない。
- [ ] `crossOriginIsolated` が `true`。

確認後、窓を閉じて Electron のプロセスが残っていないことを `Get-Process electron` で確かめる。

### 5.4 記録

- [ ] `docs/報告記録.md` へ、確認した日時(`Get-Date -Format 'yyyy-MM-dd HH:mm'`)、上記チェックリストの結果、実測値(起動時間、三角形の数、ビルド成果物のサイズ)、残った課題を記録する。
- [ ] 要件§12 の未決事項のうち P0 で決めたもの(配色テーマ = ダーク基調、Blender 互換の割当表 = タスク11 手順2 の表、座標系 = Z 上)を `docs/報告記録.md` へ書き留める。
- [ ] タスクごとにコミットする(`rules/03-品質ゲート.md` §6)。

### 5.5 P0 の完了条件(すべて満たすこと)

| # | 条件 | 確認方法 |
|---|---|---|
| 1 | 箱形状が Web 版で表示され、視点操作できる | §5.2 の全項目 |
| 2 | 箱形状が Electron 版で表示され、視点操作できる | §5.3 の全項目 |
| 3 | `scripts/check.ps1` が合格する | §5.1 |
| 4 | CI(Windows / Linux)が緑になる | push 後に GitHub 上の実際の結果を見る。**手元の合格を「CI に通った」と報告しない**(`rules/03-品質ゲート.md` §7.2) |
| 5 | 幾何カーネルが Web Worker 内で動いている | ブラウザの開発者ツールの Sources に `pointercad-kernel` の Worker が現れる |
| 6 | 要件§6.2 の7パッケージ+2アプリが揃い、依存方向が守られている | タスク6 手順12 とタスク14 手順3 の施行確認 |

---

## 6. 自己点検の結果

計画を書き終えた後に、要件と突き合わせて確認した点。

| 点検項目 | 結果 |
|---|---|
| P0 の範囲(要件§9)の網羅 | モノレポ=タスク1・2・7、CI=既存 `ci.yml` をそのまま使う(タスク1 で必須スクリプトを揃える)、Electron/Web 両起動=タスク15・16、OCCT WASM 統合=タスク3〜5、ビューポート=タスク10・11、ビューキューブ=タスク12・13。**漏れなし。** |
| 実ファイル名の一貫性 | `cameraMath.ts` の `OrbitState` を `viewCubeMath.ts`・`createViewportScene.ts`・`createViewCubeScene.ts`・`attachCameraControls.ts`・`ViewCube.tsx` が同じ名前で参照している。`MeshData`(kernel)と `PartMesh`(model)は**意図的に別名**で、`kernelBridge.ts` が変換する。`KernelApi` は kernel が定義し model が `Comlink.wrap<KernelApi>` で使う。 |
| 型の整合 | `attachCameraControls` に `setOrbit` を追加し(タスク11 手順2)、`ViewCube` の `getOrbit` / `setOrbit` と噛み合わせた。視点の正本は `attachCameraControls` の1箇所。 |
| 既存ファイルの実在確認 | `docs/requirements.md` / `scripts/check.ps1` / `.github/workflows/ci.yml` / `.gitignore` / `.gitattributes` / `rules/00`〜`06` は Read で実在を確認済み。それ以外はすべて「新規」と明記した。 |
| 依存順序 | タスク3→4→5→6→8 の一本道と、タスク7(独立)、タスク10→12(純関数)、タスク9→11→13→14→15/16→17 の順で先行が満たされる。タスク15 と16 は並行実行できる。 |
| 検査の二重管理 | 検査は `pnpm run typecheck/lint/test/build` の4つと `scripts/check.ps1` に統一。CI 専用のコマンドを一切提案していない(`rules/03-品質ゲート.md` §7.2)。 |
| 1タスクあたりの変更ファイル数 | 最大はタスク16 の 10 新規+1 変更。他は 10 以下。 |
| 規約との整合 | 作業担当の git 書込禁止、pnpm を統括が直接実行しない、依存変更は統括承認(§0.2 と §1 の表で事前提示)、アプリ起動は統括のみ(§5)、テスト期待値を緩めない(タスク4 手順8)を全タスクへ明記した。 |
