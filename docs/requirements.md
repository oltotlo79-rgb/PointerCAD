# PointerCAD 要件定義書

| 項目 | 内容 |
|---|---|
| 文書名 | PointerCAD 要件定義書 |
| 版 | 1.0(初版) |
| 作成日 | 2026-09-01 |
| ステータス | レビュー中 |

---

## 1. プロジェクト概要

### 1.1 目的

座標・数値・数式の入力を第一級の操作としたモデリング(点をプロットし、点を結んで線・面・立体を構成する)と、Blender 風の直感的な 3D 操作感を両立した、履歴パラメトリック方式の 3D CAD を開発する。個人が機械部品の設計から JIS 準拠図面の出力、3D プリント用データの出力までを 1 本で完結できることを目指す。

### 1.2 プロダクト名

**PointerCAD**

### 1.3 ターゲットユーザーと主な用途

- 機械部品設計(切削・板材などの加工に出すことを想定した実部品設計)
- 趣味・DIY・個人利用
- 3D プリント用モデリング

### 1.4 配布形態・ライセンス

- 無料公開の OSS とする
- ライセンス: **Apache-2.0**(利用ライブラリの OpenCASCADE は LGPL-2.1(例外条項付き)であり、動的リンク相当の利用で共存可能)
- 品質目標は「商用製品と同等のクオリティ」(安定性・UX・ドキュメントを含む)

### 1.5 対応プラットフォーム

| 形態 | 対象 |
|---|---|
| デスクトップ | Windows 10/11 (x64)、Linux (x64、AppImage 配布) |
| Web | フル機能 Web 版。Cloudflare Pages 上で配布し、モダンブラウザ(Chrome / Edge / Firefox 最新版)で動作 |

デスクトップ版と Web 版は同一コードベースであり、機能差は原則として設けない(ファイル保存方法などプラットフォーム固有部分のみ差異を許容する)。

---

## 2. 用語定義

| 用語 | 定義 |
|---|---|
| スケッチ | 点・線分・円弧などの 2D/3D 幾何要素の集合。立体を作るための下書き |
| フィーチャー | 押し出し・穴・面取りなど、形状に対する 1 つの操作単位。履歴ツリーに記録される |
| 履歴パラメトリック | フィーチャーの列としてモデルを保持し、過去のフィーチャーのパラメータ変更で下流を再計算する方式 |
| B-rep | 境界表現。頂点・辺・面でソリッドを表す形状表現。OCCT が採用 |
| 部品(パート) | 単一の部品を表すドキュメント。フィーチャーツリーを 1 本持つ |
| アセンブリ | 複数の部品を配置・拘束して組み合わせたドキュメント |
| 合致拘束 | アセンブリで部品同士の位置関係を定める条件(面合致・軸合致など) |
| 三面図 | 正面図・平面図・側面図。第三角法で配置する |
| 作業平面 | スケッチ入力の基準となる平面(XY/YZ/ZX または任意の面) |
| テッセレーション | B-rep 形状を表示用の三角形メッシュに変換する処理 |

---

## 3. コンセプトと全体像

1. **座標・数式ファースト**: すべての数値入力欄で数式(√・分数・π・累乗・四則演算)が使える。点のプロット→接続という明示的な作図を操作の基本とする。
2. **Blender 風の操作感**: ビューポート操作(オービット/パン/ズーム)、ショートカット体系、画面レイアウトの感覚を Blender を基準に設計する。
3. **Inventor 風の履歴パラメトリック**: 操作はフィーチャーツリーに記録され、後からパラメータを編集すると下流が再計算される。部品→アセンブリ→図面という文書構成も Inventor に倣う。
4. **説明なしで使える UX**: 初見でもツールチップ・ステータスバーのガイド・妥当なデフォルト値だけで操作が完結することを非機能要件の柱とする。

ドキュメントの流れ:

```
スケッチ(点・線・円弧・面)
   ↓ フィーチャー(押し出し/回転/縫合/穴/面取り…)
部品(パート) ── 色・材質(外観)
   ↓ 配置・合致拘束
アセンブリ
   ↓ 三面図自動生成・寸法
図面(A3/A4) → PDF / JPEG
部品・アセンブリ → STEP / STL / 3MF / OBJ / glTF
```

---

## 4. 機能要件

要件 ID の凡例: `FR-カテゴリ番号-連番`。優先度は MoSCoW(Must / Should / Could)で表す。

### 4.1 3D ビューポートと視点操作(FR-1xx)

| ID | 要件 | 優先度 |
|---|---|---|
| FR-101 | マウスによる視点操作: 中ボタンドラッグでオービット、Shift+中ボタンでパン、ホイールでズーム(Blender 互換)。マウスのみ・タッチパッドでも代替操作を用意する | Must |
| FR-102 | 透視投影/平行投影の切り替え | Must |
| FR-103 | **ビューキューブ**: 画面右上に「前・後・右・左・上・下」を各面に表記した立方体を常時表示。ドラッグで視点回転、面・辺・頂点のクリックでその要素を正面とする視点へアニメーション遷移 | Must |
| FR-104 | グリッド・XYZ 軸の表示(表示/非表示切替、グリッド間隔は自動スケール) | Must |
| FR-105 | 表示スタイル: シェーディング/シェーディング+エッジ/ワイヤーフレームの切替 | Must |
| FR-106 | 選択: 頂点・辺・面・ボディ・スケッチ要素をクリック選択、ドラッグでボックス選択。選択候補のホバーハイライト、重なり要素の切替(クリック連打またはリスト) | Must |
| FR-107 | スナップ: 端点・中点・円中心・交点・グリッドへのスナップ。有効/無効の切替とスナップ種別のフィルタ | Must |
| FR-108 | 視点のホームポジション(等角視)へのワンキー復帰 | Should |
| FR-109 | 断面表示(任意平面でのクリッピング) | Could |

### 4.2 数値・数式入力エンジン(FR-2xx)

| ID | 要件 | 優先度 |
|---|---|---|
| FR-201 | アプリ内の**すべての数値入力欄**で数式を受け付ける: 四則演算、括弧、累乗(`^`)、平方根・立方根など n 乗根(`sqrt()`, `cbrt()`, `root(x,n)`, `√` 記号入力)、分数(`3/4`)、円周率 `π`(`pi`)、定数 `e` | Must |
| FR-202 | 式は文字列のまま保存され、フィーチャー編集時に元の式が再表示・再編集できる(評価結果だけを保存しない) | Must |
| FR-203 | 式の評価は任意精度演算で行い、誤差を抑えた上で幾何カーネルへ渡す(カーネル自体は double 精度) | Must |
| FR-204 | 不正な式は入力欄上で即時にエラー表示し、理由(構文エラー、ゼロ除算等)を示す | Must |
| FR-205 | 角度入力は度(°)を既定とし、`rad()` 関数でラジアン指定も可能 | Must |
| FR-206 | ユーザー定義パラメータ(変数)表: 名前付き変数を定義し式から参照できる。変数変更で参照フィーチャーが再計算される | Should |

### 4.3 スケッチ(FR-3xx)

スケッチは 3D 空間への直接入力を基本とし、作業平面(XY/YZ/ZX・既存の平面フェイス・任意定義平面)を選択すると平面上への 2D 入力になる。

| ID | 要件 | 優先度 |
|---|---|---|
| FR-301 | **点のプロット(絶対座標)**: X, Y, Z を数式込みで指定して点を作成 | Must |
| FR-302 | **点のプロット(相対座標)**: 直前の点、または任意に選択した既存の点を基準に、シフト量(ΔX, ΔY, ΔZ)を指定して点を作成 | Must |
| FR-303 | 点のプロット(極座標): 基準点+方向(角度)+距離で点を作成 | Must |
| FR-304 | **線分**: 2 点間を結ぶ。または始点+方向・長さ・角度指定で作成 | Must |
| FR-305 | **円弧**: 中心点+半径+開始角度+終了角度で作成(作業平面上)。全周指定で円になる | Must |
| FR-306 | 3 点円弧(始点・終点・通過点)による作成 | Should |
| FR-307 | **連続描画**: 線・円弧の端点から続けて次の線・円弧を描ける(端点スナップ+チェーン入力モード) | Must |
| FR-308 | **点列生成**: 基準点+方向+間隔+個数を指定して、一直線上に等間隔の複数点を一括作成 | Must |
| FR-309 | **面の作成**: 同一平面上の閉ループ(線・円弧の連なり、または点列の順次接続)から面を作成。点のみを順に選んで面を張ることもできる(隣接点間に線が自動生成される) | Must |
| FR-310 | 面・立体の色指定: 作成された面・ソリッドは任意の色で塗りつぶし表示できる(既定色あり、面単位・ボディ単位で変更可) | Must |
| FR-311 | スケッチ要素の編集: 座標・長さ・角度・半径を後から変更でき、依存要素が追従する | Must |
| FR-312 | 非平面境界への面張り(フィリング) | Could |
| FR-313 | 幾何拘束(水平・垂直・接線・一致等の自動/手動拘束) ※本製品は座標・数値入力主体のため、拘束ソルバーは将来拡張とする | 将来 |

### 4.4 ソリッドモデリング(FR-4xx)

| ID | 要件 | 優先度 |
|---|---|---|
| FR-401 | **押し出し**: 面(またはスケッチ閉ループ)を距離・方向指定で押し出してソリッド化。テーパー角オプション | Must |
| FR-402 | **回転体**: 面と軸を指定し、角度(部分回転可)で回転ソリッドを作成 | Must |
| FR-403 | **面群からのソリッド化(縫合)**: 閉じた殻を構成する複数の面を選択してソリッドに変換 | Must |
| FR-404 | ブーリアン演算: 和・差・積 | Must |
| FR-405 | **穴フィーチャー**: 面上の点(**複数点の同時指定可**)を中心に、径・深さ(止まり穴/貫通)・方向(面法線が既定、角度指定で傾斜可)を指定して穴をあける | Must |
| FR-406 | **ねじ穴**: JIS メートル並目/細目ねじ(M2〜M64)の規格データベースを内蔵し、呼び径を選ぶと下穴径・ピッチが自動設定される。表現は「簡略表示(下穴+ねじ記号)」を既定とし、オプションで**実らせん形状の生成**(3D プリント用)を選択できる | Must |
| FR-407 | **R 面取り(フィレット)**: 辺・頂点を指定して半径指定で丸める(複数同時可) | Must |
| FR-408 | **C 面取り**: 辺・頂点を指定して面取り。指定方法は 3 通り: ①距離(45°)、②**2 距離指定(上下方向と左右方向で異なる値)**、③距離+角度 | Must |
| FR-409 | スイープ(パスに沿った押し出し) | Should |
| FR-410 | ロフト(複数断面間の接続) | Should |
| FR-411 | 直線パターン(フィーチャーの等間隔複製)。FR-308 の点列と組み合わせて複数穴の一括配置ができる | Should |
| FR-412 | 円形パターン | Should |
| FR-413 | シェル(中空化)、ミラー | Could |

### 4.5 履歴パラメトリック(FR-5xx)

| ID | 要件 | 優先度 |
|---|---|---|
| FR-501 | フィーチャーツリー: 全操作を時系列ツリーで表示。各ノードに種類アイコン・名前を表示 | Must |
| FR-502 | フィーチャー編集: ツリーから任意のフィーチャーを開き、パラメータ(式)を変更すると下流フィーチャーが自動再計算される | Must |
| FR-503 | フィーチャーの削除・抑制(サスペンド)・名前変更 | Must |
| FR-504 | 再計算エラー処理: 下流フィーチャーが成立しなくなった場合、アプリは落ちずにエラー箇所をツリー上で明示し、修正または変更の取り消しを促す | Must |
| FR-505 | Undo / Redo: セッション内で実質無制限(既定 200 段以上) | Must |
| FR-506 | ロールバックバー(ツリー途中まで一時的に巻き戻して途中に挿入) | Could |

### 4.6 部品・アセンブリ(FR-6xx)

| ID | 要件 | 優先度 |
|---|---|---|
| FR-601 | 部品(パート)ドキュメントとアセンブリドキュメントの区別。アセンブリは部品ファイルを参照配置する(コピーではない。部品の変更がアセンブリに反映される) | Must |
| FR-602 | 部品の固定(グラウンド) | Must |
| FR-603 | 合致拘束: 面合致(オフセット距離指定可)・軸合致(同軸)・角度拘束。拘束追加時に位置が解決され、部品変更時にも追従する | Must |
| FR-604 | 拘束の一覧表示・編集・削除。矛盾した拘束はエラー表示し原因の拘束を特定できる | Must |
| FR-605 | アセンブリ内での部品の色分け表示・表示/非表示切替 | Must |
| FR-606 | 同一部品の複数配置(インスタンス) | Should |
| FR-607 | 干渉チェック(部品同士の体積干渉の検出・表示) | Could |
| FR-608 | 可動拘束(回転・スライド等の自由度を残した拘束)と簡易モーション | 将来 |

### 4.7 図面(FR-7xx)

| ID | 要件 | 優先度 |
|---|---|---|
| FR-701 | 用紙: **A3 横を既定**とし、A4(縦横)へ変更可能。用紙枠・中心マーク・簡易表題欄(部品名・縮尺・投影法記号・日付・作成者)を自動配置 | Must |
| FR-702 | **三面図の自動生成**: 部品/アセンブリから第三角法で正面図・平面図・側面図を自動配置。隠線は破線で表示(表示/非表示切替可) | Must |
| FR-703 | **自動縮尺**: 用紙に収まるよう JIS の標準縮尺系列(1:1, 1:2, 1:5, 1:10, 2:1, 5:1 …)から自動選択。手動変更も可能 | Must |
| FR-704 | 図(ビュー)の移動・個別縮尺変更・削除、等角投影図の追加 | Must |
| FR-705 | **自動寸法記入**: 主要寸法(全体寸法・穴位置・穴径)を自動で記入する。自動記入後の個別削除・移動が可能 | Should |
| FR-706 | **手動寸法記入**: 長さ・角度・直径(φ)・半径(R)寸法を要素選択で記入。寸法値はモデルから取得し手入力による改竄はさせない | Must |
| FR-707 | JIS 準拠の表現: 寸法線・引出線・矢印・文字サイズの既定値を JIS B 0001 系に準拠させる。穴・ねじは JIS 簡略図示(ねじは細実線表現) | Must |
| FR-708 | 中心線・中心マークの自動生成(円筒面・穴に対して) | Should |
| FR-709 | 図面から **PDF(ベクター)** および **JPEG** への出力。PNG・SVG 出力も併せて対応 | Must |
| FR-710 | モデル変更時の図面自動更新(寸法値追従、破綻した寸法の警告表示) | Must |
| FR-711 | 注記(テキスト)・引出線注記の記入 | Should |
| FR-712 | 断面図・詳細図 | 将来 |

### 4.8 ファイル入出力(FR-8xx)

| ID | 要件 | 優先度 |
|---|---|---|
| FR-801 | ネイティブ形式 `.pcad`: ZIP コンテナ+JSON(フィーチャー履歴・式・アセンブリ拘束・図面を完全保存)。前方互換のためスキーマバージョンを内包 | Must |
| FR-802 | インポート: **STEP**(AP203/AP214)、STL、OBJ。インポートした形状は履歴のない「ベースボディ」として扱い、その上へのフィーチャー追加は可能 | Must |
| FR-803 | エクスポート: **STEP AP214**、**STL**(バイナリ/ASCII、品質(偏差)指定可)、**3MF**、OBJ、glTF(.glb) | Must |
| FR-804 | エクスポート単位: 部品単体/アセンブリ全体(STEP はアセンブリ構造を保持) | Must |
| FR-805 | 自動保存(既定 5 分間隔)とクラッシュ後の復元 | Must |
| FR-806 | Web 版の保存: ブラウザ内ストレージ(OPFS)への保存+ローカルへのダウンロード/アップロード。デスクトップ版は通常のファイルダイアログ | Must |
| FR-807 | 最近使ったファイル一覧 | Should |
| FR-808 | 図面の DXF 出力 | 将来 |

### 4.9 ヘルプ・ガイダンス(FR-9xx)

| ID | 要件 | 優先度 |
|---|---|---|
| FR-901 | **全機能の詳細ヘルプ**: 実装されている全機能について、操作手順・パラメータの意味・図解を含むヘルプをアプリ内に内蔵する(Markdown ベースで管理し、機能追加時にヘルプ追加を必須とする) | Must |
| FR-902 | ヘルプの全文検索 | Must |
| FR-903 | コンテキストヘルプ: ツール使用中に F1(または ? ボタン)でそのツールのヘルプを直接開く | Must |
| FR-904 | ツールチップ: 全ボタン・入力欄にホバーで名称+一行説明+ショートカットを表示 | Must |
| FR-905 | ステータスバーガイド: 現在のツールの「次に行う操作」を常時表示(例: 「円弧の中心点を指定してください」) | Must |
| FR-906 | 初回起動時のインタラクティブチュートリアル(簡単な部品を 1 つ作る) | Should |
| FR-907 | ショートカット一覧の表示 | Should |

### 4.10 Web 版(FR-10xx)

| ID | 要件 | 優先度 |
|---|---|---|
| FR-1001 | デスクトップ版と同一コードベースによる**フル機能 Web 版**を Cloudflare Pages で公開する | Must |
| FR-1002 | サーバーサイド処理を持たない(静的配信のみ)。モデルデータが外部送信されないことを保証する | Must |
| FR-1003 | WASM マルチスレッドのための COOP/COEP ヘッダー設定 | Must |
| FR-1004 | PWA 対応(オフライン起動) | Could |

---

## 5. 非機能要件

要件 ID の凡例: `NFR-カテゴリ-連番`。

### 5.1 UX(最重要非機能要件)

**「説明を読まなくても直感的に操作できる」** を UI 設計の最上位原則とする。

| ID | 要件 |
|---|---|
| NFR-UX-1 | 操作文法の一貫性: 「対象を選んでから操作」「操作を選んでから対象」のどちらの順でも成立させる |
| NFR-UX-2 | 直接操作優先: 数値入力はモーダルダイアログではなく、ビューポート内のその場に浮かぶ入力欄で行う(Blender 的操作感) |
| NFR-UX-3 | すべての操作が Undo 可能であること。確認ダイアログは復元不能な操作(ファイル上書き等)に限る |
| NFR-UX-4 | 全パラメータに妥当なデフォルト値を設定し、Enter 連打だけでも意味のある結果になる |
| NFR-UX-5 | エラーの事前防止: 無効な値は実行前に赤表示+理由提示。実行してから失敗させない |
| NFR-UX-6 | 空状態ガイド: 新規ドキュメントでは「最初の一歩」(点のプロット/スケッチ開始)を画面上で提示する |
| NFR-UX-7 | 操作ガイド(FR-904/905 のツールチップ・ステータスバー)を全ツールで欠かさない |

### 5.2 性能

想定規模: 1 部品あたり〜200 フィーチャー、アセンブリ〜50 部品を実用範囲とする。

| ID | 要件 |
|---|---|
| NFR-PF-1 | ビューポート操作(オービット/パン/ズーム)は上記規模で 60fps(最低 30fps)を維持 |
| NFR-PF-2 | 単一フィーチャーの適用・プレビュー更新は典型形状で 500ms 以内 |
| NFR-PF-3 | 100 フィーチャー部品の全再計算は 5 秒以内(未変更フィーチャーの結果キャッシュで短縮する) |
| NFR-PF-4 | 幾何カーネルは Web Worker 上で実行し、計算中も UI がフリーズしない(進捗表示+キャンセル可能) |
| NFR-PF-5 | 起動時間: デスクトップ 5 秒以内、Web 版初回ロード 10 秒以内(通常回線)・2 回目以降 3 秒以内 |
| NFR-PF-6 | メモリ: WASM の実質上限(〜4GB)内で動作。上限接近時に警告する |

### 5.3 信頼性・精度

| ID | 要件 |
|---|---|
| NFR-RE-1 | モデリング操作の失敗(フィレット不成立等)でアプリケーションがクラッシュしない。失敗理由をユーザーに提示する |
| NFR-RE-2 | 自動保存とクラッシュ復元(FR-805) |
| NFR-RE-3 | 内部単位は mm、幾何公差は OCCT 既定(1e-7)を基準とする |
| NFR-RE-4 | 式評価は任意精度で行い、丸めは最終段のみ(FR-203) |

### 5.4 セキュリティ・プライバシー

| ID | 要件 |
|---|---|
| NFR-SE-1 | テレメトリ・外部送信は一切行わない。Web 版も静的配信のみでモデルデータはブラウザ外に出ない |
| NFR-SE-2 | 依存ライブラリは Dependabot 等で脆弱性監視する |

### 5.5 保守性・開発プロセス

| ID | 要件 |
|---|---|
| NFR-MA-1 | モノレポ構成でパッケージ境界を明確化(§6.2)。UI から幾何カーネルへ直接依存しない(ドキュメントモデル経由) |
| NFR-MA-2 | テスト: 式エンジン・ドキュメントモデル・フィーチャー再計算はユニットテスト必須(TDD)。主要ユーザーフローは E2E テスト(Playwright)。カーネル演算は Node 上でヘッドレス実行して検証 |
| NFR-MA-3 | CI(GitHub Actions): lint・型検査・全テスト・ビルドを PR ごとに実行 |
| NFR-MA-4 | 機能追加時はヘルプ(FR-901)とテストの同時追加を必須とする |
| NFR-MA-5 | UI 文字列は全てリソースファイル(ja.json)に分離し、i18n 対応可能な構造とする(表示は日本語のみで開始) |

---

## 6. システム構成・技術スタック

### 6.1 技術選定

| レイヤ | 採用技術 | 選定理由 |
|---|---|---|
| 言語 | TypeScript(strict) | Web/デスクトップ同一コード、型安全、情報量最多 |
| 幾何カーネル | OpenCASCADE (OCCT) の WASM ビルド(opencascade.js。必要に応じ Emscripten 自前ビルド) | 商用実績のある B-rep カーネル。ブーリアン・フィレット・STEP を自作せずに済む |
| 3D 描画 | Three.js(WebGL2) | カスタム CAD UI(ビューキューブ・スナップ表示等)の作り込みに最適 |
| UI フレームワーク | React + Zustand(状態管理) | コンポーネント資産・情報量 |
| 式エンジン | 自作パーサー + decimal.js(任意精度評価) | √・n乗根・分数・π・累乗の要件を満たす最小構成 |
| デスクトップ | Electron(Windows: NSIS インストーラ / Linux: AppImage) | Web 版とのレンダリング一致・配布容易性 |
| Web 配信 | Cloudflare Pages(静的配信、COOP/COEP ヘッダー設定) | 要件指定。サーバーレスで運用費ゼロ |
| 図面 → PDF | SVG で描画し、ベクター PDF 化(jsPDF + svg2pdf.js 等)。JPEG/PNG は Canvas ラスタライズ | 図面のベクター品質を保つ |
| カーネル実行 | Web Worker + Comlink | UI スレッド非ブロッキング(NFR-PF-4) |
| ビルド | Vite + pnpm workspaces(モノレポ) | 高速な開発サイクル |
| テスト | Vitest(ユニット)+ Playwright(E2E) | NFR-MA-2 |
| CI/CD | GitHub Actions | lint / test / build / リリース(electron-builder)/ Pages デプロイ |

バージョン方針: 依存はすべて lockfile で固定し、opencascade.js は特定バージョンにピン止めして更新は検証付きで行う。

### 6.2 パッケージ構成(モノレポ)

```
pointercad/
├── packages/
│   ├── expression/      # 式パーサー・任意精度評価(依存なしの純粋ロジック)
│   ├── kernel/          # OCCT WASM ラッパー(形状生成・ブーリアン・面取り・テッセレーション・STEP/STL 変換)
│   ├── model/           # ドキュメントモデル: フィーチャー履歴・部品・アセンブリ・再計算エンジン・Undo
│   ├── drawing/         # 図面生成: 投影(HLR)・寸法・JIS スタイル・SVG 生成
│   ├── io/              # .pcad 形式、STEP/STL/3MF/OBJ/glTF 入出力、PDF/JPEG 出力
│   ├── ui/              # React コンポーネント群: ビューポート・ビューキューブ・ツリー・入力欄・ヘルプ
│   └── help-content/    # ヘルプ文書(Markdown)
├── apps/
│   ├── desktop/         # Electron シェル
│   └── web/             # Web 版エントリ(Cloudflare Pages)
└── docs/                # 本書・設計文書
```

依存方向は `apps → ui → model → kernel/expression` の一方向とし、逆流を禁止する。

### 6.3 データフロー

```
ユーザー操作(UI)
  → コマンド発行(Undo 履歴に記録)
  → ドキュメントモデル更新(フィーチャー追加/編集)
  → 再計算エンジン(変更以降のフィーチャーのみ。結果キャッシュ)
  → kernel(Web Worker 内の OCCT)で B-rep 生成
  → テッセレーション → Three.js シーン更新
```

---

## 7. 画面構成

### 7.1 メイン画面(モデリングモード)

```
┌────────────────────────────────────────────────────────┐
│ メニューバー / ツールバー(スケッチ|ソリッド|アセンブリ|図面) │
├──────────┬─────────────────────────────┬───┤
│ フィーチャー │                             │ビュー│
│ ツリー      │        3D ビューポート        │キューブ│
│ (モデル     │                             ├───┤
│  ブラウザ)  │                             │プロパ │
│           │   [その場数値入力ポップアップ]  │ティ  │
│           │                             │パネル│
├──────────┴─────────────────────────────┴───┤
│ ステータスバー: 操作ガイド | 座標表示 | スナップ設定 | 単位    │
└────────────────────────────────────────────────────────┘
```

- モードはツールバーのタブで切替: **モデリング / アセンブリ / 図面**
- プロパティパネル: 選択中の要素・実行中ツールのパラメータ(式入力欄)を表示
- ヘルプはサイドパネルまたは別ウィンドウで表示(検索ボックス付き)

### 7.2 図面モード

- 用紙(A3/A4)を WYSIWYG 表示。ビュー(三面図・等角図)をドラッグで移動、プロパティで縮尺変更
- 寸法ツール: 要素をクリックして寸法配置

---

## 8. データ仕様(.pcad 形式概要)

- ZIP コンテナ。内容:
  - `document.json` — スキーマバージョン、種別(part / assembly / drawing)、フィーチャー履歴(全パラメータは**式文字列+評価値**のペアで保存)、拘束、図面定義
  - `thumbnail.png` — サムネイル
  - 参照部品はアセンブリファイルに相対パス+内容ハッシュで記録(欠損時は警告)
- スキーマバージョンによる前方互換(旧バージョンのファイルは常に開けること)

---

## 9. 開発フェーズ計画

各フェーズは「動くものをリリースできる」単位で区切る。フェーズごとに詳細設計→実装計画を別途作成する。

| フェーズ | 内容 | 完了条件 |
|---|---|---|
| **P0: 基盤** | モノレポ・CI・Electron/Web 両起動・OCCT WASM 統合・ビューポート(カメラ操作・グリッド)・ビューキューブ | 箱形状が両環境で表示・視点操作できる |
| **P1: 式とスケッチ** | 式エンジン・点/線/円弧の作成(絶対・相対・極座標)・スナップ・連続描画・点列・面作成・色 | 座標入力で 2D/3D の下書きが描け、面が張れる |
| **P2: ソリッド基礎** | 押し出し・回転・縫合・ブーリアン・フィーチャーツリー・Undo/Redo・.pcad 保存/読込・自動保存 | 簡単な部品を作って保存・再編集できる |
| **P3: 加工フィーチャー** | 穴(複数点同時・傾斜)・ねじ穴(JIS DB・簡略/実形状)・R面取り・C面取り(2距離/距離角度)・パターン | 実用部品(穴・ねじ・面取り付き)が作れる |
| **P4: 入出力** | STEP/STL/3MF/OBJ/glTF エクスポート・STEP/STL/OBJ インポート | 他 CAD・スライサーとデータ交換できる |
| **P5: アセンブリ** | 部品参照配置・固定・合致拘束(面/軸/角度)・拘束編集 | 複数部品の組図が作れる |
| **P6: 図面** | 三面図自動生成(第三角法・HLR)・自動縮尺・寸法(手動→自動)・JIS スタイル・PDF/JPEG 出力 | A3/A4 の JIS 風図面が出力できる |
| **P7: ヘルプと仕上げ** | 全機能ヘルプ・チュートリアル・ショートカット一覧・PWA・パフォーマンス最適化 | 初見ユーザーがヘルプなしで部品を 1 つ作れる(ユーザビリティ確認) |
| **P8: 公開** | Cloudflare Pages 正式公開・GitHub リリース(インストーラ)・README/貢献ガイド | v1.0 公開 |

※ Web 版は P0 から常に併行動作させる(後付けにしない)。

---

## 10. リスクと対策

| リスク | 影響 | 対策 |
|---|---|---|
| opencascade.js のメンテ停滞・バージョン遅れ | カーネル更新不能 | Emscripten による OCCT 自前 WASM ビルド手順を早期(P0)に確立し、逃げ道を確保 |
| フィレット・ブーリアンの失敗(B-rep カーネルの宿命) | 操作不能・データ破損 | 失敗を前提とした UI(FR-504, NFR-RE-1)。適用前プレビューと検証 |
| WASM メモリ上限(〜4GB) | 大規模モデルで停止 | 想定規模の明示(§5.2)、警告表示、結果キャッシュの LRU 破棄 |
| 図面の JIS 準拠の奥深さ | 工数爆発 | 準拠範囲を FR-707 の既定値レベルに限定し、段階的に拡充 |
| Electron の配布サイズ・メモリ | 体験悪化 | 許容と明記(代償として Web 版同一コードを得る)。将来 Tauri 移行を妨げない構造にする |
| 履歴再計算の性能劣化 | 編集体験悪化 | フィーチャー単位の結果キャッシュ(NFR-PF-3)を最初から設計に組み込む |
| 開発規模が個人+Claude Code には大きい | 完成しない | フェーズごとに「動くものを出す」計画(§9)を厳守。スコープ外(§11)を守る |

---

## 11. スコープ外(明示的にやらないこと)

- サーフェスモデリング(自由曲面デザイン)、板金展開、CAM、CAE/FEM 解析
- フォトリアルレンダリング
- クラウド保存・アカウント機能・複数人同時編集
- 2D 専用 CAD としての本格製図(あくまで 3D モデルからの図面生成が主)
- 幾何拘束ソルバー(FR-313)・可動拘束(FR-608)・断面図(FR-712)・DXF(FR-808)は将来検討

---

## 12. 未決事項

| 項目 | 現状 | 決定時期 |
|---|---|---|
| ロゴ・アイコン・配色テーマ | 未定(ダークテーマ基調を想定) | P0 |
| 自動寸法(FR-705)の記入アルゴリズム詳細 | 方針のみ | P6 設計時 |
| 表題欄のカスタマイズ可否 | 固定様式で開始 | P6 以降 |
| Blender 互換ショートカットの具体割当表 | 未定 | P0〜P1 設計時 |

---

## 13. 変更履歴

| 版 | 日付 | 変更内容 |
|---|---|---|
| 1.0 | 2026-09-01 | 初版作成(要件ヒアリングに基づく) |
