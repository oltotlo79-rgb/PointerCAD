2026-09-23 08:26追記: 数値で根を探すnumericrootsと、候補の上下限を明示して使うrootintervalを通常/構造入力・元の式・名前付き数値と座標・保存再開・再編集・Undo・説明へ接続した。探索範囲・区間幅・候補順を保持し、未解決を解なしとせず、丸めのある端点・元の分母の穴・重なる根・中止を区別する。上下限を根そのものと偽らず、表示も区間を狭めない。修正後の計算54項目、関連計算115項目、表示3項目と日本語表示13項目、型と書き方、三環境の実操作3件、実画像を含む説明76項目が成功した。以前の失敗4実行と変更のない入力候補等205項目は別に保持し、重複加算しない。記録: scratchpad/tasks/6131650ed8c2-numerical-roots-rlsnnxu6/completed-related-checks.json。微分方程式・数学全体、統合・同じ内容の両OS CI・配布確認は未完了。確定533/580件を維持する。

2026-09-23 07:49追記: 有理式・多項式からなる複数の方程式を、指定した実数/複素数の範囲で解き、解の候補、未知数の順番、自由に指定する値と成立条件を保持する入口を追加した。候補番号と自由な値を明示した場合だけ座標に利用できる。割り算で除外する値と元の式を再確認し、未対応の式や表現できない解を「解なし」に変えない。通常/構造入力、保存再開、編集とUndo、説明へ接続した。変更後の計算39項目と表示4項目、未変更部分の成功結果を合わせた計算184項目・入力表示221項目、型と書き方、Chromium・Firefox・Electronの保存再開・編集Undo・F1、実画像を含む説明76項目を確認した。除外条件の判定順、入力一覧の型、同じ解の重複と不要な条件表示の失敗は隠さず記録し、修正を確認する例を追加した。三角関数等を含む一般の複数式を全対応とは扱わない。数値で探す根・微分方程式・数学全体、統合・同じ内容の両OS CI・配布確認は未完了。記録: scratchpad/tasks/6131650ed8c2-equation-systems-v0ku6hwa/completed-related-checks.json。確定533/580件を維持する。

2026-09-23 07:01追記: 一変数の等式・不等式の解集合と、多項式の根の重複回数を、通常/構造入力、明示した解の選択、名前付き数値と座標、保存再開・編集Undo・説明へ接続した。実数/複素数/端点を指定した区間を区別し、元の割り算の穴や成立条件を保持する。空集合、全域、有限解、区間、未解決を混同しない。実数全体の返信の型を修正し、元の失敗を保持した。型と書き方、関連計算145項目、入力/表示214項目のうち未変更部分、表示修正後の36項目、Chromium・Firefox・Electronの保存再開・編集Undo・F1、実画像を含む説明76項目が成功した。選択した根は実部、次に虚部の昇順とする。無限の周期解の表現、周期不等式、未表現の高次根などを全対応とは扱わない。連立・自由変数・数値根・微分方程式、数学全体、main統合・同じ内容の両OS CI・配布確認は未完了。記録: scratchpad/tasks/6131650ed8c2-equations-namx8q5p/completed-related-checks.json。確定533/580件を維持する。

2026-09-23 06:20追記: フーリエ級数の周期区間・最高次数・定数項・余弦/正弦係数を保持し、明示した係数と有限の部分和を通常/構造入力・名前付き数値・座標・保存再編集へ接続した。零の係数を含む最高次数0〜12を使い、本文の角度設定とラジアン固定の基底を区別する。部分和を元の関数の正確な値とは扱わず、証明できた収束条件と周期端点の左右極限の平均を示す。ln(x)の絶対積分が未評価になる問題は、開区間全体の符号を証明できた場合だけ正しい符号の積分へ置換して修正した。型と書き方、関連計算113項目、表示/入力205項目、Chromium・Firefox・Electronの保存再開・編集Undo・F1、実画像を含む説明76項目が成功した。計算検査の返信型の確認不足を直し、変更した26項目を確認し直した。製品ソースと期待値が同じ既存の結果は内容を照合して保持する。最初の失敗4回は保持し、26項目を113項目へ重複加算しない。記録: scratchpad/tasks/6131650ed8c2-fourier-series-89bxmg7t/completed-related-checks.json。一般方程式・不等式・微分方程式、数学全体、main統合・同じ内容の両OS CI・配布確認は未完了。確定533/580件を維持する。

2026-09-22 23:50追記: Fourier/逆Fourier、片側Laplace/逆Laplace、片側Z変換を通常・構造入力、変換後の式と成立範囲、範囲内の指定値、元の式の保存再開・編集Undo・説明へ接続した。Fourierは指数に±2π、Laplaceは0からの片側、Zは非負の整数添字からの片側とし、変換の規約は角度設定で変わらない。元の不成立、範囲外、未解決と通常の数値にできない結果を拒否する。変換後の式の指定文字だけを許可する返信処理と、既存形式の入力候補を修正した。型と書き方、計算・公開入力116項目、入力・同梱215項目、最終表示32項目、三環境の保存再開・編集Undo・F1、実画像を含む説明76項目が成功した。確認中コピーの変更で無効になった116項目の実行も保持し、コピーの準備から確認終了までを保護する実ロックを追加して、固定内容で再確認した。記録: scratchpad/tasks/6131650ed8c2-integral-transforms-z29kp06c/completed-related-checks.json。Fourier級数、一般方程式とODE、数学全体、main統合・同じ内容の両OS CI・配布確認は未完了。確定533/580件を維持する。

2026-09-22 22:41追記: 離散フーリエ変換DFT/IDFT/FFT/IFFTを通常・構造入力、元の式、成分を使う座標、実保存・開き直し・編集Undo・説明へ接続した。正変換の指数の符号は負、逆変換は正で1/N、順序は零周波数からとし、角度設定に依存しない。有限の複素数を含む並びを扱い、DFTは1〜64要素、FFTは1〜256要素の2の累乗に限定し、無断で0を補わない。元の全要素を確認し、0倍や成分選択で不成立を隠さない。共通の入力準備を一か所にまとめて返信側との設定差を修正した。計算と公開入力82項目、入力検査を強めた29項目、画面209項目、型と書き方、Chromium・Firefox・実Electronの3操作、実画像反映後の説明76項目が成功した。82と29は重複するため足し合わせない。最初の公開入力の失敗とFirefoxの待機設定の失敗も保持する。記録: scratchpad/tasks/6131650ed8c2-discrete-fourier-lz8wqjm0/completed-related-checks.json。連続変換、方程式・ODEを含むADD-26全体、数学全体、main統合・同じ内容の両OS CI・配布確認は未完了。確定533/580件を維持する。

2026-09-22 21:55追記: 複素数のerf/erfcを通常・構造入力、明示した実部/虚部の座標利用、元の式、固定した追加計算部の微分、実保存・編集Undo・説明へ接続した。実部と虚部はそれぞれ−8〜8。元の穴と範囲超過を0倍や成分選択で隠さず、必要な精度を確定できなければ値を返さない。微分の有限性判定と返信の表示表記を修正し、修正後の型・書き方、計算・公開入力258項目、入力表示・同梱205項目、三環境の新旧六シナリオ、実画像反映後の説明76項目が成功した。変更していない追加計算部の微分153項目と前段の成功・失敗は別に保持し、最終件数へ足し合わせない。記録: scratchpad/tasks/6131650ed8c2-complex-error-functions-k4te1c9s/completed-related-checks.json。一般の複素数を使う関数作図、残る特殊関数、数学全体、main統合・同じ内容の両OS CI・配布確認は未完了。確定533/580件を維持する。

2026-09-22 21:04追記: 実数のゼータ関数zetaと0〜17階のzetaderivativeを通常・構造入力、元の式、作図の値と微分、固定した追加計算部、実保存、説明へ接続した。引数−32〜128、1の発散、負の偶数の正確な零、微小な非零、角度、元の入力条件を保持する。数値の代入前の性質判定が繰り返される停止を修正し、直接実行する回帰を追加した。配布用ファイルの読み込み上限超過も、既存ファイルへの処理分割と全同梱ファイルの上限検査で修正した。修正後の型・書き方、ゼータの値・作図・計算上限の関連127項目、配布用ファイル等15項目、Chromium・Firefox・実Electronの値/入力拒否/保存再開/編集Undo/F1、実画像反映後の説明76項目が成功した。前段の値50項目・微分11項目・入力作図167項目・入力表示197項目・追加表示26項目は別記録とし重複加算しない。証拠: scratchpad/tasks/6131650ed8c2-zeta-functions-8bet21s5/completed-related-checks.json。複素引数など数学全体の残件、main統合・同じ内容の両OS CI・配布物の最終確認は未完了。確定533/580件を維持する。

2026-09-22 19:02追記: 楕円積分K/E/F/Einc/Pi/Piincを通常・構造入力、元の式、作図の範囲と高階・混合微分、固定計算部、実保存、説明へ接続した。m=k²と角度の単位、周期、元の極を保持する。高階積分の誤差配分と正確な零の除算を修正し、修正後の型/lint、作図関連162項目、入力表示195項目、Chromium・Firefox・実Electronの値/入力拒否/実保存再開/再編集Undo/F1、実画像反映後の説明76項目が成功した。初回の失敗と以前の計算・CASの確認を別に保持し、重複加算しない。証拠: scratchpad/tasks/6131650ed8c2-elliptic-integrals-dpk0psk2/completed-related-checks.json。複素楕円積分、ゼータ等の数学残件、main統合・同じ内容の両OS CI・配布物の最終確認は未完了。確定533/580件を維持する。

2026-09-22 15:50追記: Airy Ai/Biと一階の関数を原式・通常/構造入力・作図・固定計算部・実保存・説明へ接続。実数−32〜32で独立参照値、値と微分、原点、元の入力条件を確認した。型/lint、関連計算197項目、入力表示189項目、Chromium・Firefox・実Electronの入力拒否/原点/保存再開/再編集Undo/F1、実画像反映後の説明76項目が成功。以前の46項目・149項目と最初の失敗は別に保持し重複加算しない。証拠: scratchpad/tasks/6131650ed8c2-airy-functions-kecbcs58/completed-related-checks.json。複素Airyと残る数学、main統合・同じ内容の両OS CI・配布確認は未完了。確定533/580件を維持する。

2026-09-22 15:07追記: Lambert Wの実数枝0/-1を入力候補・通常/構造入力・原式・作図の値と微分・固定計算部・実保存・説明へ接続した。元の境界の欠落と内部表記が画面へ出る問題を修正後、型/lint、関連計算325項目、入力表示185項目、Chromium・Firefox・実Electronの値/原点/入力拒否/保存再開/再編集Undo/F1、実画像を反映した説明76項目が成功した。最初の失敗と各段階の成功を保持し、古い確認件数を重複加算しない。証拠: scratchpad/tasks/6131650ed8c2-lambert-real-branches-9r3b3kjc/completed-related-checks.json。残る特殊関数と数学全体、main統合・同じ内容の両OS CI・配布確認は未完了。確定533/580件を維持する。

2026-09-22 14:43追記: Lambert Wの実数枝0/-1を通常入力・原式保持・作図の値と一階二階/式の微分・固定計算部へ接続した。元の分数の上下限から40桁を確定し、独立33組と微分13組を別精度で照合。見直しで0倍・成分選択後の元の境界の欠落を実計算で再現し、整理前の枝/引数を保持して点の微分前に確認するよう修正した。修正後の型/lint、既存微分を含む41項目、同梱等14項目が成功。修正前の324項目・入力184項目と最初の失敗は別に保持し、修正後の件数へ重複して足さない。証拠: scratchpad/tasks/6131650ed8c2-lambert-real-branches-9r3b3kjc/completed-core-checks.json。入力候補・説明・三環境の実保存は次工程。複素枝、残る数学、mainと両OS CI、配布確認は未完了。確定533/580件を維持する。

2026-09-22 14:01追記: 複素数の指数・三角/双曲線関数・対数・平方根・累乗と逆関数の主値を通常入力と実保存へ接続した。負実軸・枝の両側・度/ラジアン・非常に小さい両成分・元の極を保持し、既知のπの相殺で零や90/180へ丸め残りを作らない。独立115組を含む関連計算272項目、入力184項目、型/lint、Chromium・Firefox・実Electronの入力/構造切替/保存再開/編集Undo/F1、実画像を反映した説明76項目が成功した。途中の失敗と確認用返信の修正を保持し、141項目を272項目へ重複計上しない。証拠はscratchpad/tasks/6131650ed8c2-complex-elementary-eu8pmsgq/completed-related-checks.json。残る特殊関数と数学全体、main統合・同じ内容の両OS CI・配布確認は未完了。確定533/580件を維持する。

2026-09-22 13:29追記: 整数・実数範囲のBessel J/Y/I/Kを通常入力・構造入力・作図の値と微分・追加計算部・画面・実保存へ接続。原式と元の穴を保持し、微小な非零を0へ置き換えない。前段の423項目の成功を保持し、追加計算部の極限判定の修正後は型/lint、Bessel5項目と既存の微分31項目の計36、入力184、三環境の値/原点/入力拒否/保存再開/編集Undo/F1、実画像を反映した説明76項目が成功した。最初の313項目中311成功・2失敗と診断の失敗は別に保持し、全313項目を修正後に再実行したとは扱わない。証拠はscratchpad/tasks/6131650ed8c2-bessel-input-integration-gjplo2m8/completed-related-checks.json。一般次数・複素引数、他の数学、main統合と両OS CI、配布物の起動は残り、ADD-18全体やリリース完成とはしない。確定533/580件を維持する。

2026-09-22 12:21追記: ADD-18のBesselの整数次数Y/Kの共通計算を追加。正の実引数0<x<=128、整数次数|n|<=128について、残項と定数を含む上下限から40桁を確定し、作図範囲・一階二階微分・負次数・中止・上限を保持する。独立28組を340/480桁で照合し、別方式の微分6組も照合。修正後の型/lintと既存J/I・区間を含む210項目（5ファイル）が成功した。初回209成功・1失敗は検査点が上端の次の数へ出たもので、指定端点と全標本の範囲検査に修正し、計算側の精度は変更していない。証拠はscratchpad/tasks/6131650ed8c2-bessel-second-kind-6ij_wfud/completed-calculation-checks.json。一般次数・複素数の分岐、通常入力/作図への登録・画面/保存/説明は残り、Bessel全体やADD-18全体の完成とはしない。確定533/580件を維持する。

2026-09-22 09:48追記: ADD-18のBesselの整数次数J/Iの共通計算を追加。元の分数の級数上下限と全残項の上限から40桁の一致を確定し、作図範囲・一階二階微分・原点・負の入力・中止を扱う。独立48組と既存区間を含む130項目（3ファイル）、型/lintが成功。現在は内部計算であり、Y/K・一般次数・複素数の分岐、通常入力/作図への登録・画面/保存/説明は残る。Bessel全体やADD-18全体の完成に代用しない。証拠はscratchpad/tasks/6131650ed8c2-bessel-function-kernel-72ahew_7/completed-calculation-checks.json。確定533/580件を維持する。

# 数学入力・計算部の採用資料

## 現行の差し替え構成（2026-09-16実装、9月21日結果確認）

利用者の指示により、`@cortex-js/compute-engine`の直接依存0.128.6とMathLive経由の0.58.0を取り除いた。それに伴い`@arnog/colors`の2版と`complex-esm`も導入・配布対象から外した。MathLive 0.110.0は構造入力の編集だけに使い、既存の`MathfieldElement.computeEngine = null`を維持する。未使用の接続部品を外す指定は`pnpm-workspace.yaml`とロックファイルに同じ版で固定し、上流部品のファイルを書き換えない。

読み取り・表示は独自の`mathLatexCodec`、基本計算は`nativeMathBackend`へ変更した。整数・分数は既存の正確な分数処理、小数は従来の`decimal.js 10.6.0`の40桁処理を使用する。単純な式はこの場で求め、確定できない記号計算は利用者が採用を承認したPyodide・SymPy・mpmathへ既存の検証済み形式で渡す。新しい第三者部品は追加していない。元の式、角度単位、係数のID、保存形式、結果を作図へ採用する際の確認を保持する。誤差の上限が未確認の数値積分は、表示できても正確な座標として使わせない。

文字数16384、構造の要素数4096、入れ子64、有限和・積1000回、同期計算200msの既存制限を維持する。動的コード実行を使わず、外側の計算Workerの期限と取消も保持する。40桁の数値近似を厳密な記号結果と取り違えない。

再混入は二段階で拒否する。`runtimeDependencySelection`は削除指定と導入一覧を照合し、`mathNotices`は実際の配布に取り込むモジュールから除去済みの部品を検出して作成を止める。字体20個とその許諾は引き続き同梱する。許諾の現行一覧は`licenses/math-notices.json`と`licenses/runtime-notices.json`が正本であり、旧部品の原文ファイルは過去の証拠として保持するが配布しない。

固定した依存構成の再導入、数式1717項目、入力例100項目、許諾関連23項目、型・書き方、実画面24操作（Chromium・Firefox・Electron各8操作）が9月16日22:20までに成功した。Web/Desktopの数学Workerは同じ432704バイトとなり、除去した部品を含まない配布内容を確認した。9月21日に保存された検査記録と現在の対象ファイルの内容一致を確認した。証拠は`scratchpad/tasks/6131650ed8c2-math-engine-replacement-_0mff4y0/`。両OSの最終CIとリリースは未了であり、以下は差し替え以前の経緯である。

## 以前の採用・調査記録

2026-09-15 00:30追記: 過去に外部へ置いた調査資料は、利用者の指示に従いプロジェクト内のscratchpadへ移した。下の旧パスは当時の経緯であり、現在の保存先指定ではない。追加計算の期限・中止・ブラウザー接続と、無理数成分のrankを渡す本体実装を加えたが、追加分は検証待ち。固定資産と原文・関連ソースをまとめた[導入案](exact-runtime-proposal.md)を提示し、製品への新規バイナリ追加の採否を確認中である。

2026-09-14 23:25追記: 候補の構造化入力37件・出力19件は3環境で確認済み。Firefoxは準備180秒・計算45秒の候補用枠で22:39に成功した。既存の依頼/返信経路と候補Workerをつなぐ実接続も10入力と不正配列3種類で成功した。固定した旧資産は保持し、新しい受渡し処理は外部の調査用資料として内容指紋を記録した。製品用の依存追加・有効化・配布確認は未実施で、未採用を維持する。

2026-09-14 19:43の候補調査: Pyodide 314.0.6・SymPy 1.14.0・mpmath 1.3.0の固定配布物を製品へ導入せず、実配信のCSPとChrome・Firefox・Electron Workerで検証した。初回計算、中止、別Workerでの再計算、固定入力以外の拒否、線形代数・微積分・微分方程式/変換・特殊関数の独立照合を含む17件（前提を含む）が成功。4 runtime＋2 wheelは18,133,822バイト。Firefoxの初期準備中に旧45秒期限を超えた履歴を保持し、調査だけを準備90秒・計算45秒の外側監視へ分けた。一般の利用者入力への接続、未解決結果の扱い、メモリ制限、WASM内の全許諾の照合は残るため、**未採用**であり、以下の製品依存表は変更しない。

2026-09-12。追加の数学入力13件・関数作図14件に関する依存の判断と実装中の配信計測。機能全体の受入やリリース完了の記録とは区別する。

## 版と役割

| 部品 | 固定版 | 採用する役割 | 配布物の宣言 |
|---|---|---|---|
| [MathLive](https://github.com/arnog/mathlive/tree/v0.110.0) | 0.110.0 | 分数・根号・上下付き文字などを構造として編集し、LaTeX入力を取得する | MIT |
| [Compute Engine](https://github.com/cortex-js/compute-engine) | 0.128.6 | 専用の計算Worker内で式を読み、厳密値・40桁精度の数値を扱う | MIT |

版は`pnpm-workspace.yaml`のcatalogへ完全固定し、`pnpm-lock.yaml`で推移依存も固定する。数学記号の見た目を編集する役割と、CADへ採用できる値を判断する役割を分ける。既存の短い座標式と日本語の係数名は維持する。分野ごとの対応範囲・未対応の扱い・独立した数値との照合は[数学入力計画](../plans/追加-数学入力.md)を正とする。

MathLiveのpackage.jsonはCompute Engine **0.58.0**も依存として宣言する。これを0.128.6と同一の計算部と扱わない。`loadStructuredMathField`は`MathfieldElement.computeEngine = null`を設定し、計算は明示固定した別Workerへ渡す。入力欄に計算器を取り付けない。Compute Engine 0.128.6自身の直接依存は`@arnog/colors`と`complex-esm`である。ライブラリの一部機能が使えることを、全数学機能の対応済みという根拠にしない。

## 数式保存形式の版（pointercad-math）の明文化（2026-09-24）

`StoredMathExpression`（`packages/expression/src/math/mathInputContract.ts`）の`format`欄が持つ`pointercad-math/1`（定数`MATH_INPUT_FORMAT`）は、1つの番号で次の3つをまとめて表す版であり、schema・構文・数学規約に別々の番号を割り当てていない。

1. **schema**: `StoredMathExpression`自身が持つ欄の集合（`format`・`source`・`inputNotation`・`angleUnit`・`expression`・任意の`declarations`）と、`expression`（`MathNode`）の各種類が持てる形。`decodeStoredMathStructure`（`packages/expression/src/math/decodeStoredMath.ts`）が欄の過不足を`keys()`で検査する。
2. **構文**: 通常入力・構造入力の文字列（`source`）からこの`expression`を組み立てる際に認める記法の集合（`mathTextSyntax.ts`・`parseMathLatex.ts`）。式を再計算するとき（`mathWorkExecution.ts`・`functionMathSource.ts`が使う`decodeStoredMath`）は、保存済みの`source`を同じ構文解析へ通し直し、結果が保存済みの`expression`と一致することまで確認する。ファイルを開くだけの境界（`packages/io/src/pcad/guards.ts`の`readExpressionItem`が使う`decodeMathExpressionStorage`→`decodeStoredMathStructure`）は構文解析をせず、欄の外側にある`source`文字列と`mathDefinition.source`が一致することだけを確認する（構文そのものの一致は次の再計算時に確認される）。
3. **数学規約**: 角度の既定・主値や分岐の選び方・定義域の扱いなど、`expression`の各演算IDが何を意味するかという取り決め。記法や欄の形を変えなくても、ある演算の意味づけ（例: 主値の枝）を変える改修は保存済みの式の意味を変えてしまうため、この規約も版に含める。

3つを1つの番号にまとめているのは、読み手が保存データを解釈できるかどうかの判定を「`format`の一致確認1回」だけに単純化するためである。3つのうちどれか1つでも保存時と食い違えば、その式を安全に再解釈できない点は変わらない。

版の確認はfail-closedで行う。`decodeStoredMathStructure`は`stored.format !== MATH_INPUT_FORMAT`のとき、他の欄を検査する前に例外（「数式の版または入力設定に対応していません。」）を投げて読み込みを止め、`pointercad-math`には版を1つずつ持ち上げる移行表を持たない。これは`.pcad`全体の封筒の版（`PCAD_SCHEMA_VERSION`、`packages/io/src/pcad/schema.ts`。現在17）が`SCHEMA_MIGRATIONS`で旧い文書を現在の版まで前方互換に持ち上げるのとは対照的である。両者の役割は次のように分かれる。

- `PCAD_SCHEMA_VERSION`は**`mathDefinition`欄そのものが存在するかどうか**を管理する。版14で数式定義と係数IDの欄を新設し（schema.tsの版14の注記）、版13以前の文書は`SCHEMA_MIGRATIONS[13]`がこの欄を持たない旧文書のまま持ち上げる（欄が無ければ従来の短い式として読む）。
- `format: pointercad-math/1`は**その欄が存在するときの中身**を管理する。文書全体の`schemaVersion`が最新でも、`mathDefinition.format`が現在の`MATH_INPUT_FORMAT`と一致しなければ、その式は読み込めない（移行せず拒否する）。

現在製品が書き出す版は`pointercad-math/1`のみで、これまで版を上げたことは無い。将来、上記3種のいずれかを保存済みの式の意味が変わる形で改修する場合は`pointercad-math/2`のように番号を進め、旧版を移行するか拒否するかをその変更内容に応じてそのとき判断する（本節はその判断を先取りしない）。

検査: `packages/io/src/pcad/mathExpressionJson.test.ts`に、保存した数式の版が`pointercad-math/1`に固定され文書の往復後も変わらないことを確認する検査と、版番号だけが異なる保存データ（`pointercad-math/2`・接尾辞を欠く`pointercad-math`）を数学定義として読み込まないことを確認する検査を追加した。

## 実行と入力の境界

- `createMathBackend`の生成直後に`jit = 'off'`を設定する。画面や独自スキームのCSPへ`unsafe-eval`を追加しない。
- エンジン内部はラジアンへ統一し、新規の利用者入力は度を既定とする。度とラジアンの変換は入力境界で1回行い、保存済みの単位を引き継ぐ。
- エンジンの反復上限1000・再帰上限64と同期処理200msの制限に加え、外側のWorkerの終了期限と取消を使う。同期期限へPromiseを返す処理は型で拒否する。
- 変数はX/Y/Z/T/U/V、係数は文書内IDと表示名、定数はπなどの定義として区別する。見た目の同名だけで異なる役割を合体させない。
- 関数作図はXYZの6端点を必須とし、有限値・大小関係・生成後の範囲を検査する。近似誤差の推定と数学的に保証した上限を区別し、未確認の結果を保証済みのCAD座標へ採用しない。
- 公開入口は`packages/expression/package.json`が正本。数学の内部ファイル35入口を、`math/contracts`（型・入力検証）、`math/client`（画面からの依頼）、`math/worker`（記号計算と依頼の実行）、`math/geometry`（曲線・曲面の評価）の4入口へ整理した。`packageBoundary.test.ts`で利用側の全参照と公開入口を照合し、`mathClientBoundary.test.ts`で画面用の2入口を読み込んでも計算部が実行されないことを検査する。画面本体からWorker用入口の実行時importはlintで拒否する。9/13の型・lint・公開入口検査と3環境の実画面用ビルドで確認した。以下はその後の実配布用フォルダの計測で、リリース済み版の記録ではない。

## ローカル資産と実測サイズ

字体は`mathlive/fonts.css`からビルドに含める。`fontsDirectory`・`soundsDirectory`・効果音を無効にし、外部CDNへの自動取得を使わない。CSSから字体を同梱する方式は[公式の組み込み説明](https://cortexjs.io/mathfield/guides/integration/)に沿う。字体の読込完了と期限も画面側で検査する。

9/13 14:04の実装中Web・Desktopビルドを実ファイルから計測した値。以下の数学資産は両版で内容・サイズが一致した。gzipは同じファイルを標準gzipで圧縮した比較値であり、サーバーの実際の転送方式や全機能の総増分を示すものではない。

| ビルド資産 | バイト数 | gzipバイト数 |
|---|---:|---:|
| `mathlive.min-Bkp5CN7-.js` | 800,600 | 215,706 |
| `math.worker-Dx8L4b_a.js`（製品の数学・作図処理を含む） | 3,712,164 | 1,028,363 |
| `math-input-YClINmHB.js`（数学入力の説明） | 17,310 | 6,646 |

数学WorkerのSHA-256は`9c8cacadfb76e2f39c7ee3e8a5e30686bad538c9671670efb81d22a0f9cd13b2`、MathLiveチャンクは`2e7720b349af764e99d16f705a6dfc3dc2a15ecaec5adc7be103f7e62668e29a`。字体20ファイルの各サイズ・SHA-256、両ビルドの許諾9ファイルの記録は開発証拠`.git/codex-math-build-assets-20260913-140416-243616.json`に保存した。取得したpackage.jsonの値と初回の配布調査は`math-distribution-evidence.json`に残す。最終インストーラー・公開するWeb成果物の全資産一覧はP13で改めて固定する。

## 許諾原文と残る確認

配布パッケージ内の原文を改変せず、[Compute EngineのMIT本文](licenses/compute-engine.txt)と[MathLiveのMIT本文](licenses/mathlive.txt)へ保存する。原文のSHA-256は、それぞれ`f9270498fcb583f1a8aa4664e1986910d005303ff9c5cbab432bb4d1f292f225`と`3caebd18611959ca725ab976a004d408017eb1d7093d6c9b9b09c5a4790eaff8`。この記録の作成では依存の版やvendorを変更していない。

字体の許諾はJavaScript本体のMITとは別に確認した。MathLive v0.110.0の字体20ファイルをGitの同じタグの内容と照合し、すべて一致した。実際のWOFF2に埋め込まれたnameテーブルの著作権・許諾表示を[字体別の原文](licenses/mathlive-fonts.txt)へ保存した。Design Science、Khan Academyの表示と各Reserved Font Nameを保持する。字体は変更せず、[SIL Open Font License 1.1本文](licenses/ofl-1.1.txt)を同梱する。本文は[公式の原文](https://openfontlicense.org/documents/OFL.txt)の区切り線以降をそのまま保存し、冒頭の記入例を実際の権利者表示として転用しない。

[固定版・原文・字体のSHA-256一覧](licenses/math-notices.json)を、WebとElectronの同じビルド処理が読む。依存の版、インストールされた許諾原文、字体の一覧・内容が記録と異なる場合は組み立てを失敗させる。実際に出力した字体にも欠落・差し替え・重複の検査を行う。原文は両版の`licenses/`へ出力し、`licenses/index.html`から参照できる。9/13 14:04の実ビルドで両版の9ファイルが一致することを確認した。最終インストーラー内の確認と、次段の未解決原文は残る。

推移依存ではCompute Engine 0.58.0、complex-esm 2.1.1-esm1、decimal.js 10.6.0の配布原文も保存した。**`@arnog/colors` 0.7.0/0.5.0は未解決**である。package.jsonのMIT宣言は確認できたが、配布物に独立した著作権・許諾原文がなく、記載されたソースリポジトリは404だった。これを別のライブラリの著作権表示で補わず、上記一覧の`unresolved`へ残した。実際の配信物に含まれる範囲と原文の取得を解決するまで、F13・許諾の確認を完了扱いにしない。


## 有限の数列・差分・漸化式（2026-09-22）

`sequencevalue(式,添字,位置)`、`differenceat(式,添字,位置,回数,間隔)`、`recurrencevalue(次項の式,[添字,古い順の項],初期添字,[初期値],位置)`は保存時に局所変数を持つ構造へ変換する。漸化式のk個の初期値はnからn+k−1の値、本文はn+kの値である。元の式を途中の各項へ適用し、分母の不成立や不要に見える成分の不成立を0倍・成分選択で消さない。初期添字以前を逆向きに推測しない。

初期値は16個、続けて求める項は4096回、差分は64回まで。元の構造4096項目・深さ64に加え、反復・入れ子を共通に数える262144項目と途中の整数の分子・分母16384bitの予算を設ける。数値の累乗は生成前に大きさを拒否し、超過時は途中の値を返さない。製品の追加計算部の45秒・準備180秒・全体225秒、中止と古い返信の拒否は維持する。実数座標へ渡す条件は共通の数値境界を通し、真偽・行列・非有限値を数列の値として通さない。級数の誤差や収束、漸化式の一般解をこの有限計算の完了で代用しない。

数列を含む有限総和・総積は、外側から範囲を確定し、元の添字を各項へ代入して求める。下限・上限・刻み幅はその添字を導入する前の範囲で読み、同名でも異なる変数IDは混同しない。刻み幅は正整数、空の和は0・空の積は1、各範囲は4096項までとし、数列と共通の作業予算を消費する。0となった積も後続の項を確認する。数列を含む無限範囲は収束の証明なしに有限値へしない。

数列本文の整数演算は、通常入力と同じ非負の整数余り、符号付き除数の整数商、除数と同符号の通常modを区別する。丸めはちょうど半分を偶数側へそろえる。階乗・二重階乗・組合せ・順列は実計算部の既存1000上限に合わせ、二重階乗の(-1)だけは1とする。素数と因数は試し割りによる確定値だけを返し、200000回の局所上限と数列の共有予算を併用する。素数らしいという近似判定や途中までの因数一覧を成功にしない。整数入力4096bit、因数・約数の一覧256個、丸め桁±2048も既存の通常入力とそろえる。


## 展開の係数選択（2026-09-22 05:16）

`seriescoefficient(taylor(式,変数,中心,次数),選ぶ次数)`とMaclaurin展開の同じ選択は、0を定数項として保持された次数までの正確な係数を返す。元の展開全体の成立と上限を検査してから選ぶ。外側の0倍や成分選択によって、成立しない展開を隠さない。複素数の係数は実部などを明示的に選んだ場合だけ通常の有限実数境界へ渡す。展開自体を数値とみなす操作ではない。

元の展開、中心、残す次数、選ぶ次数、係数の識別、角度は既存の保存する数式へ保持する。局所的な成立条件を確認後に取り除く際、外側の式の成立条件は残す。入れ子の展開の微分など、別の連続性確認が必要な計算を無条件に受け入れない。式変換で掛け算と外積を判別する型にも、選択後の係数を登録した。展開全体は数値型へ登録しない。

05:15までに型/lint、修正後49項目、入力・表示173項目、Web2環境と実Electronで保存再開・12→27の中心編集・Undo・F1、説明76項目が成功。証拠は`scratchpad/tasks/6131650ed8c2-taylor-coefficient-a86swpzh/completed-related-checks.json`。初回421項目の420成功・1失敗と再確認範囲を区別する。以下の過去記録にある独立文書や数値誤差保証は現状の対象外を示すもので、原要件の確認なしに新しいリリース必須作業へ加えるものではない。原式保存・成分利用・切断位置の表示を確認し、数学全体の対応範囲は別途照合する。

## Taylor展開の型と正確性の境界（2026-09-22）

`taylor(式,変数,中心,次数)`と`maclaurin(式,変数,次数)`は、局所変数を持つ元の式と展開結果を別々に保持する。次数0〜12、解決済みの有限な実数中心を要求し、次数・中心・各係数・省略項の次数・収束について確認できた範囲を専用の`series`型で返す。通常の実数・座標値として返さず、数値欄の「この式を使う」は無効になる。元の式の係数IDは数値に置換せず、計算用の式だけに現在値を代入する。

元の全構成要素と分母条件を中心で検査する。式の整理で消えた穴を埋めず、微分不能な中心を係数0として通さない。元の構成要素すべての解析性を確認できる場合だけ逐次微分の代入を使い、それ以外は両側の差商を確認する。多項式として完全に一致し、元の成立条件も残らない場合だけ省略項を0とする。`O((x-a)^(n+1))`は省略項の次数であり、数値誤差の保証ではない。

収束の表示は、全平面で確認済み・十分な開円板・未確認を区別する。多項式の極を全て列挙できた場合は複素数の極も考慮し、元の式で打ち消された分母も残す。確認した開円板は最大範囲とは断定せず、境界や外側を保証しない。分岐のある関数など証明対象外の式は「未確認」に保つ。返信では元の中心と次数の一致、閉じた有限な係数の構造、項数と全体の大きさ、数値権限がないことを検査する。等価性を確認できない中心は推測して受け入れない。

固定済みの追加計算部と既存の45秒・準備180秒・全体225秒、取消、古い返信の拒否を使い、新しい依存を増やさない。個々の点における数値誤差の保証や、展開そのものを独立した文書要素として保存する機能は残件である。2026-09-22 04:24までに型・書き方、関連する計算404項目、画面側172項目、3環境の実画面確認、実画像反映後の説明76項目が成功した。記録は`scratchpad/tasks/6131650ed8c2-taylor-series-ojq287cr/completed-related-checks.json`。ADD-21全体の完成や両OS全検査の成功を認定したものではない。


### 展開前の資源確認と巨大多項式

中心・要求次数・元の本文は、既存の正確な整数の16384bit上限と共有作業予算で読み取る。巨大な数値累乗を実際に生成する前に拒否し、0倍で消える部分でも通さない。省略項が0かの判定は、式を全展開して比較する方法から、和・積・非負整数冪の構造から求める多項式次数の上限へ変更した。上限が要求次数以下で元の成立条件が残らない場合だけ完全な一致とする。分母の根を探す場合も、次数上限12を確認してから多項式へ展開する。原式を100万次へ展開せず、(1+x)^1000000の先頭4係数を独立な二項係数と照合した。

変更後の型/lint、対象18項目（内部で11の独立したPython確認を実行）、入力・表示172項目、3環境の過大計算の拒否から正常操作・取消・保存への復帰、説明76項目が04:36までに成功した。証拠は`scratchpad/tasks/6131650ed8c2-taylor-resource-bounds-13bt0hym/completed-related-checks.json`。先行する404項目の計算確認は別の記録で保持し、今回の18項目と重複加算しない。計算期限、返す係数の精度、対応する入力を小さくして成功にしたものではない。


## Legendre多項式（2026-09-22）

`legendre(n,x)`は整数次数n=0〜128の標準多項式P_n(x)で、P_n(1)=1、P_0=1、P_1=xとする。Rodrigues公式の係数をBigIntの分数として求め、計算時だけ既存の算術へ変換する。原文・元の構造・係数ID・角度単位は変換前の入力を保存する。表示方式の変換が作る標準表記と、文書に保存する原文を混同しない。

次数と展開後の個数・深さを生成前に確認する。負の次数・非整数・真偽や一覧の引数を拒否し、未確定次数を推測しない。0次でも元の引数を計算経路に残し、1/Xの穴などを1へ消さない。関数作図は同じ算術の値・区間・微分へ接続する。複素数を座標に使うには実部や虚部の明示選択が必要で、三角関数の角度変換は引数内へだけ適用する。

固定計算部でも同じ次数制限と元の引数を確認し、級数や指定位置の微分へ渡せる。整数次数でないLegendre関数や別添字の付随関数の対応を、この多項式の対応で代用しない。独立照合は三項漸化式・既知の有理数値・複素数値と、P4の別記した微分式を使う。作業記録は`scratchpad/tasks/6131650ed8c2-special-functions-19_h7t8w/`。型/lint・修正後の計算31項目・入力表示174項目・3環境の保存再開17→5.5→Undo・実画像反映後の説明76項目が成功した。原失敗を別に保持し、ADD-18全体やリリースの合格とは区別する。


## 実数の誤差関数（2026-09-22）

`erf(x)`と`erfc(x)`は有限な実数と確認できる引数を受け付ける。元の0割り、一覧、真偽、無限大を、外側の0倍や成分選択で消さない。この実数対応時点では複素引数を未対応としていた。複素数への追加対応は本書の「複素数の誤差関数」を参照する。有限性が証明できない定数式は推測で通さない。引数の絶対値は500000までとし、通常座標へ表現できない小さい値を0へ置き換えない。

既存の正規分布用の正項級数と正の連分数を使う。erfcの小さい値は、1へ丸めたerfを引いて作らず、裾を直接計算する。内部の80桁と残項判定を保持し、外へ返す数値は有効40桁へ丸める。独立した90桁の計算から作成した27組の正負・原点・極小・接続境界・裾の値を相対誤差で照合する。数値を0に近い絶対誤差だけで許容しない。

作図の区間は端点の単調性と外向き丸め、中央の正項級数の残項、裾の隣接連分数で評価する。点での値・点での微分・区間の一階二階微分・式の微分を同じ独立な公式と照合した。固定計算部はerf/erfcを元の関数として受け渡す。Taylorでは関数自体の解析性だけで元の引数の穴を埋めず、全平面と有限な半径を区別する。根拠は[NISTの級数](https://dlmf.nist.gov/7.6)、[連分数](https://dlmf.nist.gov/7.9)、[微分](https://dlmf.nist.gov/7.10)。

表示修正後の354項目・入力表示176項目・型/lint・3環境の結果全文、保存再開、値の変更とUndo、実画像を含む説明76項目が成功した。元の型2回と計算2項目の失敗、表示修正前の402項目・176項目・3環境の成功も別に保持する。証拠は`scratchpad/tasks/6131650ed8c2-error-functions-nq53rxu0/completed-related-checks.json`。複素引数や他の特殊関数、ADD-18全体・最終CI・リリースの成功には読み替えない。


### Gamma関数の計算部（2026-09-22、入力への接続前）

`gammaFunctionNumeric.ts`は実数の正確な分数を受け、既存の正のlog Gammaと反射公式を使う。非正整数の極は小数変換前に判定し、sin(πx)は分子の整数商と余りで縮約する。対応範囲は−19999〜20000、非正整数を除く。内部80桁・出力有効40桁。中止と桁数・範囲の制限を持ち、非有限値や失われた微小値を答えにしない。座標への有限double変換は既存の境界で別途確認する。

`gammaFunctionIntervals.ts`は正の引数のStirling展開と残項、外向き丸め、反射公式から値と一階・二階微分を囲う。非正整数を含む区間は未確定とする。負の区間の三角関数は各端点を最寄り整数との差へ移し、整数間の符号と半整数の極値を使う。Γは正の領域でも単調と仮定しない。psiの導関数の残項はBinet積分の核の剰余から確認し、単に近似式を微分しただけの幅を保証としない。

独立したmpmath 1.3.0を200桁で計算した27組と、非正整数から1e-100だけ離れた8組を固定。最初の広い負の区間2失敗を残し、修正後は関連310項目と型・書き方が成功した。記録は`scratchpad/tasks/6131650ed8c2-gamma-function-sxfsu75r/completed-calculation-checks.json`。この時点では通常の入力・パレットへ未登録で、利用可能なGamma機能全体の完成とはしない。微分結果、固定計算部、保存・再編集・説明への接続を続ける。

根拠: [DLMF 5.11](https://dlmf.nist.gov/5.11)、[Binetの積分表示](https://dlmf.nist.gov/5.9)、[反射公式](https://dlmf.nist.gov/5.5)、[polygamma](https://dlmf.nist.gov/5.15)。


## Gammaの微分結果を扱う計算部（2026-09-22、接続前）

既存の正確なStirling係数をn+1回微分した係数を整数で作り、正の引数を128以上へ移してDLMF 5.15.9の展開を計算する。DLMF 5.15.5で戻す。最初の省略項を値の絶対値の1e-60以下で切り、計算途中の絶対値の和の1e-70を丸めの保守的な監視量として加える。80桁で計算して40桁を返す。監視量が答えの絶対値の1e-45を超えるほど桁が消えた場合は確定値としない。この監視量は作図区間の証明には使わない。区間計算は別の外向き丸めを使う。

負の引数はDLMF 5.15.6の反射式。cotの各階微分はP0(c)=c、P(n+1)=-(1+c²)P'nの整数係数多項式で作る。元の正確な分数を周期1で減らしてから三角計算し、-1±1e-100を-1へ丸めない。半整数ではcot=0を正確に使い、高階の大きい係数で偽の誤差を増幅しない。正・負の非整数の高階値が同じ符号と決めつけない。

## 範囲の根拠

DLMF 5.9.12/13の積分核と4.36.3のcothの部分分数展開から、t>0に対して
1/(1-exp(-t))=1/t+1/2+2t Σ[k>=1] 1/(t²+4π²k²)。
各分母に有限の幾何展開を適用すると、残りは最初の省略項と同符号で、その絶対値以下となる。正の重みexp(-xt) t^nを掛けて積分しても同じ包含が保たれる。n=0は5.9.13の差を先に取って1/tを相殺する。これによりpsiの各階についても最初の省略Stirling項が残りを囲む。単に漸近展開を微分しただけで残りの上限を断定していない。

作図用には正の範囲を32以上へ移し、8項と9項目の0までの囲いを外向き丸めで加える。係数はBigIntの正確な整数をexactDoubleIntervalで囲み、2^53を超える係数をnumberへ先に丸めない。漸化式・反射式・多項式をすべて区間演算する。非正整数を含む元の区間は最初に拒否する。負の区間のsin/cosは、Gammaで確認した同じ整数間の端点・極値の囲いを使用する。半整数の端点はsin=±1、cos=0を正確に保持する。非有限の囲いを有限値へ置き換えない。

参照: https://dlmf.nist.gov/5.15 https://dlmf.nist.gov/5.9 https://dlmf.nist.gov/4.36 https://dlmf.nist.gov/5.11#ii （2026-09-22閲覧）。

独立基準は承認済みmpmath 1.3.0の同梱wheelを240桁で計算して70桁を保存した174組と、整数±1e-100の48組。実装の係数から期待値を作らない。生成元・wheelと結果のSHAをreference-generation.jsonへ記録する。

確認結果（2026-09-22 07:30）: 型・書き方およびGamma・psi・既存分布を含む724項目/7ファイルが成功。元のGamma区間の端点では、正確な半整数をsin=±1/cos=0として扱う変更も同時に確認した。数値側の桁落ち監視量と、外向き丸めによる範囲の証明を混同しない。証拠はscratchpad/tasks/6131650ed8c2-polygamma-connection-7yy2hpso/completed-derivative-checks.json。入力登録・保存の実画面は未接続。


## 実数Gammaの入力と計算結果への接続（2026-09-22）

`gamma(x)` と `polygamma(n,x)`を共通の演算登録、通常入力、構造入力、返信の検証、値の計算、作図用の範囲と微分へ接続する。polygammaの次数は0〜17の整数、引数は−19999〜20000の有限な実数で非正整数を除く。次数や非正整数の判定は元の正確な分数で行い、-1±1e-100を同じ-1へ丸めない。定数の合成式は既存の区間計算で実数と極の不存在を確かめる。複素数、証明できない領域、上限外の値を成功の数値へ置き換えない。

作図はGammaの値・一階二階の範囲、polygammaの次数を一つまたは二つ進めた微分、式としての微分を同じ原式の領域で扱う。元の極や内側の1/Xの穴を、0倍・成分選択・微分で消さない。対応次数を超えた微分の結果は未対応とする。

固定計算部はGammaの極を元の領域条件へ保存し、指定位置の微分でその条件を確認する。Taylorの収束円はGammaの引数が実係数の一次式の場合に最も近い非正整数の逆像までを確認する。一般の合成を、どこでも正しい展開へ格上げしない。計算部のEuler定数・Catalan定数・扱える整数次数のzetaを正確なpolygammaを含む保存可能な式へ返し、丸めた定数で代用しない。

Catalan定数の受渡しは汎用のsimplifyが恒等式を認識すると仮定しない。A=Σ(4k+1)^−2、B=Σ(4k+3)^−2とすると、定義よりG=A−B、奇数平方の逆数和よりpi²=8(A+B)、[DLMF 5.15.1](https://dlmf.nist.gov/5.15.E1)よりpsi'(1/4)=16A。この正確な係数で受渡し式との差が0になることを確認する。他のpolygammaとzetaの関係は共通のzeta表記で照合する。

08:00までの確認は、最初の381項目中380成功・1失敗から、照合方法だけを修正したGammaファイル112項目を再確認して成功。本体と他の検査はハッシュが一致し、合格済みを繰り返していない。記録はscratchpad/tasks/6131650ed8c2-gamma-integration-jtfh4vrq/before-deadline-unit-evidence.md。画面と配布読込の追加後も型/lintは成功。入力候補・実画面・説明画像の確認は実行中で、完了時に追記する。


確認結果（2026-09-22 08:26）: Gamma/Polygammaの数値計算にも依頼開始から1秒の期限を接続した。Firefoxの最初のgamma(1)が262.12msで通常式200msに達した実測を根拠とし、通常式200msの目標・反復数・精度を維持。修正後の型/lint、関連計算206項目/4ファイル、入力表示179項目/6ファイル、Chromium・Firefox・実Electronの入力/点の微分/実保存再開/再編集/Undo/F1、実画像反映後の説明76項目/7ファイルが成功した。最初の381項目の接続確認と724項目の内部計算の記録は別に保持し、今回の206へ足し合わせない。GammaのPythonファイルも実配布へ登録し、読込一覧から外したときの不足検出を通常検査に加えた。最終記録は`scratchpad/tasks/6131650ed8c2-gamma-integration-jtfh4vrq/completed-related-checks.json`。数学全体・複素引数・main統合・両OS全検査・配布物の実起動は未完了。


## 正の実数Betaの計算部分（2026-09-22 08:41）

二引数a,bが正で、元の正確な和が20000以下の場合を対象とする。通常の数値計算は80桁でlogGamma(a)+logGamma(b)-logGamma(a+b)を組み立ててから指数へ戻し、40桁を出力する。片方が正確に1ならもう片方の逆数。途中のGamma値を作らないため、Gamma(19999)を浮動小数へできなくてもBeta(19999,1)は計算できる。20000+1e-100を20000へ丸めて上限を通さない。

作図の値は[DLMF 5.12.1](https://dlmf.nist.gov/5.12#E1)の正の領域の積分から導く各引数の単調減少を使い、入力の箱の端で囲む。対数Gammaの既存の余りを含む範囲を足し引きしてから指数を取る。引数の和はIEEE-754値を正確な分数へ戻して囲み、境界の19999+1を不要に上限超過にしない。Gammaの値を指数へ戻す前の対数範囲だけを公開し、既存のGammaの計算結果は変更しない。

対数の一階はpsi(a)-psi(a+b)とpsi(b)-psi(a+b)、二階はpsi1(a)-psi1(a+b)、psi1(b)-psi1(a+b)、混合項は-psi1(a+b)。これらからBetaの一階・二階・混合微分を区間演算で求める。値だけを描く入口では微分計算を行わない。正の領域外や原式の和の範囲外は未確定として返し、値が浮動小数で大きすぎる・小さすぎる場合は0や有限の点で代用しない。これは負・複素引数の一般的な解析接続の完了ではない。

独立基準は承認済みmpmathの別実装を400桁と640桁で計算した19組。値と数値微分の6成分が相対1e-75未満で一致し、正の積分から導く各微分の符号を満たすことを確認して65桁を保存した。初版160桁で微小引数の混合微分が0となった記録を保持し、基準全体の非零・有限・符号を通常検査にも加えた。数値の答えは相対1e-38未満で独立基準へ照合する。

変更後の型/lintと関連348項目/5ファイルが成功。計算コマンドは6.760秒。記録は`scratchpad/tasks/6131650ed8c2-beta-function-85oybyq7/completed-calculation-checks.json`。入力登録・CASの成立条件・作図命令への接続・保存の実画面・説明はこれからで、この計算部分だけでADD-18の完了とは認めない。


## Betaの入力・作図・保存への接続（2026-09-22 09:18）

Betaを通常入力、構造入力、式の保存、作図の値・範囲・二変数の微分へ接続した。固定計算部では正の引数を元の条件として保持し、指定位置の微分と混合微分、Taylor係数を正確な式として返す。正の実係数一次式の引数が複素平面の右半面に留まる円を十分な収束範囲とし、元の0倍で条件を消さない。未知の合成を証明済みの範囲へ格上げしない。

小さい引数の点の微分では、psi_n(x)-psi_n(x+h)を−∫[0,h]psi_(n+1)(x+t)dtとしても囲み、直接差と共通する範囲を使う。hを保持して近い値の差の桁落ちを抑え、1e-100と1・19999を独立な数値微分へ照合する。数値出力の精度・元の入力条件・許容誤差は緩めていない。

修正後の型/lint、Betaの計算・範囲・作図・入力と期限124項目、入力表示180項目、Chromium・Firefox・実Electronの入力/点の微分/保存再開/再編集/Undo/F1、実画像反映後の説明76項目が成功した。初回269項目中268成功・1失敗の記録は別に保持し、再実行していない既存Gamma等を今回へ足し合わせない。記録は`scratchpad/tasks/6131650ed8c2-beta-integration-1ghexbai/completed-related-checks.json`。負・複素引数、残る特殊関数、数学全体・main統合・両OS全検査・配布物の実起動は未完了。

## 楕円積分の通常入力と実数の境界（2026-09-22、接続作業中）

完全な第一種K・第二種E・第三種Piと、振幅を指定するF・Einc・Piincを扱う。母数mはkの二乗、第三種のnは別の係数とする。角度の指定は振幅だけに適用し、元の式を保持する。πの有理数倍は角度を小数化する前に周期・半周期を判定する。m=1の不完全な第二種はcosの絶対値を積分する。積分途中で実数範囲を外れる場合や極を越える場合に、終点だけを見て有限値を返したり主値へ置き換えたりしない。

元の入力条件を確認する処理は、対称積分RF/RD/RJの値を重複計算しない。固定した分数の上下限から最終40桁を確定し、必要な桁数を確定できない場合は値を返さない。複素引数や未対応の境界を一般の実数入力へ置き換えない。

第三種の周期をまたぐ通常入力が1秒の内部上限で停止した実測に基づき、利用者の正確性優先・妥当な上限緩和の許可を適用する。楕円積分を含む同期計算は依頼開始から3秒、従来の高精度関数は1秒、通常式は200msを維持する。関数を繰り返し呼んでも開始時刻を更新せず、親の期限を越えない。別の依頼へ延長を持ち越さず、外側のWorkerの取消と期限も維持する。40桁や境界の判定基準の緩和は行わない。mathDistributionDeadline.test.tsで通常入口・評価後・繰返し・他の関数との合成・例外・親の期限を確認する。

16:33に、通常入力への接続と上限変更後の型/lint・関連407項目・8ファイルが成功し、確認対象の内容一致をcompleted-input-checks.jsonへ保存した。前段の68項目と二段階の失敗は別に保持する。作図・追加計算部・画面・実保存と複素領域は残る。数学全体の完成や両OS合格には計上しない。記録はscratchpad/tasks/6131650ed8c2-elliptic-integrals-dpk0psk2。

17:17に追加計算部の接続を確認した。元の積分の経路と点の前後の解析性を確かめ、n=0の第三種の微分では積分内の幾何級数から得る正確な係数を使う。母数も0なら二変数の必要次数まで残す。不完全形をatan(tan(phi))型へ自動変換して周期分の値を落とさず、元の積分を保ったまま受け渡す。関連計算55項目、同梱・入力表示189項目が成功し、completed-cas-checks.jsonへ内容の一致を記録した。最初の54成功・1失敗を保持する。作図・入力候補と実画面・実保存、複素領域と数学全体は未完了。

## 整数次数Besselの入力・作図・追加計算部への接続（2026-09-22）

J/Y/I/Kを同じ原式として通常入力・構造入力・保存へ接続する。次数は整数−128〜128、引数は実数で絶対値128以下、Y/Kは正の引数が必要。0倍・成分選択・微分などで元の不成立を隠さない。通常の数値は前段の囲いから40桁を確定し、作図の点・範囲・一階二階微分へ接続する。元の数の桁数制限は数値へ丸める前に適用する。

作図の倍精度では、非零の値が0と区別できない場合、点と傾きへ渡さない。証明された厳密な0とJ/Iの原点の値・微分は保持する。区間の照合には細かい独立確認値と二進の端点の正確な値を使用し、期待値を先にNumberへ変換して消失させない。

固定計算部では原式とY/Kの正の条件を残し、指定位置の微分は、元の条件を確認した後、整数Besselを含む式の実解析的な近傍を閉じた規則で証明できる場合だけ、正確な連鎖則と隣接次数の公式を使う。絶対値・場合分け・未知の呼出しを連続性だけで微分可能としない。他の既存の式の左右極限による確認は維持する。J/Iの整数次数は整関数、Y/Kの級数は正の半平面内にとどまると確認できた円だけを返す。一般の記号値の成立性を変更せず、有限な実数と確認できたBessel定数・その和積等のみを有限として扱う。生成された次数も公開上限を通す。

一般次数と複素引数はこの実装に含めない。ADD-18全体や両OS CI・配布の完成には読み替えない。型/lint、修正後のBessel・既存微分36項目、入力184項目、三環境の実保存再編集、実画像採用後の説明76項目が成功。証拠は`scratchpad/tasks/6131650ed8c2-bessel-input-integration-gjplo2m8/completed-related-checks.json`。通常入力・作図423項目の前段の記録を別に保持する。

## 複素数の誤差関数（2026-09-22）

`erf`と`erfc`は複素引数の両成分について−8〜8を計算対象とする。これは計算資源の境界であり、数学上の定義域ではない。実部・虚部の元の正確な分数を確認し、範囲の直外を丸めて内側にしない。実数へ正確に戻った引数には既存の実数計算を使う。複素数を座標へ直接変換せず、`re`か`im`を明示した場合だけ有限な実数の境界を通す。

DLMF 7.6.1の級数を外向きに丸めた固定小数の上下限で計算する。最初の省略項と、以後の比の上限が1未満になる条件から残り全体を囲む。πと平方根の幅を伝播させ、両成分の上下限の40桁が一致したときだけ返す。軸上の正確な0は対称性から保持するが、小さい非零を0にしない。実数erfcの小さい裾は既存の専用計算を維持する。

有限な複素引数に対する誤差関数の有限性は元の引数から確認する。固定計算部の微分では元の開いた成立範囲を調べ、解析的な合成を確認できたときに正確な微分公式を使用する。元の極、尖り、範囲超過を0倍や成分選択で消さない。追加計算ファイルの同梱と読み込み上限は本体の一覧から検査する。

独立した800桁・1000桁の30組と両成分の上下限・40桁を照合する。通常の返信は全桁を保つ十進表記とし、文字列そのものを比較する。数値の等価性だけで表示の合格とはしない。内部の上下限、時間上限、拒否する入力は表示修正で変更しない。複素数を使う関数作図全般や他の特殊関数の完成を意味しない。
