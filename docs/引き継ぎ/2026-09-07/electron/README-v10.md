# v8 の変更点
- 各項目の失敗時に画面画像、本文先頭2,000文字、状態欄、再計算状態、可視レイヤー、直近30件の console を `[DIAG <項目名>]` で記録する。
- R1-1 の「新規」クリック直前・直後にも同じ診断を記録する。
- R10-2 の2回の「新規」クリック直前・直後にも同じ診断を記録する。

# v9 の変更点
- 実行ごとに `RUN_DIR/user-data` を Electron の `--user-data-dir` に指定し、BOOT で実際の `userData` と復元案内なしを検査する。
- R1-1・R10-2 の先頭で、残っている復元案内を「破棄する」で片づける。
- Electron 終了後の `finally` で実行専用の userData を削除する。

# v10 の変更点
- R10-2 の開始時に、開く・保存ダイアログの差し替え結果を控える。
- R10-2 の終了時に `finally` で両方の差し替え結果を必ず元へ戻す。
- 後続の R2-1 が控えの書き出し先を実パスへ差し替えられる状態を保つ。
