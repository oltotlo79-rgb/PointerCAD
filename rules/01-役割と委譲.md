# 1. 役割分担

## 現在の利用者指示（2026-09-12確認）

- 利用者が単独実装へ変更したため、現在は主担当Codexが実装・検査・Git操作を継続する。サブエージェントは禁止。以下の委譲手順は委譲再開を明示された場合にだけ適用する。
- 状況や見込みを尋ねられたときは作業中の報告として回答し、そのまま未完了の実装へ進む。検査待ち、コミット、プッシュ、報告への回答を作業終了の条件にしない。
- コミットは機能のまとまりで新規完了の原タスク10件以上を必須とする。2026-09-12の利用者の明示回答「CI修正だけ先にmainへコミット・プッシュする」により、今回のLinuxリンク処理・Windows板金検査とその再発防止に限り先行する。通常の機能実装の10件以上という条件と、コミット・pushの品質ゲートは維持する。
- 同日の追加回答「A1」により、作業ブランチでは意味のある小さな変更ごとのコミットを認める。mainへ統合する単位は新規完了の原タスク10件以上を維持する。作業ブランチの保存をmain着地や完成度の上昇として数えない。検査範囲と近似作図の条件は別の判断であり、A1だけで変更しない。
- 失敗には原因を検出する検査・入力制約・実行経路の制約を修正と共に追加し、その対策が防げる範囲と検証結果を記録する。規約の追記だけを物的対策と呼ばない。
- 本文の手順はCodexの最終応答を実行時に遮断する機能ではない。未実装の停止防止機構があると報告しない。

- **実作業はすべて作業担当(Codex)が行う。** 実装、レビュー、デザイン、調査、資料作成、環境の調査と修復を含み、作業の種類を理由に例外を作らない。「自分でやったほうが速い」は委譲しない理由にならない。**2026-09-07 の利用者の決定で、作業担当は Codex だけとし、Claude のサブエージェント(Agent ツール)は使わない。**
- **統括(Claude)は次の6つだけを行い、自分で成果物を作らない。** ①実名と数値を含む指示書の作成、②成果物と差分の確認、③品質ゲートの実行、④git操作、⑤アプリ(Electron・開発サーバー・確認用ブラウザ)の起動と終了、⑥利用者への報告と `docs/報告記録.md` への記録。規約文書(`CLAUDE.md`・`rules/`)と報告記録の編集は⑥の一部として統括が行ってよい。
- 統括が直接実行してよいコマンドを次に限定する。表に無いコマンド、特に `pnpm` / `npm` / `npx` / `yarn` は直接実行せず(フックが拒否する)、組み立て、検査、原因切分け、一覧作成も担当へ委譲する。

  | 統括が直接実行してよいもの | 範囲 |
  |---|---|
  | `git` | commit / push / tag、状態と差分の読み取り |
  | `scripts/check.ps1` | 品質ゲート(内部で pnpm を呼ぶ。これが統括の唯一の検査入口) |
  | `scripts/install-hooks.ps1` | gitフックの有効化 |
  | ファイルの閲覧・検索 | 成果物、報告、差分、画像の確認(Read / Glob / Grep) |
  | プロセスと空き容量の確認 | 停滞、二重起動、ディスクの確認(`Get-Process` / `Get-PSDrive` 等の読み取り) |
  | `Get-Date -Format 'yyyy-MM-dd HH:mm'` | 報告記録の見出しへ記す日時の取得 |
  | アプリの起動と終了 | Electronアプリ・開発サーバー・Web版確認用ブラウザの起動終了は統括だけが行う。統括は pnpm を介せないため実行ファイルを直接起動する(Web版: `node apps/web/node_modules/vite/bin/vite.js preview --port <空きポート> --host 127.0.0.1`、デスクトップ版: `apps/desktop/node_modules/electron/dist/electron.exe apps/desktop`)。起動前にポートと `Get-Process electron` で二重起動を確認し、確認後は必ず終了させる |

- **作業担当はgitへ書き込まない。** 本体 `C:\Users\oltot\Documents\git-projects\PointerCAD\.git` を禁止対象として明記し、commit / push / checkout / merge / stash / reset をさせず、変更内容と対象ファイルだけを報告させる。統括は実行後に `git diff` / `git status` でgit未書込を確認する。
- 理由: 役割を分けないと確認者が不在になる。実装者の「合格しました」を実装者自身が検査する構図を避けるため(ORIGAMI3での失敗の再発防止)。

# 2. 作業担当(Codex)の起動と使い方

- 統括が `codex exec` で非対話に起動する。指示書はファイルに書き、標準入力で渡す(例: `codex exec -m gpt-5.6-sol -c model_reasoning_effort="high" -s workspace-write -C <リポジトリ> --add-dir <報告先> --json -o <最終報告ファイル> - < 指示書.md`)。sandbox は `workspace-write` とし、`.git` への書き込みは sandbox が拒む。指示書には、次の規律を**この順で(1行目に①)**必ず明記する。
  1. **「子エージェント(Codex のサブエージェント機能・他の AI ツール)を起こさない(検査も自分で前景で実行する)」**(`rules/06-過去の失敗と対策.md` 10.6。検査の丸投げ・二重確認の代行も含めて禁止する)。
  2. **「gitへの書き込み禁止(`git stash` を含む)」**。検査の前後比較は `git diff` / `git status` の読み取りだけで行わせ、作業ツリーを退避・復元する書き込み操作(`stash` に限らず `checkout` / `reset` 等)を一切させない(10.4)。
  3. **「触ってよいファイルの限定」**。
  可能な限り複数の担当を並列で起動するが、工程・検査・品質を省かない。各指示には触ってよいファイル、触らないファイル、担当重複の無い範囲を実パスで明記する。**担当からの報告には必ず「子エージェントの使用: なし」と「git への書き込み: なし」の 2 行を含めさせる**(使った・書いた場合は何をどうしたかを明記)(10.6)。この 2 行が無い報告は、統括が差し戻して書き直させる。
- モデルは難易度で使い分ける。**単純な作業・機械的な置換・集計・文言修正 = `gpt-5.6-terra`(推論 medium)、やや複雑な判断・設計・原因究明・UI の配線 = `gpt-5.6-sol`(推論 high)、最高難易度(合致ソルバ・OCCT の難所・競合の解決・全体レビュー) = `gpt-6-astra`(推論 xhigh)**。質を落とさずトークンを節約するため上位モデルは必要なときだけ使い、起動ごとに選んだモデルと理由を1行で記録する。
- 投入時に `git rev-parse HEAD` と未コミット件数を記録し、全体検査は絶対値だけでなく「着手時から悪化していないこと」で判定する。着手前から存在する既知の赤を担当へ直させない。
- 理由: 作業場所の誤認、共有変更の衝突、担当ごとの世代差を、開始前の実測で切り分けるため。

# 3. 作業担当へ渡す指示の書き方

- 粗い依頼は禁止する。指示書には必ず次の7項目を含める。
  1. 対象のファイルパスと関数名を、略称でなく実名で記す。記載前に統括が Grep 等で実在を確かめる。実在を確かめられない実名は書かない(新規作成ファイルは「新規」と明記する)。
  2. 合格条件を数値で記す(例: 「typecheckのエラーが0」「対象テストがN件全て緑」「操作の応答が500ms以内」)。
  3. 変更・起動・緩和してはいけないものを列挙する。
  4. 作業を段階に分け、各段階で中間報告を義務づける。具体値を変更させる前に現在値を報告させる。
  5. 関係する過去の失敗例(`rules/06-過去の失敗と対策.md`)と、その原因を記す。該当が無ければ「該当なし」と書く。
  6. 使用可能な道具について、コマンド、対象、確認方法まで具体的に記す。
  7. 成果物の保存先パスを明記する。
- 達成単位は小さく区切り、大目標を1回で依頼しない。担当が「前提が違う」と編集前に止めたら正しい対応として扱う。
- 理由: 大きく曖昧な依頼、存在しない実名、古い現在値は、合格の自己判断、無効な作業、失敗の再発を招くため。

# 4. 委譲後の停滞監視

- 長時間の委譲では、指示書に「成果は報告書ファイルへ書く」と記し、統括は完了通知を待つだけでなく**10分間隔を目安に**報告書ファイルと `codex exec --json` の出力(events.jsonl)の更新時刻を直接確認する。
- 「プロセスが生きている」「CPUを使っている」だけでは進行と判定しない。40分以上更新が無ければ停滞とみなし、原因確認、指示の縮小・修正、再委譲を行う。
- 停滞や異常は利用者が気づく前に検知して対処し、`docs/報告記録.md` へ記録する。
- 理由: 返信・プロセス・CPU・ファイル更新のどれか1つだけでは、空回りと長時間処理を区別できないため。
