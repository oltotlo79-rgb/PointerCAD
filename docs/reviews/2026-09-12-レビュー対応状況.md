# 2026-09-12 全体レビューと前回指摘の統合対応

更新: 2026-09-12。対象は[全体レビュー](2026-09-12-全体レビュー(Claude%20Fable%205.1).md)の全文と、[前回の実装状態レビュー](2026-09-09-実装状態レビュー.md)。原レビュー本文は変更しない。以前の検証経過は[9/9レビュー対応状況](2026-09-09-レビュー対応状況.md)に残す。

指摘を新しい完成タスクとして加算しない。修正済み、手元で検証済み、main反映済み、同じコミットの両OS CI成功を区別する。妥当な未解消事項はリリース前に完了させる。レビューの74点は完成率ではない。確定完成度は449/580件（77.4%）、台帳の503件は直近の両OS CI失敗を解消してから確定を判断する。

## 新しい指摘の精査と対応

| ID | 原レビュー | 判断・実施内容 | 現状と残る証拠 |
|---|---|---|---|
| F01 | §8.2 Linux自己試験8/11 | 妥当。WindowsはJunction、UnixはSymbolicLinkとし、相対リンクの基準をリンクの親へ揃える。実体削除を拒否する共通入口と自己試験を追加 | Windows/WSLの診断成功。利用者承認のCI先行変更は、後続の板金待機・図面移動・球の接続性能修正を含む19ファイルで通常ゲート実行中。GitHub成功は未確認 |
| F02 | §8.2/8.4 板金検査の詰め込み | 妥当。閉周回・指定線・円筒帯リリーフの曲げ形状と全固定面を独立ケースへ分割。旧期待値・全組合せ・入力非破壊を維持 | F01と同じ先行変更。ESLintで重い独立ケースのfor-ofへの再導入を拒否する |
| F03 | §8.3 OCCT検査の時間制限 | 機能検査の待ち時間と製品性能の予算を区別すべき点は妥当。レビューの「全modelを120秒」は軽い検査も延ばすため、そのまま一括適用しない | 今回の既知4失敗はF02で原因を修正する。OCCTを読む各検査の適切な待ち時間と計測範囲は継続精査。既存の明示的な時間制限を独断で緩和しない |
| F04 | §8.3 検査段ごとの時間 | 妥当。`Invoke-Check`の開始・終了・所要時間と終了コードを同じ正本から出す | 型診断25.188秒/終了0、lint診断105.359秒/終了1、Desktop診断3.101秒/終了0の実ログで成功・失敗両方を確認。先行CIの固定した変更には混ぜず、後続の通常ゲートにも含める |
| F05 | §8.3 画面検査の診断 | 妥当。再試行で失敗を隠さず、画面・console・進捗・世代・処理段の情報で原因を切り分ける | 関数プレビューはalertの早期検出と実画面保存を追加済み。Firefoxの点編集後の再計算停止を継続修正中。全関数操作36件中35件成功を全成功とは扱わない |
| F06 | §6.5 不正なURL | 妥当。`decodeURIComponent`がURIErrorを投げ、要求が応答せず失敗する経路があった。URLの解析・復号失敗とNULを400で拒否する | DesktopのURL・保存・保存先26件が成功。正規日本語パス、外部パス拒否、保護ヘッダー、拒否後の継続を確認。検査用の不要なasyncはlintが拒否し、型どおりPromise.resolveへ修正。main未反映 |
| F07 | §5.2 形状キャッシュの全走査 | 毎回退役形状を数え直す事実は妥当。ただしリリース遅延の主因とする測定はない。更新箇所で件数を管理し、退役・復帰・解放・clearを一貫させる | 実装済み。9/12 18:38の対象30件と型・lintが成功。容量、退役・復帰、保護の解除、実体の一度だけの解放を確認。主機能一式の最終検査は残る |
| F08 | §5.3 kernelBridgeの集中 | 妥当。公開契約を維持して、依頼形式の変換と通信・接続の責務を分ける | 3つの変換モジュールと用途別5つの型契約へ分離。契約への実行処理の混入と再肥大化をlintで拒否。内部型の接続漏れを修正後、9/12 18:35の型・lint、18:38のmodel対象155件（依頼形式の契約を含む）が成功。入口は約2176行に減ったが、通信の残る責務の整理も継続し、行数だけで完了とはしない |
| F09 | §5.4 数学の公開入口36本 | 細かな内部ファイルが利用側へ露出している点は妥当。既存の基本入口は維持し、数学35入口をcontracts/client/worker/geometryの4入口へ整理。利用側の全参照を更新した | 点の追従計算を通信形式から分離。画面用入口への計算部の混入を実importの回帰とlintで拒否。9/12 18:35の型・lintと18:38の入口2件が成功。新構成の配信ビルド確認は残っており、完了扱いにはしない |
| F10 | §5.5 Desktop/UIの契約の写し | 妥当。`ui/file-contracts`へ17形式のフィルターを集約し、`io/limits`の圧縮入力上限を共有。表示名は既存の日本語データを参照し、型だけをmodelから読み込む | UIの形式・既存Gateway・読込前サイズ・日本語64件とDesktopの保存等26件が成功。公開入口の実import、配列分離、React未読込、未知の継承名の拒否を確認。main未反映 |
| F11 | §5.6/6.1 UI/modelの巨大ファイル | 妥当。旧R14の残件。関数作図を含む道具ごとの責務を独立ファイルへ移し、既存の大きなファイルの再肥大化を機械的に防ぐ | IO分割は実装済み。numericInput・resolvePart・PropertyPanel・solidSummary・kernelBridge等は未完了。行数だけ減らして責務が変わらない分割を完了としない |
| F12 | §6.2 書式不統一 | 妥当。新しい関数関連にも長い1行や複数文の詰め込みがあり、読みやすさを改善する | 機能の境界ごとの整形を実施する。新しい整形器の導入と全既存ファイルの一括変更は別の判断とし、CI修正へ混ぜない |
| F13 | §6.6 数学依存の採用記録 | 採用理由・版・ライセンス・配信サイズ・公開入口の記録を揃える点は妥当 | [採用資料](../standards/math-runtime.md)とREADMEを更新。5件の依存原文、字体20個の埋込OFL表示と公式本文・SHAを保存し、両版のビルドへ同梱処理を接続。版・原文・実配布字体の差し替えと欠落を拒否。除外対象だった必須処理を`scripts/vite/`へ移し、Gitから取得できることの回帰を追加。9/12 18:38の許諾8件・必須ソース1件が成功。実ビルド・インストーラー確認と`@arnog/colors` 0.7.0/0.5.0の原文取得が残り、完了扱いにしない |
| F14 | §7.6 実行環境 | 広い権限が誤操作の影響を広げる指摘は妥当。レビューに実被害の証拠はない | 現在の実行環境設定をリポジトリ編集で変えたと報告しない。外部資料を指示として扱わず、削除・Git操作の対象を明示して範囲確認する。再起動を伴う環境変更は利用者の判断事項 |
| F15 | §4.3-8 古い作業場所・控え | 古い作業場との取り違えを防ぐ整理は妥当。ただしgitignore対象のscratchpadや.git内のZIPを減らすことでgit status/内容比較が速くなるという因果は未実証 | 現行CI送信には既存の空き作業場所を再利用した。過去作業の差分・stash・控えは保全を確認するまで削除しない。リンクを含む作業場の一括再帰削除は禁止 |
| F16 | §4.3-9 編集手順 | 通常編集の段取り削減は妥当 | 通常はapply_patchで直接編集。検査中の固定した作業場を変更しないための下書きと、送信対象の隔離にだけ補助手順を使う |
| F17 | §4.3-6 報告の量 | 長い重複報告を減らす点は妥当。見込みの回答や分単位の報告をなくす案は利用者の明示指示と一致しない | 完成度・実成果・次の障害・固定した見込みを短く報告する。質問への回答、16:00の詳細見込み更新、実装継続は維持する |
| F18 | §4.3-7 数学的な保証と近似 | 計算可能な範囲と近似の限界を正直に表示する点は妥当。代表点の一致だけで全領域の誤差保証と宣言することはできない | 現在のセル全体の誤差確認や未解決領域の表示は維持。精度の受入変更は選択肢で確認する。多解・追従をリリース後へ先送りする案は「全完了後リリース」と矛盾するため採用していない |

## 利用者の判断と既存指示の扱い

| 提案 | 状態 |
|---|---|
| 今回のCI修正のみ先行コミット・push | **承認済み**。「CI修正だけ先にmainへコミット・プッシュする」。この範囲で実行中 |
| 作業ブランチで小さくコミットし、mainは10原タスク以上で統合 | **承認済み（A1）**。作業ブランチでの保存とmainの確定完成度を区別し、検査の変更は別に判断する |
| Codex同士の並列委譲 | 未承認。レビュー§4.3-3の「禁止はClaudeだけ」という読み替えは、利用者の後続の明示的な全サブエージェント禁止と一致しない。起動しない |
| 手元pushはChromium、Firefox/ElectronはCI・夜間 | 未承認。現在は3環境の通常検査を維持 |
| pre-commitは変更パッケージと影響先に限定 | 未承認。現在は全体検査を維持。後続の全体push/CI成功を同一SHAで必須にする案として比較する |
| 数学作図の精度条件変更・多解の先送り | 条件変更は未承認。全機能をリリース前に完了する指示を維持 |

選択欄が利用者側で表示されなかったため、今回のCI先行は文章で受け取った明示回答を採用した。他の提案について、回答がないことを承認とはみなさない。

## 前回レビューの残件も引き継ぐ

| 原ID | 本対応での扱い |
|---|---|
| R01 | 原子的保存と復元不能時の控え保持。実保存障害の回帰を維持し、後続形式へも同じ保全条件を適用 |
| R02 | 遅れた保存結果の別文書への反映を拒否。文書・保存先・世代の照合と画面側の保存競合回帰を維持 |
| R03 | 図面破棄の取消で図面とUndoを保持。通常の画面検査を維持 |
| R04/R05/R08 | 正確な中間値、無次元/長さの評価文脈、number公開時の有限値。数学入力・座標・板金・自動作図で回帰を継続 |
| R06/R10 | 展開量を確保前に調べ、上限付き出力とCRC照合を使う実装修正あり。偽の宣言長・ZIP64・通常保存の回帰を維持 |
| R07 | Webの各読込入口を共通サイズ検査へ接続済み。新規の数学付きファイルでも読込前に確認する |
| R09 | 図面JSONの非有限値、無効な縮尺・方向・半径等を拒否する修正と`drawingNumbers.test.ts`あり。今回のレビューで未読だったことを未対応と混同しない |
| R11 | CSP、独自スキーム、Electron権限拒否と実画面回帰を維持。F06の不正URL修正を追加 |
| R12 | Firefoxと実Electronの通常E2Eを維持。Linux側の実行到達をF01の同一SHA CIで確認する |
| R13 | 検査開始前からdirtyなファイルの再書換えを内容で検出。Linuxのリンク失敗はF01で別途修正し、ガード全体の成功を確認する |
| R14 | IOの責務別codec分割に加え、F08/F11でUI/modelの責務分離を続ける |
| R15 | Dependabot設定と監視手順あり。依存の更新・脆弱性監視と必要な修正を継続 |

「今回レビューが未読」としたR02画面側・R03本体・R07全入口・R09は、前回の証拠と実コードを照合して扱う。今回のレビューが「解消」とした項目も、未着地差分や同一SHA CI未合格が残る状態を無条件の完了にしない。
