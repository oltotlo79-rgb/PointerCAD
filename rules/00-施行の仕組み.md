# 0. 施行の仕組み

各条項を「守るつもり」ではなく仕組みで守らせる。この表を正とし、仕組みを追加・変更したらこの表も同じコミットで更新する。

## 施行の一覧

| 条項 | 施行手段 | 区分 |
|---|---|---|
| 図面の設定入口の接続漏れ（06 §10.181） | SettingsPanelを図面の上部へ接続し、settingsEntryPoints.test.tsが部品・組立・図面の入口と状態不変を確認する。図面の設定・注記・保存等は3環境の実操作と既存の配置確認で検査する | 関連49件・実操作と配置等18件が成功 |
| 作業用の写しで保存範囲を取り違えた（06 §10.182） | task-workspace.selftest.pyが実Gitの写しから共通プロジェクト内の出力とGit領域への書込み拒否を確認する | 通常フックで再確認前 |
| ひな形選択待ちの通常編集の上書き（06 §10.180） | runNewFromTemplateが文書ID・差替え番号・部品/組立/図面の同一性を照合する。実ファイルの返信を保留し、通常編集と差替えで文書・保存先等の保持を確認する | 関連処理484件成功 |
| 画面操作の追加による無関係な単体の再実行（06 §10.178） | local_change_scope.pyが台本・共通補助の変更でも全E2Eを保持し、本体変更と全利用先の単体を選ぶ。実Git・PowerShellの自己試験で削除・改名・CI全段・失敗拒否を確認する | 実Git等25件成功・短縮時間未計測 |
| 待ち合わせ関数の型と補助入力の文字コード（06 §10.179） | templateDefaultsActions.test.tsの続行関数に戻り型を明示し、既存の先行型確認で検出する。補助処理は日本語パスを既存UTF-8記録から取得し、許可した修正以外の指紋変更を拒否する | 型・関連処理484件成功 |
| 厳密な有理数の指数表記（06 §10.172） | 上限付き厳密整数判定を使い、分子/分母の指数・非整数・ゼロ・予算と実際の極小値評価を通常単体で検査する | 通常単体・両OS CI |
| プロジェクト外・Git内の一時保存（06 §10.173） | task_workspace.pyは同じプロジェクトのscratchpad内に限定し、外部指定・Git内指定・外へ解決するリンクを作成前に拒否する | 保存範囲の制約を修正済み・変更後の検証待ち |
| 視点操作中の不要な作図更新（06 §10.161） | 実カメラと作図処理の12条件で100移動・4操作方式・3終了方式、文書保持と吸着再開を検査する。4分割の実操作・実選択も維持する | 通常UI単体・E2E・両OS CI |
| 利用者指示による正確性優先と初期化期限（06 §10.162〜164） | `releasePerformance.ts`へ時間・全4描画の判定を集約し、元の目標・実測・実用境界を同時記録する。`releasePerformance.test.ts`で境界・停止・非有限値、`perfBudget.test.ts`で環境に依存しない判定、`qualityGateOrder.test.ts`で既存経路と有限な機能待機を固定する。数学初回期限・取消・古い返信の検査を保持する | 通常単体・E2E・両OS CI |
| 画面外の係数操作と操作中の配置変化（06 §10.157） | 画面全体の列幅を窓幅内に保つ。`functionCoefficientFlow`が1008×681の3環境で、画面内への収容・マウス位置・ドラッグ前後の入力位置と幅・実値・Undo一回・実保存再開を確認する | 通常3環境E2E・両OS CI |
| 文書だけの送信が予期せず全体検査へ戻る（06 §10.159） | 退避対象の内容とindex全体のtree不変に加え、空のGit状態表示と正本の対象別判定をpush前に照合する。不一致なら送信開始前に止める。今回の送信補助で実施し、通常フックは維持する | 手順・今回の送信補助で照合済み（共通フックへの追加ではない） |
| 数式確定直後の保存キーの不達（06 §10.158） | 数式画面が閉じることを待ち、共通`savePart`が開いたdialogの不存在と作図面の実焦点を確認してから保存キーを送る。特異値分解・固有空間と既存保存操作で実ファイルと原式・再編集・Undoを照合する | 通常3環境E2E・両OS CI |
| 確認図の軸名と候補ボタンの重なり（06 §10.156） | 点の確認図で軸名専用の領域を確保し、投影単体で境界のボタンと選択枠まで分離する。`functionPreviewBounds.ts`が回転ごとの実X/Y/Z表示と候補の四辺の非交差を検査する | 通常単体・3環境E2E・画像確認 |
| 高解像度指定と実描画環境の不一致（06 §10.155） | `math-high-dpi.spec.ts`が独立したFirefox起動にも実密度2を指定し、両Web環境で表示倍率・解像度条件・CSS寸法・実PNGの2880×1800画素を照合した後、XYZ回転と点の位置・保存再編集を検査する | 通常E2E・両OS CI |
| 大きなPDFの全内容照合による検査超過（06 §10.154） | `drawingPerformance.test.ts`が実図面のPDF生成成功を2回確認し、長さと全バイトを直接照合する。生成時間の基準・5000ms上限を維持し、照合時間と全長も記録する | 通常UI単体・両OS CI |
| OSごとのブラウザー準備の期待値漏れ（06 §10.153） | `local-change-scope.selftest.py`が実OSの準備指定を含め、実Git pushの全コマンドと順序を照合する。準備や余分な処理をフィルターで隠さず、Linuxの依存導入と両ブラウザー・Electronを保持する | 正本自己試験・実フック・両OS CI |
| 表示部品の分離後の共有先の接続漏れ（06 §10.152） | `PropertyPanelAppearance.test.ts`が実プロパティを描画し、アルミ・鋼の外観と質量特性の材料/密度、文書保持、選択解除を確認する。`reviewBoundaries.test.ts`が実効lint設定の責務境界と分離先の末尾空行の拒否を検査する。全型・lintと既存の外観保存/質量表示の実操作も保持する | 通常単体・型・lint・実画面 |
| 自己試験の本番保存先への書込み（06 §10.151） | `Start-IsolatedGitSelftest`が実入口の継承Git環境を分離し、初期化直後に専用保存先を照合する。`git-selftest-environment.selftest.py`が実Gitの5条件で元のHEAD・index・設定・作業内容と失敗コードを検査する | 正本自己試験・実フック・両OS CI |
| 本体変更の利用先漏れと長い全体検査の反復(06 §10.148) | `local_change_scope.py`が実indexの全workspace依存から変更先と全利用先を求め、未確認の影響は全体へ戻す。`local-change-scope.selftest.py`が改名・削除・リンク・未stageの依存表・循環と全利用先、実Gitフック、対象別失敗の中止、CI/Fullの全5段、対象別成功のB3不発行を検査する。正本`check.ps1`が本体の対象別Pushへ既存の描画性能・Firefox/Electron起動を必須にする | 通常自己試験・実フック・両OS CI |
| 陰関数の重複微分と公開戻り型の配置依存（06 §10.146） | `scalarFirstDerivatives.test.ts`が値の区間評価1回・両角度の端点一致・独立解析微分・入力変更と未使用二次演算の削減を検査する。`implicitGrid.test.ts`は既存5000msと全形状の検査を保持して工程時間を出す。板金の公開戻り型は`JSX.Element`を明記する | 通常単体・型・両OS CI |
| 曲面の計算重複とCIでの発見遅れ(06 §10.140) | `trigonometricIntervals`が同一の両端・関数だけを最大1024件再利用する。`trigonometricIntervals.test.ts`が隣接する値・両単位・sin/cos・追出し前後を照合する。ルートのtestはexpressionを先行し、成功時だけ残りを数学除外で1回ずつ実行する。`qualityGateOrder.test.ts`で順序・成功条件・除外と専有実行を固定する | 通常ユニット・両OS CI |
| 日本語のコミット件名と禁止接頭辞(06 §10.137) | `commit_message.py`と実commit-msgフックが日本語のない件名・禁止接頭辞・BOM・不正UTF-8を拒否する。`commit-message.selftest.py`が実Gitの拒否とHEAD保持、日本語の受理、全3フックのLFと実行属性を検査する | Gitフック・通常自己試験・両OS CI |
| 描画で取得した所有形状の解放(06 §10.147) | `meshOwnership.test.ts`が実OCCTで正常・型読取例外・抽出例外の全取得を観測し、反復描画の内容一致と全解放を検査する | 通常単体・型・lint・実画面 |
| 再計算間の数式準備の再利用と終了(06 §10.149) | `mathWorkerReuse.test.ts`と`recomputePartWithMath.test.ts`が実要求の原式・世代・期限、同時使用、所有者終了、30秒待機と16回保持の上限、例外時の解放を検査する | 通常単体・型・lint・3環境の実画面 |
| 実形状計算の同時実行防止(06 §10.138) | expression/model/kernelはCI・Commitを含む全モードで`fileParallelism: false`とし、`qualityGateOrder.test.ts`が設定を検査する。直列化のみで性能問題の解消とは判定しない | 通常ユニット・両OS CI |
| 図面の表示と操作可能の同期(06 §10.103) | 図面の`aria-busy`と`waitForDrawingReady`を使う。`gdt.spec.ts`で実依頼を保留し、文字の更新中は削除を拒否し、計算終了後に削除・Undoが成立することを確認する | 通常ユニット・実画面・CI |
| 関数の定数と点の証明区間(06 §10.102) | `scalarConstantEnclosures.test.ts`で丸め値を誤差0と扱わず、有理数の正確な比較と平方根の二乗で上下界を検査する。`solveCurveFunctionPoints.test.ts`でXYZ全軸の精度と範囲、共通解、接点、未解決領域を検査する | 通常ユニット・CI |
| 関数上の点の確定と保存条件(06 §10.101) | `functionPointDraft.test.ts`が未完了の探索からの作成を拒否して文書を保持する。`functionPointContinuationWork.test.ts`が共通の保存入口で必須値の欠落と元入力の変更を検査する | 通常ユニット・CI |
| 起動の完了・書体・描画失敗からの文書保持(06 §10.99) | `smoke.spec.ts`で実描画、同梱書体の1280/1440px、意図的な3D初期化失敗後の点作成・実保存・再試行を検査する。FirefoxとElectronの起動project成功を後続の前提とする | 通常ゲート・CI |
| 合致の列参照を評価内でまとめる(06 §10.100) | `mateResiduals.test.ts`で150合致の列参照数を部品ごと6回以内に制限し、次の評価で固定を変えた値と微分の一致を検査する。`assemblyPerformance.test.ts`の絶対・相対性能条件は維持する | 通常ユニット・CI |
| 不変な字形のSVG再利用(06 §10.98) | `fontStore.test.ts`が共有輪郭の書換を拒否し、`toSvg.test.ts`が移動・色・別字形と可変外部入力の再検証を確認する | 通常ユニット・CI |
| 関数上の点を変更時に別枝へ移さない | `continueImplicitFunctionPoint.test.ts`で正負2枝・枝消失・途中合流・固定根・改名・範囲変更を検査する。`functionPointContinuationWork.test.ts`で過去入力の改変・別世代・既知値変更の返信・取消を拒否する | 通常ユニット・CI |
| 自動作図の容量判定を時間切れにしない | `scriptGuestBootstrap`は保持した組込み正規表現で文字の連続部分を数える。`runtimeAdapter.test.ts`は8MiBの命令制限と6種類のUTF-8境界、組込み改変、catch後の失敗保持と再実行を実VMで検査する。5秒と容量の上限は変更しない | 通常ユニット・CI |
| 陰関数の接触・区間依存と公開入口(06 §10.92) | `implicitGrid.test.ts`が球・トーラス・分離成分・境界接触・特異点を確認する。区間微分と実メッシュへの距離で未解決領域の被覆を検証し、予算超過や未被覆は生成を止める。`packageBoundary.test.ts`は実importの公開入口を解決し、ローカルexpression診断にも自動追加する | 通常ユニット・CI／診断補助 |
| OCCTの借用と所有を区別する(06 §10.91) | `borrowHandle`の戻り型からdeleteを除外し、`eslint.config.js`がOCCT実装の`keep(handle.get())`を拒否する。`functionCurveSketch.test.ts`で実CAD切断・節点抽出・破棄・面張り・取消後の再生成を検査する | 型・lint・通常ゲート |
| 実Electronの起動同期(06 §10.87) | `launchDesktop`が固定済みPlaywrightのready保留ローダーを製品mainより先に読み、未設置・早過ぎるready・既存windowを即時拒否する。`electron-startup.spec.ts`で独立した実起動5回と再読込・正常終了を通常ゲート/CIへ含める | 機械施行 |
| 操作追加後の共有ツールバーと接続入/切(06 §10.85) | `p8-drawing.spec.ts`で1280/1440pxの高さ・あふれ・名前を固定し、`sketch-extended.spec.ts`と`intersectionsFlow.ts`で未分割の部分トリムと自動分割後の区間削除を分けて検査する | 通常ゲート・CIの実操作検査 |
| 保存完了の通知と実行件数(06 §10.86) | `saveConcurrency.test.ts`で控え削除の保留と編集競合を検査し、実ブラウザーで保存直後に再読み込みする。進捗読出しは周回全体の成功/失敗を集計する | 通常ゲート・CIと報告読出し |
| 保存形式の診断と版整合(06 §10.83) | `check.ps1`がio診断へ`schemaVersion.test.ts`、model診断へ`exportPart.test.ts`と`createPartDocument.test.ts`を必ず含める。ファイル欠落は失敗する | 機械施行 |
| 固定時刻の桁幅と編集欄の焦点(06 §10.84) | `scriptClock.test.ts`が実VMの時刻上端・入力拒否を検査し、`scriptsFlow.ts`が3環境でTab/Shift+Tabの移動と本文保持を検査する | 機械施行 |
| store登録と検査の目録統一(06 §10.81) | 製品と`storeSlices.test.ts`が同じ`STORE_SLICE_CREATORS`を使い、全欄一致と所有欄の重複0を検査する | 自動検査 |
| 説明書の用語と実行可能な例(06 §10.82) | `topics.test.ts`が自動作図を含む全章の禁止語を検出し、`scriptKernel.test.ts`がAPIとmoduleの原文例を実VM・実OCCTで実行する | 自動検査 |
| スクリプトの資源超過を保持(§10.77) | native patchの印を`runtimeResourceLimit.ts`が確認し、`runtimeAdapter.test.ts`がcatch後の失敗・無修正版拒否・正常再実行を検査。ビルド入力/patch/出力のSHA-256を`build-script-runtime.py`で照合 | 自動検査＋再現ビルド |
| 加工案内の背景と説明書画像(§10.76) | `camFlow.ts`が3環境で不透明な計算済み背景色・左揃え・F1の実画像2枚の読込を検査。`helpLibrary.test.ts`が同梱画像への参照解決を検査 | 自動検査＋実画像の目視 |
| 統括は `pnpm` / `npm` / `npx` / `yarn` を直接実行しない(01 §1、02) | `.claude/settings.json` のPreToolUseフックが `.claude/hooks/block-direct-package-managers.ps1` で Bash / PowerShell のコマンドを構文解析して拒否。`cmd /c`・`powershell -c`・`bash -c`・`iex`・`Start-Process`・`corepack` 経由の入れ子も検査する。禁じているのは統括の直接実行だけなので、フック入力に `agent_id`(サブエージェント発の呼出にだけ付く識別子)が非空で含まれる作業担当発の呼出は通す。作業担当(Codex)は Claude Code のフックの外で動くので、この施行の影響を受けない。 | 機械施行 |
| コミット前の検査(03 §7、B3または承認済みの範囲選択) | `scripts/hooks/pre-commit` が `scripts/check.ps1 -Level Commit -ReceiptPhase Commit` を実行する。B3は全5段成功済みの同一内容だけを共用する。揃わなければstage済みの写しで全型・lint・両ビルド・§7.2に従う全単体を実行し、失敗なら中止する。判定不能は全4段とする。写し・依存リンクと継承Git環境の隔離は`gitTreeGuard.ps1`を正とする(06 §10.7/10.8) | 機械施行(要: `scripts/install-hooks.ps1`) |
| mainの10件統合・作業ブランチの途中保存・明示承認(03 §6、06 §10.96) | `scripts/hooks/pre-commit`から`check-commit-batch.ps1`を実行する。A1に従い作業ブランチでの小さな保存を認め、mainの通常実装は新規完了ID10件以上・報告増分の一致・既存完了ID削除0件を必須にする。利用者の件数例外はGit管理領域の承認理由・親SHA・stageのtreeが全て一致する変更だけに適用し、品質検査は省略しない。削除も実装変更として判定し、detached HEADは拒否する。`check-commit-batch.selftest.ps1`を通常の自己試験から呼ぶ | 機械施行・fail-closed |
| コミット件名へ UTF-8 BOM を混入させない(03 §6、06 10.29) | `scripts/hooks/commit-msg` がメッセージ先頭の3バイトを検査し、`EF BB BF` ならコミットを中止する。PowerShell 5.1 でメッセージファイルを書く場合は `[IO.File]::WriteAllText` と `UTF8Encoding($false)` を使う | 機械施行(同上) |
| push前の検査(03 §7、B3または承認済みの範囲選択) | `scripts/hooks/pre-push`は実際に送信するSHAを確認し、`check.ps1 -Level Push -ReceiptPhase Push`で同一の全5段成功記録を照合する。揃わなければ§7.2の変更先・利用先検査と、本体変更時の描画性能・Firefox/Electron起動を実行する。判定不能・CI・Fullは全5段、失敗ならpushを中止する。B3成功記録は送信前に消費し、対象別結果からは発行しない | 機械施行(同上) |
| 重複検査の同一内容だけの共用(03 §7 B3、06 §10.104) | `validation_receipt.py`がsource・index・依存・設定・ツール・生成物の指紋と親SHA・20分の有効期間を照合する。自己試験が変更/改変/未追跡追加/別index/中断/期限切れ/二重使用/CIを拒否する | 通常の自己試験・CI |
| 共用できない記録での不要な全走査の拒否(06 §10.150) | 記録が無い・改変済みの場合は依存とブラウザーを読み始める前に通常検査へ戻す。正当な記録は従来どおり全入力を照合し、自己試験が拒否時0回・正当時1回の走査を検査する | 通常の自己試験・実フック・CI |
| CIで同じ検査(03 §7.2) | `.github/workflows/ci.yml` が Windows / Linux 両方で `scripts/check.ps1 -Install` を実行する。検査の定義は `check.ps1` の1箇所だけにあり、`ci.yml` と食い違う余地を作らない | 機械施行 |
| 保存・復元とも失敗した場合の正常コピー保持(06 10.45) | `apps/desktop/src/main/pcadDialogs.test.ts`が部分書込・復元不能・後始末失敗を実保存入口へ与え、控えの内容と削除対象を検査する。desktopのtestを通常の再帰検査へ組み込む | 機械施行 |
| 古い保存の完了を別文書へ反映しない(06 10.46) | `saveConcurrency.test.ts`・`desktopFileGateway.test.ts`・`pcadIpcTargets.test.ts`がUIと本体の実保存入口で保存順・文書切替・同一文書の追加編集・保存先解除と非同期完了を組み合わせて検査する | 機械施行 |
| 図面の戻る操作で未保存の編集を無確認で捨てない(06 10.47) | `closeDrawing.test.ts`と`p8-drawing.spec.ts`が破棄の拒否時の図面・Undo保持と同意時の終了を検査する | 機械施行 |
| 50部品の描画性能を同じWebGL経路で測る(06 10.37) | `qualityGateOrder.test.ts`がWebGL専用SwiftShader引数と性能projectの専有実行を固定し、`assembly.spec.ts`が実描画器名・解像度復元・50部品の実描画と要件§5.2の判定を検査する。ブラウザ全体へのSwiftShader強制へ戻さない | 機械施行 |
| 自動寸法と手動文字の重なりを検出する(06 10.41) | `autoDimension.test.ts`が字体の下端基準、`autoDimensionCommands.test.ts`が手動寸法の保持、`p8-drawing.spec.ts`が実画面上の文字矩形を検査する | 機械施行 |
| SVG再描画中の古い要素参照を寸法消失と誤認しない(06 10.42) | `p8-drawing.spec.ts`が現在の要素と矩形を同一DOM処理で取得し、全7文字の非重複も同じ描画状態で判定する。存在・正の面積・移動量・Undoの条件を維持する | 機械施行 |
| 必須スクリプト(typecheck / lint / test / build)の欠落検出(03 §7.1) | `scripts/check.ps1` が `package.json` を読み、欠けていれば失敗する(fail-closed) | 機械施行 |
| テストが追跡ファイルを書き換えない(03 §7.1 #0) | `-Level Push`(push/CI)は `scripts/check.ps1`(`scripts/lib/gitTreeGuard.ps1`)が検査前後の `git status --porcelain --untracked-files=no` を比較し、差があれば失敗する。`-Level Commit`(pre-commit)は本物の作業ツリー・index に一切触れない写し(git worktree)の中で検査するため、前後比較そのものが構造的に不要(2026-09-05 に方式変更。rules/06 10.7) | 機械施行 |
| 依存方向 `apps → ui → model → kernel / expression`(04) | `eslint.config.js` の `no-restricted-imports` がパッケージごとの禁止 import を検出し `pnpm run lint` で落とす。型検査は副作用のみの import を素通しするため、施行の主体は lint(2026-09-02 実測) | 機械施行 |
| UI 文字列の `ja.json` 分離(04、NFR-MA-5) | `packages/ui/src/i18n/i18n.test.ts` が `.tsx` への日本語直書きとキーの網羅を検査し `pnpm run test` で落とす | 機械施行 |
| 性能要件(NFR-PF-2 / NFR-PF-3、03 §7.1) | `solidPerformance.test.ts`、制約の`solve.test.ts` / `diagnose.test.ts`等は`expectWithinBudget`から共通の時間判定を使う。要件§5.2の旧目標を記録し、実用上限超過は全モードで失敗。`POINTERCAD_PERF_STRICT`は実行条件とB3の識別だけに残す。1パッケージずつの実行・`PerformanceFirstSequencer`、既存の解の照合と退避、入力規模は保持し、`qualityGateOrder.test.ts`と正本自己試験で接続を確認する | 機械施行 |
| 作業担当のgit書込禁止(01 §1) | 指示書への明記+統括が委譲後に `git status` / `git diff` で未書込を確認する | 手順 |
| 依存の無断変更禁止・警告封じ禁止(02) | 統括が `git diff --cached` 全文を読み、`package.json` / `pnpm-lock.yaml` の差分と `eslint-disable` / `@ts-ignore` / `any` の混入を照合する | 手順(検査スクリプト化を検討) |
| 指示書の7項目・実在確認(01 §3) | 統括がGrepで実在確認してから指示書を書く | 手順 |
| 停滞監視(01 §4) | 統括が10分間隔を目安に報告書ファイルの更新時刻を確認する | 手順 |
| 報告記録(01 §1-⑥) | `docs/報告記録.md` へタスクごとに追記する | 手順(記録検査のスクリプト化を検討) |
| mainへの着地と完成度報告(06 10.43) | 報告用処理が`git ls-remote`の実SHAに保存された進捗を読む。ローカルmainとの一致・未送信分を別々に返し、取得失敗時には成功扱いしない。CI成功は実行結果を別途確認する | 読取補助と手順 |
| 格子の棄却順と定常性能の測定(06 10.44) | 3軸それぞれの接触・交差を総当たりと照合。初回を記録し、固定20回の準備後に5000枚×2の7標本を測り、2msの改善目標と要件§5.2の実用上限を判定する | 機械施行 |
| 完了タスクの二重計上・予定タスクの欠落防止(06 10.23、10.26) | `docs/progress.json` を進捗数の機械可読な正本とし、`packages/test-utils/src/progressLedger.test.ts` が予定・完了タスクIDの重複、予定外ID、phaseごとの予定件数、合計と報告値の不一致を `pnpm run test` で検出する | 機械施行 |
| リリース前検査(05) | 手動チェックリスト。`scripts/check-release-ready.ps1` をP13までに実装して機械施行へ移す。予定する検査項目にヘルプと取扱説明書の整合検査(NFR-MA-6): ①目録の全項目に対応する章があり見出しが一致、②本文中の道具名・ボタン名の文言一致、③説明書の画像が撮影台本の生成物であること、④説明書とヘルプの機能の過不足が無いこと、の4点を含める(v1.9) | 未実装(P13予定) |
| 失敗の記録と仕組み化(06) | 失敗発生時に06へ追記し、対策をこの表へ反映する | 手順 |
| 関数曲線の通常操作と表示(06 10.128) | `functionCurvePaths.test.ts`で経路/案内の選択と保存参照、`drawFunctionCurve.test.ts`で原式と描画の一致、`functionPlotFlow.ts`で3環境の実スイープと独立した体積を検査する | 通常ゲートで機械施行 |
| 図面開始の重複処理(06 10.129) | `makeHiddenLineViews.test.ts`で可視/隠線の継ぎ目走査共有と参照/解放、`drawing-performance.spec.ts`で100段・三面図・50寸法の開始5秒を目標として記録し、要件§5.2の実用上限を検査する | 通常ゲートで機械施行 |
| 関数の後続操作と3D測定(06 10.130) | `functionPlotFlow.ts`で円の計算完了と面の生成を確認してから掃引する。`sketchMeasure.test.ts`で点ID・距離・線長・角度・拒否・古い測定結果、`functionDirectionFlow.ts`で実画面の長さ10→6→Undo10を検査する | 通常ゲートで機械施行 |
| 日本語編集と描画後の符号確認(06 10.61/62) | UI部分診断・E2Eの前にjaMessages.test.tsを正規check経由で自動実行する。派生図のE2Eで符号・倍率と保存往復を確認し、見出し込みの実画像を確認する。未確定の読取パスはrgで発見し、一意な実在パスだけ読む | 機械施行＋画像確認 |
| 未stage差分の検査対象誤認(06 10.38) | check.ps1が空indexのCommit検査を拒否する。自己試験が実入口で空indexと不正なユニット診断指定を検査する | 機械施行 |
| timer取消の処理順(06 10.39) | 共通queueでtimerフェーズを待つ。K53の直接呼出し10回とB20の実Worker通信がCommon後・mesh前の取消を通常ゲートで検査する | 機械施行 |
| HLR性能検査の形状負荷(06 10.40) | 100押し出し全ボディの投影と可視線400本を必須にし、200/2000/5000msの改善目標と要件§5.2の実用上限を判定する | 機械施行 |
| Windows PowerShell 5.1 の読取コマンド構文(06 10.25) | 独立した読取はシェル内で結合せずツール呼出しを分ける。コマンド文字列へ `&&` / `||` を書かず、`rg` の拡張子指定はパスglobでなく `-g` を使う | 手順 |
| STEPアセンブリの名前とidの正規化(06 10.27) | `stepAssemblyMetadata.test.ts` がNAUO内の引数位置に依存しない復元、日本語・引用符・null、印欠落時のfail-closedを検査し、実OCCTの書込・読込・決定性検査も通常の `pnpm run test` に含める | 機械施行 |
| STEPの一時印と永続IDの分離(06 10.28) | 包含しない接頭辞へ分け、共有デコーダーによる可逆性、異なるOCCT通算IDから同一バイトへの正規化、独立2回の実書き出しID一致を通常の `pnpm run test` で検査する | 機械施行 |
| 関数の点と方向の編集を1検査へ合算した時間超過（06 10.141） | functionDirectionScenariosの5種類をWeb/Firefox/実Electronの独立E2Eへ接続し、空文書から作成・保存再開・測定・Undoを各180秒以内で検査する | 機械施行 |

## フックの性質

- 図面を開く計算費用の未分離（06 10.145）は、明示診断の画面/Worker CPU記録で処理を特定する。`primitiveReuse.test.ts`で同形の独立複製・全参照・入力差・所有権を検査し、通常の図面5000ms/16msを維持する。
- 計算失敗と入力の変化の混同（06 10.144）は、E2Eの数学工程記録、実Electron起動の非focusable・文字入力検査と板金の操作記録で区別する。計算部の共用は`mathWorkerGroup.test.ts`が取消・期限・応答照合・解放の境界を確認する。

- 原因調査が前提projectの失敗で止まる問題（06 10.143）は、check.ps1の対象限定手動診断と、実入口を使うvalidation-receipt.integration.selftest.pyで対策する。通常・CI・フックからの前提省略と、診断からの全体成功記録発行を拒否する。

- 進捗報告の失敗見落としと、失敗後の再計算待機（06 10.142）は`validation-progress.mjs`の実CLI検査と`recomputeState.test.ts`で防ぐ。報告の読取は前者へ接続し、E2Eの`recompute.ts`は世代を照合して最新の失敗を即時に知らせる。検査未終了を成功とは表示しない。

- 強度計算の文書切替時の破棄、通常編集・Undo時の結果保持、材料条件・未掲載値・式の境界（06 10.74）は`strengthSession.test.ts`と3環境の`strengthFlow`を通常ゲートへ含める。FirefoxではElectron専用ファイルを除外し、専用projectで検証する。

- P9の公差値・単位・実体記号・データムの合法性、紙上の記入枠とデータム三角、保存版の連続移行（06 10.63）はgdt.test.ts、frameGeometry.test.ts、gdtJson.test.tsと既存documentJson.test.tsで通常ゲートに含める。入力の保存値をそのまま正しい値と扱わず、旧版の期待値更新だけで移行を証明しない。

- desktopの配布用WASM経路(06 10.51)は`desktop-renderer.spec.ts`で実ビルドへ通信失敗/404を与え、箱と図面の生成まで通常E2Eで確認する。Web版の圧縮経路の成功だけでは代用しない。

- `.claude/hooks/block-direct-package-managers.ps1` は**fail-open**とする: フック自身が判定不能(構文解析の例外等)になった場合は警告を出して実行を通す。フックの不具合で全作業が止まることを避けるため。fail-openで通った実行は、統括が規約違反でないことを手動で確認する。
- フックの自己試験は `.claude/hooks/block-direct-package-managers.test.ps1`。フックを変更したら必ず実行する。
- gitフックは `git config core.hooksPath scripts/hooks` で有効化する(`scripts/install-hooks.ps1` が設定する)。クローン直後は無効なので、環境構築の最初に必ず実行する。

- 基本形状の複製した分割の再利用（06 §10.160）は、完了記録・両精度の一致・複製先の実分割検査を必要条件にする。`primitiveReuse.test.ts`が精度変更・実分割の消失・元形状解放後の実形状と参照番号を検査する。実OCCTのコンストラクターを回数観測のために置き換えない。

## 機械施行の限界

- フックとgitフックはこのリポジトリの設定に依存する。`--no-verify` や設定解除で迂回できるため、迂回しないことは規約(03 §7)で禁じ、迂回が必要な例外は利用者の明示指示がある場合に限る。
- 機械が証明するのは「検査を実行して合格した」ことまでであり、指示を理解したこと、報告が真実であることは証明しない。だから統括の再検査とdiff全文読みを省かない。
