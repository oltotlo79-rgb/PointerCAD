- 組立検査の読込口の登録待ちと切断診断の照合（06 §10.311）は、`assemblyTestSupport.ts` の `waitForFileGatewayEntrance`（上限15秒・理由付き失敗）と、`assembly.spec.ts` の登録遅延・未登録の2確認、`browser-security.spec.ts` の `expect.objectContaining` で検出・防止する。前提 project を含む最終確認は独立コピーの全体検査で行う。
- 規約変更の委譲による差し戻し（06 §10.310）は、規約の変更を統括が rules/01 §1 ⑥で自ら行い、担当の依頼文に利用者指示の記録箇所を書く手順で防ぐ（手順。機械施行ではない）。

- 記号の値と集合の条件（06 §10.306）はmathDeclaredValues.test.tsで実計算・保存・表示往復、選択枝だけの評価、不正な条件と型違反の拒否を照合する。記号の値を変えても古い座標を使わないことはmodelのdeclaredMathValues.test.tsで確認する。

- 記号の改名後の古い表示（06 §10.304）は読取り専用の名前欄を最新の定義へ結び、mathDeclaredRenameFlow.tsでフォーム名だけでなく入力欄の実値を三環境で照合する。
- 図形を変えないUndoの待機誤り（06 §10.303）はmathDeclaredRenameFlow.tsで実操作後の画面・保存文書の復元と計算依頼が増えないことを照合して防ぐ。図形を変える操作の再計算待ちは維持する。
- 保存する自由記号と作図変数の混同（06 §10.301）はdocumentFunctions.tsの型で用途を区別し、unresolvedMathProblems.test.tsから実際の係数改名を通して検出する。作図側の変数制限を解除せず、名前・意味・種類・識別番号と未解決状態の保持を確認する。
- 極限の表示切替（06 §10.300）はlimitBounds.test.tsで係数付き整数列と通常の極限の意味を往復照合し、mathProductTypes.test.tsで一般の曖昧な積の拒否を維持する。mathLimitBoundsFlow.tsは三環境で実際の切替・保存再開・座標追従・編集Undoを確認する。
- 追加計算部の実ファイルの読込み上限（06 §10.299）はlocalExactMathSources.test.tsが配布一覧と大きさを照合する。候補の検査準備も実際に渡すファイルを確認し、本体側だけの確認を成功根拠としない。

- 組み立て入力の保存漏れ（06 §10.298）はWebのbuild内TypeScriptを保存対象にし、既存buildSources.test.tsで両版の設定から参照する全入力の実体とGit保存対象を照合する。生成物の除外は維持する。
- 独立コピーの参照漏れ（06 §10.297）は実際の一覧処理・選択処理・設定の組を自己試験へ写して検出する。部品不足の試験は指定部品名を含む実際の失敗理由まで照合し、検査自体の参照漏れを期待した拒否と混同しない。
- 全体の見積もり漏れ（06 §10.295）は残工程を状態と所要時間の一覧へ残し、review-1130.pyで各工程の上下限を合計して見込みへ反映する。scope_inventory.pyで計画の全32分野・入力7条件・原タスク13件の欠落、重複、見積もり先、根拠の内容を照合し、未照合があれば全範囲確認済みとしない。未接続の分野を照合時間だけに置き換えず、未完成の全対応表を完成済みとは扱わない。
- 式の一覧と名前の型判別（06 §10.294）は読取り専用の一覧を返す明示的判別と、文字を連結する直前のtypeof確認で守る。85項目の関連確認と型・書き方が同じ内容で成功してから実画面を開始する。
- 計算と保存用返信の取違え（06 §10.293）はdifferentialEquationNotation.test.tsの型付き共通入口で公開返信を検証し、検証した保存定義だけから式を取得する。原式・条件・表示切替の意味を照合し、画面確認前に通常の型検査を通す。
- 検査準備の部品参照とファイル存在（06 §10.292）は、作業用check.pyでコピー前に選択した検査と所属package.jsonを照合する。保存確認は公開model型の固定した式と条件を使い、未宣言の計算部を呼ばない。
- 独立座標の解曲線上の点の追従（06 §10.291）はcontinueCurveFunctionPoint.test.tsの一意な追従と不成立拒否、odeFunctionCurve.test.tsの実計算・保存入力・条件編集・法線・Undo、functionOdeCurveFlow.tsの三環境の実保存再編集で確認する。枝を選んだ点の原式変更を無条件に許可しない。
- 解曲線の確認用入力の誤り（06 §10.290）は、odeFunctionCurve.test.tsで公開係数入力・独立した放物線の点と方向・直線の主法線の拒否を確認する。製品の拒否を外して検査を通さない。curves-1の失敗を保持し、変更した確認ファイルだけ再実行する。
- 微分方程式の条件の内部形式への依存（06 §10.289）は、cas_ode_test.pyで候補・条件・元の式の途中値を実際に評価して防ぐ。正常位置と禁止位置、初期条件、有限な複素数の途中値を確認し、differentialEquations.test.tsで確認式の欠落や変数のすり替えを拒否する。関連87項目、補強後26項目、表示15項目、型・書き方が成功。曲線全体は未完了。
- 複素誤差関数の返信表記（06 §10.270）は独立比較値の40桁と生の返信文字列、公開返信、実計算から画面の表示全文の三段階で確認する。数値比較のための再変換だけで表示の合格としない。丸めと誤差上限を変えず、既存の小数変換で表示用の表記へ揃える。
- 確認用コードの数値表記・改行（06 §10.269）は複素誤差関数の作業用check.pyが検査開始前に拒否し、生成元も修正する。過去の確認結果の共用は、製品の内容一致と、表記だけを変えた2ファイルの変更前バイト列の復元一致を必須とする。型・書き方の正式確認も省略しない。
- 複素誤差関数の微分の有限性判定（06 §10.268）はcas_error_functions_test.pyが実際の入力変換・微分依頼・公開返信を通して確認する。元の引数の成立範囲、尖り、0倍で隠れた範囲超過も確認し、直接の数値計算だけで接続の成功としない。同梱漏れとファイル上限はlocalExactMathSources.test.ts、配布経路は三環境の実操作で確認する。
- 整数ゼータの再計算停止（06 §10.267）はzetaNumeric.test.tsの処理量・独立比較値・上下限と、mathDistributionDeadline.test.tsの負荷付き係数再評価で検出する。整数判定は元の分数で行い、時間上限・40桁・元の入力条件を維持する。
- 計算用ファイルの読み込み上限超過（06 §10.266）はlocalExactMathSources.test.tsが実際の同梱一覧・本体の上限・全ファイルの文字数を照合して検出する。上限を緩めず、入力と微分の処理を既存の二ファイルへ分け、関連する元の入力条件と微分も確認する。
- 計算上限時の内部例外と画面表示の混同（06 §10.265）はmathEditorResult.test.tsが実計算・公開返信の検証・画面文言まで通して検出する。引数と次数の上限、外側の0倍と成分選択でも値を返さず、既存のstopped/budgetと表示全文を要求する。実画面でも同じ入力の拒否と適用禁止を保持する。
- 微分前の性質判定の再帰（06 §10.264）はcas_zeta_functions_test.pyが一階〜15階の記号微分と未確定の性質の問い合わせを直接実行して検出する。有限性は自分の性質を再び尋ねず元の引数から確認する。修正後の型・書き方と実返信・保存11項目の成功、元の5失敗を別に保持する。
- 数値の比較基準の桁数（06 §10.263）はzetaDerivatives.test.tsが参照値の最終桁と計算した上下限の幅を比較し、100倍以上細かい参照であることを必須にする。独立した460/520桁の一致を確認した300桁の参照と、元の失敗記録を保持する。
- 正確な0の除算（06 §10.262）は分母の有限性と0の不包含を条件とし、carlsonJets.test.tsで0/0の拒否と正負の微小値の保持を同時に確認する。高階微分の確認は四分の一周期の厳密な0と、その両側の非零を公開作図経路まで通す。
- 高階の積分の誤差配分（06 §10.261）はellipticQuadrature.test.tsが独立したsin冪の積分で17階・異なる上端・混合微分を確認し、全体の上下限の幅を指定精度と比較する。中止・分割上限で途中値を返さない。計算部は全区間の合計から誤差の大きい部分を選び、標本の値を精度保証に使わない。
- 独立した数値微分の厳密な0（06 §10.260）は基準採用時に定義の積分の微分式と正確な角度代入で証明する。数値の小ささだけで0にせず、元の値と証明を保持する。ellipticIntervals.test.tsは半周期の厳密な0と、その両側の非零・符号を確認する。初回187成功・2失敗を残す。
- 作図用の正確な0の保持（06 §10.259）はcarlsonJets.test.tsで定数・無関係な変数の一階二階と、非零の最小値の二乗を同時に検査する。一般の区間や非零を0へ丸めず、正確な0の二乗だけを保持する。初回118成功・5失敗を残す。
- 第三種楕円積分の係数0の微分（06 §10.258）は、元の経路と解析性の確認後に積分から求めた係数を使う。通常のcas_elliptic_functions_test.pyが四階・三引数の同時変化・正負の周期を独立計算で照合する。初回54成功・1失敗を保持し、Pythonのみの修正と未変更の型検査対象を内容番号で区別する。修正後の関連計算55項目と同梱・入力表示189項目が成功した。
- 楕円積分の入力名と重複計算（06 §10.257）はellipticFunctions.test.tsで六種類の通常入力・独立値・表示の往復を照合し、元の成立確認がRF/RD/RJの計算を呼ばず途中の極を拒否することを検査する。許可済みの時間上限調整は楕円積分のみ3秒とし、期限の繰返し更新・別入力への漏出・親の期限超過をmathDistributionDeadline.test.tsで拒否する。二段階の失敗を保持し、修正後の型/lintと407項目が成功した。
- 楕円積分の確認値の保存桁不足（06 §10.256）は、採用時に生成360桁以上・保存260桁以上と内容番号を確認し、310桁で本体の上下限を照合する。sin(1)へ一致する第二種の境界例を固定し、旧失敗を保持する。
- 楕円積分の基準値の桁不足（06 §10.255）は生成器で二精度の一致と境界近くの固定入力を採用前に必須とする。180/260桁の失敗を保持し、260/360桁・1e-200一致で47組を準備した。依存と基準の内容番号を記録し、製品未適用を明示する。
- Airyの値と原式の混同（06 §10.254）はairyFunctions.test.tsで40桁の値と座標の一致、異なる原式の保持、正確な式の形の相違を同時に検査する。独立した値と保存の確認は維持し、初回145成功・4失敗と修正後の型・書き方・計算197項目の成功を別に保持する。
- Lambert Wの表示（06 §10.253）は、公開返信へ返す前に全桁を保持して通常の十進表記へ変換する。lambertWFunctions.test.tsが独立値との40桁一致と表示文字列をともに検査し、実画面の期待値は維持する。修正後の型/lint・計算325・入力185・三環境の実保存・説明76項目が成功。
- Lambert Wの元の境界（06 §10.252）は整理前の枝・引数の記録と、点の微分前の開いた成立範囲の確認で守る。lambertWOriginalDomain.test.tsが両枝の0倍・成分選択と定数の境界値を実計算で区別する。修正前の4条件の失敗を保持し、修正後の検査結果を別に記録する。
- Lambert Wの確認値と返信表記（06 §10.251）は通常の型検査、公開返信の十進数の40桁全体の一致、原式・枝・表示変換・保存の独立した照合で確認する。修正後の入力・作図65項目が成功。一度の時間超過は別の未確定事項として保持し、表記修正だけで解決済みとしない。
- 複素数の返信形式とπの丸め残り（06 §10.250）は、公開返信を通すcomplexElementary.test.tsと有界なπの積・商の整理で検出・防止する。独立115組、既知の零・90/180・元の極・保存の定義を含む141項目を通常検査へ追加。修正後の型/lintと関連272項目が成功。実画面は別工程とする。
- Besselの微分の極限判定（06 §10.249）は、解析的な近傍を閉じた規則で証明した場合だけ正確な微分公式を使う。原点・独立値・高階・尖り・元の穴を通常検査へ追加し、修正後の型/lint・36項目が成功。最初の失敗は保持する。
- Besselの確認値の桁表記と期待式の符号（06 §10.248）は、参照小数の保持と明示変換、代数的関係と独立数値の二経路の微分照合で対処する。型/lintは成功、追加計算部は10.249の修正を経てBessel・既存微分36項目が成功。
- Besselの極小値の点への誤変換（06 §10.247）は、区間から点を作れる条件と原点の厳密値を分けて拒否する。元の高精度基準の包含と、非零を0の点・傾きにしない検査を通常確認へ加えた。初回421成功・2失敗を保持し、修正後の型/lintと423項目が成功。
- Besselの検査点の区間外への丸め（06 §10.246）は、指定端点の直接使用と全標本の範囲内検査で防ぐ。初回209成功・1失敗と端点差を保存し、修正後の型/lintと関連210項目・5ファイルが成功。本体の精度・包含の条件は維持する。
- 数式入力ボタンのキー遮断（06 §10.244）はEnter/Spaceと従来のTab移動を親から隔離し、command-registry.spec.tsがCtrl+S/Oの実操作と保存内容・未確定入力の保持を確認する。修正前の二失敗を保持し、最終の型/lint・23項目・両ブラウザーの焦点と実操作が成功。
- 次の面までの時間基準（06 §10.245）はreleasePerformanceのsolid-end区分を両計算経路に適用する。一般操作への漏出・5秒境界・異常値を単体で検査し、形状と体積の確認は維持する。型/lint・基準37項目・実形状71項目が成功。
- 起動前の取得失敗（06 §10.243）はstartupRecoveryの保存確認付き一回再読込、HTMLの常設リンク、startupBundleの静的依存検査で対処する。19単体とChromium/Firefoxの失敗復旧6件が成功。アプリ動作中は自動再読込しない。
- Besselの独立基準生成の制限と桁不足（06 §10.242）は原点の係数照合、入力指数に応じた追加精度、300/420桁での相対一致1e-240を必要条件にして検出する。負の整数次数は厳密な関係を使い、全確認値の実数表記・有限性・非零も通常単体で検査する。修正後の48組の内容照合後にだけ本体へ渡す。修正後の型/lintと関連130項目・3ファイルも成功した。通常入力と保存への接続、Bessel全体は未完了。
- Betaの点の微分の桁落ち（06 §10.241）は微分関数の積分で差を囲み、直接差との共通範囲を使う。betaFunctionPlot.test.tsで1e-8と4、1e-100と1・19999を独立基準へ照合する。初回268成功・1失敗を保持し、修正後の型/lint・計算124・入力180・3環境・説明76項目が成功。
- Betaの基準値の桁不足（06 §10.240）は生成時の400/640桁の一致と、betaFunctionNumeric.test.tsの全基準の非零・有限・符号の確認で検出する。生成19組の照合と、変更後の型/lint・計算と範囲348項目が成功。入力と保存は次工程。
- 最終集計の記録形式（06 §10.239）はGamma用finalize.pyで着手記録のheadの40桁形式と現在のGitの版を先に照合し、出力のsourceCommitへ明示変換する。実記録による再集計が成功。
- Gamma/Polygammaの期限接続漏れ（06 §10.238）はmathDistributionDeadline.test.tsで両入口の1秒境界、評価後の箱、繰返し・例外・入れ子の期限を検査する。Firefoxの実測262.12msによる失敗記録を保持。修正後の計算206・入力179・3環境・説明76項目が成功。
- 特殊定数の別表記（06 §10.237）はcas_gamma_functions_test.pyで定義する和の正確な係数へ戻して照合し、汎用の式整理が同値を認識できるとの仮定を置かない。初回380成功・1失敗を保持し、修正ファイル112項目が成功。他の本体と検査の内容が不変であることを照合。
- Gammaの反射区間（06 §10.235）はgammaFunctionIntervals.test.tsで広い負の区間・半整数の極値・極の左右・微小な負数を照合する。分母の不確定を0や有限値に置き換えない。初回2失敗を保持し、修正後の型・書き方・関連310項目は07:06に成功。数値と区間の内部計算までの確認で、入力・保存と全体の認定は含まない。

- 開いた区間の表記往復（06 §10.296）は区間の端点に限定したopen表記と、formatMathText.test.ts・setBounds.test.tsの開閉4通り、実計算、保存と両入力方式の往復で確認する。単独のOpenと範囲外の局所変数は拒否を維持する。

# 0. 施行の仕組み

各条項を「守るつもり」ではなく仕組みで守らせる。この表を正とし、仕組みを追加・変更したらこの表も同じコミットで更新する。

## 施行の一覧

| 条項 | 施行手段 | 区分 |
|---|---|---|
| 丸め済みの差を期待値へ流用（06 §10.236） | polygammaNumeric.test.tsで独立240桁の値との相対照合と、被演算数の大きさによる漸化式の残差を分離する。桁落ちでの確定拒否も照合 | 動作検査前に修正し、変更後の型・書き方・関連724項目が成功 |
| 誤差関数の補助桁の表示漏れ（06 §10.234、10.219） | 数値出力の出口で有効40桁へ丸める。通常・極小の値と返信文字列を単体で、結果全文を実画面で照合 | 最終の型/lint・計算354・表示176・3環境・説明76項目が成功。元の失敗と表示修正前の成功を別に保持 |
| 誤差関数の無限大表記と級数の領域接続（06 §10.233） | 単体・画面は正式な定数記号を使う。固定計算部で厳密係数、元の穴、0倍での隠蔽、有限半径1を照合 | 最終の型/lint・計算354・表示176・3環境・説明76項目が成功。元の失敗と表示修正前の成功を別に保持 |
| 保存座標の種類未確認（06 §10.232） | `mathErrorFunctionsFlow.ts`が3段階で点と絶対座標の両方を確認してから式・数値を照合。型成功と内容一致を次の確認の開始条件にする | 最終の型/lint・計算354・表示176・3環境・説明76項目が成功。元の失敗と表示修正前の成功を別に保持 |
| 作図の別経路の微分登録漏れ（06 §10.231） | `scalarDifferential.ts`の未定義分岐は型確認で拒否。`errorFunctions.test.ts`が値・区間微分・点の微分を解析式へ照合 | 最終の型/lint・計算354・表示176・3環境・説明76項目が成功。元の失敗と表示修正前の成功を別に保持 |
| 入力検査へ別計算部の拒否文言を流用（06 §10.230） | `legendrePolynomial.test.ts`と`mathLegendreFlow.ts`で4入力の具体的理由と適用不可を照合。画面での文言不一致は15秒で検出 | 修正後の31項目、型/lint、3環境の拒否と保存再編集が成功。初回3失敗を別に保持 |
| 標準表記と保存原文の混同（06 §10.229） | `legendrePolynomial.test.ts`が標準表記・構造の意味・計算値を照合し、保存原文とJSON再開の文字列保持を別に要求 | 初回180項目中1件の失敗を保持。修正後31項目・3環境の表示切替と保存再編集が成功 |
| 係数選択の結果型と表示の掛け算判定（06 §10.228） | `mathProductTypes.test.ts`が係数選択と展開全体を二つの積記号で区別し、`taylorCoefficient.test.ts`と実画面が倍率付きの表示往復を確認 | 初回421項目中1件の失敗を保持。修正後49項目・入力表示173項目・3環境・説明76項目成功 |
| 係数選択の保存検査でのnull未確認（06 §10.227） | `taylorCoefficient.test.ts`が保存する式と表示の存在を再読込み前に確認。通常型検査と、型の成功を要求する今回の検査起動補助を併用 | 初回TS2322を保持。修正後の型/lint・保存往復を含む49項目・実画面3環境成功 |
| 終了結果のない検査の誤認（06 §10.226） | 今回の検査補助は実行番号を記録し、既存ログをコピー前に拒否。次の工程は終了番号0と内容一致を要求 | Windows上の実行番号と正常終了404項目を照合。自動復旧・自動報告は未実装 |
| 級数の計算用の式と保存する原式の混同（06 §10.225） | `taylorExpansion.test.ts`で実計算・同名の係数と局所変数・係数変更・返信の原式を照合。返信の原式照合と座標権限の拒否を維持する | 対象18項目中の不一致1件を再現し修正。型・計算404項目・表示172項目・実画面3環境・説明76項目成功 |
| 数列と和積・整数演算の接続（06 §10.224） | `sequenceCalculations.test.ts`と`cas_sequence_composition_test.py`が独立した整数・分数の答え、元の不成立、範囲と共有上限、成分選択前の実計算経路を確認 | 型/lint・関連351件・表示169件・3環境の保存再編集・説明76件成功。単体の成功を合成の成功へ代用しない |
| 分布の計算方法・結果表現・局所変数と事象の独立性（06 §10.222） | `cas_probability.py`と`cas_result.py`で成立条件を保って接続。`generalProbability.test.ts`と実Pythonの確認が解析値・保存・表示・事象の重なりを照合 | 変更後の型/lint・計算589件・表示166件・一般確率3環境と整数条件3環境・説明76件成功。計算できない条件を未確定で止め、全分布の完成とは扱わない |

| 検査本体の成功表示と全体終了の混同（06 §10.221） | 作業用`check.py`が先行成功状態・入力内容の不一致を拒否。後続起動は先行ツールの終了コード0を条件とする | 今回の早過ぎる開始を既存の台本が拒否。終了後に計算496件・入力150件・3環境・説明76件成功。常設の全体フック追加とは区別 |
| 名前欄と数式表示の文字集合の不一致、準備失敗後の長い待機（06 §10.220） | `mathLatexLabel.ts`が既存の識別子判定を共用し、表示・文書準備の検査が中点等の往復を確認する。`mathEditorReady.ts`は準備失敗を直ちに返し、2環境で待機を打ち切る専用確認を持つ | 変更後の型/lint・関連384件・画面準備等139件・3環境の元の操作と失敗即時検出2環境成功。名前と保存内容を維持 |
| 分布の内部補助桁の出力漏れ（06 §10.219） | `gammaBetaDistributionNumeric.ts`が収束後の出力を既存の有効40桁へ揃え、`gammaBetaDistribution.test.ts`が通常返信の文字列・出力桁数・端点への丸め拒否を検査する。`mathDistributionDeadline.test.ts`が分布1秒と通常式200msの期限・復元を検査する | 変更後の型/lint・関連384件・入力例139件・3環境・説明76件成功。数値と画面の期待値は維持 |
| 数値分位点と平均の全桁相殺（06 §10.218） | `normalDistributionNumeric.ts`が中央値以外の不確定な0を拒否し、`normalDistribution.test.ts`が相殺する入力を計算と通常返信で照合する | 変更後の型/lint・関連258件成功。一般の数値演算全体の誤差保証とは区別する |
| 統計入力の対応範囲と不成立理由の混同（06 §10.217） | `probabilityMoments.test.ts`が不正な確率と未対応の平方根・πを別々の理由で拒否し、確率0・外側0倍・成分選択でも値を採用しないことを確認する | 同じ入力を残して拒否理由を明確化。関連193件・入力例111件・型/lint・3環境・説明76件成功 |
| 検査で返信形式を再度取り違えた（06 §10.216） | 今回の分布の診断入口 `check.py` は型・lintの成功と全対象の内容一致を要求し、変更後の古い成功記録では次の確認へ進めない | 今回の診断入口に機械施行。共通の全入口への施行とは区別する |
| 面積分の角度換算と返信条件の取り違え（06 §10.215） | `regionIntegrals.test.ts`が固定計算部で度・ラジアンの球面、反転と流束、保存と表示往復、元の不成立を照合する。範囲の証明では符号不明を安全側に含める | 関連45件・型/lint・3環境の実操作・説明76件成功。全体の完成認定とは分ける |
| 前の機能の画面検査との取り違え（06 §10.214） | 今回の作業用`verify-point-browser.py`が、対象台本の選択名と、3環境の撮影記録・台本/実装の内容番号を照合してから成功を記録する。元の結果を別保存して混入を防ぐ | 今回の診断入口に機械施行。全ての入口への施行とはしない |
| 配布する説明書の自己申告だけによる一致判定（06 §10.212） | `verifyCurrentManualEdition`が現在の章・本文表示・操作/機能対応・画像を読み、`verifyManualConsistency`で配布出力と比較する。Web/Desktopの組立てから実行し、出力と記録を一緒に変えた例も通常試験で拒否する | 18項目、型/lint、実際の107章・99画像の生成と照合が19:15に成功。撮影元・全本文・全PDFの受入とは分ける |
| 取り出した行と内積の組合せ（06 §10.213） | 微分の展開と行の取り出しを終えてから内積を計算用の式へ直す。元の全成分の成立条件を保持し、`vectorCalculusOperations.test.ts`で行の内積・一成分の積・配列の誤変換・穴を検査する | 追加の組合せ確認で再現し、修正後の関連118項目が19:34に成功。通常の数式検査へ接続 |
| CIの長い直列待ち（03 §7.2） | 別の実行機3台へ画面操作を分配し、実Playwrightの全件と3組の和集合を照合。既存の合格名は両OS全6組の成功に依存させ、実check.ps1で全5段と診断・手元での分割拒否を検査する | 選択範囲と関連21項目、実入口1項目が18:13までに成功。短縮時間の実測は未了 |
| 独立コピーの準備不足（06 §10.211） | `isolated_checkout_preflight.py`から範囲・版・Git・フック・入れ子パス・依存参照を確認後に通常pushへ進む。実Git・実部品解決・実フックで正常送信と拒否条件を自己試験する | 共通入口と通常自己試験へ接続。実際の送信と拒否条件の4項目が18:13に成功 |
| 製品検査が呼出元のGit設定・保存対象へ書き込む（06 §10.210） | `check.ps1`の全子検査からGit保存先情報を外し、配布元の読取りと確認用Git操作も`localGitEnvironment`で分離する。実検査入口へ通常・worktreeの保存先を渡し、失敗時もHEAD・index・設定・編集中の内容の全バイトと終了番号を照合する | 修正前3件で混入を再現。修正後3件を含む実フック7件、配布元等6件、型・lint成功（17:38）。通常自己試験へ接続済み |
| 微分の反復回数と変数順序の表示欠落（06 §10.209） | `mathLatexCodec.test.ts`と`functionDerivatives.test.ts`で実表示往復から作図計算まで照合する。点と区間の成立条件、元の穴・尖点、原式の保存再開・編集Undoも確認する | 関連70件・型/lint・3環境の実操作・説明76件、撮影範囲の修正後の3環境と画像反映後の説明76件が17:12までに成功。全体の完成認定とは区別する |
| 計算部の同時起動中に待機期限を超過（06 §10.208） | 共通上限150秒と最新世代の正常終了判定を維持し、試験用ビルドでは取得・展開照合・初期化の到達段階を既存の失敗traceへ残す | 型・lint・関連41件・元の2操作成功。CIでの停止再現と解決認定は未了 |
| 微分の局所変数の型が表示切替で失われる（06 §10.207） | 指定位置の微分だけ局所変数をスカラーとして復元し、一般の関数の引数型は変更しない。`exactDerivatives.test.ts`が入れ子の積・変数・位置・回数の実表示往復と計算・保存を照合する | 修正前1件失敗を保持。関連105件、最終の入力制約を含む41件、型/lint、3環境の実操作、説明76件と画像反映後の76件が成功（16:07）。全体の完成認定とは分ける |
| 入れ子の積分の未確認条件が式整理で消える（06 §10.206） | `cas_integrals.py`が外側の変数を含む区間の連続性または分離した因子の収束を確認し、未確認の積分を式整理へ渡さない。`exactIntegrals.test.ts`が通常入力・実計算・保存する式を通し、0倍での隠蔽拒否と有限・無限・逆向き・複数範囲の正しい値を照合する | 修正前に再現、関連109件・型/lint・3環境の実操作・説明76件が成功（15:33）。全体の完成認定とは分ける |
| 極限の左向き指定の表示落ち（06 §10.204） | 極限の方向の数値だけを表示用に整え、`exactLimits.test.ts`で左右・両側・0の表示往復と、違う方向・本体・近づける点の拒否を実計算と照合する。`mathLimitsFlow.ts`をWeb/Electronの保存再編集とF1へ接続する | 関連112件・3環境の実画面成功 |
| 数式入力の準備前に操作待ちが終了（06 §10.205、§10.313） | `mathEditorReady.ts`で入力欄を既存225秒内に待ち実時間を記録する。3手順の全入口へ適用する。手順全体の上限は`exactMathScenarioTimeout`（直列の準備回数×90秒＋重い計算の回数×45秒＋60秒）で決め、同じ構造の14手順は600秒、無限和積は825秒とする（2026-09-24変更。旧: 共通600秒）。`undoMathEdit`はUndoの前に直前の編集の再計算の成功を確かめ、操作前後の依頼番号と正常終了を照合し、前後の所要をテスト名付きで記録する。入力準備の再利用確認も引数全体を型付きで転送し、型検査後に画面操作へ進む | 型・lint・単体29件・3手順×3環境の実画面9件・説明76件が成功。全体検査の合格とは区別する |
| 生成したキー一覧と検索本文の不一致（06 §10.184） | helpLibraryが表示と同じ表を現在の割当から索引化する。変更・復元後の検索と原文保持を単体で、アプリと説明書の検索順を既存試験で照合する。画面の直書きは既存i18n検査で拒否する | 修正後の再確認前 |
| 操作の共通化の文書型と確認補助の取り違え（06 §10.183） | documentKind.test.tsが非null文書とnullable文書の型・実判定を固定する。既存の登録検索、uiMessages、撮影URL型を使い、先行型検査で不一致を拒否する | 修正後の再確認前 |
| 図面の設定入口の接続漏れ（06 §10.181） | SettingsPanelを図面の上部へ接続し、settingsEntryPoints.test.tsが部品・組立・図面の入口と状態不変を確認する。図面の設定・注記・保存等は3環境の実操作と既存の配置確認で検査する | 関連49件・実操作と配置等18件が成功 |
| 作業用の写しで保存範囲を取り違えた（06 §10.182） | task-workspace.selftest.pyが実Gitの写しから共通プロジェクト内の出力とGit領域への書込み拒否を確認する | 通常フックで再確認前 |
| ひな形選択待ちの通常編集の上書き（06 §10.180） | runNewFromTemplateが文書ID・差替え番号・部品/組立/図面の同一性を照合する。実ファイルの返信を保留し、通常編集と差替えで文書・保存先等の保持を確認する | 関連処理484件成功 |
| 画面操作の追加による無関係な単体の再実行（06 §10.178） | local_change_scope.pyが台本・共通補助の変更でも全E2Eを保持し、本体変更と全利用先の単体を選ぶ。実Git・PowerShellの自己試験で削除・改名・CI全段・失敗拒否を確認する | 実Git等25件成功・短縮時間未計測 |
| 待ち合わせ関数の型と補助入力の文字コード（06 §10.179） | templateDefaultsActions.test.tsの続行関数に戻り型を明示し、既存の先行型確認で検出する。補助処理は日本語パスを既存UTF-8記録から取得し、許可した修正以外の指紋変更を拒否する | 型・関連処理484件成功 |
| 厳密な有理数の指数表記（06 §10.172） | 上限付き厳密整数判定を使い、分子/分母の指数・非整数・ゼロ・予算と実際の極小値評価を通常単体で検査する | 通常単体・両OS CI |
| プロジェクト外・Git内の一時保存（06 §10.173） | task_workspace.pyは同じプロジェクトのscratchpad内に限定し、外部指定・Git内指定・外へ解決するリンクを作成前に拒否する | 保存範囲の制約を修正済み・変更後の検証待ち |
| 視点操作中の不要な作図更新（06 §10.161） | 実カメラと作図処理の12条件で100移動・4操作方式・3終了方式、文書保持と吸着再開を検査する。4分割の実操作・実選択も維持する | 通常UI単体・E2E・両OS CI |
| 利用者指示による正確性優先と初期化期限（06 §10.162〜164） | `releasePerformance.ts`へ時間・全4描画の判定を集約し、元の目標・実測・実用境界を同時記録する。`releasePerformance.test.ts`で境界・停止・非有限値、`perfBudget.test.ts`で環境に依存しない判定、`qualityGateOrder.test.ts`で既存経路と有限な機能待機を固定する。数学初回期限・取消・古い返信の検査を保持する | 通常単体・E2E・両OS CI |
| 画面外の係数操作と操作中の配置変化（06 §10.157） | 画面全体の列幅を窓幅内に保つ。`functionCoefficientFlow`が1008×681の3環境で、画面内への収容・マウス位置・ドラッグ前後の入力位置と幅・実値・Undo一回・実保存再開を確認する | 通常3環境E2E・両OS CI |
| 文書だけの送信が予期せず全体検査へ戻る（06 §10.159） | 退避対象の内容とindex全体のtree不変に加え、空のGit状態表示と正本の対象別判定をpush前に照合する。不一致なら送信開始前に止める。今回の送信補助で実施し、通常フックは維持する | 手順・今回の送信補助で照合済み（共通フックへの追加ではない） |
| 数式確定直後の保存キーの不達（06 §10.158） | 数式画面が閉じることを待ち、共通`savePart`が開いたdialogの不存在と作図面の実焦点を確認してから保存キーを送る。特異値分解・固有空間と既存保存操作で実ファイルと原式・再編集・Undoを照合する | 通常3環境E2E・両OS CI |
| 確認図の軸名と候補ボタンの重なり（06 §10.156） | 点の確認図で軸名専用の領域を確保し、投影単体で境界のボタンと選択枠まで分離する。`functionPreviewBounds.ts`が回転ごとの実X/Y/Z表示と候補の四辺の非交差を検査する | 通常単体・3環境E2E・画像確認 |
| 高解像度指定と実描画環境の不一致（06 §10.155） | `math-high-dpi.spec.ts`が独立したFirefox起動にも実密度2を指定し、両Web環境で表示倍率・解像度条件・CSS寸法・実PNGの2880×1800画素を照合した後、XYZ回転と点の位置・保存再編集を検査する | 通常E2E・両OS CI |
| 大きなPDFの全内容照合による検査超過（06 §10.154） | `drawingPerformance.test.ts`が実図面のPDF生成成功を2回確認し、長さと全バイトを直接照合する。生成時間の基準・5000ms上限を維持し、照合時間と全長も記録する | 通常UI単体・両OS CI |
| OSごとのブラウザー準備の期待値漏れ（06 §10.153） | `local-change-scope.selftest.py`が実OSの準備指定を含め、実Git pushの全コマンドと順序を照合する。準備や余分な処理をフィルターで隠さず、Linuxの依存導入と両ブラウザー・Electronを保持する | 正本自己試験・実フック・両OS CI |
| 表示部品の分離後の共有先の接続漏れ（06 §10.152） | `PropertyPanelAppearance.test.ts`が実プロパティを描画し、アルミ・鋼の外観と質量特性の材料/密度、文書保持、選択解除を確認する。`reviewBoundaries.test.ts`が実効lint設定の責務境界と分離先の末尾空行の拒否を検査する。全型・lintと既存の外観保存/質量表示の実操作も保持する | 通常単体・型・lint・実画面 |
| 自己試験の本番保存先への書込み（06 §10.151） | `Start-IsolatedGitSelftest`が実入口の継承Git環境を分離し、初期化直後に専用保存先を照合する。`git-selftest-environment.selftest.py`が実Gitの5条件で元のHEAD・index・設定・作業内容と失敗コードを検査する | 正本自己試験・実フック・両OS CI |
| 本体変更の利用先漏れと長い全体検査の反復(06 §10.148) | `local_change_scope.py`が実indexの全workspace依存から変更先と全利用先を求め、未確認の影響は全体へ戻す。`local-change-scope.selftest.py`が改名・削除・リンク・未stageの依存表・循環と全利用先、実Gitフック、対象別失敗の中止、CI/Fullの全5段、対象別成功のB3不発行を検査する。正本`check.ps1`が本体の対象別Pushへ既存の描画性能・Firefox/Electron起動を必須にする | 通常自己試験・実フック・両OS CI |
| 陰関数の重複微分と公開戻り型の配置依存（06 §10.146） | `scalarFirstDerivatives.test.ts`が値の区間評価1回・両角度の端点一致・独立解析微分・入力変更と未使用二次演算の削減を検査する。`implicitGrid.test.ts`は既存5000msと全形状の検査を保持して工程時間を出す。板金の公開戻り型は`JSX.Element`を明記する | 通常単体・型・両OS CI |
| 曲面の計算重複とCIでの発見遅れ(06 §10.140) | `trigonometricIntervals`が同一の両端・関数だけを最大1024件再利用する。`trigonometricIntervals.test.ts`が隣接する値・両単位・sin/cos・追出し前後を照合する。ルートのtestはexpressionを先行し、成功時だけ残りを数学除外で1回ずつ実行する。`qualityGateOrder.test.ts`で順序・成功条件・除外と専有実行を固定する | 通常ユニット・両OS CI |
| 日本語のコミット件名と禁止接頭辞(06 §10.137) | `commit_message.py`と実commit-msgフックが日本語のない件名・禁止接頭辞・BOM・不正UTF-8を拒否する。`commit-message.selftest.py`が実Gitの拒否とHEAD保持、日本語の受理、全3フックのLFと実行属性を検査する | Gitフック・通常自己試験・両OS CI |
| 描画で取得した所有形状の解放(06 §10.147) | `meshOwnership.test.ts`が実OCCTで正常・型読取例外・抽出例外の全取得を観測し、反復描画の内容一致と全解放を検査する | 通常単体・型・lint・実画面 |
| 再計算間の数式準備の再利用と終了(06 §10.149) | `mathWorkerReuse.test.ts`と`recomputePartWithMath.test.ts`が実要求の原式・世代・期限、同時使用、所有者終了、30秒待機と16回保持の上限、例外時の解放を検査する | 通常単体・型・lint・3環境の実画面 |
| 実形状計算の同時実行防止(06 §10.138) | expression/model/kernelはCI・Commitを含む全モードで`fileParallelism: false`とし、`qualityGateOrder.test.ts`が設定を検査する。直列化のみで性能問題の解消とは判定しない | 通常ユニット・両OS CI |
| 図面の表示と操作可能の同期(06 §10.103) | 図面の`aria-busy`と`waitForDrawingReady`を使う。`gdt.spec.ts`で実依頼を保留し、文字の更新中は削除を拒否し、計算終了後に削除・Undoが成立することを確認する | 通常ユニット・実画面・CI |
| 関数の定数と点の証明区間(06 §10.102) | `scalarConstantEnclosures.test.ts`で丸め値を誤差0と扱わず、有理数の正確な比較と平方根の二乗で上下界を検査する。`solveCurveFunctionPoints.test.ts`でXYZ全軸の精度と範囲、共通解、接点、未解決領域を検査する | 通常ユニット・CI |
| 関数上の点の確定と保存条件(06 §10.101) | `functionPointDraft.test.ts`が未完了の探索からの作成を拒否して文書を保持する。`functionPointContinuationWork.test.ts`が共通の保存入口で必須値の欠落と元入力の変更を検査する | 通常ユニット・CI |
| 起動の完了・書体・描画失敗からの文書保持(06 §10.99) | `smoke.spec.ts`で実描画、同梱書体の1280/1440px、意図的な3D初期化失敗後の点作成・実保存・再試行を検査する。FirefoxとElectronの起動project成功を後続の前提とする | 通常ゲート・CI |
| 合致の列参照を評価内でまとめる(06 §10.100) | `mateResiduals.test.ts`で150合致の列参照数を部品ごと6回以内に制限し、次の評価で固定を変えた値と微分の一致を検査する。`assemblyPerformance.test.ts`の絶対・相対性能条件は維持する | 通常ユニット・CI |
| 不変な字形のSVG再利用(06 §10.98) | `fontStore.test.ts`が共有輪郭の書換を拒否し、`toSvg.test.ts`が移動・色・別字形と可変外部入力の再検証を確認する | 通常ユニット・CI |
| 関数上の点を変更時に別枝へ移さない | `continueImplicitFunctionPoint.test.ts`で正負2枝・枝消失・途中合流・固定根・改名・範囲変更を検査する。`functionPointContinuationWork.test.ts`で過去入力の改変・別世代・既知値変更の返信・取消を拒否する | 通常ユニット・CI |
| 自動作図の容量判定を時間切れにしない | `scriptGuestBootstrap`は保持した組込み正規表現で文字の連続部分を数える。`runtimeAdapter.test.ts`は8MiBの命令制限と6種類のUTF-8境界、組込み改変、catch後の失敗保持と再実行を実VMで検査する。5秒と容量の上限は変更しない | 通常ユニット・CI |
| 陰関数の接触・区間依存と公開入口(06 §10.92) | `implicitGrid.test.ts`が球・トーラス・分離成分・境界接触・特異点を確認する。区間微分と実メッシュへの距離で未解決領域の被覆を検証し、予算超過や未被覆は生成を止める。`packageBoundary.test.ts`は実importの公開入口を解決し、ローカルexpression診断にも自動追加する | 通常ユニット・CI／診断補助 |
| OCCTの借用と所有を区別する(06 §10.91) | `borrowHandle`の戻り型からdeleteを除外し、`eslint.config.js`がOCCT実装の`keep(handle.get())`を拒否する。`functionCurveSketch.test.ts`で実CAD切断・節点抽出・破棄・面張り・取消後の再生成を検査する | 型・lint・通常ゲート |
| 実Electronの起動同期(06 §10.87) | `launchDesktop`が固定済みPlaywrightのready保留ローダーを製品mainより先に読み、未設置・早過ぎるready・既存windowを即時拒否する。`electron-startup.spec.ts`で独立した実起動5回と再読込・正常終了を通常ゲート/CIへ含める | 機械施行 |
| 操作追加後の共有ツールバーと接続入/切(06 §10.85) | `p8-drawing.spec.ts`で1280/1440pxの高さ・あふれ・名前を固定し、`sketch-extended.spec.ts`と`intersectionsFlow.ts`で未分割の部分トリムと自動分割後の区間削除を分けて検査する | 通常ゲート・CIの実操作検査 |
| 保存完了の通知と実行件数(06 §10.86) | `saveConcurrency.test.ts`で控え削除の保留と編集競合を検査し、実ブラウザーで保存直後に再読み込みする。進捗読出しは周回全体の成功/失敗を集計する | 通常ゲート・CIと報告読出し |
| 保存形式の診断と版整合(06 §10.83) | `check.ps1`がio診断へ`schemaVersion.test.ts`、model診断へ`exportPart.test.ts`と`createPartDocument.test.ts`を必ず含める。ファイル欠落は失敗する | 機械施行 |
| 固定時刻の桁幅と編集欄の焦点(06 §10.84) | `scriptClock.test.ts`が実VMの時刻上端・入力拒否を検査し、`scriptsFlow.ts`が3環境でTab/Shift+Tabの移動と本文保持を検査する | 機械施行 |
| store登録と検査の目録統一(06 §10.81) | 製品と`storeSlices.test.ts`が同じ`STORE_SLICE_CREATORS`を使い、全欄一致と所有欄の重複0を検査する | 自動検査 |
| 説明書の用語と実行可能な例(06 §10.82) | `topics.test.ts`が自動作図を含む全章の禁止語を検出し、`scriptKernel.test.ts`がAPIとmoduleの原文例を実VM・実OCCTで実行する | 自動検査 |
| 数学の説明変更を実画面確認前に検出(06 §10.282) | 数学作業の検査用`browser.py`は`topics.test.ts`の成功記録と説明の内容一致を開始条件にする。未実行・失敗・別検査・変更後の記録を拒否し、画像追加後の説明全体確認も残す | 自動検査 |
| スクリプトの資源超過を保持(§10.77) | native patchの印を`runtimeResourceLimit.ts`が確認し、`runtimeAdapter.test.ts`がcatch後の失敗・無修正版拒否・正常再実行を検査。ビルド入力/patch/出力のSHA-256を`build-script-runtime.py`で照合 | 自動検査＋再現ビルド |
| 加工案内の背景と説明書画像(§10.76) | `camFlow.ts`が3環境で不透明な計算済み背景色・左揃え・F1の実画像2枚の読込を検査。`helpLibrary.test.ts`が同梱画像への参照解決を検査 | 自動検査＋実画像の目視 |
| 統括は `pnpm` / `npm` / `npx` / `yarn` を直接実行しない(01 §1、02) | `.claude/settings.json` のPreToolUseフックが `.claude/hooks/block-direct-package-managers.ps1` で Bash / PowerShell のコマンドを構文解析して拒否。`cmd /c`・`powershell -c`・`bash -c`・`iex`・`Start-Process`・`corepack` 経由の入れ子も検査する。禁じているのは統括の直接実行だけなので、フック入力に `agent_id`(サブエージェント発の呼出にだけ付く識別子)が非空で含まれる作業担当発の呼出は通す。2026-09-23 以降の作業担当(Claude のサブエージェント)の呼出には `agent_id` が付くため通る(2026-09-07〜09-23 の Codex はフックの外で動いていた)。 | 機械施行 |
| コミット前の検査(03 §7、B3または承認済みの範囲選択) | `scripts/hooks/pre-commit` が `scripts/check.ps1 -Level Commit -ReceiptPhase Commit` を実行する。B3は全5段成功済みの同一内容だけを共用する。揃わなければstage済みの写しで全型・lint・両ビルド・§7.2に従う全単体を実行し、失敗なら中止する。判定不能は全4段とする。写し・依存リンクと継承Git環境の隔離は`gitTreeGuard.ps1`を正とする(06 §10.7/10.8) | 機械施行(要: `scripts/install-hooks.ps1`) |
| mainの10件統合・作業ブランチの途中保存・明示承認(03 §6、06 §10.96) | `scripts/hooks/pre-commit`から`check-commit-batch.ps1`を実行する。A1に従い作業ブランチでの小さな保存を認め、mainの通常実装は新規完了ID10件以上・報告増分の一致・既存完了ID削除0件を必須にする。利用者の件数例外はGit管理領域の承認理由・親SHA・stageのtreeが全て一致する変更だけに適用し、品質検査は省略しない。削除も実装変更として判定し、detached HEADは拒否する。`check-commit-batch.selftest.ps1`を通常の自己試験から呼ぶ | 機械施行・fail-closed |
| コミット件名へ UTF-8 BOM を混入させない(03 §6、06 10.29) | `scripts/hooks/commit-msg` がメッセージ先頭の3バイトを検査し、`EF BB BF` ならコミットを中止する。PowerShell 5.1 でメッセージファイルを書く場合は `[IO.File]::WriteAllText` と `UTF8Encoding($false)` を使う | 機械施行(同上) |
| push前の検査(03 §7、B3または承認済みの範囲選択) | `scripts/hooks/pre-push`は実際に送信するSHAを確認し、`check.ps1 -Level Push -ReceiptPhase Push`で同一の全5段成功記録を照合する。揃わなければ§7.2の変更先・利用先検査と、本体変更時の描画性能・Firefox/Electron起動を実行する。判定不能・CI・Fullは全5段、失敗ならpushを中止する。B3成功記録は送信前に消費し、対象別結果からは発行しない | 機械施行(同上) |
| 重複検査の同一内容だけの共用(03 §7 B3、06 §10.104) | `validation_receipt.py`がsource・index・依存・設定・ツール・生成物の指紋と親SHA・20分の有効期間を照合する。自己試験が変更/改変/未追跡追加/別index/中断/期限切れ/二重使用/CIを拒否する | 通常の自己試験・CI |
| 共用できない記録での不要な全走査の拒否(06 §10.150) | 記録が無い・改変済みの場合は依存とブラウザーを読み始める前に通常検査へ戻す。正当な記録は従来どおり全入力を照合し、自己試験が拒否時0回・正当時1回の走査を検査する | 通常の自己試験・実フック・CI |
| CIで同じ検査(03 §7.2) | `.github/workflows/ci.yml` が Windows / Linux 両方で `scripts/check.ps1 -Install` を実行する。検査の定義は `check.ps1` の1箇所だけにあり、`ci.yml` と食い違う余地を作らない | 機械施行 |
| 保存・復元とも失敗した場合の正常コピー保持(06 10.45) | `apps/desktop/src/main/pcadDialogs.test.ts`が部分書込・復元不能・後始末失敗を実保存入口へ与え、控えの内容と削除対象を検査する。desktopのtestを通常の再帰検査へ組み込む | 機械施行 |
| 古い保存の完了を別文書へ反映しない(06 10.46) | `saveConcurrency.test.ts`・`desktopFileGateway.test.ts`・`pcadIpcTargets.test.ts`がUIと本体の実保存入口で保存順・文書切替・同一文書の追加編集・保存先解除と非同期完了を組み合わせて検査する | 機械施行 |
| 図面の戻る操作で未保存の編集を無確認で捨てない(06 10.47) | `closeDrawing.test.ts`と`p8-drawing.spec.ts`が破棄の拒否時の図面・Undo保持と同意時の終了を検査する | 機械施行 |
| 50部品の描画性能を同じWebGL経路で測る(06 10.37) | `qualityGateOrder.test.ts`がWebGL専用SwiftShader引数と性能projectの専有実行を固定し、`assembly.spec.ts`が実描画器名・解像度復元・50部品の実描画と要件§5.2の判定を検査する。ブラウザ全体へのSwiftShader強制へ戻さない | 機械施行 |
| 自動寸法と手動文字の重なりを検出する(06 10.41) | `autoDimension.test.ts`が字体の下端基準、`autoDimensionCommands.test.ts`が手動寸法の保持、`p8-drawing.spec.ts`が実画面上の文字矩形を検査する | 機械施行 |
| SVG再描画中の古い要素参照を寸法消失と誤認しない(06 10.42) | `p8-drawing.spec.ts`が現在の要素と矩形を同一DOM処理で取得し、全7文字の非重複も同じ描画状態で判定する。存在・正の面積・移動量・Undoの条件を維持する | 機械施行 |
| 必須スクリプト(typecheck / lint / test / build)の欠落検出(03 §7.1) | `scripts/check.ps1` が `package.json` を読み、欠けていれば失敗する(fail-closed) | 機械施行 |
| テストが追跡ファイルを書き換えない(03 §7.1 #0) | `-Level Push`(push/CI)は `scripts/check.ps1`(`scripts/lib/gitTreeGuard.ps1`)が検査前後の `git status --porcelain --untracked-files=no` を比較し、差があれば失敗する。`-Level Commit`(pre-commit)は本物の作業ツリー・index に一切触れない写し(git worktree)の中で検査するため、前後比較そのものが構造的に不要(2026-09-05 に方式変更。rules/06 10.7) | 機械施行 |
| 依存方向 `apps → ui → model → kernel / expression`(04) | `eslint.config.js` の `no-restricted-imports` がパッケージごとの禁止 import を検出し `pnpm run lint` で落とす。型検査は副作用のみの import を素通しするため、施行の主体は lint(2026-09-02 実測) | 機械施行 |
| UI 文字列の `ja.json` 分離(04、NFR-MA-5) | `packages/ui/src/i18n/i18n.test.ts` が `.tsx` への日本語直書きとキーの網羅を検査し `pnpm run test` で落とす | 機械施行 |
| 性能要件(NFR-PF-2 / NFR-PF-3、03 §7.1) | `solidPerformance.test.ts`、制約の`solve.test.ts` / `diagnose.test.ts`等は`expectWithinBudget`から共通の時間判定を使う。要件§5.2の旧目標を記録し、実用上限超過は全モードで失敗。`POINTERCAD_PERF_STRICT`は実行条件とB3の識別だけに残す。1パッケージずつの実行・`PerformanceFirstSequencer`、既存の解の照合と退避、入力規模は保持し、`qualityGateOrder.test.ts`と正本自己試験で接続を確認する | 機械施行 |
| 作業担当のgit書込禁止(01 §1) | 指示書への明記+統括が委譲後に `git status` / `git diff` で未書込を確認する | 手順 |
| 依存の無断変更禁止・警告封じ禁止(02) | 統括が `git diff --cached` 全文を読み、`package.json` / `pnpm-lock.yaml` の差分と `eslint-disable` / `@ts-ignore` / `any` の混入を照合する | 手順(検査スクリプト化を検討) |
| 指示書の7項目・実在確認(01 §3) | 統括がGrepで実在確認してから指示書を書く | 手順 |
| 停滞監視(01 §4) | 統括が20分間隔(2026-09-23 利用者指示)で各担当の `scratchpad/claude/agents/<担当名>/progress.md` の更新時刻と最終行を確認する(Codex の担当は `events.jsonl` の更新時刻も見る。2026-09-24〜)。セッション内の定期実行(CronCreate)で20分ごとに監視の手順を起動する。40分無更新は停滞 | 手順(定期起動は機械、判断は統括) |
| Codex の担当の起動の検査と記録(01 §2、06 §10.320) | `scratchpad/claude/tools/launch_codex.py` が起動前にモデル(gpt-6-astra・gpt-6-sol)・指示書の①②③と報告の2行・先の時刻・二重起動を検査して外れたら起動しない。台帳の行と終了の記録を時計の時刻で書き、`codex exec` を前景で待って完了の通知を確実にする。子プロセスだけに `safe.directory` の環境変数を渡す。自己試験 `scratchpad/claude/tools/launch_codex.selftest.py`(18件) | 機械(道具)。統括が道具を使うこと自体は手順 |
| 記録に実際より先の時刻を書かない(06 §10.320) | `.claude/hooks/record_time_guard.py` を PreToolUse(Write・Edit・MultiEdit)に登録し、報告記録・台帳・待ち行列・進捗・指示書・rules/06 へ現在より2分を超えて先の時刻を書く変更を拒否する。PostToolUse(Bash・PowerShell)で直近3分に更新された対象ファイルを調べ、先の時刻があれば統括に直させる(事後検出)。`--scan` で既存の記録を調べる。自己試験 `.claude/hooks/record_time_guard.selftest.py`(17件、`scripts/check.selftest.ps1` から実行) | 機械(Write・Edit は事前拒否、シェルからの書込みは事後検出) |
| opus の担当の同時実行は3本まで(01 §2、06 §10.318・§10.319) | `.claude/hooks/opus_concurrency_guard.py` を PreToolUse(Agent・SendMessage)に登録し、セッションの `subagents/agent-*.meta.json`・`agent-*.jsonl` から稼働中の opus(未完了かつ45分以内に更新)を数え、3本以上なら新しい起動と止まった opus の再開を拒否する。依頼文に `【opus例外:理由】` があるときだけ通し `scratchpad/claude/state/opus-exceptions.jsonl` に記録する。自己試験 `.claude/hooks/opus_concurrency_guard.selftest.py`(13件) | 機械(同時に出した複数の起動の競合は残る) |
| 担当の編集の反映の判定は SHA-256 で(06 §10.320) | 統括は再開の連絡を書く前に `python -B -X utf8 scratchpad/claude/tools/worker_files.py <担当名>` で、担当の progress に記録された SHA-256 と実ファイル、未追跡を含む `git status` の状態を照合する。自己試験 `scratchpad/claude/tools/worker_files.selftest.py`(20件。2026-09-24 に `.tsx` 等の拡張子・省略形の値・共通規律 §5 の 5b の「パス 値」一覧に対応) | 手順(照合は機械) |
| 台帳は道具で編集する(01 §2、06 §10.320) | `scratchpad/claude/tools/registry.py` が表の途中の空行・区切り行をまたいで全行を読み、`add`・`set-status --kind`・`archive --done` で時計の時刻を自動で付け、台帳と控えの行数の合計が変わらないことを確かめてから一時ファイル経由で置き換える。`launch_codex.py` も同じ関数を使う。日付の無い時刻は `record_time_guard.py` が「0分超180分以内の先」だけを先の時刻とする。自己試験 `registry.selftest.py`(15件)・`record_time_guard.selftest.py`(21件)・`launch_codex.selftest.py`(20件、実際の経路の `codex --version` を含む) | 機械(道具)。統括が道具を使うこと自体は手順 |
| 担当は背景実行と Monitor を使わない(共通規律 §2、06 §10.324) | `.claude/hooks/subagent_background_guard.py` を PreToolUse(Bash・PowerShell・Monitor)に登録し、フックの入力の `transcript_path`(`<セッション>/subagents/agent-*.jsonl`)と `agent_id` で担当の呼出しと判定したら `run_in_background` と Monitor を拒否する。統括の呼出しと判定できない入力は通す。自己試験 `.claude/hooks/subagent_background_guard.selftest.py`(13件、`scripts/check.selftest.ps1` から実行) | 機械 |
| 画面検査の止まりと sandbox での Electron(06 §10.325) | `scratchpad/claude/tools/launch_codex.py` が Codex の子プロセスに `PCAD_WORKER_KIND=codex` を渡し、`diag.py e2e` はその環境で Electron のプロジェクトを起動前に拒否する。`diag.py e2e` は利用者の指定が無ければ Playwright の `--global-timeout`(既定3時間。過去の最長の個別 E2E 2452.64秒と全体検査の画面検査約87分に余裕を加えた値)を付け、Playwright 自身が上限で終わる。`diag.py status` は出力が20分以上更新されない E2E を「止まっている疑い」と表示する。自己試験 `diag.selftest.py`(20件)・`launch_codex.selftest.py`(21件) | 機械 |
| Codex からの画面検査の全拒否と、結果の後に止まった検査の終了(06 §10.326、利用者の 07:2x の許可) | `diag.py e2e` が `PCAD_WORKER_KIND=codex` の環境からの画面検査を project の指定に関わらず全て理由付きで拒否する。結果の印(✓・✘・-・`N passed` 等)が1つ以上出た後に出力が600秒(既定)止まったら、`taskkill /F /T` で Playwright とその子の木だけを終了し、`result.json` の `stallGuard` に記録する。結果の印の前は終了しない。自己試験 `diag.selftest.py`(28件) | 機械 |
| 全体の eslint の同時実行によるメモリの逼迫(06 §10.315・§10.327) | `diag.py` が `static` の lint の段(`eslint .`)だけを `scratchpad/claude/locks/full-lint.lock` で機械全体で同時1本に絞る(重い確認の枠・画面検査の排他とは別)。`typecheck` の段と `diag.py lint <ファイル>` は対象外。自己試験4件(相互排他・static への組込み・部分の lint は対象外・実スレッド2本で重ならない)、実機 RUN 20260924-081330 で他の担当の lint の段の終了を待ったことをログで確認 | 機械 |
| 数式の検査が計算部を直接起動しない(06 §10.323) | `eslint.config.js` の `packages/expression/src/**/*.test.ts` の規則で `spawnSync('python', …)` を `no-restricted-syntax` で拒否し、`exactRuntimeTestSupport.ts` の `spawnExactRuntime` を使わせる。わざと違反を書いた一時的なファイルで発火を確認して削除。全体の `eslint .` で新しい規則による誤り0件 | 機械(lint) |
| 計算部の1ファイルの上限超えを検査の起動前に止める(06 §10.328) | `diag.py` が `unit`(`--package expression`・`ui`)・`static`・`lint`(対象に `exactRuntime/` を含む)の実行の前に、配布される `exactRuntime/cas_*.py` の文字数を検査と同じ CRLF 換算で測り、上限 32,768 を超えるファイルがあれば vitest・eslint を起動する前に理由(ファイル名・文字数・上限・別のモジュールへ移す案内)付きで失敗させ `result.json` に記録する。それ以外の実行は警告1行。自己試験 `diag.selftest.py`(42件) | 機械 |
| 担当のリポジトリ全体の検索を拒否する(06 §10.330) | `.claude/hooks/subagent_search_scope_guard.py` を PreToolUse(Bash・PowerShell)に登録し、担当の呼出しで、実際に取り出した対象のパスの中にリポジトリの根(`.`・根の絶対パス・パスの省略)がある `grep -r`・`find`・`Get-ChildItem -Recurse`・`Select-String` の再帰・`rg --no-ignore`/`-u` を理由付きで拒否する。`cd` で根へ移った後でも対象を `packages/…` 等に絞った検索と、`.gitignore` を守る既定の `rg`、統括の呼出しは通す。自己試験 `subagent_search_scope_guard.selftest.py`(51件) | 機械 |
| 数式の欄の焦点の処理の最中に操作しない(06 §10.329) | `eslint.config.js` の e2e の規則 `mathFieldDirectCallGuard`(no-restricted-syntax)が `math-field` の Locator への直接の `.press(`・`.focus()` を拒否し、`e2e/tests/mathEditorReady.ts` の `focusField`(MathLive の `focusBlurInProgress` の終わりを待つ)を使わせる。既存の `mathInputFlow.ts` の1か所は対象外の区画(安全性の確認は待ち行列)。違反を書いた一時的なファイルで発火を確認 | 機械(lint) |
| 先の時刻の許容を0分にする(06 §10.320・§10.333) | `.claude/hooks/record_time_guard.py` の `ALLOWANCE` を2分から0分にし、書いた時刻の「分」が今の「分」より後なら拒否する(同じ分は許す)。検査の対象に `scratchpad/claude/deliveries/*/MANIFEST.txt` を加えた。自己試験 `record_time_guard.selftest.py`(32件) | 機械 |
| 指示書の見出しと記入の時刻も先の時刻を拒否する(06 §10.339) | `.claude/hooks/record_time_guard.py` が、指示書(`scratchpad/claude/instructions/*.md`)の年月日の無い時刻のうち、1行目の見出し・「HH:MM に 取得/作成/測/不在/確認」・「状態(HH:MM)」の文脈にあるものを、今の分より後なら拒否する(数式の例・過去の時刻の説明は見ない)。自己試験 `record_time_guard.selftest.py`(39件、OPS-16 の7件を含む) | 機械施行 |
| 指示書の「新規」・SHA-256・時刻を起動の前に確かめる(06 §10.334・§10.339、OPS-12) | `.claude/hooks/instruction_check_guard.py` を PreToolUse(`Agent`)に登録し、起動の依頼文が指す `scratchpad/claude/instructions/<名前>.md` について、新規と書いたパスが既にある・`パス`(16桁)の SHA-256 が今の内容と違う・見出しや記入の時刻が先、のいずれかなら起動を理由付きで拒否する(検査はフックのファイルの中で完結し、`scratchpad/` に頼らない)。統括は `python .claude/hooks/instruction_check_guard.py --check <指示書>` で起動の前に確かめられる。自己試験 `instruction_check_guard.selftest.py`(40件) | 機械施行 |
| 全体検査の前にフックの自己試験を流し、画面検査の順番を予約する(06 §10.336、OPS-15・OPS-17) | `scratchpad/claude/tools/deliver.py gate` が、E2E の排他を要求する前に独立コピーで `scripts/check.selftest.ps1` の一覧のフックの自己試験を check.ps1 と同じ道のり(`scripts/../.claude/hooks/`)で実行し、失敗なら排他を取らずに理由付きで終える。待つ間は `scratchpad/claude/locks/e2e.reserve.json` に予約を置き、担当の `diag.py e2e` は他の生きた予約があれば終了コード75で譲る(待っている間の再試行でも確かめる)。自己試験 `diag.selftest.py`・`scratchpad/claude/agents/w2c-delivery-tool/selftest.py` | 機械施行(統括の道具。git の外) |
| 統合点で作業中の担当の着手前の内容を候補に使う(OPS-13) | `scratchpad/claude/tools/deliver.py snapshot --base-dir <控えのフォルダー>` が、控えの `MANIFEST.txt` の各パスの実ファイルの SHA-256 を記録の値と照合してから(不一致は理由付きで中止)、そのパスは作業ツリーの今の内容でなく控えの内容を候補にする(`snapshot.json` に `source:"base"`)。控えが HEAD と同じパスは候補から外す。`--exclude` と重なれば中止。`sync` は控えから複写する。自己試験 `scratchpad/claude/agents/w2c-delivery-tool/selftest.py`(68件)。統括は担当の起動のたびに編集可のファイルの着手前の内容を控えに足す | 機械(道具)＋手順 |
| 見えない文字を書き込まない(06 §10.346、OPS-24) | `.claude/hooks/invisible_char_guard.py` を PreToolUse(Write・Edit・MultiEdit)に登録し、`.ts`・`.tsx`・`.mts`・`.mjs`・`.js`・`.ps1`・`.py`・`.json`・`.md` へ書く内容に U+00A0・U+200B・U+200C・U+200D・U+2028・U+2029・先頭以外の U+FEFF があれば、行番号と「エスケープを書かず chr() などで組み立てる」案内付きで拒否する。U+3000 は対象外(文字列の外は ESLint の `no-irregular-whitespace` が止める)。`--scan` で既存のファイルを調べる。自己試験 `invisible_char_guard.selftest.py`(36件、`scripts/check.selftest.ps1` から実行) | 機械(事前拒否) |
| 全体検査の間は重い枠を3つ保持し、静かな状態で事前の確認をする(06 §10.347、OPS-25) | `scratchpad/claude/tools/deliver.py gate` が、事前の確認の後から終わりまで重い枠を定数 `GATE_HEAVY_SLOT_HOLD_COUNT`(3)個と画面検査の排他を先に取り、別のプロジェクトの Playwright・vitest・x64 の配布物が無くなるまで待ってから事前の確認をし、空くまで待ち(gate.log に記録)、成功・失敗・中止・例外のどれでも try/finally で解放する。統括の手作業の lockhold はやめる。自己試験 `scratchpad/claude/agents/w2c-delivery-tool/selftest.py` | 機械施行(統括の道具。git の外) |
| 共有の画面検査の補助の変更は、呼出し元の spec の全 project の成功の証拠が無ければ候補に入れない(06 §10.349・§10.356・§10.357、OPS-29) | `scratchpad/claude/tools/e2e_coverage.py` が候補の e2e の変更から import の依存で影響する (spec, project) の組を全て求め、変更の後の `diag.py e2e` の実行(`scratchpad/claude/runs/`)か全体検査の gate.log に成功の証拠が無い組があれば、`candidate.py`(check・take)と `deliver.py snapshot` が候補を取らない(飛ばす引数は無い。外すのは除外の一覧に理由を書く形だけ)。自己試験 `e2e_coverage.selftest.py`(28件)・`candidate.selftest.py`(22件) | 機械(統括の道具。git の外) |
| 台帳に手書きの時刻を書かない(06 §10.354・§10.357) | `scratchpad/claude/tools/registry.py` の `add`・`set-status` が、文の中の日付の無い `HH:MM` を理由付きで断る(時刻は道具が付ける)。自己試験 `registry.selftest.py`(18件) | 機械 |
| 画面検査の間は担当の単体の並列を絞る(06 §10.347、OPS-27) | `diag.py unit` は E2E の排他か全体検査の重い枠があれば vitest に `--maxWorkers=2` を渡し、判定を command.json と出力の先頭に記録する(統括は `PCAD_DIAG_UNIT_MAX_WORKERS` で上書き可) | 機械 |
| Electron の画面検査の保存先のパスの長さ(06 §10.331) | `e2e/tests/electronAppFlow.ts` の `launchDesktop` が Electron の `userData` をプロジェクト内の短い固定の場所 `scratchpad/temp/e/<6文字>` に置き、起動前に `assertUserDataPathBudget` が絶対パスの長さを検査して、上限(名前付きの定数の式 260−170=90 文字。独立コピー `scratchpad/c3` の中の87文字も収まる。E2E-01b)を超えたら理由(パス・長さ・Windows の260文字の上限)付きで失敗させる。15分より古いプロファイルは次の起動で消す。上限を一時的に10へ下げた実行で理由付きの失敗を2回確認(RUN 20260924-075937・081051) | 機械(検査の補助自身が起動時に強制) |
| 報告記録(01 §1-⑥、06 §10.200) | `docs/報告記録.md`へタスクごとに追記する。`validation-progress.mjs`が同じ作業場所の最新見出しを読み、10分以上古い・日時がない・時計が逆行した場合に更新警告を出す。内容の作成と実ファイルの読戻しは担当が行う | 読取り時に警告。境界を含む関連17件成功。自動記録・自動送信ではない |
| mainへの着地と完成度報告(06 10.43) | 報告用処理が`git ls-remote`の実SHAに保存された進捗を読む。ローカルmainとの一致・未送信分を別々に返し、取得失敗時には成功扱いしない。CI成功は実行結果を別途確認する | 読取補助と手順 |
| 格子の棄却順と定常性能の測定(06 10.44) | 3軸それぞれの接触・交差を総当たりと照合。初回を記録し、固定20回の準備後に5000枚×2の7標本を測り、2msの改善目標と要件§5.2の実用上限を判定する | 機械施行 |
| 完了タスクの二重計上・予定タスクの欠落防止(06 10.23、10.26) | `docs/progress.json` を進捗数の機械可読な正本とし、`packages/test-utils/src/progressLedger.test.ts` が予定・完了タスクIDの重複、予定外ID、phaseごとの予定件数、合計と報告値の不一致を `pnpm run test` で検出する | 機械施行 |
| リリース前検査(05) | 手動チェックリスト。`scripts/check-release-ready.ps1` をP13までに実装して機械施行へ移す。予定する検査項目にヘルプと取扱説明書の整合検査(NFR-MA-6): ①目録の全項目に対応する章があり見出しが一致、②本文中の道具名・ボタン名の文言一致、③説明書の画像が撮影台本の生成物であること、④説明書とヘルプの機能の過不足が無いこと、の4点を含める(v1.9) | 未実装(P13予定) |
| 失敗の記録と仕組み化(06) | 失敗発生時に06へ追記し、対策をこの表へ反映する | 手順 |
| 和・積の刻み幅の表示保持(06 §10.201) | `mathLatexCodec.test.ts`と`exactDiscreteRanges.test.ts`が実変換と実計算で刻み・局所添字・原式を照合する。範囲を返す型は通常の型確認で検査し、抑止しない | 関連39件成功。追加した実画面確認は未実行 |
| 無限の和・積の元の成立条件(06 §10.202) | `cas_discrete.py`で対象範囲の各項と末尾の収束を別に検査し、`exactInfiniteRanges.test.ts`と実計算部で取消された分母・外側の0倍・発散・正確な極限・保存する原式を照合する | 関連111件と型・lint成功。追加した実画面確認は未実行 |
| 関数曲線の通常操作と表示(06 10.128) | `functionCurvePaths.test.ts`で経路/案内の選択と保存参照、`drawFunctionCurve.test.ts`で原式と描画の一致、`functionPlotFlow.ts`で3環境の実スイープと独立した体積を検査する | 通常ゲートで機械施行 |
| 図面開始の重複処理(06 10.129) | `makeHiddenLineViews.test.ts`で可視/隠線の継ぎ目走査共有と参照/解放、`drawing-performance.spec.ts`で100段・三面図・50寸法の開始5秒を目標として記録し、要件§5.2の実用上限を検査する | 通常ゲートで機械施行 |
| 関数の後続操作と3D測定(06 10.130) | `functionPlotFlow.ts`で円の計算完了と面の生成を確認してから掃引する。`sketchMeasure.test.ts`で点ID・距離・線長・角度・拒否・古い測定結果、`functionDirectionFlow.ts`で実画面の長さ10→6→Undo10を検査する | 通常ゲートで機械施行 |
| 日本語編集と描画後の符号確認(06 10.61/62) | UI部分診断・E2Eの前にjaMessages.test.tsを正規check経由で自動実行する。派生図のE2Eで符号・倍率と保存往復を確認し、見出し込みの実画像を確認する。未確定の読取パスはrgで発見し、一意な実在パスだけ読む | 機械施行＋画像確認 |
| 未stage差分の検査対象誤認(06 10.38) | check.ps1が空indexのCommit検査を拒否する。自己試験が実入口で空indexと不正なユニット診断指定を検査する | 機械施行 |
| timer取消の処理順(06 10.39) | 共通queueでtimerフェーズを待つ。K53の直接呼出し10回とB20の実Worker通信がCommon後・mesh前の取消を通常ゲートで検査する | 機械施行 |
| HLR性能検査の形状負荷(06 10.40) | 100押し出し全ボディの投影と可視線400本を必須にし、200/2000/5000msの改善目標と要件§5.2の実用上限を判定する | 機械施行 |
| Windows PowerShell 5.1 の読取コマンド構文(06 10.25) | 独立した読取はシェル内で結合せずツール呼出しを分ける。コマンド文字列へ `&&` / `||` を書かず、`rg` の拡張子指定はパスglobでなく `-g` を使う | 手順 |
| STEPアセンブリの名前とidの正規化(06 10.27) | `stepAssemblyMetadata.test.ts` がNAUO内の引数位置に依存しない復元、日本語・引用符・null、印欠落時のfail-closedを検査し、実OCCTの書込・読込・決定性検査も通常の `pnpm run test` に含める | 機械施行 |
| STEPの一時印と永続IDの分離(06 10.28) | 包含しない接頭辞へ分け、共有デコーダーによる可逆性、異なるOCCT通算IDから同一バイトへの正規化、独立2回の実書き出しID一致を通常の `pnpm run test` で検査する | 機械施行 |
| 関数の点と方向の編集を1検査へ合算した時間超過（06 10.141） | functionDirectionScenariosの5種類をWeb/Firefox/実Electronの独立E2Eへ接続し、空文書から作成・保存再開・測定・Undoを各180秒以内で検査する | 機械施行 |

## フックの性質

- 図面を開く計算費用の未分離（06 10.145）は、明示診断の画面/Worker CPU記録で処理を特定する。`primitiveReuse.test.ts`で同形の独立複製・全参照・入力差・所有権を検査し、通常の図面5000ms/16msを維持する。
- 計算失敗と入力の変化の混同（06 10.144）は、E2Eの数学工程記録、実Electron起動の非focusable・文字入力検査と板金の操作記録で区別する。計算部の共用は`mathWorkerGroup.test.ts`が取消・期限・応答照合・解放の境界を確認する。

- 原因調査が前提projectの失敗で止まる問題（06 10.143）は、check.ps1の対象限定手動診断と、実入口を使うvalidation-receipt.integration.selftest.pyで対策する。通常・CI・フックからの前提省略と、診断からの全体成功記録発行を拒否する。

- 進捗報告の失敗見落としと、失敗後の再計算待機（06 10.142）は`validation-progress.mjs`の実CLI検査と`recomputeState.test.ts`で防ぐ。報告の読取は前者へ接続し、E2Eの`recompute.ts`は世代を照合して最新の失敗を即時に知らせる。検査未終了を成功とは表示しない。

- 強度計算の文書切替時の破棄、通常編集・Undo時の結果保持、材料条件・未掲載値・式の境界（06 10.74）は`strengthSession.test.ts`と3環境の`strengthFlow`を通常ゲートへ含める。FirefoxではElectron専用ファイルを除外し、専用projectで検証する。

- P9の公差値・単位・実体記号・データムの合法性、紙上の記入枠とデータム三角、保存版の連続移行（06 10.63）はgdt.test.ts、frameGeometry.test.ts、gdtJson.test.tsと既存documentJson.test.tsで通常ゲートに含める。入力の保存値をそのまま正しい値と扱わず、旧版の期待値更新だけで移行を証明しない。

- desktopの配布用WASM経路(06 10.51)は`desktop-renderer.spec.ts`で実ビルドへ通信失敗/404を与え、箱と図面の生成まで通常E2Eで確認する。Web版の圧縮経路の成功だけでは代用しない。

- `.claude/hooks/block-direct-package-managers.ps1` は**fail-open**とする: フック自身が判定不能(構文解析の例外等)になった場合は警告を出して実行を通す。フックの不具合で全作業が止まることを避けるため。fail-openで通った実行は、統括が規約違反でないことを手動で確認する。
- フックの自己試験は `.claude/hooks/block-direct-package-managers.test.ps1`。フックを変更したら必ず実行する。
- gitフックは `git config core.hooksPath scripts/hooks` で有効化する(`scripts/install-hooks.ps1` が設定する)。クローン直後は無効なので、環境構築の最初に必ず実行する。

- 基本形状の複製した分割の再利用（06 §10.160）は、完了記録・両精度の一致・複製先の実分割検査を必要条件にする。`primitiveReuse.test.ts`が精度変更・実分割の消失・元形状解放後の実形状と参照番号を検査する。実OCCTのコンストラクターを回数観測のために置き換えない。

- 説明検索の欄の種類と表示名（06 §10.185）は`command-registry.spec.ts`で実searchboxと翻訳JSONを参照し、Chromium/Firefoxで検索から現在のキー一覧まで確認する。前後章ボタンを結果として選ばず、失敗時の画像とtraceを再実行前に保存する。

- 数列の局所変数一覧（06 §10.223）は通常の型検査と`sequenceCalculations.test.ts`で、構造入力の一覧の種類・重複・初期値の個数・保存と表示の意味を照合する。型の強制変更で検査を迂回しない。

- 写像の定義域と逆写像（06 §10.305）は`mathMappings.test.ts`から固定版の実計算を起動し、元の式の定義不能点、集合の対応、逆数との違い、保存と構造入力の往復を照合する。未知の包含関係や一意性を成功に変えない。

## 機械施行の限界


- フックとgitフックはこのリポジトリの設定に依存する。`--no-verify` や設定解除で迂回できるため、迂回しないことは規約(03 §7)で禁じ、迂回が必要な例外は利用者の明示指示がある場合に限る。
- 機械が証明するのは「検査を実行して合格した」ことまでであり、指示を理解したこと、報告が真実であることは証明しない。だから統括の再検査とdiff全文読みを省かない。
